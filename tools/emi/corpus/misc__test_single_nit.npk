// Ultra simple test - single nit field

struct:SingleNit = {
    nit:value;
};

func:failsafe = int32(tbb32:err) { exit 1; };

func:main = int32() {
    println("Testing single nit field...");
    
    // This should work: 0 fits in nit range
    SingleNit:s = SingleNit { value: 0 };
    
    println("Success!");
    exit 0;
};
