// Test ensures clause

func:abs = int32(int32:value)
  ensures result >= 0
{
    if (value < 0) {
        pass 0 - value;
    }
    pass value;
};

func:failsafe = int32(tbb32:err) { exit 1; };

func:main = int32() {
    int32:r = abs(-5);
    if (r != 5) {
        exit -1;
    }
    exit 0;
};
