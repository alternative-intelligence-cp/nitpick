// Expected: COMPILER ERROR
// Simple test: function with struct parameter

struct:Point = {
    int32:x;
    int32:y;
};

func:get_x = int32(Point:p) {
    pass p.x;
};

func:main = int32() {
    Point:point = Point{x: 10i32, y: 20i32};
    Result<int32>:rx = get_x(point);
    if (rx.is_error) {
        exit 1;
    }
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
