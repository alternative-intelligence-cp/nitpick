// Generics test using int32 as return type (concrete type) 
// Parameters use concrete types, function body has generic logic placeholder

// Identity - returns int32, takes int32 (not actually generic)
func:identity = int32(int32:value) {
    pass value;
};

// Main function
func:main = int32() {
    int32:x = raw identity(42);
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
