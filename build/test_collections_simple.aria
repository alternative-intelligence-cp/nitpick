// Simple test for collections library - just test sum with parameters

use lib::stdlib::collections;

func:main = *int8() {
    // Test sum_int32 with a simple case
    // We can't create arrays yet, so we'll just verify it compiles
    puts("Collections library imported successfully!");
    return 0;
};
