// test_wildx_array_index_decimal.aria
// Test writing to wildx buffer using array indexing with decimal values

func:main = int8() {
    print("=== Wildx Array Indexing Test (Decimal) ===");
    
    // Allocate wildx buffer
    wildx uint8:code = 0;
    
    // Write decimal values (same as hex but easier to verify)
    code[0] = 72;   // 0x48 in decimal
    code[1] = 199;  // 0xC7
    code[2] = 192;  // 0xC0
    code[3] = 42;   // 0x2A
    code[4] = 0;
    code[5] = 0;
    code[6] = 0;
    code[7] = 195;  // 0xC3
    
    print("✓ Wrote machine code to wildx buffer");
    print("  Using decimal values for verification");
    
    *0;
};
