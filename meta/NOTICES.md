# The notices sent to the library listener

The compiler side tells the library side, before a language-visible change lands
and after every landing, what moved: the six ladder rows with the PREVIOUS digests
quoted (a delta alone authenticates only the previous size — D-265), the generated
manifest numbers, the harness's own summary lines verbatim, and the notice number.
The listener keeps its own count on its own board, and its count is the authority;
this file exists so ours can be checked without asking it. The number lived only
in two sessions' heads until 1.5.8b: a duplicate happened once (step 3 was sent as
24 where the board said 34, corrected by the listener), and the text of a notice
does not survive the session that sent it.

## The rule

Every landing notice carries, in this order: the number; the commit and the step;
what it is (the language-visible change first, or "no language change"); the six
ladder rows at the landed commit, each with the previous digest and size when it
moved; the manifest numbers (`nitpick.obligations`, `runtime/npkrt.obligations`)
as the runners print them; the harness block verbatim (`programs`, `verify`,
`floor`, `parity`, `ok N test(s) passed`); and what is next, with whether the
floor moves at it (a re-pin). An ADVANCE notice precedes any landing that
reserves a name (D-239), adds an arm every program must name, changes a refusal,
or moves the floor; it is unnumbered and names the number the landing will take.

## The count

| n | date | commit | landing | note |
|---|---|---|---|---|
| F5 | 2026-09-18 | — | FORECAST: 1.5.8 planned; S-84…S-87 ratified as D-304…D-307 | `decreases`/`unbounded` announced |
| 22 | 2026-09-19 | `cd1ed86` | 1.5.8 step 0 | |
| 23 | 2026-09-19 | `754b510` | 1.5.8 step 1 (`CastRange`, DEF-58/61) | |
| 24 | 2026-09-19 | `d5ad3c9` | 1.5.8 step 1b (DEF-65) | |
| 25 | 2026-09-19 | `f481dab` | 1.5.8 step 2 (the split-stack prologue, D-305) | corrected by 26 (the failsafe count) |
| 26 | 2026-09-19 | `cae3997` | 1.5.8 step 2b (DEF-64) | |
| F6 | 2026-09-19 | — | FORECAST | |
| 27 | 2026-09-19 | `4d5fd77` | 1.5.8 step 2c (DEF-67, `hold-at:`) | pin anchor `4d5fd77` |
| 28 | 2026-09-19 | `b7244d2` | 1.5.8 step 3 (`MachineFault`, D-307) | |
| 29 | 2026-09-19 | `0cf0f78` | 1.5.8 step 3b (DEF-66) | |
| F7 | 2026-09-19 | — | FORECAST | |
| 30 | 2026-09-19 | `6340d5c` | 1.5.8 step 3c (DEF-69) | the floor's last move before 1.5.8b step 6c |
| 31 | 2026-09-19 | `35ad9e1` | 1.5.8 step 4 (the refresh; 1.5.8 closed) | |
| 32 | 2026-09-19 | `f241766` | 1.5.8b step 0 (the plan, D-308…D-314) | documents only |
| 33 | 2026-09-19 | `410d405` | 1.5.8b step 1 (`sealed`/`hidden`) | |
| 34 | 2026-09-19 | `3592de2` | 1.5.8b step 3 (the `overflow` rows) | sent as "24"; the listener corrected the count. Steps 1b and 2 landed without a notice of their own and rode 33 and 34 |
| 35+36 | 2026-09-19 | `0439819`, `3207f72` | 1.5.8b steps 4 and 5 (`+% -% *%`; the `bounds`/`cast-range` rows) | one message; its "since 1.5.4e" for the floor's last move was wrong (`6340d5c` is right — the listener caught it) |
| 37 | 2026-09-20 | `c5ba885` | 1.5.8b step 6 (a field's own `limit<Rules>`) | its "byte-identical emission" sentence contradicted its own ladder (the listener caught it; the plan's sentence was the defect — fixed at step 7) |
| — | 2026-09-23 | — | ADVANCE for step 6b (the reach fix) and for step 6c (the ceiling, `ListLen`, the arms, the floor) | |
| 38 | 2026-09-24 | `14ef02f` | 1.5.8b step 6b (DEF-86: the reach analysis follows calls into the prelude) | the rows 38–40 were written at step 7 (2026-09-23) ahead of their landings; each was sent 2026-09-24 |
| 39 | 2026-09-24 | `3e4b47d` | 1.5.8b step 6c (D-308 §§6–7; the floor moves) | re-pin (`npkrt.o` 72,560 → 72,576 B) |
| 40 | 2026-09-24 | `3156b72` | 1.5.8b step 6d (`intern.npk`'s `*%`; DEF-88) | |
| 41 | 2026-09-24 | `aee4dd9` | 1.5.8b step 7 (the docs and the close; 1.5.8b COMPLETE) | every ladder row unchanged from `3156b72` |
| — | 2026-09-24 | — | ADVANCE for 1.5.8c step 1: `decreases`/`unbounded` become keywords (none in the libraries, measured 2026-09-23), `DecreasesViolated` an arm every program with a `decreases` names, `terminate` guarded; the landing takes the number after 1.5.8c step 0's | sent to `nitpick-libs_s5` (the seat changed hands 2026-09-24) |
| 42 | 2026-09-24 | `68b6e05` | 1.5.8c step 0 (the plan and the codes) | no language change |
| 43 | 2026-09-24 | `f578e6b` | 1.5.8c step 1 (the mechanism: the two keywords, the loop and function clauses, the check, the `terminate` rows; DEF-90) | sent by `nitpick-compiler_s13` (the seat after s12) |
| 44 | 2026-09-24 | `5ea6053` | 1.5.8c step 2 (the one-hop snapshot refresh: the committed builder parses the clauses and carries DEF-90's fix; the builder rows move) | carries the SWEEP RECIPE for the libraries (step 3's tool and idioms; D-316) |
| 45 | 2026-09-24 | `275442f` | 1.5.8c step 3 (the sweep of our tree: every `while`/`when` clausal, 392 by the tool, 563 by the reading, 72 `unbounded`; the evaluator checks a measure; every failsafe names `(DecreasesViolated)`) | no language change; the record `decreases_read.txt` is the libraries' worked example |
| — | 2026-09-24 | — | ADVANCE for 1.5.8c step 4: TYPE-072's `neither` shape becomes a refusal; a FUNCTION's `decreases` is live (the recursive groups, TYPE-074/075, TYPE-073's 64-bit width for a function measure, the check at every call inside the group, the `terminate` call rows, the `stack-depth` rows derived by both runners); `index.txt` gains two fields and `rows.txt`'s fifth admits `d`; DEF-92 named | sent to `nitpick-libs_s5` by `nitpick-compiler_s13` before the landing |
| 46 | 2026-09-24 | `def2728` | 1.5.8c step 4 (the refusal, the groups, the function measure, the `stack-depth` rows) | the landing the advance above announced |
| 47 | 2026-09-24 | `d7a8092` | 1.5.8c step 4b (DEF-92: the generic-instance interner's index -- the frontend 5.4x faster, every program's emission byte-identical) | no language change; a re-pin gets a faster compiler and nothing else |
| 48 | 2026-09-24 | `624d71f` | 1.5.8c step 5 (the subcycle's close: the residue report over `terminate` and `stack-depth`, the docs, `1.5.8d.md` planned) | no language change; the commit cell is filled at 1.5.8d, the next landing in the tree |
| 49 | 2026-09-25 | `c93d80d` | 1.5.8d step 0 (D-317: a by-value aggregate's identity and field functions in the encoder; DEF-94: the implicit pointer receiver is an escape; D-318 recorded, E-5 decided out) | no language change; the manifest re-recorded; no floor byte, no snapshot refresh; next: 1.5.8d steps 1-3 (the refresh from the final `src/`, the doc sync, the archive to `done/1.5/` -- ONE commit; the builder rows move at it) |
| 50 | 2026-09-25 | `c3bdae2` | 1.5.8d steps 1–3 — THE CLOSE of cycle 1.5 (the one-hop refresh from the final `src/`; the doc sync; the archive to `done/1.5/`) | no language change; EVERY BUILDER ROW MOVES (a re-pin of the builder, not of the floor: `npkrt.o` unchanged); the manifest unchanged from 49; next: cycle 1.6 (the analyzer evidence), no floor move planned |
| 51 | 2026-09-25 | `a3b917f` | 1.6.0 step 0 (the bring-up gate PLANNED execution-grade, measured first; D-319 NIKOS the IKOS candidate, D-320 the gate's decision rule, D-321 Alive2's budget and solver pin; the gate's tools README, census script and probe log) | no language change; every ladder row unchanged from `c3bdae2`; both manifests unchanged; sent to `nitpick-libs_s6` (the seat since the 1.5 close); next: 1.6.0 step 1 (the pinned builds, outside the tree), no floor move planned |
| 52 | 2026-09-25 | `2cd5176` | 1.6.0 step 1 (the pinned builds: `engines.sh`, `pins.txt` with every engine's commit and every binary's digest, Alive2's recorded rlimit patch; the static z3 link found impossible — shared, rpath-resolved, digest-pinned instead, D-321's note) | no language change; every ladder row unchanged from `c3bdae2`; both manifests unchanged; next: 1.6.0 step 2 (the inputs and the seven planted controls), no floor move planned |
| 53 | 2026-09-25 | `8fbde77` | 1.6.0 step 2 (the input set by `gate_inputs.py` — the three plain emissions, the whole-program forms by text with the floor's eight roots read off its own text, the verified emission, the datalayout twins, the opt pairs, every digest recorded; the seven planted-defect controls with `plant.py` and `gate_controls.py`, each RUN plain and planted and shown to exhibit its defect) | no language change; every ladder row unchanged from `c3bdae2`; both manifests unchanged; next: 1.6.0 step 3 (the runs), no floor move planned |
| 54 | 2026-09-25 | `dfbaf1a` | 1.6.0 step 3b (DEF-95: the reach analysis armed `BadStep` for every counted loop where the emitter guards only a computed step — the library listener's finding; the archived `loop_dump.npk` paths and TYPE_REFERENCE §9.1.2's example fixed) | no language change; a DEMAND REMOVED (a `failsafe` with `(BadStep)` over literal steps keeps compiling); every ladder row unchanged; the manifest RE-RECORDED — 82 rows re-keyed by type-id renumbering, ZERO verdicts moved, ZERO discharged counts fell (the first harness on this tree was red for want of the re-record); next: 1.6.0 step 3c (DEF-96) and step 3 (the runs), no floor move planned |
| F8 | 2026-09-25 | — | ADVANCE for 55: 1.6.0 step 3c (DEF-96) — a `main` that is not exactly `int32(cstring[]:argv)` REFUSES at the signature (`NITPICK-TYPE-083`: the arity, the parameter type, the `int32` return), and `failsafe`'s `int32` return joins TYPE-044; the listener's six two-parameter files move to `cstring[]:_~argv` | a refusal ADDED; sent to `nitpick-libs_s6` with the measurement |
| 55 | 2026-09-25 | `d156c4f` | 1.6.0 step 3c (DEF-96: the two-parameter `main` compiled and its `argc` read the argv pointer's low word — the library listener's finding; `main`'s and `failsafe`'s signatures checked whole) | no language change (D-089 §4 said it; the check is new); a REFUSAL ADDED as F8 announced; every ladder row unchanged except the emission's text where the checker grew; the manifest RE-RECORDED — 86 rows re-keyed by type-id renumbering, ZERO verdicts moved, ZERO discharged counts fell; the listener's exposure measured at seven of its 144 `main`s (six regex files, its canary `int64()`), the canary already moved |
| 56 | 2026-09-25 | `f758995` | 1.6.0 step 3d (DEF-97: a generic instance used only inside a generic function had its type definition emitted after its first use and `llc` refused the module — the library listener's finding; `irw_type_def` places every late definition ahead of every function) | no language change; a refusal REMOVED (a module `llc` refused compiles and runs); every ladder row unchanged except where the manifest re-record moves nothing (the compiler's own emission has no late header); the manifest RE-RECORDED for the writer's new function |
| 57 | 2026-09-25 | `395308f` | 1.6.0 step 3e (DEF-98: the lexer closed a block string on two quotes where §6.3's grammar closes it on three — the library listener's finding; the grammar stands, the lexer reads three) | no language change (the reference is the rule; the lexer deviated); a refusal REMOVED (a `""` inside a block string lexes as the grammar says); every ladder row unchanged except the emission's text where the lexer grew; the manifest RE-RECORDED for the lexer's new function |
| F9 | 2026-09-25 | — | ADVANCE for 58: 1.6.0 step 3f (DEF-99) — a `move(...)` or the implicit move at `pass` out of a `fixed` binding, or any part of one, REFUSES when the value owns (`NITPICK-TYPE-084`); the copy was TYPE-046 already, `.clone()` is the reading | a refusal ADDED; sent to `nitpick-libs_s6` with the code, as asked |
| 58 | 2026-09-25 | `6fb85d3` | 1.6.0 step 3f (DEF-99: a move out of `fixed` storage holding an owning value compiled and faulted — the library listener's finding; the move refuses as the copy does) | no language change (D-287's reason, asked of the write a move makes); a REFUSAL ADDED as F9 announced; every ladder row unchanged except the emission's text where the checker grew; the manifest RE-RECORDED for the checker's new function |
| F10 | 2026-09-25 | — | ADVANCE for 59: 1.6.0 step 3g (DEF-102, DEF-103) — a plain by-value parameter of an OWNING type is read-only (`NITPICK-TYPE-085`: no assignment to or into it, no `@`/`$$i`/`$$m`, no pointer-receiver call, no stateful operation; a `move` parameter or a `.clone()` is the spelling), and a keyword as a declared function or type name is PARSE-001 | two refusals ADDED; sent to `nitpick-libs_s6` with the codes |
| 59 | 2026-09-25 | `5bdae98` | 1.6.0 step 3g (DEF-102: an assignment to an owning field of a lent parameter dropped the caller's value and `@p` let a callee free the caller's storage — the library listener's finding; a loan is read-only when it owns. DEF-103: a keyword as a declared name. DEF-104: a lent `T` in a generic body escaped both loan gates — both ask D-264's predicate now. `T[0]` stated as supported) | no language change (D-004's loan and D-266's view, one rule); two REFUSALS ADDED as F10 announced; every ladder row unchanged except the emission's text where the checker and the parser grew; the manifest RE-RECORDED |
| 60 | 2026-09-25 | `c1a4a05` | 1.6.0 step 3h (DEF-105: an imported `fixed` binding's declared type resolved in the importer's scope, not its home module's — the library listener's finding; the loud form was TYPE-001 at the fixture's line, the silent form read a table with the wrong stride past its end; two checker sites resolve in the home scope now, D-137) | no language change; no refusal added (a loud error becomes acceptance, a silent misread the right read); every ladder row unchanged except the emission's text where the checker moved; the manifest RE-RECORDED |
| 61 | 2026-09-25 | `0da2be7` | 1.6.0 step 3 (the gate's runs: `gate_run.py` over step 2's set, 316 tasks over the three engines, each twice — NIKOS reads every form but the compiler's datalayout twin, finds seven of the eight planted defects in both modes and misses `uaf` for want of a free it knows, reaches 12 of 14 functions in the sync program and ONE (`main`) of the async program and of the compiler — the executor wall, read off its inliner; Clam's eight memory settings abort at the cell mapping on every input, find nothing on the controls, and exceed the machine on the compiler's whole-program form (killed at 129 GiB resident seven times); Alive2's fifteen `incorrect` verdicts are inter-procedural facts measured through a no-inlining twin, escalated and recorded under D-321, with E-7 for 1.6.2; the datalayout twins move no program verdict and NIKOS refuses the compiler's; the record in `1.6.0.md`, the report in `.internal/gate/runs/`) | no language change; no `src/` byte, no emission change, no floor byte, no manifest row; every ladder row unchanged; tools and docs only |
| 62 | 2026-09-25 | `996784e` | 1.6.0 step 4 (the reading: each engine's port distance sized by reading its pinned source with file and line — NIKOS's executor wall (an unknown callee is assumed to have no side effects; the floor's kernel memory is one absolute location), the allocator-entry model, the overflow intrinsics, `llvm.assume`, the signedness guess, the entry seeding, a one-token datalayout fix and a native `select`/`switch` import, roughly 600–900 lines in named files; Clam's cell-mapping abort is sea-dsa's SOUND refusal of our by-value aggregate arguments, so the S-101 demotion patch has no sound instance and none joined the pin — the "with" column measured by an unsound instrument outside every gate, which finds a second wall in Crab's typed regions; NIKOS analysed every plain form under LLVM's DEFAULT layout — E-8, the emitter writing its own datalayout line, the user's; three NIKOS defects found by reading; the `dbm` and `-opt=none` readings; the scorecard filled: under D-320's rule 1 Clam is disqualified and NIKOS wins, offered to the user at S-102) | no language change; no `src/` byte, no emission change, no floor byte, no manifest row; tools and docs only |
| F11 | 2026-09-25 | — | ADVANCE for 63: 1.6.0 step 4b (DEF-106) — a write INTO A PART of a `fixed` binding REFUSES (`NITPICK-TYPE-086`: an element, a plain field, a compound assignment, a stateful operation on a part, a `fixed` local's element, anything under a `fixed` field; the whole binding and a `fixed` field's own assignment stay ASSIGN-002); `.clone()` or a plain binding is the reading | a refusal ADDED; sent to `nitpick-libs_s6` with the code and the measurement (their O-N24; their exposure 0 of 63 `fixed` bindings) |
| 63 | 2026-09-25 | `f87d2df` | 1.6.0 step 4b (DEF-106: a write into a part of a `fixed` binding compiled and stored into the LLVM `constant` global — the library listener's fuzzer's finding; TYPE-086 refuses every write form into a part, through the one helper every write form asks) | no language change (D-287's reason, asked of the write into a part); a REFUSAL ADDED as F11 announced; every ladder row unchanged except the emission's text where the checker grew; the manifest RE-RECORDED |
| 64 | 2026-09-25 | `4e467bc` | 1.6.0 step 5 — 1.6.0 COMPLETE (D-322: NIKOS is leg A's engine by D-320's rule 1 on row 2, Clam/Crab decided out with its scorecard kept; E-8 settled — the emission will state its own data layout at 1.6.1 step 1; `[verify.nikos]` the manifest key; `1.6.1.md` planned execution-grade: transform-free ingestion, the port item by item as pin moves, the `analyze` stage, the alarm ledger `nitpick.alarms`; S-103…S-105 owed to the user) | no language change; no `src/` byte, no emission change, no floor byte, no manifest row; every ladder row unchanged; docs and the plan only. FOR THE LISTENER: 1.6.1 step 1 will change EVERY emission's text (a `target datalayout` line beside the triple; the binary does not move) with a snapshot refresh — a re-pin notice will precede it |
| 65 | 2026-09-25 | `77314e9` | 1.6.0 step 5b (two documents, the library listener's 0.1.4b planning: D-292's sentence "`NPK_HEAP_STATS` counts region bytes as allocated" does not describe the runtime — the 1.5.6 landing departed from it and its record says so; a dated note now says a `peak_live` bound excludes the failsafe region. DEF-107 REGISTERED, a language question for the user (S-106): a view's root may be written while the view is live and the view then reads freed memory, in safe code on both legs — D-249 stops a view escaping, D-286 freezes nothing for a view, D-266 froze a lending pick's selector alone; the recommendation is the freeze for the view's lexical lifetime; until it lands, write views as copies) | no language change; no `src/` byte, no emission change, no floor byte, no manifest row; every ladder row unchanged; docs only |
| F12 | 2026-09-26 | — | ADVANCE for 66: 1.6.0 step 5c (DEF-108, D-323) — a function whose body can reach its own closing brace REFUSES (`NITPICK-FLOW-001` at the function): every path ends in `pass`, `fail`, `return Result{…}`, `exit` (in `main`/`failsafe`) or a trap, a `NIL` function passes `NIL`; an `if` without `else` completes, an `if`/`else` if either arm does, a `pick` if any arm does, a `while (true)` with no `break` never does | a refusal ADDED; sent to `nitpick-libs_s6` with the code, the walk's rules and the measurement (their O-N26; their exposure measured by the check itself at the pin) |
| 66 | 2026-09-26 | `c970483` | 1.6.0 step 5c (DEF-108: a function that fell off its end compiled and returned a zero value, a fallible one a silent SUCCESS, `main` an exit 0 — the library listener's fuzzer's finding, the user's rule D-323; `NITPICK-FLOW-001` from the bindings analysis's `stmt_completes`, the conservative twin of `stmt_exits`) | no language change stated anew (D-323 writes down what the language always meant; the emitter's fall-off return had stood in for a check); a REFUSAL ADDED as F12 announced; every ladder row unchanged except the emission's text where the checker grew; the manifest RE-RECORDED; the tree's 906 files hold no fall-through outside the new test |
| F13 | 2026-09-26 | — | ADVANCE for 67: 1.6.1 step 0 (DEF-107, D-325; DEF-113, DEF-114, DEF-115) — a VIEW's root is FROZEN while the view is live (`NITPICK-BORROW-015`: a write-capable access of storage a live view names — the root reassigned, `@root`/`$$m root` handed to a callee that writes through it, a pointer-receiver call, a `move` out, `list_push` on a viewed list; reads, `$$i`, a disjoint field, a disjoint numeral range and a callee that writes nothing through what it is handed stay; `.clone()`/`string_concat("", …)` or ending the view's block is the reading); `BORROW-001` reaches a by-value parameter's address and a `List` of addresses (DEF-109, DEF-112), a `dyn` returned after its impl stored a borrow into its cell (DEF-113), a `move` parameter returned after a call stored a borrow into it (DEF-114); `BORROW-002` a view of the storage a store overwrites, a store a callee's own body makes into a parameter's pointee or a lent `dyn`'s cell (DEF-115); `BORROW-012` a temporary handed to a viewing callee; a trait's method is read as the union of its impls' bodies, a function value by the shape rule | a refusal ADDED and four reaching new shapes; sent to `nitpick-libs_s6` with the codes and the measurement (1,030 files swept: the tree's 799 and their 231; zero sites outside the new tests) |
| 67 | 2026-09-26 | | 1.6.1 step 0 (DEF-107 FIXED under D-325: a view's root is frozen for the view's lexical lifetime, a fourth party kind of D-286's aliasing walk, `NITPICK-BORROW-015`; D-325's provenance summaries — what a call views, passes, stores and writes, read off each callee's own body at the escape fixpoint, a trait's method the union of its impls'; DEF-109…DEF-115 found by its probes and fixed with it; the D-223 counterweight read both ways) | a refusal ADDED and four reaching new shapes, as F13 announced; no language change; no emission change for a program the rule admits; the manifest RE-RECORDED (the gate over shared rows moved nothing; three pairs are moves of rows with their bodies); no floor byte; no snapshot refresh; the checker over `src/npkc.npk` 27.5 s → 9.6 s (DEF-111) |

The messages themselves are not kept here; each seat keeps the text it sent in its
own `.internal/handoff_*/` scratch for as long as that survives, and this table is
what survives them.
