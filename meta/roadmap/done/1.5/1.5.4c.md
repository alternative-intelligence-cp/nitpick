# 1.5.4c — A module symbol means one thing (D-273): the qualified call, `use` over a module path, the alias's scope

**Planned 2026-09-07 on `dce7092` by `nitpick-compiler_s2`; ratified as D-273 the
same day (DEF-31). Execution-grade: every decision is settled; nothing here is
the executor's to decide. Scheduled AHEAD of 1.5.4b, since it is small and the
module system must not carry a known half-feature into the theories work.**

## 1. Where it starts

MODULE_REFERENCE §1.1 defines inline and nested modules and §2.1 the alias
import; the checker's `namespace_member` (`src/frontend/type_members.npk`)
resolves `m.name` as a VALUE through the scope a module symbol opened (a
function member as its function type, a global as its type) and the resolver's
member-access comment says `math.sqrt` was to be told apart from `p.x` there.
Measured at planning (DEF-31, `da1f911`):

- `m.f(x)` parses as an `ExprMethodCallExpr` whose receiver is `m`;
  `type_method_call` (type_members.npk:1673) types the receiver as a value, so
  `hidden` is TYPE-007 before the namespace is asked. The same for an ALIAS:
  `use "./lib.npk" as lib;` binds `lib` as a module symbol
  (`apply_one_import`'s `IMPORT_NAMESPACE` arm, module_graph.npk) with NO
  `inner` scope set, and no test calls through it.
- `use hidden.*;` is a LOGICAL path (segments, no path string; parse_decl.npk
  ~844); `graph_load_import` and `apply_one_import` return at once for one
  ("the driver resolves those" — the standard library's), so nothing binds and
  a later bare `fetch` is RESOLVE-002.
- Symbol names already nest: three functions named `go` (file-level, in
  `mod:a`, in `mod:b`) emit `@"npk.two_inline.go"`, `.a.go`, `.b.go` and
  link (`mangled_fn_sym` over `mod_canonical`). No naming work is owed.
- Inline modules in the tree: seven test files, none for the namespace's sake
  (`whole_grammar.npk`, `parse_decls.npk`, `symbols.npk` ×3 units,
  `hygiene.npk` §6, `nested_reach.npk`, `entry_in_module.npk`,
  `owned_names.npk` ×4, `forms_tail.npk`, `inline_mod.npk`); the library
  workbench: none in 170 `mod` uses.

## 2. What lands (D-273 §1–§4)

1. **The qualified call is a DIRECT call.** A call whose base names a module —
   an inline module, an alias, a nested path `core.math.f(x)` — calls that
   member with no receiver; the checker records the member as the callee, the
   emitter lowers it as `emit_call` does a direct call, the SMT walk records
   its rows as at any direct call (`requires` `bypass`, `limit-subsume`,
   `uf_value` for a `pure never fails` callee), and a `pure never fails`
   member is admissible in a contract as any named function is.
2. **`use` over a module path.** A `use` whose first segment names a module
   symbol in the importing file's scope binds that module's public names in
   every form the file forms have — `use hidden.*;`, `use hidden.{f, Point};`,
   `use hidden.f;`, `use core.math.*;` — through the SAME `bind_wildcard`/
   `bind_named` the file forms use. This is the only way an inline module's
   TYPES are named from outside (there is no qualified type path, for files
   either). A first segment that names no module symbol in scope and is not
   `std` is `RESOLVE-002` naming the segment (a typo is never silent); `std.…`
   stays the driver's as today; `std` joins D-239's owned names for a module
   declaration or a file (`RESOLVE-001`).
3. **Visibility from either spelling.** A private member reached by a
   qualified path from OUTSIDE its module, or named by a `use` over its
   module, is `RESOLVE-004` ("`f` is private to `m`", the note at the
   declaration) — the resolver's code, imported by the checker for the
   qualified spelling (one mistake, one code; D-240). Inside the module (the
   accessing scope has the module's scope among its ancestors) everything is
   reachable, as §3 says. A `pub mod` is an exported symbol a file's wildcard
   or named import binds WITH its scope, so `helpers.f()` works across files.
4. **One field.** A module symbol's `inner` is the scope its members live in:
   set by collection for an inline module (`symtab_set_inner`), by
   `apply_one_import` for an alias (the source module's `scope`), and copied
   by `bind_or_report` whenever a module symbol is bound into another scope.
   `nested_module_scope` and the new `namespace_path` read it; nothing
   searches for a scope (D-115's rule).

Two readings of D-273 the plan makes executable, recorded for the user to
veto (neither changes a program that compiles today):
**R-1** an unknown first segment refuses (§2 item 2) rather than binding
nothing; **R-2** within a file, namespace and file imports are applied
BEFORE logical ones (two passes in `apply_imports`), so `use lib.*;` works
whether it is written above or below `use "./lib.npk" as lib;` —
MODULE_REFERENCE §2.4: resolution is never order-dependent.

## 3. Steps (D-228: one worktree per step, one full harness per cumulative prefix, landed on `main` by cherry-pick with the record appended here)

### Step 0 — the checker, the emitter and the encoder: the qualified call

- `namespace_path(e, ExprId) -> int32` in `type_members.npk`: an identifier
  whose symbol is `SYM_DECL` over a `DeclModuleDecl` with `inner != 0`, or a
  member access whose operand is a namespace path and whose member (by
  `scope_lookup_local(inner, name)`) is such a symbol — returns the module's
  SYMBOL; anything else 0. The visibility rule of §2 item 3 is asked at every
  hop taken from outside (`scope_is_inside(e.tab, e.scope, inner)`: walk
  `scope_parent`).
- `namespace_member` uses it for its base (nested paths work as values too):
  a member that is a module in VALUE position is TYPE-007 "`m` is a module,
  not a value"; a type name likewise "is a type, not a value" (the identifier
  arm's sentence, reused — no new text); a function → `fn_type`; a global →
  its type. A member the module lacks stays `TYPE_NO_METHOD`'s "module `m` has
  no member `x`".
- `type_method_call`: FIRST `namespace_path(e, receiver)`; nonzero → look the
  method name up in `inner` (visibility, existence as above); it must be a
  `DeclFunctionDecl` (else TYPE-007 "`m.x` is not a function"); then type the
  call through the SAME path a direct call of a known function symbol takes —
  factor `type_call`'s known-function-symbol body into
  `type_direct_call(e, id, n, sym, arg_start, arg_count)` and call it from
  both (a second implementation of the call rule is the 1.3.3 `op=` mistake).
  It records `callee` and `callee_sym` as a direct call does and writes the
  new per-expression slot **`ns_callee`** (`expr_types.npk`: the callee's decl
  index; 0 for every other node). Contract context (`contract_method_refused`)
  and purity (`pure_method_rules`) treat a namespace call as the NAMED call it
  is (admissible when `pure never fails`).
- Every reader of an `ExprMethodCallExpr`'s RECEIVER asks `ns_callee` first
  and skips the receiver when it is set: the executor lists every
  `ExprMethodCallExpr` site in `src/` (`rg -n ExprMethodCallExpr src`) in the
  record, classified — the emitter (`emit_method_call`, ir_expr.npk:8469 →
  the direct path of `emit_call`, 6632, with the node's argument window and
  NO receiver; an awaited `async` member through the same await-site code a
  direct awaited call uses), the SMT encoder's call arms (`smt_encode.npk`:
  the callee's rows and `uf_value`), and the analyses (`bindings`, `escape`,
  `reach`, `suspend`, `locks`, `exhaust`, the purity walk, `macro/`) — each
  either skips the receiver or needs no change, said per site. A namespace
  call that any walker still treats as a value receiver is the defect this
  step exists to not leave.
- Tests: `inline_mod.npk` calls `hidden.fetch(3i32)` and exits 7 (its
  comment rewritten); `tests/backend/programs/mod_qualified.npk` — nested
  `core.math.sq(3i32)`, an alias `lib.f()` over a fixture module, a global
  `hidden.MAX`, an `async` member awaited, a `pure never fails` member in a
  `requires` clause, exit through the sum; `tests/types/rejection/
  mod_not_value.npk` (TYPE-007: `int32:x = hidden;`, `hidden.nosuch(1i32)`
  → TYPE_NO_METHOD, `hidden.MAX(1i32)` → "not a function");
  `tests/modules/rejection/mod_private_qualified.npk` (RESOLVE-004 through
  `hidden.internal()`); `tests/verify/req_qualified.npk` (a `requires` on a
  member called qualified: one `requires` row, `bypass`, discharged; the
  `failsafe-post` rows named). `nitpick.obligations` must not move (no call in
  `src/` is qualified); a moved manifest is red.

### Step 1 — `use` over a module path, the alias's scope, `std`

- `apply_one_import`: a logical path's first segment is looked up
  `scope_lookup_local` in the importing file's scope; a module symbol → walk
  the remaining segments through `inner` (each hop a module symbol, exported
  when reached from outside), then `bind_wildcard`/`bind_named` over the final
  scope with the dotted text as `target` for messages; `std` first → return
  as today; anything else → RESOLVE-002 "cannot find module `x` in this
  scope" (R-1). `apply_imports` runs two passes per file: file/alias forms,
  then logical (R-2).
- `bind_or_report` copies `inner` for a module symbol; the alias arm sets it
  to `src.scope`.
- D-239's `owned_name_error`: `std` for a module declaration and for a file's
  header (a `std.npk` in a program is refused at its header).
- Tests: `mod_qualified.npk` grows `use hidden.*;` (bare `fetch`), `use
  core.math.{sq};`, a struct type imported from an inline module and used in
  `main`, and a cross-file `pub mod` reached as `helpers.f()` after `use
  "./mod_helpers.npk".*;` (a fixture beside it, header `mod:mod_helpers;`);
  `tests/modules/rejection/mod_private_use.npk` (RESOLVE-004 via `use
  hidden.internal;`), `mod_use_unknown.npk` (RESOLVE-002 via `use nosuch.*;`),
  `std_declared.npk` (RESOLVE-001 for `mod:std = { … };`), and an
  order-independence pair in `tests/accept/` (`use lib.*;` above and below its
  alias). `whole_grammar.npk`'s `use std.math.*;` keeps parsing and binding
  nothing.

### Step 2 — the docs

MODULE_REFERENCE §1.1 (an inline module's members are reached as `m.f(x)` and
imported with `use m.*`; nested paths), §2.2 rewritten (a logical path resolves
against module symbols in scope first, `std` the library's, R-1/R-2), §3
(private from either spelling); D-273's landing note; D-239's table gains
`std`; DEF-31 struck in OPEN_DECISIONS; CLAUDE.md's status and a gotcha ("a
module member is called `m.f(x)`; `use m.*` imports it; an alias carries the
file's scope"); the 1.5 README row DONE. No snapshot refresh: `src/` adopts
nothing (D-205).

## 4. Questions for the user

None beyond R-1 and R-2 above, which the plan proceeds under.

## 5. Protocol

D-228 throughout; commit per step with the record appended here and the
harness paragraph in the commit message; push; `npkg build` for the six
digests (D-265) and a notice to the library listener per landing; both
runners' self-checks unchanged (no new belt — the harness's `parity` stage
covers both runners' verdicts); `check_refs.py` (the workbench's) holds 63.

### Step 0 — LANDED (2026-09-10): the qualified call is a direct call

**What landed.** `namespace_path` (`type_members.npk`) is the one walk: an
identifier naming a module symbol whose name opened a scope, or a member
access whose operand is such a path and whose member, in that scope, is
another module — the last module's SYMBOL, with the visibility rule asked at
every hop taken from outside (`scope_is_inside`, `symbols.npk`; a private hop
is RESOLVE-003 "`x` is private to `m`" with the loader's note, TYPE-019 for a
member the module lacks) and a PROBE form that reports nothing, so the await
typer can ask before it types a receiver and the call typer reports once.
`type_method_call` asks it first: a namespace receiver makes the call
`type_namespace_call` — the member looked up (present, visible, a function;
else "`m.x` is a global, not a function", TYPE-007), recorded as `callee`
and as the node's new **`ns_decl`** slot (`expr_types.npk`), and typed by
**`type_direct_call`**, factored out of `type_call` so `f(x)` and `m.f(x)`
share one implementation of the contract rule, the purity rule, the async
rule, the generic instantiation and the argument check
(`check_gives_call_position` and `type_generic_call` take the callee's
declaration instead of re-deriving it from the node's slot `a`, which on a
method-call node is the receiver). `namespace_member` reads a value through
the same walk (nested paths work as values; a global its type, a function its
function type, an error constant `Error`; a module, struct, trait or macro
"is a module, not a value" — the identifier arm's sentence) and records
`ns_decl` on the member-access node. The emitter's `emit_direct_call` is
factored out of `emit_call` verbatim and `emit_method_call` reaches it when
`ns_decl` is set; `emit_decl_value` out of `emit_ident`, and `emit_member`
reaches it the same way (a global loads from its symbol, an error constant is
its code, a function its symbol); `addr_of` answers a global's place through a
path. The await path (`emit_child_frame`, `async_callee_name`) and the encoder
(`enc_call`) skips the receiver; the alias import sets the
module symbol's `inner` to its file's scope (D-273 §4), so `lib.f()` works
across files — moved up from step 1 because this step's program calls
through it.

**The plan's slot is `ns_callee`; the slot is `ns_decl`.** A namespace VALUE
access (`hidden.MAX`, `?! hidden.Boom`) has the same two readers to redirect
— the emitter's member read and the reach analysis's operand — so one slot
records what a path resolved a node to, on a method-call node the member
called and on a member-access node the member read. (`hidden.MAX` was typed
by the checker and died in the emitter as EMIT-002 before this step: no arm
knew a base that is not a value.)

**The plan's code is RESOLVE-004; the code is RESOLVE-003.** D-273 §3 and the
plan name "RESOLVE-004's private to"; `RESOLVE_PRIVATE` is
`NITPICK-RESOLVE-003` (RESOLVE-004 is the two-files ambiguity), and the tests
assert the code the compiler emits. Recorded here and in D-273's landing note.

**Every `ExprMethodCallExpr` reader in `src/`, classified** (`rg -n
ExprMethodCallExpr src`, 36 sites; a namespace call's receiver is a module,
never a value, and each reader either skips it or reads nothing from it):

| Site | Reads of the receiver | Verdict |
|---|---|---|
| `resolve.npk:220` | resolves the receiver's identifier (the module symbol — needed) and the arguments | no change |
| `type_members.npk:54` `type_access` | `contract_method_refused`/`pure_method_rules` read the receiver's recorded type and the callee | CHANGED: gated on `ns_decl == 0`; the direct call's rules ran in `type_direct_call` |
| `type_members.npk` `type_method_call` | types the receiver as a value | CHANGED: `namespace_path` first |
| `type_expr.npk:1868/1883` (the unwrap licence, `unwrap_callee_is_async`) | `callee_ft`, `callee` | no change: both recorded |
| `type_expr.npk:3688` (the await typer) | typed the receiver as a value | CHANGED: probes first |
| `analysis/bindings.npk:1205` | walked the receiver as a read; the arena `destroy` rule by its type | CHANGED: the receiver is not read when `ns_decl` is set |
| `analysis/escape.npk:1416` | the receiver as a borrow source/destination | CHANGED: `expr_none()` as the receiver when `ns_decl` is set |
| `analysis/locks.npk:256/283` (`mutex_acquire_level`, `sync_wait_level`) | the receiver's recorded type | no change: 0 for a namespace, no level |
| `analysis/locks.npk:317` (`call_target`) | re-found the method on the receiver's type | CHANGED: `ns_decl` first — the callee's `acquires` summary was never followed for a namespace call |
| `analysis/locks.npk:696` | walks the receiver expression | no change: an identifier naming a module symbol is no acquisition and no call |
| `analysis/suspend.npk:609` | the receiver's recorded type (a channel await) | no change: 0 |
| `analysis/suspend.npk:777` (generic) | `reg_use` of the identifier's symbol | no change: `reg_use` ignores a `SYM_DECL` symbol |
| `analysis/reach.npk:591` (generic) | walks the identifier | no change: an identifier registers nothing |
| `analysis/reach.npk` (the `drop` spawn arm) | only an identifier callee | CHANGED: reads the recorded callee for a method-call node (the found defect below) |
| `analysis/exhaust.npk:616` (generic) | walks the identifier | no change |
| `macro/expand.npk:293/961/1152` | clone, blank, expand — structural | no change |
| `ir_expr.npk:323` `gives_callee_of` | `exprtypes_callee` | no change: recorded |
| `ir_expr.npk:463/493` (stash, temporaries) | by node kind and callee | no change |
| `ir_expr.npk:837` (the dispatch arm) | `member_enum_decl`, the receiver's recorded type per kind | no change: a module symbol is no enum, type 0 matches no kind, the arm ends at `emit_method_call` |
| `ir_expr.npk` `emit_method_call` | emits the receiver | CHANGED: `ns_decl` first → `emit_direct_call` |
| `ir_expr.npk:4531` `await_callee_decl` | `exprtypes_callee` | no change |
| `ir_expr.npk:4569/4579` `emit_child_frame` | seeds slot 12 from the receiver | CHANGED: not when `ns_decl` is set |
| `ir_expr.npk:4679` `async_callee_name` | the recorded symbol, the receiver's type | CHANGED: the free-function path, the declaration's name |
| `ir_expr.npk:5332…5371` (channel/lock/condvar/barrier awaits) | the receiver's recorded type | no change: 0 |
| `ir_expr.npk:6396` (the `dyn` await) | the receiver's recorded type | no change: 0 |
| `ir_stmt.npk:2779` (frame slots) | the receiver's recorded type | no change: 0 |
| `ir_stmt.npk:2861` (the `gives` stash) | `exprtypes_callee` | no change |
| `ir_stmt.npk:2912/3086`, `ir_rules.npk:662/812` (generic scans) | walk the identifier | no change: an identifier reserves and collects nothing |
| `smt_encode.npk:866` `enc_call` | encoded the receiver as parameter 0 | CHANGED: no receiver term, `off` 0 |
| `smt_encode.npk:1743` `expr_children` | pushes the receiver as a child of a SITES walk | no change: an identifier, or a path of member accesses, holds no site and its term is discarded — and threading the node's id through this function to skip it moved its own `div-min` row's hash in `nitpick.obligations` (a parameter is a symbol of the encoding), which this subcycle must not do; the first pre-check of the manifest found it |
| `smt_encode.npk:1957/1998` | by node kind → `enc_call` | no change |
| `ast.npk:819`, `ast_kind.npk:106`, `parse_expr.npk:560` | the shape, the kind, the constructor | no change |

**Found on the way, fixed in this step.** (1) `Trait.method(recv, s)` passed no
callee to the argument check — `callee_decl_index(e, n)` reads the node's slot
`a`, the trait's name, and answered 0 — so a `move` parameter never demanded
`move(s)` through the qualified spelling: `Eat.eat(d, t)` against `move
string:s` was accepted bare where `d.eat(t)` refuses TYPE-046, the callee
dropped `t`'s body at its exit and the caller dropped it again at its own
(proven on `f071d43`: the same program exits 0 through `main`'s `exit`, which
runs no drops, and double-frees from a returning function).
`type_trait_qualified_call` passes `mdecl`; `trait_qualified_call.npk` gains
the TYPE-046 case and `trait_qualified.npk` the positive `move(t)` from a
returning function. (2) An async METHOD spawn — `drop j.run()` through an impl,
D-182's trait-method spawn — armed no `DeadlineExceeded`: the reach analysis's
spawn arm read only an identifier callee, so `failsafe` compiled without the
arm the join's deadline can hand it, while `drop run(3i32)` was refused
(proven on `f071d43`, the two forms side by side with no `await` in the
program). The arm reads the recorded callee for a method-call node now —
the namespace form's read — and `reach_method_spawn.npk` holds the method
form to REACH-002. (3) A struct, trait, module or macro read through a
module (`int32:v = hidden.P;`) was typed INVALID WITH NO DIAGNOSTIC and died in
the emitter as EMIT-002 (`namespace_member`'s silent `ns_yes(0)` for every
member kind but a function and a global); it says what the name is now, the
identifier arm's sentence (`mod_not_value.npk`).

**Recorded for the user (OPEN_DECISIONS).** S-49: a member-less `mod:name;`
after the header loads `name.npk` and binds `name` as a module symbol whose
scope is EMPTY (collection opens one over its zero members), so `name.f()`
now reports "module `name` has no member `f`" (TYPE-007 "a module, not a
value" before); whether the file-module import carries the loaded file's
scope, as the alias does under D-273 §4, is the user's — recommended yes
(measured: no program in the tree calls through the form; `entry_in_module.npk`
uses it to load). S-50: an error constant declared inside an inline module
hashes under the FILE's name (`error_code_of` uses the file's basename, D-179),
so its `failsafe` arm is spelled `(file.Name)` — `mod_qualified.npk` writes
`(mod_qualified.Boom)` for `hidden`'s `Boom` — and `(hidden.Boom)` would hash
a name no constant has; whether an inline module qualifies the identities it
declares is the user's.

**Tests.** `inline_mod.npk` exits 7 through `hidden.fetch(3i32)`;
`mod_qualified.npk` (+ the `mod_qualified_lib.npk` fixture): the nested path,
the alias call and read, the module binding, the awaited and the spawned
`async` member, the `pure never fails` member in a `requires`, the error
constant through the path; `mod_not_value.npk` (TYPE-007 ×4, TYPE-019 ×2);
`mod_private_qualified.npk` (RESOLVE-003 ×3 — in `tests/types/rejection/`,
since the CHECKER refuses the qualified spelling and `tools/resolve_check.npk`
runs the loader alone); `req_qualified.npk`; `reach_method_spawn.npk`; the two
trait-qualified files. `nitpick.obligations` did not move.

**Harness (2026-09-10, worktree p0 at `a510fbf`, run detached beside step 1's rerun: every stage green, 52/52.** 235 real-backend programs (each also through `opt -O2`), 16 module / 88 type / 30 analysis / 19 expansion / 9 derive rejection files under D-237's exact matching, 22 acceptance files, 15 conformance and 32 frontend units, 662 real-parser checks, 2 objects, 2 runtime-floor tests, 67 verified programs, 7 cost units (the compiler's own build at `allocated=420,170,851 peak_live=123,256,061 count=9,356,775` under the 225,827,260 ceiling), `selfhost`, `repro` and `absent-fact` green, every whole-tree check green (104 prelude functions kept), the compiler's own 184 obligations (170 discharged, 8 open) matching `nitpick.obligations` with 170 guards elided and the verified compiler rebuilding itself byte-identically, and `parity`: 1,215 verdicts agreeing between the two runners, `npkc` byte-identical, the verified compiler byte-identical — every count 1.5.4's close's but the programs (234 → 235: `mod_qualified.npk`; its fixture is built, never run), the type rejections (86 → 88), the analysis rejections (29 → 30), the verified programs (66 → 67), the real-parser checks (656 → 662) and `parity` (1,204 → 1,215 verdicts). The first run of this prefix, at `2361d25`, was red on ONE whole-tree belt alone — `check_slot_sites_agree`, whose table paired `check_args` with `emit_call`, which no longer calls `emit_fit` — and green on every stage; the pairing follows the argument loop into `emit_direct_call`, and this is the run on the amended commit.

### Step 1 — LANDED (2026-09-10): `use` over a module path, the alias's scope, `std`

**What landed.** `apply_logical_import` (`module_graph.npk`): a `use` path
whose first segment is not `std` resolves against the module symbols the
importing file's scope holds — the first segment an inline module, an alias
or a `pub mod` an earlier import bound (`module_symbol_inner`, the loader's
side of the checker's `module_symbol_scope`), every later segment a module
EXPORTED by the one before, and the last module's scope bound through the
SAME binders the file forms use: `bind_wildcard` for `use hidden.*;`,
`bind_named` for `use hidden.{f, Point};` and `use hidden.f;`, and
`bind_or_report` over the module's own declaration for `use core.math as cm;`
(the alias form is one of "every form the file forms have", D-273 §2). A
first segment naming no module symbol is RESOLVE-002 "cannot find module `x`
in this scope" (R-1); one naming a function or a global "`x` is a function,
not a module" (RESOLVE-002); a later segment the module lacks is the named
import's RESOLVE-007 "`core` has no `nosuch`", a private one its RESOLVE-003
with the note at the declaration, and one naming a non-module "`core.g` is a
function, not a module". `std` first is the standard library's and binds
nothing, as it always did (`whole_grammar.npk`'s `use std.math.*;` and
`mod_use_unknown.npk`'s `use std.collections.*;` both stay silent).

**One field, wherever a module symbol is bound (§4).** `bind_or_report` takes
the source symbol's `inner` and sets it on the bound symbol (idempotent under
the fixed point); the alias arm hands it the file's scope — step 0's
post-hoc lookup is retired — `bind_named` its symbol's, and `bind_wildcard`
copies it, so a `pub mod` bound by `use "./mod_helpers.npk".*;` answers
`helpers.f()` across files.

**`std` in D-239's table.** `owned_name_error` refuses `std` declared as a
module — an inline `mod:std = { … };`, an import `mod:std;`, and a file's
header, which the owned-names walk visits as item 0 of the root — with
RESOLVE-001 (`std_declared.npk`).

**R-2 without a second pass.** The plan asked `apply_imports` to run
namespace and file imports before logical ones so order never matters. The
fixed-point loop already gives the property: every round re-applies every
import until a round adds no binding, and the ONE reporting round runs after
the point — so `use lib.*;` written above `use "./lib.npk" as lib;` finds
`lib` unbound on the first round (silently), bound on the second, and R-1's
refusal can only fire once the point is reached. A second pass would be a
second mechanism for one property (the user's standing stance against two
mechanisms for one job); the pair `tests/accept/order_alias_above.npk` /
`order_alias_below.npk` over `order_alias_lib.npk` proves the property is
held, in both orders, in silence.

**Found on the way, fixed in this step — three holes in the inline module
itself, none reachable before a program could name an inline module's
members.** (1) The checker resolved a BODY's type names inside an inline
module in the FILE's scope: `e.scope` is set once when the typer is built and
`check_decl`'s module arm passed `inner` only as its parameter (the
signature's scope), so `S:s = S{ … }` in `m`'s own function reported "there
is no type named `S`" while the signature saw it — `e.scope` follows the walk
now, saved and restored around the members. (2) The emitter wrote no type
header for a struct or enum declared inside an inline module: `emit_type_headers`
visited a file's top level only, so `Pair` was used by its D-156 name and
never defined (`llc`: "cannot allocate unsized type") — `emit_headers_in`
descends with the module's scope, as the function and global walks do. (3)
The emitter's own resolver read the file's scope the same way
(`emit_nested_module` left `x.scope` at the file's), so with the header
present a body inside the module still could not lower a local of the
module's struct (TYPE-001 from the emitter's resolver, then EMIT-002) — and
`mod_qualified.npk` passed only because `use hidden.*;` had bound `Pair` into
the file scope as well; `x.scope` follows the descent now. `inline_struct.npk`
holds all three: a struct declared and used inside `m` with nothing importing
it, exiting through the value it builds; `mod_qualified.npk`'s `hidden.mk(3i32)`
builds a `Pair` inside its module and reads it in `main`.

**Tests.** `mod_qualified.npk` grows `use hidden.*;` (`fetch(1i32)` bare,
`MAX`, the TYPE `Pair` in a struct literal — the only way an inline module's
types are named from outside), `use core.math.{sq};`, `use core.math as cm;`
(`cm.sq(1i32)`), `helpers.f()` through `use "./mod_helpers.npk".*;` (a
new fixture) and `hidden.mk(3i32)`; it exits 79; `inline_struct.npk` exits 3. `tests/modules/rejection/` gains
`mod_private_use.npk` (RESOLVE-003 + note), `mod_use_unknown.npk`
(RESOLVE-002 ×3, RESOLVE-007) and `std_declared.npk` (RESOLVE-001), and its
README the three rows. `nitpick.obligations` did not move.

**Harness (2026-09-10, worktree p1 at `65aa760`, run detached beside step 0's rerun and then step 2's run: every stage green, 52/52.** 236 real-backend programs (each also through `opt -O2`), 19 module / 88 type / 30 analysis / 19 expansion / 9 derive rejection files under D-237's exact matching, 25 acceptance files, 15 conformance and 32 frontend units, 670 real-parser checks, 2 objects, 2 runtime-floor tests, 67 verified programs, 7 cost units (the compiler's own build at `allocated=421,223,749 peak_live=123,349,537 count=9,370,395` under the 225,827,260 ceiling), `selfhost`, `repro` and `absent-fact` green, every whole-tree check green (104 prelude functions kept), the compiler's own 184 obligations (170 discharged, 8 open) matching `nitpick.obligations` with 170 guards elided and the verified compiler rebuilding itself byte-identically, and `parity`: 1,230 verdicts agreeing between the two runners, `npkc` byte-identical, the verified compiler byte-identical — every count step 0's but the programs (235 → 236: `inline_struct.npk`; `mod_helpers.npk` is a fixture, built and never run), the module rejections (16 → 19), the acceptance files (22 → 25), the real-parser checks (662 → 670) and `parity` (1,215 → 1,230 verdicts). The first run of this prefix, at `82035ce` over the unamended step 0, was red on the same slot-sites belt alone and green on every stage, `parity` included (1,230 verdicts); this is the run on the rebased commit.

### Step 2 — LANDED (2026-09-10): the docs

MODULE_REFERENCE §1.1 says a module symbol means one thing wherever it stands
(the qualified call as a direct call of an inline module's, an alias's or a
nested path's member; `use m.*` and its forms as the only way an inline
module's types are named from outside; a module or a type in value position
refused), §2.2 is rewritten (a logical path resolves by its FIRST segment:
`std` the library's, any other a module symbol in scope, every later segment
a module the one before exports, the last module's names bound through the
file forms' binders; R-1's refusal by name, RESOLVE-007 and RESOLVE-003 for
a later segment; order never matters by the fixed point; `std` owned), and
§3 carries the private-from-either-spelling rule. D-273 carries its landing
note — with the code correction: the private code is RESOLVE-003, and the
-004 the decision text names is the two-files ambiguity — and D-239 a dated
note admitting `std`. DEF-31 reads FIXED in OPEN_DECISIONS; S-49 and S-50
stand there for the user. The 1.5 README row and ROADMAP's 1.5 row read
DONE; CLAUDE.md's status reads as the close (the five defects found on the
way, S-49/S-50 recorded) and gains a gotcha (a module member is called
`m.f(x)`, read `m.MAX`, imported `use m.*;`; an alias carries the file's
scope; a private member refuses from either spelling; the two open items).
The plan's own text keeps its "RESOLVE-004" and "`ns_callee`" — settled text
is annotated, not rewritten (the D-085/D-202 pattern); the step records
above carry both corrections. The workbench's `check_refs.py` holds 63. No
snapshot refresh: `src/` adopts none of it (D-205). **1.5.4c CLOSES.**

**Harness (2026-09-10, worktree p2 at `6d3e0ed`, run detached, alone after step 1's rerun: every stage green, 52/52.** 236 real-backend programs (each also through `opt -O2`), 19 module / 88 type / 30 analysis / 19 expansion / 9 derive rejection files under D-237's exact matching, 25 acceptance files, 15 conformance and 32 frontend units, 670 real-parser checks, 2 objects, 2 runtime-floor tests, 67 verified programs, 7 cost units (the compiler's own build at `allocated=421,223,749 peak_live=123,349,537 count=9,370,395` under the 225,827,260 ceiling), `selfhost`, `repro` and `absent-fact` green, every whole-tree check green (104 prelude functions kept), the compiler's own 184 obligations (170 discharged, 8 open) matching `nitpick.obligations` with 170 guards elided and the verified compiler rebuilding itself byte-identically, and `parity`: 1,230 verdicts agreeing between the two runners, `npkc` byte-identical, the verified compiler byte-identical — every count step 1's, as a docs-only prefix must give. **1.5.4c CLOSES.**

> **Note (2026-09-10):** S-49 and S-50, recorded by step 0, were ratified the same day as **D-274** and **D-275** and land at 1.5.4d.
