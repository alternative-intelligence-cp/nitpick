# 1.4.9 — the cycle close: self-hosting declared, the snapshot refreshed, the docs synced, the folder archived

> Opened 2026-09-02 by the fresh session the 1.4.8b close briefed by message.
> Plan first, then the record — the 1.3.8 and 1.4.8b shape. Before this file
> was written, the user ratified S-11 and S-12 and the outgoing session landed
> them on main as **1.4.8c** (D-239, D-240; `1.4.8c.md`) under its own full
> harness, ahead of this close's refresh so the refreshed snapshot carries the
> rule. The close waited for that commit (D-228 R2: one writer for `src/`).

## Where you are

- HEAD at open: `80784f3` (housekeeping, docs only — the user's tidy-up that executed the repository half of `meta/SWITCH.md`), on top of `d0e597c` (1.4.8c) and `e3bbe1f` (1.4.8b step 2).
  The tree is clean, no worktrees, nothing running, **nothing pushed since the
  cycle opened** — the push is per cycle (ROADMAP), and this close is the
  cycle's end, so it is this subcycle's.
- The last full harness (1.4.8c's, on the committed tree): every stage green,
  `ok 58 test(s) passed`, `parity` agreeing between the two runners with
  `build/npkc` byte-identical, `check_decisions_current` reporting nothing.
- Nothing is pending on the user for the close. S-11 and S-12 were the last
  open rows of §2e and are settled and landed.

## What a close is

The 1.4 README's row: *self-hosting declared against D-202's criterion, the
snapshot refreshed, docs synced, the cycle to `done/`.* From the precedents
(1.3.8, the 1.4.7 close, 1.4.8 step 3) that is seven things, in this order,
each with its reason:

1. **The declaration under D-202.** Self-hosting is the fixpoint of the
   compiler's emission of itself: the first current-source compiler and the
   one it builds must emit the compiler byte-identically. The `selfhost` stage
   has measured exactly that on every full run since 0.8.1 (D-202: the harness
   was right, the spec sentence was the defect), so the declaration is a
   RECORDED STATEMENT with the run's evidence, not new code. It is recorded in
   five places, each the place a reader would look: this file (the numbers),
   the 1.4 README (the cycle's own account), ROADMAP's 1.4 row, one dated line
   under BUILD_REFERENCE §6's criterion, and a dated annotation on D-202.
   CLAUDE.md's status line says it in one sentence.
2. **The snapshot refresh** — `bootstrap/seed/README.md` "Refreshing it",
   verbatim in shape: from the tree root, `src/main.npk` spelled RELATIVELY,
   stage2 == stage3 the criterion, STAGE 2 installed, sha256 and byte count
   into `STAMP` with `source-commit` = HEAD at the refresh (1.4.8c's; `src/`
   does not change in the close, so the snapshot IS the emission of that
   commit's source). Before the harness, because the run's first act is
   building `src/` with the snapshot and the `repro` stage asserts the STAMP
   and the absence of absolute site rows. D-203: the snapshot refreshes at
   cycle closes, with the push.
3. **Doc sync** — the list in "The doc sync, item by item" below. The target
   for `check_decisions_current` is an empty list WITH 1.4 under `done/`,
   measured standalone before the run (the instrument reads `done/`).
4. **The archive.** `meta/roadmap/1.4/` → `meta/roadmap/done/1.4/` with this
   file inside, `__pycache__` removed first (ignored, untracked, not an
   archive). BEFORE the harness: `check_rung_names_open_cycle` and
   `check_decisions_current` read `done/`, so the run must see the archived
   state — measured in a scratch copy first: no rung names 1.4 (the nine that
   remain name 1.5), and the candidate list is empty.
5. **HANDOFF.md retires** (its own header: "Retire this file at the 1.4 cycle
   close"). Nothing in it evaporates: the re-homing map is below.
6. **One full harness on the final tree**, `--verdicts` written for the record;
   `repro`, `selfhost`, `parity` green; then ONE commit in the house style and
   the per-cycle push (`git log origin/main..main` read first; the local
   `refs/notes/commits` compared with origin's — they were equal at
   `84b7a1d` when the close opened).
7. **The briefing for 1.5.0**, by message to the idle session, ending with an
   invitation to ask; questions answered before this session closes.

## The doc sync, item by item

Each item: the file, the change, the reason. Nothing under `done/` and no
settled decision text is rewritten — dated annotations only (the D-085/D-202
pattern).

- **CLAUDE.md** — the status line to "cycle 1.4 COMPLETE"; "CYCLE 1.4 … is
  UNDERWAY" to COMPLETE with the `done/1.4/` pointer; the 1.4.9 sentence at
  the tail of the 1.4 paragraph (the declaration, the refresh's numbers, what
  the close synced, and that 1.5 is next); the directory block's `tests/`
  comment counted FOUR rejection suites and names four while six exist
  (`expansion/`, `derive/` — the same paragraph two screens down already says
  "six"); the `harness/` comment said "runs until `npkg` parity", and parity
  is a stage on every run now — it runs beside `npkg` until `meta/SWITCH.md`
  retires it (D-206); the reserved-words table gains `fails` and `end` (each
  cost the 1.4.8 executor a build); and a short "rules that bite" list under
  "Building and testing" absorbs the HANDOFF's tool-shaped rules that live
  nowhere else (strings are move-only owners; the walkers-total excuse
  tables; `gdb -ex "break npk_trap"`; a compiler fix reaches `tools/` only
  through the snapshot).
- **meta/roadmap/1.4/README.md** — the 1.4.7b row was never marked DONE
  (1.4.7b.md's own record says every step landed); the 1.4.9 row marked DONE
  with the record; a closing note under the title; and a new section **"What
  cycle 1.4 taught"** — the fuller account, each lesson with the incident
  behind it, absorbing the HANDOFF's "what this cycle proved", "how to read a
  plan", "four things this stretch cost", the family-conversion method, and
  the width.
- **meta/roadmap/ROADMAP.md** — the 1.4 row closed with the cycle's record in
  the 1.3 row's style; a compact **"What cycle 1.4 taught"** after the Phase C
  table (the Phase A precedent: `What cycle 0.4/0.5/0.6 taught` live in this
  file); the ordering note "Verification is 1.4" was missed by both
  renumberings (it is 1.5) — annotated, not rewritten.
- **meta/roadmap/ORCHESTRATION.md** — §4's calibration paragraph gets the
  measurement it asked for (six concurrent full runs green at 1.4.7b; twelve
  still owed, the orchestrator's run before the first 12-wide window); §8's
  item 3 is answered; §6 gains the escalation protocol the HANDOFF carried
  (write the findings into the subcycle file before escalating; the escalated
  session in its own worktree — R1's incident). The file is D-228's rationale,
  so these are dated notes, not rewrites of R1–R9.
- **bootstrap/seed/README.md** — under the D-205 rule, the mirror direction
  the HANDOFF recorded: `build_tool` compiles `tools/` with the SNAPSHOT, so a
  compiler fix does not reach the checker the harness runs until the snapshot
  carries it (measured at 1.4.7: npkc-built checker rc=0, snapshot-built rc=3,
  same sources). `STAMP` is written by the ritual.
- **meta/specs/BUILD_REFERENCE.md §6** — one dated line under the criterion
  paragraph: declared at 1.4.9, with the measurement.
- **meta/specs/DECISIONS.md** — D-202 gets a dated "DECLARED" annotation with
  the close's numbers (the D-203 "LANDED" note is the shape). No other
  decision text moves.
- **meta/SUBSET_1.md:26** — "verified in cycle 1.4" was renumbered once and
  missed once; verification is 1.5–1.6. Annotated in place.
- **nitpick.toml** — the `[verify]` note said the tooling arrives "in cycle
  1.5 (verification; `npkg` itself in 1.4 …)": `npkg` landed at 1.4.8 and
  refuses `verify` by name until 1.5 (`npkg/main.npk`); the note says so.
- **The D-233 doc-sync batch** (OPEN_DECISIONS §6, "sweep with the next
  doc-sync pass" — this is that pass): the six living references that still
  name Astrée as the verification endpoint each read "the evidence campaign
  (D-233)" now — `TYPE_REFERENCE.md` ("tractable for Astrée"; "the single
  Astrée run"), `TRAITS_REFERENCE.md` ("Astrée analyses monomorphized
  output"), `CONCURRENCY_REFERENCE.md` ("the single Astrée run"),
  `IO_REFERENCE.md` ×2 ("io_uring refused before Astrée" — D-184's rule reads
  "before the evidence campaign closes"), `meta/SWITCH.md` ("Astrée-facing
  work"). The §6 paragraph is marked swept.
- **meta/roadmap/OPEN_DECISIONS.md** — §3's heading ("Decisions blocking 1.4")
  annotated all-settled; §7's ordering item 4 ("C-10…C-12 before 1.4
  anything") struck as done; **G-2 struck and marked SETTLED by D-231**, whose
  own text opens "G-2, answered in two halves" while the row still read
  "next free" — a stale-row defect this close found by reading the table;
  §6's D-233 batch marked swept.
- **meta/LAYOUT.md** — the tree diagram still drew `bootstrap/runtime/` with
  a "re-homes at 1.4.6" note and the harness "until parity is proven"; the
  prose two sections down was already past tense. The diagram now shows
  `runtime/` at top level and the harness beside `npkg` until SWITCH.md.
- **meta/roadmap/1.5/README.md** — "The state this cycle starts from" said
  the spawn primitive "arrives with 1.4.8": it has — `clone_exec` in the
  floor, `lib/nproc.npk`'s `proc_spawn`/`proc_wait`/`proc_reap` (both pipes
  captured, every wait bounded, a deadline kills-reaps-retires), which is what
  1.5.0 spawns z3 through. Annotated with the landed names and a "start here"
  list for the executor, since HANDOFF.md no longer exists to carry one.

## HANDOFF.md — where each part went

| Section | Disposition |
|---|---|
| Where you are; the decisions settled this cycle; where things live; first action | Superseded by CLAUDE.md's status and the cycle records; retired. (The 1.5.0 executor gets a ONE-TIME `meta/roadmap/1.5/HANDOFF-1.5.0.md` instead of a message, because the folder rename at this close's end made the message-based handoff impossible at the boundary — see the record's last section.) |
| Four things this stretch cost | The refresh criterion is in `bootstrap/seed/README.md` since 1.4.7; the snapshot-carries-the-fix rule joins it there; REACH's import scoping is in `diag_writer.npk`'s header and TYPE-037 is a checker rule — all four also listed in the 1.4 README's lessons. |
| Converting a collection family — the method | 1.4 README "What cycle 1.4 taught" item 10, beside `convert_family.py`, which archives with the folder. |
| Running harnesses in parallel — THE WIDTH IS 2 | Superseded by 1.4.7b's calibration; ORCHESTRATION §4 carries the measurement and the owed 12-wide run. |
| OWED | Every item closed (1–8 in the file itself; the 1.4.7-close items by D-234/D-235/D-236, the width by 1.4.7b step 4, the `uint512` fold by 1.4.7b step 3, the provenance headers by 1.4.7b step 0; D-230/D-231 landed; D-232 superseded). The "checked and NOT a defect" measurement (a borrow-carrying concrete coerced to `dyn` is caught by the escape analysis, not the coercion — four probes) is carried into the 1.4 README so it is not re-derived. |
| RETIRED: 1.4.6 | 1.4.6.md and 1.4.4.md carry it; the owed `chan_elem_ok` item closed as OWED-8/TYPE-057. |
| What the 1.4 subcycles should change about how you read a plan | ROADMAP "What cycle 1.4 taught" (compact) and the 1.4 README (full, with every example). |
| The rules that bite; what you must NOT do | CLAUDE.md's "rules that bite"; the rest is already CLAUDE.md, the pinned memories, D-228 R6 and the seed README. |
| Escalation | ORCHESTRATION §6 (the protocol); the Fable-reserve budget figure was transient. |
| What this cycle proved | ROADMAP and the 1.4 README, as above. |

## What the close does NOT change

- No `src/`, no test, no harness code. The instruments were current at 1.4.8b
  and 1.4.8c; the close's measurement is the run. Anything the run finds is a
  defect escalated under D-228 R6, not a close item.
- S-11 and S-12 are 1.4.8c's, not this file's.
- The 12-wide calibration stays owed, with its owner named in ORCHESTRATION §4.
- The harness does not retire: D-206 — both runners run through 1.5, the
  harness the run whose result means the suite is green, retirement under
  `meta/SWITCH.md`. BUILD_REFERENCE §7.1 already says so.

## Acceptance

One commit under a full harness on the final tree: every stage green,
`repro` (the STAMP matches, zero absolute site rows, cwd-independent, llc
deterministic), `selfhost`, `parity` green, `check_decisions_current` empty
with 1.4 archived; `origin/main == main` after the push; the briefing sent and
its questions answered; the tree clean and nothing running.

## Execution record

### The tree at open, and the hold (2026-09-02)

The close was briefed by message at the 1.4.8b close and then asked to HOLD:
the user had ratified S-11 and S-12, and the outgoing session landed them on
main as 1.4.8c (`d0e597c`, its own full harness: 58/58, parity 906) ahead of
this refresh so the snapshot carries the rule — D-228 R2, one writer for
`src/`. The user's tidy-up followed as `80784f3` (docs only): the repository
half of `meta/SWITCH.md` executed early — the prototype and its docs archived
on GitHub as `nitpick-prototype`/`nitpick-prototype-docs` and every predecessor
under `REPOS/ARCHIVE/`, this repository renamed
`alternative-intelligence-cp/nitpick` with `origin` repointed, an empty
`nitpick-docs` created for the official documentation after the compiler is
finished. The hold (~2.5 h) was spent drafting every close document outside
the tree and dry-running the two instruments that read `done/` in a scratch
copy with 1.4 archived: zero candidates, zero failures. Nothing the two
commits changed collided with the plan; three things they added to it are
recorded at the end of this record.

### Step 2 — the refresh

The README ritual, verbatim in shape, in a script that echoes each step and
refuses to install on a failed comparison (`refresh.sh`, scratchpad
intermediates instead of `/tmp`; nothing else differs): from the tree root,
`src/main.npk` spelled relatively.

- the builder (the `d210a56` snapshot, 1.4.8 step 2b's) compiled the current
  `src/` in 179 s;
- **stage2 == stage3 at 15,631,627 bytes** — the fixpoint holds; zero absolute
  site rows in stage2 (the harness's own pattern, applied before installing);
- stage 2 installed; `STAMP` source-commit `80784f3` (the ritual's
  `git rev-parse HEAD`; `src/` is identical to `d0e597c`'s), sha256
  `9ce0ec8d3de5b2c83da4a1f11d3f89965728f6cf938f70042ea053eff5defaaf`,
  15,631,627 bytes; 553 s wall clock.

For the record: stage1.new (the OLD builder's emission of the current source)
differs from stage2 by 84 lines in seven hunks — expected by construction,
since the snapshot being refreshed predates 1.4.8 step 4, and every hunk
traces to that step: six are the PRELUDE's `byte_reader_open` and
`byte_writer_create` (the prelude's two `open` calls, which the compiler's own
emission carries because the prelude is compiled into every program), where
the old backend folded the three flag constants into one literal
(`i64 526336`) and the current one loads `O_RDONLY`/`O_NONBLOCK`/`O_CLOEXEC`
from the prelude's generated constants and ORs them (the typed flag families,
D-230); the seventh is `@npk.site.lines`, whose prelude rows moved with the
lines those calls now occupy. 1.4.8c itself, refusal-only, moved nothing, as
its record predicted. The criterion is stage2 == stage3 and it held;
stage1.new is the file the README says NOT to install, and this refresh is
the second time that sentence earned its place.

### Step 3 — the doc sync

Thirty-nine edits in eighteen files, applied by one script that asserted every
anchor matched EXACTLY ONCE before writing anything — every anchor a whole
sentence or block, never a line number (item 4 of "What cycle 1.4 taught").
Then CLAUDE.md's four `meta/roadmap/1.4/` citations re-pointed at `done/1.4/`
(the 1.3 precedent: living documents follow the move; DECISIONS.md's six such
citations are settled text and stay as written). Two things the sync found by
reading rather than by plan: **G-2** (OPEN_DECISIONS §5) still read "next
free" though D-231's own text opens "G-2, answered in two halves" — struck and
marked SETTLED; and ROADMAP's ordering note "Verification is 1.4" had been
missed by BOTH renumbering sweeps (it is 1.5) — annotated, not rewritten. The
D-233 batch (§6) is drained: six living references and `SWITCH.md`'s line.

### Steps 4 and 5 — the archive and HANDOFF

`__pycache__` (ignored, untracked) removed; `git mv meta/roadmap/1.4
meta/roadmap/done/1.4` with this file inside; `git rm meta/roadmap/HANDOFF.md`
— the re-homing map is above, and nothing in the file lacks a home. Both
`done/`-reading instruments run standalone on the REAL tree after the move:
`check_decisions_current` zero candidates, `check_rung_names_open_cycle` zero
failures (the nine remaining rungs name 1.5).

### Step 6 — the run
Full harness on the final tree (2026-09-02 → 03, ~100 min, `--verdicts`
written): every stage green, `ok 58 test(s) passed`; `grammar` 505 real-parser
checks and `modules` 11 (1.4.8c's `owned_names.npk` in both);
`selfhost    stage 1 rebuilt itself byte-identically`;
`repro       emission cwd-independent, llc deterministic` — the STAMP matched
and the site table carried zero absolute rows, which is what validates a
refresh; `absent-fact` green; **`parity      906 verdict(s) agree between the
two runners; npkc byte-identical`** (1.4.8c's count — nothing in the close
changes a verdict); `check_decisions_current` printed nothing with 1.4 under
`done/`. The verdict list is `.internal/verdicts_149.txt` (906 lines) and the
log `.internal/harness_149.log`. No `src/`, test or harness code changed in
the close; the run is the close's measurement, and it measured the tree that
is committed.

### Step 6b — the commit and the push
One commit in the house style (the numbers above in its message), then the
per-cycle push to the renamed remote `git@github.com:alternative-intelligence-cp/nitpick.git`
— 51 commits since `831ccf3`, the whole cycle from 1.4.0's ratification to this
close; its dry run succeeded before the run and `refs/notes/commits` was
already equal on both sides (`84b7a1d`). `origin/main == main` is checked
after the push, and the rename below is the last act.

### Three things the two commits added to the close

1. **The local folder rename**, `REPOS/nitpick-native` → `REPOS/nitpick`,
   the user's ask relayed by the outgoing session and recorded in HANDOFF's
   last "where you are": the LAST act, after the push, because every path in
   both live sessions is under the old name. Done by `rename_last_act.sh`
   after the push — it refuses if the tree is dirty, unpushed, or has a runner
   in it — and the 1.5.0 session is started in the renamed folder. That made
   the message-based handoff impossible at this one boundary (the incoming
   session cannot exist before the rename, and the rename cannot precede the
   push), so the brief is a ONE-TIME FILE, `meta/roadmap/1.5/HANDOFF-1.5.0.md`,
   committed with the close and retired when 1.5.0 closes; future cycles hand
   off by message as before (the user's call, 2026-09-03).
2. **Claude Code's per-project memory directory** is keyed by the path
   (`~/.claude/projects/-home-randy-Workspace-REPOS-nitpick-native/`).
   Decision on contact: **copied** to the new key, not renamed — two live
   sessions (this one and the outgoing one) hold their transcripts under the
   old key, and a rename would pull the directory out from under both; the old
   directory is the user's to remove once both sessions are closed.
3. **`tag.sort = v:refname` on `nitpick-docs`** when it is first cloned
   (SWITCH.md's tag-ordering note; this repository already has it) — not this
   repository's act; recorded here so it is not lost.
