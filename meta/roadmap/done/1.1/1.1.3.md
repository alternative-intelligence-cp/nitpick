# 1.1.3 — `Duration`, the clock, and the deadline substrate (D-176)

**Cycle 1.1.** B-2 settled and built: the type, the clock, the pinned code —
everything 1.1.4+ takes a deadline through, before any of it exists to wait.

## What it builds

- **D-176 settled** (B-2 struck): `Duration` = prelude `{ int64:ns }` with
  `duration_ns`/`duration_ms`/`duration_secs` (`never fails`; overflow traps
  via ordinary int64 arithmetic); deadline parameters are RELATIVE spans named
  `within`; no user-facing `Instant`.
- **`mono_now() → int64`** — CLOCK_MONOTONIC nanoseconds through the floor
  (`@npk_mono_now`, syscall 228 with a stack timespec; the impossible-failure
  branch traps via D-061's posture). Registered three ways — npkrt.ll,
  `rt_sig`, the seed's RUNTIME — plus the declare list and the builtin column
  (`never fails`).
- **`DEADLINE_EXCEEDED` pinned at −4107** in the D-141/D-142 space: one value
  for both channels — the catchable `Result` error a timed wait returns, and
  the trap a JOIN raises when a task outlives its mandatory deadline.
- Executor waits (1.1.5) are pinned ABSOLUTE-monotonic
  (`FUTEX_WAIT_BITSET | FUTEX_CLOCK_MONOTONIC`), so re-arm loops cannot drift.

## Outcome

`tests/backend/programs/duration.npk` runs to exit 42 under the real backend:
`mono_now()` strictly non-decreasing and positive, the three constructors
exact, spans ordering with one compare. CONCURRENCY_REFERENCE carries the
substrate note; D-142's table carries −4107; the B-2 row is struck. The
fixpoint holds; the full harness is green.
