# The probes `1.6.0.md` §1 was written from (2026-09-25, on `c3bdae2`)

Every number in the plan's "Where it starts" comes from one of these
commands, run on this machine with nothing else running. `SCR` is the
session's scratch directory; the emissions came from
`bootstrap/harness/quickemit.py --keep` (the two programs) and the ladder's
`build/npkc.ll` (the compiler). Tools were the clones the workbench already
held (`REPOS/nikos` Debug build; `REPOS/alive2` built 2026-04-15) and the
probe builds this planning made under `~/.local/src/` (Clam master at LLVM
18 with crab/sea-dsa/llvm-seahorn; NIKOS v2.4.0 Release in a worktree;
Alive2 `02ec3af8` Release; z3 `ddb49568` as a static library) — step 1
rebuilds all of them through `engines.sh` into the versioned directories.

## The emission

```
python3 meta/roadmap/1.6/tools/census.py --assemble build/npkc.ll .internal/quickemit/p_dyn_slots_npk.ll runtime/npkrt.ll
```
- `!dbg` 0, `target datalayout` 0, `target triple` 1 in each; `module asm` in the floor only.
- Opcodes and intrinsics as §1 lists them; `with.overflow` call sites in `build/npkc.ll`: 2,810.
- `llvm-as-14`: `ptr type is only supported in -opaque-pointers mode`, rc 1/74. `llvm-as-18`, `llvm-as-20`: rc 0 on all.
- `opt -O2 -S` (20.1.2) on any emission adds `target datalayout = "e-m:e-p270:32:32-p271:32:32-p272:64:64-i64:64-i128:128-f80:128-n8:16:32:64-S128"` to its output.

## NIKOS (`REPOS/nikos`, v2.4.0 = `94b54c2c`; `NK` = its `build/` or the Release worktree's `build-rel/`)

```
/usr/lib/llvm-20/bin/llvm-as dyn.ll -o dyn.bc
$NK/frontend/llvm/ikos-pp -opt=basic -entry-points=main dyn.bc -o dyn.pp.bc        # 0.01 s
$NK/frontend/llvm/ikos-import --format=no dyn.pp.bc                                  # "warning: input module has no debug information"
$NK/analyzer/ikos-analyzer -a=boa,dbz,nullity,uva,sio,uio,shc,poa -d=interval --entry-points=main --proc=inter \
    --display-checks=all --name-values dyn.pp.bc -o dyn.db                          # Release: 0.04 s, 18 MB
sqlite3 dyn.db "select status, count(*) from checks group by status"                 # 0|1058 1|53 3|134  (0 ok, 1 warning, 3 unreachable)
```
- Without `--name-values`: names like `%0x56deaaf45e40`; with it, two runs `diff` identical (rc 0).
- Warnings: `check_mem_access(pointer=%main.t8, access_size=4): no points-to information for pointer`,
  `check_null_dereference(%main.t8): pointer may be null`, the same on `%npk.dyn_slots.vt.Cat.Speaks.say.1` (a vtable slot).
- `[unreachable]` on `check_mem_access(call %0x…(%d))` — the indirect vtable call.
- Per function (`select f.name, c.status, count(*) … group by f.name, c.status`): `main` 620/30/93, `npk.drop.1920` 168/0/16,
  `speak` 34/0/8, `choose` 14/2/5, `twice<int32>` 27/1/4, `Holder.compare` 44/1/8, `boxed` 12/2/0, `broken` 3/0/0,
  `Cat:Speaks.say` 12, `Dog:Speaks.say` 5, `vt.Cat.Speaks.say` 84/12/0, `vt.Dog.Speaks.say` 35/5/0 (ok/warning/unreachable).
- `rg -n -i "with_overflow|with\.overflow" frontend analyzer ar` → nothing. `rg "llvm\.assume|ikos_assume" frontend/llvm/src` → only `__ikos_assume`.
- The compiler: `llvm-as-20 npkc.ll`; `ikos-pp -opt=basic -entry-points=main` 1.76 s 200 MB; the analysis (inter, intervals) 6.16 s 448 MB;
  `select count(distinct s.function_id) from checks c join statements s on c.statement_id=s.id` → 1; the checks: 77 ok, 2 warning.
  `main` after preprocessing is 36 lines and calls `@npk_exec`, `@npk_rq_push`, `@npk_run_until`. `-opt=none`: 8.91 s 692 MB, the same one function.
- `--proc=intra -j 1` over the compiler: started 07:45; still running at 16 minutes and 819 MB when the plan was written (the record appends it).
- Whole program: `llvm-link -S dyn.ll runtime/npkrt.ll` gives 190 defines where 28 + 187 = 215 (25 dropped, `npk_start_main` among them);
  `ikos-analyzer … --entry-points=npk_start_main` → `error: could not find function 'npk_start_main'`.
  Text concatenation (the program's `declare`s of floor-defined symbols stripped — 89 —, the floor's `declare i32 @main(...)` stripped,
  one `target triple`) → 215 defines, `llvm-as-18` and `llvm-as-20` rc 0.

## Clam (master `e80f9745` at LLVM 18.1.3; crab `dev` `49773d0a`, sea-dsa `dev18` `b43a9006`, llvm-seahorn `dev18` `6cfacb81`)

```
cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=$HOME/.local/src/clam/install -DLLVM_DIR=/usr/lib/llvm-18/lib/cmake/llvm \
      -DCRAB_ROOT=$HOME/.local/src/crab -DSEADSA_ROOT=$HOME/.local/src/sea-dsa -DCLAM_INCLUDE_TESTS=OFF ..   # llvm-seahorn symlinked into the source tree
cmake --build . -j40 && cmake --build . --target install                                                      # 78.8 s, 790 MB
export PATH=/usr/lib/llvm-18/bin:$PATH; /usr/lib/llvm-18/bin/llvm-as dyn.ll -o dyn18.bc
python3 $CL/bin/clam.py dyn18.bc --crab-inter --crab-track=mem --crab-heap-analysis=cs-sea-dsa --crab-dom=zones --crab-check=bounds \
    --crab-lower-with-overflow-intrinsics --crab-check-verbose=1 -o dyn.clam.bc
```
- Memory settings (`cs-sea-dsa` inter, `ci-sea-dsa` inter, `cs-sea-dsa` intra, `--crab-track=num` inter): rc 25,
  `ERROR: callee node Node … alloca sites=[ %t2 = extractvalue { ptr, ptr } %a0, 0] not mapped to a caller node.` then
  `CLAM ERROR: caller cell cannot be mapped to callee cell at …/lib/Clam/SeaDsaHeapAbstraction.cc:416`, 0.08 s.
- `--crab-heap-analysis=none --crab-track=num --crab-check=assert` (intra and inter): rc 0, `0 Number of total safe/error/warning checks`, 0.08 s.
- The compiler under the numeric setting (intra): `ClamPP 4.02`, `Clam 59.01`, 64.7 s, 782 MB, 0 checks.
- `lib/Clam/CfgBuilder.cc:3498` `doArithmeticWithOverflowIntrinsic`: the exact op, the overflow bit `false` (with `--crab-lower-with-overflow-intrinsics`).
- `lib/Clam/CfgBuilderUtils.cc:209` `isAssumeFn`: `llvm.assume` among the assume functions.
- sea-dsa `lib/seadsa/DsaLocal.cc:1109` `visitInsertValueInst`, `:1147` `visitExtractValueInst` exist; the abort is Clam's callee/caller cell mapping.

## Alive2

```
opt -O2 -S dyn.ll -o dyn.opt.ll
REPOS/alive2/build/alive-tv --smt-to=20000 dyn.ll dyn.opt.ll     # the old build, v21.0-6-gf9ad3cb4: 7 correct, 0 incorrect, 3 failed-to-prove, 2 errors; 132.9 s, 4.3 GB
                                                                  # errors: "Unsupported attribute: nocapture" (x2); the 3 failures: Timeout
git -C REPOS/alive2 log -S'nocapture' -- llvm_util/               # 9ff342f2 2025-01-31 "support captures(none) instead of nocapture (#1165)"
git -C REPOS/alive2 log -1 --until=2025-01-20                     # 02ec3af8 2025-01-17 — the pin candidate (v19.0-139-g02ec3af8)
cmake -GNinja -DCMAKE_BUILD_TYPE=Release -DLLVM_DIR=/usr/lib/llvm-20/lib/cmake/llvm -DBUILD_TV=1 .. && ninja -j40   # 13.7 s; tv/tv.so built
alive-tv --smt-to=20000 dyn.ll dyn.opt.ll                         # the pin: 7 correct, 0 incorrect, 3 Timeout, 2 "Source and target don't have the same return domain"
alive-tv --help-hidden | grep -i -E "rlimit|seed|smt-to"          # --smt-random-seed, --smt-to=<ms>; no rlimit
rg -n "rlimit|\"timeout\"" REPOS/alive2/smt/*.cpp                 # smt/ctx.cpp:36 Z3_global_param_set("timeout", …); no rlimit
cmake -GNinja -DCMAKE_BUILD_TYPE=Release -DZ3_BUILD_LIBZ3_SHARED=OFF -DZ3_BUILD_EXECUTABLE=OFF -DZ3_BUILD_TEST_EXECUTABLES=OFF \
      -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_C_COMPILER=clang ~/.local/src/z3 -B ~/.local/src/z3/build-lib && ninja -C ~/.local/src/z3/build-lib   # libz3.a, 82,807,520 B
```
