# The `raw`/`drop` licence worklist — CLOSED (D-163 fully implemented, 1.1.2)

Every tree reached zero; the licence (`NITPICK-TYPE-042`) refuses what the
instrument counted, and the instrument is retired — its job was the
measurement, and an instrument that duplicates a checker is two
representations of one fact. The record of the campaign lives in
`1.1.1.md` and `1.1.2.md`'s outcomes.
