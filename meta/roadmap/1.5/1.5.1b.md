# 1.5.1b — The workbench's three defects, and the two leaks under them

**Status: PLANNED and RATIFIED (2026-09-03) — the user ratified S-19…S-22 as
recommended, so D-246…D-249 are SETTLED as §7 states them and are entered in
DECISIONS.md at the close-out with their landing notes. Starts the moment 1.5.1
closes; 1.5.2 waits for it (the user, 2026-09-03: "finish up 1.5.1b before we
move on"). Every step may begin once 1.5.1 is on `main`.**

## 0. Why this subcycle exists

On 2026-09-03 the library workbench (`nitpick-libs`, planning `nitpick-time`)
reported three compiler defects against the 1.5.0-close compiler, all recorded in
`meta/roadmap/OPEN_DECISIONS.md` §2f with the reporter's numbers, the riders,
and this session's bisection. None was found by any suite in the tree; each came
from building a real library. The user's ruling is that a defect found that way
is fixed in the compiler at once, before planned work, and the real program
becomes the regression case.

| | Reported as | What it is, measured |
|---|---|---|
| **DEF-1** | compile time and memory QUADRATIC in one declaration's size, three axes | three accumulate-by-concatenation loops in `src/backend/` (time), riding a managed-regime defect — **an owning temporary is never dropped** (memory) |
| **DEF-2** | a root file with a mismatched `mod:` header silently merges a sibling; two `@main`, exit 0, `llc` refuses | the loader cannot tell a file's header from an import written first (its own comment says so), and nothing forbids `main` outside the root module |
| **DEF-3** | a `uint8[]` view of a local string is returned out of its frame; exit 0; reads freed memory | the escape analysis knows `@x` and the range-view; it names neither `string_bytes` nor `string_from_bytes` |
| **DEF-4** (added 2026-09-03, the workbench's O-N10) | `#[derive(Eq)]` on a payload enum refuses inside `<derived-1>`; `#[derive(Ord)]` on the same compiles to a TAG-ONLY order — `Literal(7).cmp(Literal(9))` is `Equal` at exit 0 | every derived comparison is an operator, and operators are refused on named types: the enum's payload, and (measured at step 0) a struct's field of a derived struct type, never worked; the tag-only `cmp` was a 1.0.9d design comment right for `Hash` and wrong for an order — **S-23 → D-250, step 3b** |
| **DEF-5** (added 2026-09-03, the workbench's O-N11) | a root with `main` and no `failsafe` compiles at exit 0, `llc` refusing the calls into nothing; the reach analysis's arm contract is discharged by deleting the handler | `reach_settle` returned early at `failsafe_decl == 0`, before the named-coverage loop — **step 1b** |

The bisection found two facts larger than the report:

- **The compiler compiling itself peaks at 11 516 824 KiB of resident memory**
  (`npkc src/main.npk`, 15.6 MB of output); the frontend alone (`check
  src/main.npk`) at 10 956 716 KiB. Two causes, both by construction: the
  temporaries above, and `List<T>` (`src/frontend/list.npk`, 1.4.7) is a
  `wild` block whose comment says "nothing here drops" — 153 by-value List
  fields and parameters across 28 files, never freed. The compiler is a bump
  allocator that happens to build itself. Four harnesses in parallel do not
  fit a 16 GiB machine (the 1.4.8 UI freeze has a candidate cause).
  > **Corrected at step 0 (2026-09-03):** this machine is not a 16 GiB machine —
  > `free -m` reports 160 768 MB, ORCHESTRATION R4's "48-core / 157 GB" — and
  > four 11 GiB builds fit it four times over; the 1.4.8 freeze keeps no
  > candidate cause from this paragraph. The 16 GiB figure is the workbench's
  > (a consumer's machine, and their CI), and it is right about those.
- **The two probes that settle where the memory goes** (built with the 1.5.1
  worktree's compiler, both exit 0): `t = string_concat(t, "b")` 20 000 times
  peaks at 260 KiB — the overwritten binding frees its old body (D-186's
  fallout, for locals); `t = string_concat(string_concat(t, "b"), "c")`
  20 000 times peaks at 429 740 KiB — the inner result is an unbound
  temporary passed as an argument, and nothing ever frees it. This is
  D-183's "statement-end temporaries" item, recorded at 1.2.4a ("temporaries
  do not drop yet — that cell rides the recorded statement-end-drops debt")
  and never scheduled since.

So the subcycle is five fixes and one instrument, seven commits, each under a
full harness. The order puts the two small refusals the workbench is blocked on
first, the emitter's builders next (that alone makes the reported curves linear
in time and memory), and the two lowering changes last, because they change the
compiler's emission of itself and end with a snapshot refresh.

## 1. Ratified inputs and the decisions this subcycle needs

Already settled and binding here: D-183 (the managed lowering; its
statement-end item is what step 5 closes), D-186 (`string_slice` copies;
`string_from_bytes` is the explicit view primitive), D-178 (an `await` is its
statement's first evaluation — what bounds the coroutine case in step 5),
D-014 (a trap runs no drops), D-004 rule 2 (a borrow cannot travel up),
D-201/1.4.2 (BUILTIN_REFERENCE's marked regions are the one signature
authority; 1.5.1 step 4 added the `Pure` column the same way), D-205 (an
emission change reaches the compiler's own build at the next refresh), D-228
(the orchestration rules: one writer for `src/`, a harness per commit, the
cumulative-prefix protocol for developing ahead, R5 — a red prefix is a stop
sign, R6 — no workaround for a compiler defect), D-237 (exact diagnostic
sets), D-240 (one mistake, one report).

**Proposed here, for the user (§7): D-246 statement-end temporaries, D-247
`List<T>` owning, D-248 the file header and the entry points, D-249 the `Views`
column.** Steps 1 and 2 need D-248 and D-249; step 3 needs nothing; steps 5 and
6 need D-246 and D-247. Nothing in this file starts before the decisions it
needs are ratified; the executor may begin step 0 and step 3 at once.

## 2. Step 0 — The instrument and the baseline (commit 1)

**The fix must be a number, and wall-clock is never a verdict** (the harness
already refuses a z3 wall-clock knob as "a hang net, never a verdict"). The
number is the allocator's, which is deterministic for a given input.

- **`NPK_HEAP_STATS`.** `runtime/npkrt.ll`'s one allocation core
  (`npk_alloc_impl(n, tracked)`, entered by `npk_alloc` at 4578,
  `npk_alloc_managed` at 4587, `npk_ralloc` at 4650, freed by `npk_dalloc` at
  4767) keeps three words: bytes requested in total, bytes live now, and the
  peak of live. `npk_exit` (2858) prints one line to fd 2 when the environment
  carries `NPK_HEAP_STATS` (the runtime's `_start` already holds envp for
  `environ()`, 1.4.8 Part A): `heap: allocated=<n> peak_live=<n> count=<n>`.
  Three adds and a compare on the hot path; a debug instrument that stays,
  like `@npk_quarantine` and the 0xAA poisoning. The printed line is data for
  the harness, never a verdict inside the program.
- **The `cost` harness stage** (and its `npkg` twin — D-238: one table, both
  runners), permanent:
  - **The three DEF-1 recipes as generated programs**, parameterised by N,
    regenerated at N and 4N by the stage itself (the workbench's
    `nitpick-time` `tests/probe/defect/README.md`, commit
    `8066e6229c77aed28be7ab471209962a03534b0f`, is the source of the shapes;
    their `big_fixed_array_cost.npk` is checked in as
    `tests/backend/programs/big_fixed_array.npk` and must compile and run at
    exit 0 like any program). The stage compiles each pair with the compiler
    under test under `NPK_HEAP_STATS` and asserts
    `allocated(4N) / allocated(N) ≤ 6` and `peak_live(4N) / peak_live(N) ≤ 6`
    on all three axes — linear is 4 with slack, quadratic is 16. Every
    compile must exit 0 (the reporter's trap: a fast failure looks like a fast
    compile).
  - **The two probes as programs** — `tests/backend/programs/temp_bound.npk`
    and `temp_nested.npk`, the 20 000-iteration shapes above, each asserting
    its final length and exiting 0 — RUN under `NPK_HEAP_STATS`, the stage
    asserting `peak_live(nested) ≤ 2 × peak_live(bound)`. Before step 5 this
    assertion FAILS by design; the stage carries the expectation as an explicit
    `expected: fails until D-246 lands` row that step 5 removes, so the
    instrument is proven to bite before the fix exists (the runner self-check's
    negative-control discipline, D-237).
  - **The compiler's own build**: `npkc src/main.npk` under `NPK_HEAP_STATS`,
    REPORTED on every full run (the `check_decisions_current` pattern) and
    bounded by a hard ceiling set at the close of step 6 from the measured
    value with 2× headroom, so an 11 GiB regression fails loudly.
- **The baseline**, recorded in §8 before any other edit, at the 1.5.1 close
  commit, with the recipes at 1 000 / 2 000 / 4 000 / 8 000 rows (axis 1),
  1 000 / 4 000 statements (axis 2), 60 k / 480 k bytes (axis 3), the two
  probes, and `npkc`/`check` on `src/main.npk` — allocated, peak_live, count,
  and the exit code of every point. Wall-clock is recorded beside them as
  colour only.

No `src/` change in this commit. Harness green is the commit's condition, the
`cost` stage's one designed failure excepted as written above.

## 3. Step 1 — DEF-2: the header and the entry points (D-248, commit 2)

**Mechanism, from the loader's own text** (`src/frontend/module_graph.npk`
~477–489): "THE FILE'S OWN HEADER IS INDISTINGUISHABLE FROM AN IMPORT written
first — both are a member-less `mod:name;` — so the loader cannot say 'your
header is wrong'; what it can do is name that reading when the file is not
found (1.0.8)." `graph_load_imports` loads every member-less `mod:X;` as a
file module; when `X` names an existing sibling, the sibling is a second
module of the program, and if it declares `main` the emitter names both
`@main` (`ir_types.npk:292`, by name alone; `ir_writer.npk:294`;
`ir_func.npk:51`; `type_decl.npk:36` types both as entry points). Measured on
the tree: 525 source files, **240 without a header** (`src/main.npk`, the
three tools, `npkg/main.npk`, `tests/grammar/whole_grammar.npk`, most of
`tests/rejection/`, …); three files whose first `mod:` is not the basename,
all legitimate today (an inline module first in `tests/rejection/inline_mod.npk`
and `tests/modules/rejection/owned_names.npk`; `mod_ambiguous.npk`'s
`mod:twoways;` is the test's subject).

**D-248 (proposed; the recommendation and the alternative in §7):**

1. **The header is mandatory.** Every source file's FIRST declaration is its
   header, `mod:<basename>;` — member-less, naming the file's own basename. A
   file whose first declaration is anything else, or a `mod:` naming another
   module, is refused at that declaration's span (line 1 for a missing
   header): `NITPICK-RESOLVE-012`, one code, two texts — "file `foo.npk` has
   no header: its first declaration must be `mod:foo;`" and "file `foo.npk`
   declares `mod:bar;` first: a file's header names the file — `mod:foo;`;
   an import of `bar` is written after it". The header binds nothing and
   loads nothing (`graph_load_imports` skips item 0 by rule instead of
   loading the file into itself); a LATER member-less `mod:X;` is a
   file-module import exactly as today, and RESOLVE-005's 1.0.8 hint text
   loses its "or your header is wrong" reading, which the loader can now say
   directly. The prelude's constant text carries `mod:prelude;` first (it does
   today; `graph_load_prelude` is unchanged).
2. **`main` and `failsafe` are declared in the ROOT module only** — the file
   the driver was given. A declaration of either in any other module is
   refused at its span: `NITPICK-RESOLVE-013` "`main` is an entry point and
   is declared in the root module; `bar.npk` is a module of this program".
   Checked in `graph_check_owned_names`' position (`module_graph.npk:307`, the
   whole-graph pass 1.4.8c added) since it needs every module loaded and the
   root's index (`graph_load_all`'s `first`). With 1, a mismatched header
   can no longer load a sibling; 2 is the belt for a sibling imported ON
   PURPOSE that happens to be a program — a library cannot smuggle an entry
   point, and the emitter's `@main`/`@npk_failsafe` naming becomes the
   root's by construction (the four by-name sites are left as they are and
   annotated: the rule upstream is what makes them safe).

**The sweep.** 240 files gain a first line. Mechanical, by a script kept in
`meta/roadmap/1.5/tools/` for the record and deleted after: insert
`mod:<basename>;` as line 1, and for every file in a rejection suite shift each
`// expect-error-at: LINE` pin by one — D-237's exact matching then proves the
shift complete, because any missed pin fails by name. `mod_ambiguous.npk` keeps
its `mod:twoways;` as its SECOND declaration (the test's subject is the
two-file ambiguity, not the header). The harness's and `npkg`'s generated
sources — `selfcheck.py`'s and `npkg/selfcheck.npk`'s embedded programs, the
`nf-inert` twins (copies of files that now carry headers), `quickemit`'s
temporaries — get the header their temporary basename requires; an execution
item, found by running them. `tests/modules/README.md` gains the two rows.

**Tests** (all D-237-pinned): `tests/modules/rejection/header_missing.npk`
(a file with no header: RESOLVE-012 at line 1), `header_mismatch.npk` +
its sibling `header_sibling.npk` (the reporter's six-line reproduction — the
mismatched header names the sibling; RESOLVE-012 at the header, and the
sibling is NOT loaded: no second diagnostic, which D-237's exact set proves),
`entry_in_module.npk` + `entry_lib.npk` (a header-correct root importing a
module that declares `main`: RESOLVE-013 at the module's `main`). Accept:
`tests/accept/` gains nothing — every file in the tree is now the positive
case.

## 4. Step 2 — DEF-3: view-makers are borrows (D-249, commit 3)

**Mechanism.** `escape_call_like` (`src/frontend/analysis/escape.npk:1230`)
asks `escape_arg` (1600) whether each argument IS a borrow; `string_bytes(s)`
passes the local `s` by value, which is no borrow, so the call's result is
not borrowy and `escape_return_value` (414; `BORROW_RETURNED` at 429) lets
it out. The result is a `{ptr, len}` into `s`'s heap body, which `s`'s
scope-exit drop frees. `arr[lo...hi]` is known to exactly one walk — the
suspend walk (`suspend.npk:606`, D-191) — and to the escape analysis not at
all; the same hole, one layer over.

**D-249 (proposed):** BUILTIN_REFERENCE's marked regions gain a **`Views`**
column beside `Pure` (1.5.1 step 4's shape, the same generator path in
`bootstrap/generator/gen_tables.py`, which hard-fails on a cell it cannot
read): `—`, or the 1-based index of the argument whose storage the result
aliases. Rows: `string_bytes` 1, `string_from_bytes` 1 (its pointer
argument), every other row `—`. `builtins.npk` gains `builtin_views(name) →
int32` (0 for `—`). The escape analysis treats a call whose callee has a
`Views` index as a borrow ROOTED WHERE THAT ARGUMENT IS ROOTED — as if `@`
had been written at the argument — so D-004 rule 2 (return), the struct-literal
refusal, rule B (a destination among the arguments), the channel-send rule and
the launder-through-a-call rule apply to it unchanged. The range-view
`arr[lo...hi]` gets the same treatment by kind. A user function returning a view
of its own PARAMETER stays legal (`borrows_only_param_rooted`, 507); one
returning a view of a local, or of a local's field, is BORROW-001. Whether a
`wild` operand's view is wild-provenanced is already `wild_provenance`'s
(622) answer and is unchanged.

**Tests.** `tests/analysis/rejection/view_escape.npk`, pinned exactly, carrying
the reporter's CONTRAST (their committed reproduction — `nitpick-time` `0667ecb`,
`tests/probe/defect/view_escape/`, six cases and a verbatim transcript,
independently verified at `9113487` — has the transcript): `pass (@x)` BORROW-001 (today),
`Holder{ p: @local }` BORROW-001 (today), `pass (string_bytes(local))`
BORROW-001 (new), `pass (string_from_bytes(local.ptr, local.len))`
BORROW-001 (new), `pass (local_arr[1...3])` BORROW-001 (new), a view stored
into an outer struct through a pointer parameter (rule B, new). Accept:
`tests/accept/views.npk` — a view of a parameter returned, a view used inside
its frame and passed down, `string_bytes` on a field of a `->` parameter. The
`check_builtin_sig_texts` instrument learns the column exists;
`check_codes_tested` needs nothing new (no new code).

## 5. Step 3 — DEF-1's builders through a `Sink` (commit 4)

The three sites, one shape, one fix — write pieces into a geometrically growing
`Sink` (`src/frontend/diagnostics.npk:268`; `sink_write` 274, `sink_view` 294,
`sink_append` 312) instead of returning an accumulated string:

- **Axis 1 — `emit_global_const` (`src/backend/ir/ir_expr.npk:9398`) and its
  two helpers** `emit_global_struct_const` (9434, `out = cat3(out, fll.text,
  …)` per field) and `emit_global_array_const` (9479, `out = cat3(out,
  ell.text, …)` per row). They become `emit_global_const_into(x, eid, Sink->)`
  — the nested shapes (a struct in an array in a struct) write into the same
  sink in order — and the one caller in `emit_program.npk` (1003, the global
  declaration line) writes the line's head, then the initialiser, then the
  tail, through `irw_sink(w)` directly (the emitter is single-threaded;
  nothing interleaves). The "" failure return
  (an unfoldable constant) becomes a `bool` the callers already branch on.
- **Axis 2 — `emit_site_tables` (`src/backend/emit_program.npk:1981`)**: the
  per-path escaper (`enc = string_concat(enc, esc_byte(b))` per byte of a
  path — short, but the same shape), `ls = cat3(ls, ", i32 ", …)` and
  `ps = cat5(ps, …)` per site become piecewise `sink_write`s of the three
  table lines.
- **Axis 3 — the string-literal escaper in `strtab_add` (`ir_expr.npk:146`,
  the loop at 193)**: `enc` becomes a sink sized `4 × len`, `esc_byte`'s
  result written into it per byte, one `sink_view` copy at the end.

**The emitted bytes do not change.** The step's record shows it directly:
the IR of every real-backend program and of `src/main.npk` byte-compared
before/after (the harness's `selfhost`, `nf-inert` and `repro` stages say the
same in the large).

**The sweep, classified.** The tree holds **332** statements of the shape
`x = catN(x, …)` / `x = string_concat(x, …)` (`derive.npk` 104, `ir_expr.npk`
41, `smt_encode.npk` 30, `prelude.npk` 22, `smt_manifest.npk` 17,
`smt_writer.npk` 14, `emit_program.npk` 14, `ir_types.npk` 11,
`diagnostics.npk` 10, `ir_func.npk` 10, the rest smaller). Each is classified in
the record as **bounded** (a handful of pieces — a diagnostic message, a type
text, a `ToString` body) and left, or **program-sized** (a loop over
statements, sites, fields of a user struct, members of a user enum, bytes of
user text) and converted. The SMT encoder's per-statement term building
(`smt_encode.npk`'s `bind_assign` and the path-condition strings) is the one
expected to be program-sized; the derive generator's are bounded by a user
type's field count and converted only if a field count can plausibly reach the
thousands (a 26 838-row table is one declaration, not one struct). The
classification is the deliverable; "converted N of 332, the rest bounded by
construction" is the record line.

**After this step the `cost` stage's three axis assertions pass** (the sink
creates no prefix-sized temporaries, so memory is linear too); the probes'
assertion still fails by design until step 5.

## 3b. Step 1b — DEF-5: a root with `main` and no `failsafe` (commit 2b; added 2026-09-03)

The workbench's O-N11, raised while step 1 was being written (§2f): a root that
declares `main` and no `failsafe` compiled at exit 0 — the emitter wrote every
trap path as a call to `@npk_failsafe` into nothing and `llc` refused the
result — and `reach_settle` (`src/frontend/analysis/reach.npk`) returned at
`failsafe_decl == 0` BEFORE the named-coverage loop, so D-179's arm contract
(REACH-002) was asked of programs that had a handler and of nothing that had
none. The fix is the same pass's missing question, in the analysis that holds
the answer: the walk records the root's `main` as it records `failsafe` (D-248
admits both at the root's top level only); with `main` and no `failsafe` it
reports `NITPICK-REACH-003` at `main`, LISTING the identities the set holds —
the set it had just computed at the line it used to return from — with the
count; a root with neither (a library checked alone) stays silent, nothing to
settle against. Test: `tests/analysis/rejection/missing_failsafe.npk` (a user
identity, the four system identities every executable carries, `IntOverflow`
from a guarded `+`; one report, pinned). D-013 carries its enforcement note;
the analysis README gains the three REACH rows it never had.

## 5b. Step 3b — DEF-4: derived comparisons over NAMED types (D-250, commit 4b; added 2026-09-03)

**Mechanism, from the generator's own text** (`src/frontend/macro/derive_gen.npk`,
`derive.npk` until step 1). Derived bodies are SOURCE, spliced before collection,
so the generator sees TYPE NODES and not types (`dv_field_is_simd` branches on a
node's builtin token; that is the technique). Every comparison it writes is an
operator: `gen_eq_struct` `self.f != other.f`, `gen_cmp_struct` `self.f <
other.f`, `gen_eq_enum` `self == other`, `gen_cmp_enum` the tags `self =>!
int32` alone — and an operator is refused on a named type (TYPE-034 "has no
built-in `==`", TYPE-008 "has no ordering"), inside `<derived-1>`, a file the
user cannot open. Measured at step 0 with the step-1b checker: the reporter's
`#[derive(Eq)] enum:Part = { Literal(uint16); Year4; }` refuses TYPE-034 in
`<derived-1>`; `#[derive(Eq, Ord)] struct:Outer = { Inner:i; int32:b; }` with
`Inner` derived the same refuses TYPE-034 ×1 and TYPE-008 ×2 in `<derived-1>`.
No test in the tree derives over a named type — every derived struct has scalar
fields and every derived enum is payload-less — so the path was written and
never run. The step is therefore "derived comparisons over named types",
structs and enums alike:

1. **An operand's spelling follows its type node.** A builtin-named node
   compares by operator, as today; a NAMED type compares through its own
   trait method — `a.eq(b)`, `a.cmp(b)`, `a.partial_cmp(b)` — with the call's
   envelope handled as the prelude DECLARES the trait (read `Eq`/`Ord`/
   `PartialOrd` in `prelude_source.npk` at the step: a `never fails` method is
   called under `raw` and the derived body is then `never fails` itself; a
   may-fail one is relayed; the record says which the prelude says). A generic
   parameter as a field type (`Box<T>`, D-161) is a named node and takes the
   method form — what D-161's no-bound story already relies on.
2. **An enum with payloads compares the TAG first** (declaration order, as
   today), then for equal tags the payload of that variant: a `pick (self)` per
   variant whose arm picks `other` for the same variant and compares the bound
   payloads by rule 1; a payload-less variant is `true`/`Equal`; `PartialOrd`
   wraps as today (D-099). D-123's order-over-variants text keeps its meaning
   for `Hash`, which stays tag-only (a colliding hash is correct, if weak).
3. **An OWNING payload refuses the derive by name** (a `DERIVE-` code, at the
   user's declaration, naming the variant and its payload's type): a `pick`
   over a borrowed enum cannot bind an owning payload without consuming it
   (S-5/D-216's consuming form is the only pick that binds one), so the impl
   is written by hand until a borrowing form exists — a separate question, not
   needed to close DEF-4. Owning is read off the payload's type NODE: `string`,
   `buffer`, `List<…>`, `dyn`, `OwnedFd`, the arenas and the sync primitives,
   listed by spelling in the generator; a named USER type whose drop cannot be
   known before collection is admitted and compared through its own method,
   whose by-value parameter is a borrow at the call.
4. **A payload or field whose type has no `Eq`/`Ord` still refuses at the type
   checker (TYPE-034/-008), but the span is the USER'S**: the derive reader
   records which field or variant it generated a comparison from, and a
   diagnostic at a `<derived-N>` site is reported at the `#[derive]` attribute
   with a note naming the field. If that re-homing needs more of the diagnostic
   machinery than the step affords, the fallback is the generator refusing by
   name for the kinds it can see, and the record says so.
5. `gen_eq_enum`'s stale comment ("that is why `Ord` on an enum is refused
   rather than generated") goes.

**Tests.** `tests/derive/rejection/payload_owning.npk` (an owning payload,
refused by name at the declaration, D-237-pinned); `tests/backend/programs/
derive_payload.npk` — the reporter's `Part`: `Literal(7).cmp(Literal(9))`,
`.cmp(Literal(7))`, `.cmp(Year4)` exiting 321 (their case 2's corrected
expectation), `eq` on the same, and `Outer{Inner}` compared through `eq` and
`cmp`; `tests/accept/` gains the shapes. DECISIONS D-250 is entered with its
landing note at this step; the workbench's `derive_payload_enum` transcript
re-pins at the landing.

## 6. Steps 4 and 5 — The two leaks (commits 5 and 6)

### Step 4 — Statement-end temporaries (D-246)

**D-246 (proposed):** An owning value that no place takes is a **temporary of
the statement that produced it**. It is dropped when that statement ends, on
every path the statement leaves by — its normal end, `relay`, `pass`, `fail`,
`return` — in reverse order of creation and after the statement's own effect
(a returned value is computed and stored before its statement's temporaries
drop; D-207's seam order — store, unwind, return — gains "drop the statement's
temporaries" between store and unwind). A temporary is **taken**, and not
dropped by its statement, when it is bound by a declaration, assigned into a
place, passed to a `move` parameter, stored into a struct, array or `Result`
literal's slot, sent on a channel, or returned. A temporary passed to a plain
parameter is BORROWED by the callee (D-004) and dropped by the statement after
the call returns; a receiver (`string_concat(a, b).len`) and an operand
(`f() == g()`, `is_err(f())`) likewise; `discard(f())` discards a temporary and
drops it. A trap (`?!`, `!!!`) runs no drops (D-014). In a coroutine, the only
temporary alive across a suspension is an `await`'s own argument (D-178); it
lives in a frame slot and drops when the statement ends after the resume.
`dyn` coercion arguments (D-183's 1.2.4a note) are ordinary temporaries under
this rule.

**Mechanics** (the executor's map; every site read this session):

- `emit_stmt` (`src/backend/ir/ir_stmt.npk:526`) already opens a per-statement
  mark (`irw_stmt_mark`, D-177). A **per-statement temporary list** on the
  function emitter (`x.fe`) opens beside it: entries of `(tid, ll type, ref)`.
- **Registration** happens where an owning value is produced and NOT taken.
  The producer is `emit_call` (`ir_expr.npk:6360`) and its kin (`emit_await`
  6083, method calls, `?|` on a `Result` whose value owns, interpolation,
  `ToString`, the `dyn` coercion). Rather than teaching every producer, the
  rule is applied at the CONSUMERS: `emit_fit` (476) is the one seam every
  value crosses into a slot ("A FIELD IS A SLOT", "AN ELEMENT IS A SLOT",
  the call-argument fit at 6426); the emitter marks a fit as TAKING (a
  declaration's initialiser, an assignment's value, a `move` parameter — the
  signature's move flag, `builtin_sig_param_move` for builtins — a literal
  slot, a send, a return) or BORROWING (a plain parameter, a receiver, an
  operand). A borrowing fit of a non-place owning value registers the
  temporary; a taking fit does not. `type_drops` decides "owning"; a place
  (an identifier, a field of a place, an element of a place) is never a
  temporary — the move analysis already refuses copying one.
- **Emission** at the statement's normal end (`emit_stmt`'s return paths) is
  one `call void @"npk.drop.<tid>"(...)` per entry, reverse order, through the
  generated bodies `emit_drop_bodies` (`emit_program.npk:1225`) already
  produces for every owning type — no new drop shape.
- **Early exits within a statement**: `emit_relay` (7311) is the only
  in-statement return; it drops the statement's temporaries so far before
  the unwind. `emit_pass` (1432), `emit_fail` (1458), `emit_return` (1527)
  drop theirs after storing the result and before `run_scope_drops` (380).
  `?!` and `!!!` trap: nothing.
- **Coroutines**: an owning temporary that is an `await`'s argument is spilled
  to a frame slot (one new frame role, beside D-207's role 41; `emit_await`
  builds the child frame from the argument values, so the spill is the
  caller keeping its own copy of the trio) and dropped from the slot at the
  statement's end after the resume; the suspend walk records nothing new
  because D-178 already guarantees no other temporary crosses.
- **The move analysis** is untouched: temporaries were never places.

**Tests.** `temp_bound.npk`/`temp_nested.npk` (step 0's, now green); a
`dyn` temporary (`report(coerce(x))`); a struct temporary with an owning field;
an `OwnedFd` temporary passed 2 000 times to a borrowing function — EMFILE
proves the drop, the `overwrite_owned.npk` technique; a temporary before a
`relay` that fails on the 10th call (descriptor count again); `discard(f())` of
an owning result; an owning temporary as an `await` argument in a coroutine
with `// stress: 40`; a temporary on both sides of `==`. The checker's
`ExprTypes` needs nothing new.

**What changes downstream.** Every program with a borrowed owning temporary
emits differently: `selfhost` (stage 2 == stage 3 must hold with the new
lowering — stage 1 is built by the OLD snapshot and its body still leaks;
stage 2 is the first compiler whose body drops, and it is the one measured),
`nf-inert`, opt-O2, the stress loops, `repro`, `parity`. `nitpick.obligations`
is expected unchanged (a drop adds no obligation); the record says whether it
was.

### Step 5 — `List<T>` owns its elements (D-247)

**D-247 (proposed):** `List<T>` is **compiler-known** and **owning**: it is
declared in the prelude (moved from `src/frontend/list.npk`, its API —
`list_init`, `list_reserve`, `list_push`, `l.items[i]` — unchanged), `type_drops`
answers true for it unconditionally (as for `dyn`), and its generated drop
body drops the `count` elements it holds through `T`'s drop and then frees
the block with `npk_dalloc`. It is therefore move-only under TYPE-046, which
also refuses the aliasing copy today's `wild` block permits (two copies of a
List, one `list_push` that `ralloc`s, one dangling `items`). A struct holding
a List drops it through its own generated body; a List that must outlive its
scope is moved, like any owner. Growth stays `ralloc` on the block; the drop
frees whichever block is current. The precedent is `OwnedFd` (TY 39): a
compiler-known type whose drop does the manual thing on the author's behalf,
by decision.

**Why not the alternatives.** A `buffer`-backed List frees its block but
leaks its owning elements (`List<Root>` carries two strings per element) — a
generated struct drop knows fields, not elements. A user-declared drop hook
is a destructor design the language does not have and should not acquire for
one type. Manual `list_free` + `defer` at 153 holders is the regime the
managed lowering exists to replace, and the compiler's own tables are exactly
the data whose lifetime should follow their pass.

**Mechanics.** The drop generator (`emit_drop_bodies`) gains a List arm keyed
on the prelude declaration (the way `Duration`/`Error` are resolved by name
ahead of lookup, D-179): a loop over `count` calling `T`'s drop on
`items[i]` where `T` drops, then `npk_dalloc(items)` where `cap > 0`.
`type_drops` and the walkers-total instrument (`check_type_walkers_total`)
learn the kind; the escape analysis already treats `wild T->` fields by
provenance. **The sweep**: 153 by-value List fields and parameters, 4
`List<T>->` parameters, 335 `.items[i]` reads (unchanged — reads through the
field are what they were), and every holder struct whose value is copied
between bindings becomes move-only — the checker's own TYPE-046 refusals
enumerate the sites, the same way 1.4.7's adoption found its 26. A List
returned from a factory is `move`d or returned by value (a return takes).
Expected: per-pass tables (a `Resolver`, an `Escape`, an `ExprEmitter`)
die at their scope's end and the compiler's peak falls to its live data.

**Tests.** `tests/backend/programs/list_drop.npk`: a `List<string>` of 10 000
elements built and dropped 100 times in a loop under `NPK_HEAP_STATS`, the
`cost` stage asserting `peak_live` under 2× one list's size; a `List` inside
a struct inside a `List` (nested drops); a moved List (the source refuses
reuse, TYPE-047); `List<OwnedFd>` of 1 000 descriptors dropped — EMFILE
proves the element drops. The compiler's own build ceiling in the `cost`
stage is set here from the measured value.

## 7. Questions for the user — the four decisions (ratify, amend, or refuse)

- **S-19 → D-246, statement-end temporaries.** The rule as §6 step 4 states
  it. Recommendation: ratify as written. The one design choice inside it is
  "taken vs borrowed" being decided at the SLOT (the fit), which is where
  the language already says what a position means; the alternative (a
  temporary is dropped at the END OF THE FUNCTION) is simpler to emit and is
  what the leak becomes if the statement rule is refused — a coroutine's
  frame would then hold every temporary, and the compiler's own memory
  would fall by less.
- **S-20 → D-247, `List<T>` owning and compiler-known.** Recommendation:
  ratify as §6 step 5 states it. The alternative that keeps the language
  smaller — `buffer`-backed, elements leaked — is decided OUT because it
  leaks exactly the strings the compiler's tables hold.
- **S-21 → D-248, the mandatory header and the root-only entry points.**
  Recommendation: ratify BOTH halves. The alternative for the header —
  header optional, identified by NAME (a member-less `mod:` naming the
  basename is the header wherever it stands, anything else is an import) —
  costs no sweep and closes the reported defect through the entry-point rule
  alone, but leaves a mismatched header a silent import of the sibling
  whenever the sibling declares no `main`, and leaves the loader unable to
  say "your header is wrong", which cost a cycle at 1.0.8. Explicit over
  implicit: every file says what it is. The sweep is 240 mechanical lines
  and a pin shift the harness verifies.
- **S-22 → D-249, the `Views` column.** Recommendation: ratify. The
  alternative — a hard-coded pair of names in the escape analysis — is one
  more parallel authority beside the signature table 1.4.2 made the ONE
  authority.
- **S-23 → D-250, derived comparisons on a payload enum — DEF-4, the
  workbench's O-N10. RATIFIED by the user 2026-09-03 ("ratify S-23 as
  recommended, add step 3b").** OPEN_DECISIONS §2e carries the row and §2f the
  defect; §5b below is step 3b, inserted between step 3 and step 4 — frontend
  only, displacing nothing the workbench waits for; the standing rule (a defect
  a real program finds is fixed before planned work) puts it in this subcycle
  rather than the backlog. The struct half was measured the same day and is in
  the step's scope.

Also for the user, not blocking: whether `NPK_HEAP_STATS` should print on
every exit path (a trap's `failsafe` exit included) — recommended yes, the
line is diagnostic output on fd 2 and the harness ignores it where it did not
ask.

## 8. Execution record

(One entry per commit, with the harness's stage lines and the `cost` numbers;
the baseline first. Nothing below exists until the corresponding commit does.)

### Protocol

D-228 as 1.5.1 ran it: `src/` has one writer; each commit's tree runs a full
harness (plus `parity`) before it lands on `main`; work ahead of a running
harness happens in a detached worktree and is validated as a CUMULATIVE PREFIX
(`p1 = step 1`, `p2 = steps 1–2`, …) so that every landed prefix has been
green as a whole; a red prefix stops the line (R5) and is never retried
unchanged. The snapshot is refreshed once, at the close, from the final tree
(README's refresh, `stage2 == stage3`), after which `npkc src/main.npk`'s
number under `NPK_HEAP_STATS` is taken with the REFRESHED builder and written
into the `cost` stage's ceiling and into §8.

### Close-out

The docs, as dated annotations: DECISIONS D-246…D-249 entered, D-183's
statement-end item annotated closed, D-186's view-maker inventory annotated
with `string_bytes`; MEMORY_REFERENCE §2 gains "Temporaries" and the List
rule; MODULE_REFERENCE §1.1 gains the header rule and the entry-point rule;
BUILTIN_REFERENCE's column note gains `Views`; SAFETY_ARCHITECTURE's borrow
bullet names views; OPEN_DECISIONS §2f closes with the after-numbers and S-19…
S-22 settled; ROADMAP's 1.5 row; CLAUDE.md's status paragraph and the
"reserved words" table (no new words — `mod:` headers are a rule, not a word);
the 1.5 README's row. The workbench is messaged at the landing with the
commit, whether `build/` was written after it, and the after-numbers on their
own recipes, and asked to re-pin.

> **Numbering, corrected at step 0 (2026-09-03):** §2 and §5 say the probes'
> assertion "FAILS by design until step 5"; §6 numbers D-246 step 4 and D-247
> step 5, and OPEN_DECISIONS' S-19 row says step 4. The two references predate
> the insertion of step 0 and mean step 4; `tests/cost/temporaries.toml`'s
> `until` names step 4. The record below follows §6.

### Step 0 — LANDED (2026-09-03): the instrument and the baseline

The `cost` stage exists in both runners, `NPK_HEAP_STATS` in the floor, the
three programs are in the tree, and the baseline below is the 1.5.1-close
compiler's — measured with the step-0 tree, whose only difference from the
close is the instrument itself. Decisions taken on contact, each with its
reason:

- **An expected failure is a PASSING unit, not a red run.** The plan said the
  harness would be green "the `cost` stage's one designed failure excepted";
  a red run with an exception is a run whose colour means nothing under R5.
  A unit whose `expect` is `"fail"` PASSES exactly while its bound is
  violated — the runner self-check's negative-control shape (D-237) — and
  FAILS the day the bound holds, until its row is removed in the commit that
  made it pass. Four units are so marked today (the three axes until step 3,
  the temporaries until step 4); each printed "confirmed" on this run.
- **The units are TOML files under `tests/cost/`, one per verdict**, read by
  both runners (the harness with `tomllib`, npkg with its own strict reader),
  because "the stage carries the expectation as an explicit row" needs a
  table both runners read and the manifest's schema is not the place for
  per-unit data. The schema is BUILD_REFERENCE §7.1's `cost` row; a key the
  schema lacks, a kind or recipe nobody implements, an expected failure
  with no `until` are each refused by name (the manifest's discipline).
- **The recipes are generated by both runners from one text** (the
  workbench's README recipes, commit `8066e62`, in the seven-arm wrapper),
  the harness's `cost_recipe_text` and npkg's `cost_recipe` writing the same
  bytes; npkg writes into the compiler's `Sink` because a recipe at 4N is a
  quarter of a megabyte and this runner is compiled by the compiler whose
  temporaries do not yet drop. Axis 3's pair is 30 000 / 120 000 bytes, not
  the baseline's 60 000 / 480 000: the ratio is the verdict, not the size,
  and 480 000 costs five minutes per runner at the baseline.
- **The number is the allocator's, and `allocated` carries axis 3's time.**
  The reporter measured axis 3 as quadratic in time with memory flat; under
  the instrument that reads as `allocated` ×N² with `peak_live` flat — the
  escaper's bound accumulator frees its old body each time, so the bytes
  REQUESTED are the pathology — which is why both ratios are held.
- **The instrument's first version faulted the compiler's own build.** The
  report walked the environment slice at exit, after `src/main.npk`'s
  `wild_release_all()` had unmapped the chunk it lived in. Two fixes:
  `npk_hs_arm` decides at `_start` from the kernel's raw envp and the report
  reads a flag; and the argv/environ arrays are their own mapping outside
  the releasable heap — the latent hazard §2f records, with
  `argv_after_release.npk` as its case (exit 0 here, 139 on the close's
  runtime). `npk_aalloc`'s unlocked over-aligned path is the second fix
  the accounting found (§2f).
- **The switch is the variable's NAME**, present alone or with any value,
  matched exactly (`NPK_HEAP_STATSX` and `NPK_HEAP_STAT` do not switch it
  on), so no value spelling is a second way in; measured.
- Reserved words met: `fails` (a struct field named for the failure list;
  the never-fails clause's second word), and D-004's conservative rule at
  two helpers handed `@local` whose results flowed into a returned value
  (by-value parameters instead — a borrow the rule accepts).

**The baseline (the 1.5.1-close compiler under the step-0 floor; `allocated`
/ `peak_live` / `count` are the allocator's, seconds are this loaded machine's
colour; every point exit 0):**

| point | exit | allocated (bytes) | peak_live (bytes) | count | colour (s) |
|---|---:|---:|---:|---:|---:|
| fixed-array 1000 | 0 | 110 088 643 (105.0 MiB) | 46 914 777 (44.7 MiB) | 185 520 | 0.50 |
| fixed-array 2000 | 0 | 381 151 135 (363.5 MiB) | 138 234 178 (131.8 MiB) | 252 757 | 1.27 |
| fixed-array 4000 | 0 | 1 447 378 562 (1.35 GiB) | 495 173 354 (472.2 MiB) | 386 182 | 4.48 |
| fixed-array 8000 | 0 | 5 651 828 407 (5.26 GiB) | 1 899 062 179 (1.77 GiB) | 643 110 | 17.02 |
| statements 1000 | 0 | 157 251 990 (150.0 MiB) | 117 105 067 (111.7 MiB) | 313 317 | 0.94 |
| statements 4000 | 0 | 1 794 283 089 (1.67 GiB) | 1 306 962 463 (1.22 GiB) | 898 336 | 7.42 |
| literal 60000 | 0 | 1 818 955 580 (1.69 GiB) | 16 099 098 (15.4 MiB) | 238 344 | 5.94 |
| literal 480000 | 0 | 115 224 071 781 (107.31 GiB) | 19 640 989 (18.7 MiB) | 1 078 348 | 331.14 |
| big_fixed_array (4000) | 0 | 1 615 610 014 (1.50 GiB) | 551 269 269 (525.7 MiB) | 386 182 | 5.01 |
| check src/main.npk | 0 | 10 866 704 175 (10.12 GiB) | 10 798 793 937 (10.06 GiB) | 3 000 391 | 170.72 |
| npkc src/main.npk | 0 | 14 239 407 968 (13.26 GiB) | 11 152 984 794 (10.39 GiB) | 6 543 294 | 282.97 |

Read across: axis 1 grows ×3.5 / ×3.8 / ×3.9 in `allocated` and ×2.9 / ×3.6
/ ×3.8 in `peak_live` per doubling — quadratic, converging on 4; axis 2 ×11.4
/ ×11.2 from 1 000 to 4 000; axis 3 ×63 in `allocated` from 60 000 to
480 000 bytes (107 GiB requested to escape one literal) with `peak_live`
flat at 15–19 MiB — the reporter's "quadratic in time, flat in memory",
which is why `allocated` is held. The reporter's committed 4 000-row file
peaks 11 % above the generic recipe at the same row count (551 MiB against
472 MiB): their unconfirmed fourth observation, confirmed and explained —
the builders copy accumulated TEXT, so longer identifiers lengthen every
copied prefix; the same pathology, not a fourth. The compiler over itself:
the frontend alone (`check`) requests 10.12 GiB and peaks at 10.06 GiB —
3.0 million allocations and almost nothing freed — and `npkc` requests
13.26 GiB, peaks at 10.39 GiB, 6.5 million allocations. The probes: the
nested form's peak is 9 992× the bound form's (400 100 040 against 40 041
bytes; both request the same 200 MB per 20 000 iterations of the inner
concat). The `cost` stage's own run at this tree: axis 1 ×13.2 / ×10.6,
axis 2 ×11.4 / ×11.2, axis 3 ×15.4 / ×1.07, temporaries ×9 992 — four
designed failures confirmed in both runners, five verdicts agreeing.

The first full run was red by one row: `tcb-floor` reported `@npk_hs_arm` —
the arming function the argv/environ fix added to the floor — as a define
TCB.md's table did not list (P-26's check: the table is held to the
classifier, never hand-trusted). The row was added and the run repeated;
every other stage was green on the first run, `parity` agreeing on 965
verdicts.

**Harness (the whole tree, 2026-09-04): every stage green — `ok 58 test(s)
passed`; `verify` 141 obligations, 116 discharged, 25 open, `nitpick.obligations`
matches, the verified compiler rebuilds itself; `parity` 965 verdicts agree between
the two runners, `npkc` and the verified compiler byte-identical; `selfhost`,
`undef-ban`, `repro`, `absent-fact` green; `cost` 5 unit(s), 4 designed failure(s) confirmed.**

### Step 1 — LANDED (2026-09-03): DEF-2, the header and the entry points (D-248)

Both halves as ratified; two things the plan did not foresee, decided on
contact:

- **A module name is an identifier, so fifteen files were renamed.** The
  conformance suite's `00_minimal.npk` … `14_alloc_growth.npk` cannot carry
  `mod:00_minimal;`; they are `c00_minimal.npk` … `c14_alloc_growth.npk`
  (`git mv`, the reading order kept, the suite README saying why). Nothing
  live referenced the old names — only the retired generator's `selftest.py`
  comment, which already named a file that no longer exists.
- **The generator did not know about the header** (found at step 2, fixed
  there): `gen_tables.py`'s `write` regenerated `builtins.npk` without the
  `mod:builtins;` line this step's sweep had given it, so the sweep's "a
  second run is a no-op" was true only until the table was next regenerated;
  step 2's regeneration for the `Views` column showed it, and `write` emits
  the header now.
- **A keyword cannot be a module name either, so six more files were renamed**,
  and the reason none of them had a header before is the same reason:
  `mod:derive;` is a parse error, and the snapshot's own loader read it as an
  import of a module named nothing. `src/frontend/macro/derive.npk` is
  `derive_gen.npk` (one importer, `pipeline.npk`); the programs `arena.npk`
  and `shared_arena.npk` are `arena_basic`/`shared_arena_basic`; the rejection
  tests `types/arena`, `types/assoc` and `analysis/wildx` are `arena_rules`,
  `assoc_rules`, `wildx_rules`. The sweep script reads the lexer's keyword
  table and refuses such a file by name now; the first applied run had
  headed them, which the builder's refusal found on the first build.
- **The snapshot's collector binds a header, so four compiler files were
  renamed** (D-205: `src/` is bounded by what the committed builder accepts).
  A member-less `mod:name;` was collected as a symbol `name` in the file's own
  scope by every compiler until this step, so a header naming a function the
  file declares or imports clashed under the builder: `src/main.npk` against
  its own `main` (RESOLVE-001), `resolve_type.npk` and `type_access.npk`
  against their own functions, `type_decl.npk` against the `type_decl` it
  imports from `types.npk` (a RESOLVE-008 shadow that turned every call into
  a call on a module). They are `src/npkc.npk`, `type_resolve.npk`,
  `type_members.npk`, `type_decls.npk`; the compiler built from this source
  skips the header slot in collection (`collect_file_module`) and in
  resolution, so the header binds nothing and the clash cannot recur once
  the snapshot is refreshed. `nitpick.toml`'s entry, the harness, the seed
  README and CLAUDE.md name `src/npkc.npk` now. The clash set was measured
  three times before it was complete — own declarations, then keywords,
  then every name a file can see — and the builder's refusal found each
  gap; the record keeps the count.
- **The sweep inserted the header before the first non-comment line, not at
  line 1**, so a file keeps its directives-then-header shape (the shape the
  293 files that already had one use); every pin still shifts by exactly one.
  243 files gained a header, not the plan's 240: the count moved with the
  tree between planning and the step (the three step-0 programs among them).
- **Both self-checks' generated cases had hyphenated names** and no header;
  they are identifiers with a header after their directives now, the texts
  byte-identical between the two runners as the 1.5.1 record required.
- **A `dir/mod.npk` is headed by its directory's name** (`mod:nested;` in
  `tests/modules/graph/nested/mod.npk`): the module a `mod.npk` supplies is
  the directory's, and the header names the module. MODULE_REFERENCE §1.1
  says so.
- **The entry-point pass sees inline modules too**: a `main` inside
  `mod:inner = { … }` in the root is refused like one in a sibling file — the
  emitter names `@main` by name alone, so the root's TOP LEVEL is the rule.
- Measured with the built checker before the harness: `header_missing.npk`
  RESOLVE-012 at 9:1, `header_mismatch.npk` RESOLVE-012 at 11:1 and nothing
  else (the sibling stays unloaded), `entry_in_module.npk` RESOLVE-013 ×3 on
  its pinned lines, `mod_ambiguous.npk` still RESOLVE-004, `owned_names.npk`
  still RESOLVE-001 ×6 on its shifted pins, the compiler compiles its own
  source; ten real-backend programs emit byte-identical IR under the step-0
  and step-1 compilers (the emitter is untouched), and the compiler's own
  emission moves exactly where its source moved — the four edited modules,
  the five renamed modules' paths and qualifiers, and `ModuleGraph`'s new
  `root_index` field (measured by mapping the renames back: the residue is
  those and nothing else).
- **DEF-5 arrived while this step was being written** (the workbench's
  O-N11, §2f): a root with `main` and no `failsafe` compiles at exit 0 and
  the reach analysis's arm contract returns early without a handler. It is
  the same pass's missing question and lands as step 1b, right after this.

**Harness (the whole tree, 2026-09-04): every stage green — `ok 58 test(s)
passed`; `verify` 141 obligations, 116 discharged, 25 open, `nitpick.obligations`
matches, the verified compiler rebuilds itself; `parity` 973 verdicts agree between
the two runners, `npkc` and the verified compiler byte-identical; `selfhost`,
`undef-ban`, `repro`, `absent-fact` green; `cost` 5 unit(s), 4 designed failure(s) confirmed.**

### Step 1b — LANDED (2026-09-03): DEF-5, a root with `main` and no `failsafe`

The workbench's fifth defect (their O-N11, `nitpick-time` `b092a9e`), raised
while step 1 was being written and taken into the batch at once: it is the
same question the entry-point pass asks of the root, from the other side.

- **Where the rule lives**: in the reach analysis, not the loader — the set
  the absent handler owes is computed there, at the very line that used to
  `pass NIL` when no handler existed, so the refusal can list it (the
  reporter's ask, granted in full). `REACH-003` at `main`, with the count and
  every identity in the set's order; a root with neither `main` nor
  `failsafe` is a library checked alone and stays silent.
- **What the early return had been doing**: D-179's contract (REACH-002) was
  enforced against every program that HAD a handler and asked of none that
  lacked one — the contract was discharged by deleting the handler, and the
  emitter then wrote calls to `@npk_failsafe` into nothing for `llc` to
  refuse at the next step of every recipe. The workbench's harness had read
  `npkc` exit 0 as "well-formed"; it does not any more.
- **Blast radius, theirs**: three files across six repositories, every one a
  negative probe; `nitpick-posix`'s `probe02g_cross_module.npk` looked like
  a repository resting on a macro-generated handler until its transcript
  showed the recorded verdict was MACRO-007 and PX-100 had replaced the
  shape with a generated literal `failsafe` — which is what DEF-5 and D-248
  both want. Their two reproduction files re-pin from "npkc 0, llc 1" to
  "npkc refuses, naming the arms".
- **The corpus had thirteen of them.** Twelve `tests/accept/` roots and
  `tests/analysis/rejection/untracked_borrow.npk` carried a `main` and no
  `failsafe` — invalid under D-013 since each was written, silent for the
  reason DEF-5 names. Step 2's whole-tree sweep found them; this step's
  first prefix harness was stopped at the finding (the accept stage
  requires silence, and the sweep had already shown twelve refusals) rather
  than run to its red; each file gained the handler its REACH-003 dictated,
  every owed identity named, and is silent again.
- Measured with the built checker: `missing_failsafe.npk` REACH-003 at
  18:1 naming `Wobble, IntOverflow`; `failsafe_reach.npk` and
  `failsafe_nopick.npk` unchanged.

**Harness (the whole tree, 2026-09-04): every stage green — `ok 58 test(s)
passed`; `verify` 141 obligations, 116 discharged, 25 open, `nitpick.obligations`
matches, the verified compiler rebuilds itself; `parity` 975 verdicts agree between
the two runners, `npkc` and the verified compiler byte-identical; `selfhost`,
`undef-ban`, `repro`, `absent-fact` green; `cost` 5 unit(s), 4 designed failure(s) confirmed.**

### Step 2 — LANDED (2026-09-03): DEF-3, view-makers are borrows (D-249)

As ratified, with the refinement the compiler's own idioms demanded and two
consequences the rule forced:

- **A view of a pointer-shaped root is the pointee's, not the frame's.** `@`
  borrows the storage under a place; a view of a `string`/`buffer`/array/
  struct local does the same (that storage dies at the frame's exit), but
  `string_from_bytes(buf, n)` over a `wild int8->` local, `v[1...3]` over a
  slice, a `cstring`'s bytes, a `Handle` — these alias what the pointer
  names, which lives where the pointer's provenance says. Without the
  distinction `esc_byte`, `text_join` and every `sink_view` caller would have
  been refused for returning views of heap blocks that outlive the frame.
- **Rule A is off for builtins.** A builtin's result aliases an argument only
  as the `Views` column says; the carries-a-pointer heuristic that closes
  `launder` for user functions would have called `string_concat(view, "")` a
  borrow — the idiom the tree copies views with.
- **A `move` parameter is not the caller's storage.** `root_is_param`
  admitted every parameter, so `pass (@p)` with `move T:p` — dropped at this
  frame's exit — travelled up into freed storage; a view of one would have
  too. Both refuse now.
- **A view of a temporary is refused, not tracked** (BORROW-012): tracking it
  would need a root with no name, and `@f()` is unspellable for the same
  reason; the message says to bind it first.
- **The generator dropped step 1's headers** on its first run after the
  sweep (found here, fixed here: `write` emits `mod:<name>;`).
- **The first sweep reported eight sites and refined the rule three times.**
  A view over `#ptr_add` (the substring idiom in `smt_manifest.npk`,
  `nfs.npk`, `nbridge.npk`) was "a view of a temporary" — the root walk now
  looks through `#ptr_add`/`#wild_slice` to their pointer, and `#wild_ptr`
  roots nothing. A `for` whose range bound read a view-holding local's
  `.len` (`smt_writer.npk`, `npkg/main.npk`) refused as BORROW-009 — the
  refusal guards the loop BINDING, so it asks whether the element type can
  carry a pointer (a range's integers cannot), failing closed on anything
  else. `lexer_init` returned a local bound to a literal holding a view of
  its parameter — a binding records THAT it holds a borrow, not whose — so
  the second look and rule 3's rooting walk struct and array literals, and
  the lexer returns its literal directly. Three programs changed:
  `srcmgr_text_proven` returns the view it already had, `npkg`'s `ctx_init`
  copies the root of its `move` parameter instead of viewing it, and rule B
  learned that a view is stored AS ITSELF — `arg_pointee_ty` answers a view
  with its own type and `can_connect` counts a `uint8[]`/`string` slot as
  its destination — which is why the reporter's case 6 (`stash(h,
  string_bytes(s))`) had not fired on the first pass. The second pass caught
  the rooting checks stopping at the `#ptr_add` node the classifier looks
  through (the substring idiom over a PARAMETER refused as a local's), so one
  walk — `view_root` — now answers kind and identifier for every consumer,
  and a `#` builtin that is not a view carries what its arguments carry.
  `views.npk` grew the three shapes. Third pass: 454 files under the checker (the
  compiler, `npkg`, the three tools, nine libraries, every test unit whose
  stage is the checker's) report nothing beyond their own expectations.
- **O-N12 rode along as a document fix** (the workbench's recommendation):
  TYPE_REFERENCE's `>>>` row struck — `>>` is already `lshr` on an unsigned
  operand — and BUILTIN_REFERENCE §2's "fast compiler intrinsics" sentence
  replaced by the truth (only the marked tables resolve).
- Measured with the built checker: `view_escape.npk` reports its seven
  findings on their pinned lines (BORROW-001 ×5, BORROW-002, BORROW-012), one
  each; `views.npk` and `view_in_frame.npk` pass; `borrows.npk`,
  `path_shapes.npk`, `wild_store.npk` unchanged; the compiler, `npkg`, the
  tools and the libraries check clean under the rule after the sweep.

**Harness (the whole tree, 2026-09-04): every stage green — `ok 58 test(s)
passed`; `verify` 141 obligations, 116 discharged, 25 open, `nitpick.obligations`
matches, the verified compiler rebuilds itself; `parity` 981 verdicts agree between
the two runners, `npkc` and the verified compiler byte-identical; `selfhost`,
`undef-ban`, `repro`, `absent-fact` green; `cost` 5 unit(s), 4 designed failure(s) confirmed.**

### Step 3 — LANDED (2026-09-03): DEF-1's builders through a `Sink`

The three axes, one shape, one fix — and the sweep the plan asked for,
classified:

- **Axis 1**: `emit_global_const` RETURNED the initialiser's text, and its two
  helpers built it by `out = cat3(out, …)` per field and per row. It is
  `emit_global_const_into(x, eid, Sink->)` now — a struct in an array in a
  struct nests into the same sink — answering `bool`; `emit_one_global`
  renders into a sink of its own and appends it to the module stream as the
  line's tail through `irw_raw_sink`, so a failure midway (an unfoldable
  constant, a defect) leaves nothing half-written.
- **Axis 2**: `emit_site_tables` wrote its three tables through `enc`, `ls`
  and `ps`, each a prefix grown per byte and per site; the pieces go into the
  module stream where they land (`irw_write`/`irw_newline`, the IR writer's
  new piece-of-a-line primitives; `irw_sink` public for the byte escaper).
- **Axis 3**: `esc_byte` returned a one- or three-byte VIEW of a wild block
  per byte of every literal — an allocation per byte, never freed — and
  `strtab_write_all` concatenated those onto `enc`. `esc_byte_into(Sink->,
  byte)` writes where the bytes land (`sink_write_byte`, one new sink
  primitive), and the literal's line is its pieces.
- **Two more program-sized builders converted beyond the plan's three**:
  `smt_table.npk`'s `obl_index_text`/`obl_rows_text` (a line per obligation
  file and per row — the manifest's inputs) and, in the runner, `npkg`'s
  `verdicts_text` (a line per verdict, ~940 today) and `manifest_of` (a row
  per obligation): each a `Sink` copied out once.
- **The emitted bytes did not change**: 193 emissions byte-compared between the compiler built before this
  step and the one built after it — 191 identical, and the two that moved
  (`diag_capture`, which imports the changed module, and the compiler over
  itself) moved only because their SOURCE changed: the before-compiler over
  the NEW source emits the after-compiler's bytes exactly, 17 011 164 of
  them for `src/npkc.npk`.
- **The sweep, classified.** The tree holds 329 statements of the
  shape `x = catN(x, …)` / `x = string_concat(x, …)` under `src/` (the plan
  counted 332 against an older tree; 651 with `lib/` and `npkg/`). Converted,
  because the loop they sit in is over the PROGRAM — its bytes, its sites,
  its rows, its obligations: 14 in `src/` (axis 3's one, axis 1's two, axis
  2's three, `smt_table.npk`'s eight) and 14 in `npkg/` (`verdicts_text`,
  `manifest_of`). Left as BOUNDED BY CONSTRUCTION, by family — (a) a loop
  over ONE declaration's members or one call's arguments: a call's arity
  (`args` at eight emitter sites, `sargs`, `params`), a function's
  parameters and cross-statement slots (`ir_func.npk`'s `slots`, `params`),
  a struct's fields and an enum's variants (`ir_types.npk`'s `body`,
  `emit_program.npk`'s vtable `entries` and `pick` `arms`), a simd width
  (`mo`), a coroutine's suspension points (`cases`), a type's generic
  arguments (`m`) — and every one of `derive_gen.npk`'s 104, which generate
  source per field or variant of the ONE declaration being derived (the
  plan's rule: converted only if a member count can plausibly reach the
  thousands, and a 26 838-row table is one array literal, axis 1, not one
  struct); (b) a fixed handful of pieces: a diagnostic message
  (`diagnostics.npk`, `type_*.npk`, `exhaust.npk`, `reach.npk`, 50 sites), an
  SMT declaration, hypothesis, goal, comment, header or row (`smt_encode`,
  `smt_writer`, `smt_manifest`, `smt_obligations`, 69 sites — the
  per-statement term building the plan expected to be program-sized is
  bounded per obligation, and the file text is assembled through
  `smt_writer`'s line list), a `ToString` body's digits (`prelude.npk`, 20),
  a path's segments (`paths.npk`, 2), a 16-nibble hex render, the writer's
  five sinks joined once (`irw_text`). Nothing in `lib/` accumulates over a
  program. The record line the plan asked for: converted 28 of 651, the rest
  bounded by construction.
- **The `cost` stage's three axis assertions pass** and their `expect =
  "fail"` negative controls are removed: COST_NUMBERS. The temporaries probe
  keeps its designed failure until step 4.

**Harness (the whole tree, 2026-09-04): every stage green — `ok 58 test(s)
passed`; `verify` 141 obligations, 116 discharged, 25 open, `nitpick.obligations`
matches, the verified compiler rebuilds itself; `parity` 981 verdicts agree between
the two runners, `npkc` and the verified compiler byte-identical; `selfhost`,
`undef-ban`, `repro`, `absent-fact` green; `cost` 5 unit(s), 1 designed failure(s) confirmed.**
