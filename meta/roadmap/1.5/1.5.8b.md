# 1.5.8b — The guarded kinds' rows, and the fields that make their facts true: `sealed` and `hidden`, the constants, `overflow`, the wrapping family, `bounds`, `cast-range`, field limits

**PLANNED 2026-09-19 by `nitpick-compiler_s11` on `35ad9e1` (1.5.8's close), measured first with a throw-away
prototype and four read-only code maps. The user settled SEVEN questions, each the day it was asked, all
first-hand to this seat. Their answers are recorded at step 0 as D-308…D-314 (§3 quotes each).** The last three
were not on the agenda when planning opened. D-311 was a conflict the library workbench (`nitpick-libs_s4`) found
by measuring D-310's reach. D-313 and D-314 answer three memory-safety holes that planning found while working
out what D-308's facts rest on. They come before any row: the facts a verifier trusts must first be true.

## 0. What 1.5.8b is

D-210 §4 promised that "1.5 proves the traps away": overflow obligations join the D-218 catalogue, and a
discharged obligation elides its guard through `llvm.assume`, never `nsw`/`nuw` (D-218.9). VERIFICATION_REFERENCE
§7b assigns this subcycle three kinds with a guard to elide: `overflow`, `bounds`, and `cast-range` (D-306's guard,
1.5.8 step 1). Planning measured before it wrote, and found four things:

- **Half the work is done by facts the encoder already has.** A prototype that writes an `overflow` row at every
  plain-integer `+ - *` of the compiler's own source discharges 1,158 of 2,343 rows. It moves no other verdict.
- **The other half needs facts the language could not state.** To the solver a field read is opaque, and D-220's
  `limit` binds no field. D-308 lets a field carry `limit<Rules>`, and D-309 decides what happens to the rows
  nothing proves: they stay guarded, and are measured and reported.
- **The facts a length rests on were not true.** Any program could write a string's `.len` (DEF-72), the prelude
  `List`'s `cap` (DEF-73), or any element through `List`'s raw `items` pointer (DEF-74). All three are confirmed
  by probes, as out-of-bounds reads and heap corruption. D-313 (`sealed`) and D-314 (`hidden`, and checked `l[i]`)
  close them first.
- **Two smaller defects sit in the constants.** A negated literal is emitted as a checked subtraction that can
  never fire (DEF-70). The constant folder wraps where the run time traps (DEF-71). D-310 refuses a certain
  constant overflow, and D-311 settles how `uint64`'s upper half is written without one. The user also raised the
  wrapping family `+% -% *%` himself (D-312), for Nikola's hot paths, before verification closes.

| step | what | decisions / defects |
|---|---|---|
| 0 | this plan, the seven decisions, the dated notes | D-308…D-314 |
| 1 | `sealed` and `hidden`: the mechanism, and the compiler-known containers' headers | D-313, D-314; DEF-72 |
| 1b | `List`: `l[i]`, its fields sealed and hidden, its operations, the ~1,700-site sweep, the bridging refresh | D-313, D-314; DEF-73, DEF-74 |
| 2 | the constants: the folder obeys D-210, TYPE-076, the emitter's fold, `~0u64` | D-310, D-311; DEF-70, DEF-71 |
| 3 | the `overflow` rows and their elision; the residue and the speed measured | D-309; DEF-75 |
| 4 | the wrapping family `+% -% *%` | D-312 |
| 5 | the `bounds` and `cast-range` rows and their elision | — |
| 6 | field limits, the allocation ceiling, the length producers, the built-in length fact | D-308; DEF-76 |
| 7 | the docs and the close; 1.5.8c planned execution-grade | — |

## 1. Where it starts (measured 2026-09-19 on `35ad9e1`; the refreshed snapshot `b7585e71…` IS the compiler's own emission)

- **The guard census.** Counted over CODE lines: the emitter's own source text puts `npk_trap(i32 …)` strings into
  `@.str` constants, and a count that includes them is off by those strings. The census:
  - `IntOverflow` (−4110): 2,250 guards;
  - `OutOfBounds` (−4099): 15 texts, some of them strings;
  - `CastRange` (−4117): 1.

  Every other kind already has rows: `DivByZero`, `DivOverflow`, `DeadlineExceeded`, `EnsuresViolated`,
  `ShiftRange`, and one each of `BorrowOverlap`, `LimitViolated`, `Unreachable`, `BadStep` and `TbbErr`.
- **The prototype** (scratch worktree, never committed). It is `record_overflow` at every plain-integer binary and
  compound `+ - *`, with the goal `smt_range_axiom(result, width, signed)`, a hypothesis after the site as
  `shift-range`'s is. An operand the encoder cannot express records the row `unencoded`, because an opaque result
  carries its width's axiom and would discharge vacuously. Over `src/npkc.npk`:
  - **2,343 `overflow` rows: 1,158 discharged, 1,176 open, 9 budget, 0 unencoded.**
  - Every other kind was unmoved (275 discharged, 138 open, 22 unencoded, 6 checker), exactly the committed
    manifest.
  - Deciding the rows took about three minutes with 8 niced workers under load.
  - With every `int64` `.len`/`.count`/`.cap` read ASSUMED in [0, 2^48], **1,325 discharged (+167)** and 1,009
    stay open.
  - The open rows' shapes: opaque field reads plus or minus a constant; `int32` counters with no stated bound;
    capacity doubling (`nc * 2`) and products (`elem_size * count`); `0 - mag`. 317 of them are in the prelude's
    numeric cores (Dragon4, `tfp_q_decimal`, the frac cores, `list_reserve`).
- **Rows against guards, per function.** The prototype's 2,343 rows against the emission's 2,250 guards:
  - 109 rows sit in 26 functions the emission never holds, all trimmed prelude items. The cross-check already
    counts a row only for a held function (`_row_function_held`).
  - An async function's rows are keyed to its source symbol, and its guards sit in the resume function. The
    belts already map one to the other.
  - Negation has no row. Its guard is emitted with site 0 (`ir_expr.npk:1747`, although the unary node's id is
    in scope).
  - `.sum()` puts N−1 traps under one site (`ir_expr.npk:5593`, one call per fold step).
- **DEF-70 (the dead guards).** 84 guards in the compiler's emission are a negated literal lowered as
  `llvm.ssub.with.overflow(0, k)`, 53 of them `i32` and 25 `i64`. 85 have both operands constant. No arithmetic
  folding exists in the emitter: `IrVal` has no constant flag, and nothing reads `.ref`'s text.
- **DEF-71 (the folder).** `fold_const` (`type_resolve.npk:858`) computes in one `int64` `num` whatever the type:
  - `0u64 - 1u64` folds to −1, the bit pattern of 2^64−1;
  - an `int32` sum past 2^31−1 keeps its 64-bit value;
  - at the 64-bit boundary the COMPILER ITSELF traps `IntOverflow`, since it is built under D-210.

  Measured on step 4's tree: `fixed uint64:B = 0u64 - 1u64` equals `~0u64`, and the same subtraction at run time
  exits 93. D-148 and LEXICAL_REFERENCE §6.2 prescribe that very spelling for `uint64`'s maximum. The library
  workbench has exactly two such sites and this tree none in code. `derive_gen.npk:201-207`'s `BASIS_TEXT` and
  `PRIME_TEXT` are dead, with a "wraps by definition (D-037)" comment.
- **DEF-72 (the headers).** `.ptr`/`.len`/`.cap` of `string`, `cstring`, slices and `buffer` are typed as places
  (`type_members.npk:445-477`), accepted by `require_place`, and stored through a GEP (`ir_expr.npk:9566-9585`).
  Probe: `s.len = 4096` on a 6-byte string, then `string_slice(s, 4000, 4096)` SUCCEEDS, a 96-byte out-of-bounds
  heap read. No test exercised it.
- **DEF-73 (the List's fields).** `List<T>` is `{ wild T->:items; int64:count; int64:cap; }` (`prelude.npk:2087`),
  written by `list_init`, `list_reserve` and `list_push` only. Probe: `a.cap = 100` on a one-element list, then
  forty `list_push`, overwrites a second list's element (exit 3). `src/` writes `.count` directly about 150 times
  by text (truncations to a mark, pops by `- 1`).
- **DEF-74 (the List's elements).** Every element access in the tree is `l.items[i]`, an unchecked `wild T->`
  index legal anywhere. Probe: `a.items[1..5] = …` on a one-element list overwrites the second list (exit 3). Sites:
  `src/` 665, `npkg/` 1,011, `tests/` 22. The library workbench has 103, all inside their declaring modules.
- **The length producers.** No allocation ceiling exists: `npk_alloc_impl`'s only size check is the sign
  (`npkrt.ll:5970`), and the spec promises `n < 2^63`. The producers fall into three groups:
  - bounded by an allocation the floor made: `string_concat`, `string_slice`, `int_to_string`, `to_cstring`,
    `read_file`, `read_stdin`, `buffer_new`;
  - bounded by a source length: range views, `string_bytes`;
  - supplied by the caller and unchecked: `string_from_bytes` (BUILTIN_REFERENCE says it "traps on misuse", and
    nothing checks) and `#wild_slice` ("legal only in `wild` context", enforced nowhere).

  `tests/backend/programs/oom.npk` asks for 2^62 bytes and expects `HeapOom`. The floor spec's `npk_dalloc` and
  `npk_hunmap` boundary sentences say `HeapBadRequest` where the IR traps −4102 (DEF-76).
- **The runners' tables** (`harness.py:3101-3132`; `npkg/verify.npk:536-632`):
  - `TRAP_OF_KIND` knows none of −4110, −4099 or −4117.
  - `ASSUME_KINDS` lacks `cast-range` although `emit_cast_range_guard` already elides into `llvm.assume`
    (`ir_expr.npk:4329-4330`). This gap is found here.
  - npkg's `read_rows` silently skips a `rows.txt` line with the wrong field count or an unknown role, where the
    harness fails the run (DEF-75).
  - `check_obligation_kinds_agree` checks the guard column but not that every guarded kind has a trap code.
  - No npkg twin of that check exists.
- **The verify suite.** 83 of the 98 programs gain rows of their own, 198 in all; a test names its own rows, never
  the prelude's. The runner self-checks' programs VDIV, VLIM and VPROVE subtract, so their exact-count headers gain
  rows too (`selfcheck.py:1077/1092/1094`, `selfcheck.npk:639/645/650`).
- **What the library workbench measured.**
  - No writes to built-in headers.
  - No use of the prelude `List`: their own `Vec<T>` has DEF-73's shape, and they will seal it and hide its
    `items` at no call-site cost.
  - A container needs get, set, pop, insert, remove, swap_remove, truncate, clear and a drop-owning free.
  - Their foreseeable wrapping consumers are FNV-1a 64 per byte and splitmix64. The published constants are mostly
    above 2^63−1, written per D-311 as `(1u64 << 63u64) | 0…hexu64` (compiled and checked on step 4's tree).

## 2. The design

### 2.1 `sealed` and `hidden` (step 1; D-313, D-314; DEF-72)

- **Two keywords, two field qualifiers.** `sealed` means readable everywhere and written only by the declaring
  module. `hidden` means neither read nor written outside it. They enter LEXICAL_REFERENCE's keyword table (the
  generated region), and `gen_tables.py` regenerates the token tables. Both are measured as identifiers across
  `src/`, `lib/`, `npkg/`, `tools/`, `tests/` and the library trees before they land, and a collision is renamed in
  the same commit (the reserved-word table in CLAUDE.md gains the pair). `p_qualifier_bit` (`parser.npk:271`)
  gains `QUAL_SEALED` and `QUAL_HIDDEN`. On anything but a field declaration they are refused where the other
  field-only qualifiers are.
- **"The declaring module"** is the module whose item window holds the struct declaration. For a generic
  instance it is the template's module (K-4). For an inline module it is that module.
- **The write forms** (D-313 §2): an assignment through any path, a compound assignment, a struct literal, a move
  or `pass` out of the field, `@`, `$$m`, and a `Self->` receiver call. A `$$i` claim reads. **The read forms**
  (D-314 §1): a load, a copy out, a `$$i` claim, and a struct pattern binding the field. Outside the module a
  write to a sealed field is `NITPICK-TYPE-079`, and any touch of a hidden field is `NITPICK-TYPE-080`, reported
  at the field access's span. The checker's precedent is TYPE-071's `place_fixed`/`struct_field_fixed`
  (`type_stmt.npk:1143-1209`), which reads a field's qualifier bit and follows a pointer base. The same walk
  answers "sealed/hidden, and whose".
- **The compiler-known containers' headers are sealed by definition.** No declaration exists to carry the bit, so
  the member typer (`type_members.npk:445-477`) marks `ptr`/`len`/`cap` of `string`, `cstring`, a slice and
  `buffer` as non-writable places. Every write form above is TYPE-079 in every module, the prelude included,
  because no module declares them. The prelude's own writes to them are measured at step 1. If any exists, it
  becomes a floor or emitter primitive, never an exemption.
- **Tests.**
  - `tests/types/rejection/sealed_fields.npk`: every write form outside the module; a `$$i` claim accepted; the
    declaring module's own writes accepted.
  - `hidden_fields.npk`: every read and write form.
  - `header_writes.npk`: each built-in kind's three headers under each write form.
  - A program whose module keeps a two-field invariant through sealed fields and functions that maintain it.
  - `lenwrite.npk` (DEF-72's probe) becomes a rejection case.

### 2.2 `List` (step 1b; D-313 §5, D-314 §2-4; DEF-73, DEF-74)

- **`l[i]`.** The checker types an index on a `List<T>` like a slice's: the element's type as a place, readable,
  writable and claimable. The emitter computes the element address from `items` inside its own lowering and
  guards it with the slice's guard, `idx <u count`, `OutOfBounds`, with the same site convention (step 5 adds the
  site parameter and the row). The encoder gives the element read an opaque term, as a slice element has, and the
  index a site.
- **The prelude.** The fields become `hidden wild T->:items; sealed int64:count; sealed int64:cap;`. New checked
  functions sit beside `list_init`/`list_reserve`/`list_push`:
  - `list_pop`: moves the last element out; an empty list traps `OutOfBounds`;
  - `list_truncate(l, n)`: drops elements `n…count−1`; `n` outside `[0, count]` traps `OutOfBounds`;
  - `list_clear`;
  - `list_insert(l, i, v)` for `i` in `[0, count]`;
  - `list_remove(l, i)`: order-preserving, returns the element;
  - `list_swap_remove(l, i)`.

  Each drops or moves owning elements as D-264 requires. Which functions `src/` needs is MEASURED by classifying
  every direct `.count` write first. A pattern none of them serves is recorded and raised, never forced.
- **The sweep.** Every `.items[i]` outside the prelude becomes `l[i]`, and every direct `.count` write becomes the
  operation it performs. The sites are `src/` 665, `npkg/` 1,011 and `tests/` 22. The compiler's own emission of
  itself changes, so the step measures it: the guard census grows by one bounds guard per element access the
  optimizer does not prove, and the step reports the compiler's build time before and after.
- **The bridging refresh** (`bootstrap/seed/README.md`, the 1.5.1b step 5b variant). The builder's embedded prelude
  has neither `l[i]` nor the new functions.
  - **Hop 1, the bridge, never committed:** the tree with `List` indexing, the new prelude functions, and the
    fields NOT yet qualified, with `src/`, `npkg/` and `tests/` unswept. The committed snapshot builds it.
  - **Hop 2, the commit:** the fields sealed and hidden, and the sweep applied. The bridge builds it.

  Then stage 2 == stage 3, installed with a STAMP naming the commit's own tree.
- **Tests.**
  - `listcap.npk` and `listidx.npk` (the DEF-73/74 probes) become rejection cases (TYPE-079/080).
  - `list_index_oob.npk`: `l[l.count]` traps `OutOfBounds`.
  - `list_ops.npk`: each operation, including an owning `List<string>` whose truncate, remove and pop are measured
    by `NPK_HEAP_STATS`.

### 2.3 The constants (step 2; D-310, D-311; DEF-70, DEF-71)

- **The folder computes at the operation's width and signedness** (K-7). `ConstVal` keeps its 64-bit `num`, read
  through the type: an unsigned 64-bit value above 2^63−1 is held as its bit pattern and compared as unsigned.
  - For widths up to 64, `+ - *` and negation are computed exactly: in 64-bit arithmetic with an overflow check,
    or in a 128-bit intermediate where the 64-bit form cannot hold an operand pair. The result is range-checked
    against the declared width. An out-of-range value is `NITPICK-TYPE-076` at the operation.
  - For widths above 64, the folder folds only when both operands and the exact result fit the 64-bit window.
    Otherwise it declines (`cv_none`), and the run-time guard stays: safe, never a wrong value.
  - The folder never traps. Its arithmetic is written so that its own overflow is impossible: every step is
    guarded before it is taken.
- **TYPE-076's reach is the folder's reach:** literals, negated literals, `fixed` constants and `comptime` values.
  A run-time operand is never folded. The code is defined beside TYPE-070 in `type_codes.npk`. It is reported by
  the checker at every binary `+ - *` and unary `-` whose operands fold (`type_expr.npk`), and at `fixed`
  initialisers through `is_const_initialiser`.
- **The emitter folds** (DEF-70). A unary `-` over an integer literal emits the negated constant. A `+ - *` whose
  operands the resolver's folder evaluates, and whose value fits (the checker has refused the rest), emits the
  value. Both skip the intrinsic, the guard and the row. The fold asks `fold_const` at the node, never `.ref`'s
  text.
- **`~0u64`** (D-311). The folder's `~` already folds (`type_resolve.npk:1006`). What changes is the example text:
  LEXICAL_REFERENCE and D-148 carry dated notes already, as of step 0. `derive_gen.npk:201-207` is removed.
- **Tests.**
  - `tests/types/rejection/constant_overflow.npk`: `127i8 + 1i8`, `0u64 - 1u64` in a `fixed` initialiser,
    `-(1i32 << 31i32)`-shaped folds, a `comptime` product, an `int32` sum past 2^31−1, and a wide width that
    declines and is accepted.
  - `constant_fold.npk`: `-1i32`, `2i64 * 8i64`, and `~0u64` beside `(1u64 << 63u64) | 04BF29CE484222325hexu64`,
    each with an exact expected value, and an IR assertion (the `ll_is` pattern) that no `with.overflow` call
    remains for them.
- **The measurement.** The compiler's guard census drops by the constant pairs, 85 by the reconciliation.

### 2.4 The `overflow` rows (step 3; D-309; DEF-75)

- **One row per `IntOverflow` guard**, at the guard's own site key (K-9):
  - binary: `SITE_EXPR` and the node id, as the division rows are;
  - compound: the target's id;
  - negation: the unary node's own id, where the emitter's 0 is corrected to that id;
  - a `simd` integer lane: ONE row over the lanes' conjunction (D-282's shape), because the emitter's guard is one
    any-lane trap;
  - `.sum()`: ONE row at the site with traps N−1 (`record_at`'s traps field), because the scalar core guards each
    fold step at that one site.

  The goal is `smt_range_axiom(result, width, signed)`. It is a hypothesis after the site, and inside a
  proposition it belongs to the proposition (DEF-33). An inexpressible operand makes the row `unencoded`.
- **Elision (D-218.9).** A discharged row's branch becomes `call void @llvm.assume(i1 %no)` over the intrinsic's
  negated overflow bit. The intrinsic stays, and `llctx_elided(ctx, SITE_EXPR, site, OK_OVERFLOW)` is asked
  exactly where the division pair asks. The same applies to the vector guard (`ir_expr.npk:5479`, "No obligation
  row yet").
- **Both runners' tables.**
  - `TRAP_OF_KIND["overflow"] = "-4110"` in both runners.
  - `ASSUME_KINDS` gains `overflow` and `cast-range`: the second is the gap §1 found, and `bounds` joins at step
    5. npkg's list is inline at `verify.npk:628`.
  - `check_obligation_kinds_agree` gains the rule the three kinds lacked: every kind `ok_has_guard` calls guarded
    has a trap code in `TRAP_OF_KIND`. An npkg twin of the check is written.
  - DEF-75: npkg's `read_rows` fails a malformed line by name, as the harness does.
  - `smt_table.npk:8`'s header comment names the eleventh field (tier).
- **The manifests.** `nitpick.obligations` is re-recorded in this step's commit (D-040) with about 2,250 new rows.
  - The 83 verify programs gain their own rows' `expect-obligation` lines. Each count is read from the new
    compiler's `rows.txt` and checked by reading the program, never copied blind.
  - The runner self-checks' three programs gain theirs.
  - New verify tests: a discharged row whose guard is gone from the elided build; an open one whose guard stays;
    the lane form; the `.sum()` form; negation; compound.
- **D-309's report.**
  - The residue by function, open and budget, with the shapes, is written into this plan's record.
  - The compiler's speed is measured three ways: `build/npkc` (plain), the verified compiler (elided), and both
    compiling `src/npkc.npk`. Each is taken under `NPK_HEAP_STATS` beside wall-clock, with instruction counts from
    `valgrind --tool=callgrind` on one compile. The measurement is recorded, never gated (K-13).

### 2.5 The wrapping family (step 4; D-312)

- **Tokens and grammar.** `+%`, `-%` and `*%`, with `+%=`, `-%=` and `*%=`, enter LEXICAL_REFERENCE's operator
  production (the generated region, then `gen_tables.py`). OP_REFERENCE gains the rows. The binary operators sit
  at `+`/`-`/`*` precedence and the compound forms beside `+=`. No existing program changes meaning: no such token
  exists today, and `%` never starts an expression.
- **The checker.** The operands must be the same plain integer type (`intN`/`uintN`), or `simd` integer lanes. On
  any other kind the operator is `NITPICK-TYPE-078`, which names the kind and says why: `tbb` would launder ERR;
  `tfp`, the ternary kinds, `frac`, `dim256` and `complex` have their own arithmetic; floats are IEEE. The
  compound form's target follows the same rule.
- **The emitter.** A plain LLVM `add`/`sub`/`mul` with no flags, or the vector form for lanes. No intrinsic, no
  guard, no row. REACH arms nothing for them (`reach.npk:316-326`). The folder folds them with the wrap and never
  reports TYPE-076. The encoder models the result exactly:
  - unsigned: `(mod (op a b) 2^N)`;
  - signed: two's complement, `(- (mod (+ (op a b) 2^(N−1)) 2^N) 2^(N−1))`;
  - lane by lane for `simd`.
- **Tests.**
  - `tests/backend/programs/wrap_ops.npk`: every operator at `int8`, `uint64` and `int128`, including wrapping
    across MIN/MAX, with python-computed expected values.
  - `wrap_fnv1a.npk`: FNV-1a 64 over three strings against their published digests, with the offset basis written
    per D-311.
  - `wrap_splitmix64.npk`: the first five outputs from seed 0 against the reference sequence.
  - `tests/types/rejection/wrap_kinds.npk`: TYPE-078 on each refused kind.
  - A verify test in which a `*%` site has no row and the value after it is known to the solver.
- **The prelude's FNV step** (`fnv_mix`, `prelude.npk:459`) uses `uint128` widening today. It adopts `*%` if the
  measurement shows the IR smaller and no row regresses. FNV's modular multiply is the stated intent, so the
  operator states the truth, which D-309 allows (K-15).

### 2.6 The `bounds` and `cast-range` rows (step 5)

- **`emit_bounds_guard` gains a site parameter** (`ir_expr.npk:1806`). Every caller has the index expression's id
  in scope: `.trit`/`.nit` 1969, slice 9611, array 9645, `simd` lane 9666, and step 1b's `List` index. The guard
  asks `llctx_elided(..., OK_BOUNDS)`.
- **The encoder records `bounds` rows** with the goal `(and (<= 0 i) (< i len))` over the sign-extended index:
  - for a fixed array, `len` is its type's constant (DEF-22);
  - for a slice, it is the slice's `.len` term;
  - for a `List`, it is `count`'s read term (step 6's fact makes it bounded).

  The range slice's combined guard (`ir_expr.npk:10339-10354`) gets one row, `(and (<= lo hi) (<= hi len))`. The
  inclusive form's `hi + 1` is not a separate row: a wrap there fails the unsigned compare and traps, and the
  row's term is the exact `hi + 1`.
- **`cast-range` rows** at `emit_cast_range_guard`'s two callers (`ir_expr.npk:4660`, `5662`). The goal is
  D-306's condition in the float theory: not NaN, and `LO < f < HI` in `double`, with a `flt32` operand widened
  exactly. A `simd` cast is one row over the lanes. The guard's elision is already written.
- **Both runners:** `TRAP_OF_KIND` gains `bounds` → −4099 and `cast-range` → −4117, and `ASSUME_KINDS` gains
  `bounds`. The manifests are re-recorded. The verify suite gains the new rows' lines, and new verify tests cover
  a proven index, an unproven one, a proven cast and an unproven cast.

### 2.7 Field limits, the ceiling, and the lengths (step 6; D-308; DEF-76)

- **Grammar.** `p_parse_field` (`parse_decl.npk:1029`) accepts `limit<R>` after the qualifiers, as a local does
  (`parse_stmt.npk:642`), into the field's unused slot b (`DeclFieldDecl`). The macro expander's `splice_field`
  (`expand.npk:1787-1828`) clones it, where today it drops b. A `field_limit` accessor is added.
- **Resolver.** `resolve_limit` runs for each field, at module scope. RESOLVE-002 and RESOLVE-011 apply as for
  every limit site.
- **Checker.**
  - TYPE-059 (the subject is the field's type by identity).
  - TYPE-063 extended: `place_limited` (`type_stmt.npk:1094`) answers true for a place whose own field is
    limited, not only a limited root, so a limited field has no address.
  - **TYPE-077 (new):** the rule is folded over the field's vacant value at the declaration (D-225; the vacant
    value of a scalar is 0, of `OwnedFd` −1), and refused if the fold is `false`. A rule the folder cannot decide
    over the vacant value is refused too, since there is no write point to check it at.
- **The emitter's write points** (`ir_rules.npk:390`, `emit_assign_limit`, extended):
  - a store to a limited field through any path, `p.f = v` through a pointer included, checks the field's value
    with the field's rule;
  - a compound store the same way;
  - a struct literal checks each limited field's value;
  - a move or `pass` out of a limited field writes the vacant value, which TYPE-077 has already proven.

  `LimitViolated` is armed by REACH wherever a limited field is written.
- **The encoder.**
  - Every write to a limited field is a `limit` row: the rule over the written value. The field-write branch of
    `walk_stmt` records it (`smt_encode.npk:4851-4902`).
  - Every read of a limited field is a FACT: the member-access branch (`smt_encode.npk:4132`) pushes the rule over
    the read's fresh term as a hypothesis for later rows (K-17). A proposition's read contributes to the
    proposition (DEF-33).
- **The allocation ceiling.** `npk_alloc_impl` refuses a request above 2^47 bytes with `HeapBadRequest`
  (−4104), beside its sign check (`npkrt.ll:5970`). `npk_alloc_internal`'s spec clause
  `(ensures (< n 9223372036854775808))` becomes `(ensures (<= n 140737488355328))`, decided with the floor's rows.
  The boundary texts that mention the ceiling are updated: `npk_alloc_impl` "past the size ceiling", VR §9.2's
  note, TCB.md. DEF-76 is corrected in the same edit: `npk_dalloc` and `npk_hunmap` trap −4102. `oom.npk` moves to
  a deterministic refusal below the ceiling: it lowers its own `RLIMIT_AS` through `sys`, as
  `thread_stack_release.npk` does, and asks for more (K-16). `tcb_floor.py --write` runs before and after `npkg
  verify --floor-only --record`, and the D-303 sweep runs after.
- **The length producers.** `string_from_bytes` and `#wild_slice` check `0 <= len <= 2^47` and trap
  `OutOfBounds` otherwise (K-18). This makes BUILTIN_REFERENCE's "traps on misuse" true. `#wild_slice`'s "legal
  only in `wild` context" is enforced as its row says: TYPE-054's rule for a builtin's call context. The producers
  bounded by an allocation or a source need nothing. The kernel-given `argv`/`environ` lengths are bounded by the
  kernel's `ARG_MAX`, far below the ceiling; the floor's `npk_cstr_slice` spec states it.
- **The built-in length fact.** The encoder's member-access branch pushes `(and (<= 0 t) (<= t 2^47))` for a read
  of `.len` or `.cap` of `string`, `cstring`, a slice or `buffer`. This is sound because the headers are sealed
  (step 1) and every producer is bounded (above). The prelude `List`'s `count` and `cap` carry
  `limit<ListLen>` with `ListLen = { $ >= 0i64, $ <= 140737488355328i64 }`: sealed and limited.
- **Tests.**
  - `tests/types/rejection/field_limits.npk`: TYPE-059, TYPE-063 (`@s.f`, `$$m`, a receiver call) and TYPE-077.
  - `tests/backend/programs/field_limit_trap.npk`: a write outside the rule traps `LimitViolated` through a
    pointer.
  - verify tests: a field limit discharging a later division; a built-in length discharging `s.len + 1`; a List
    count discharging `l.count - 1` under `l.count > 0`.
  - `string_from_bytes_len.npk`: a negative length and one above 2^47, each trapping `OutOfBounds`.
- **The measurement.** The compiler's `overflow` discharge is measured against step 3's baseline and the
  prototype's 1,325.

### 2.8 What 1.5.8b does not do

- `decreases`/`unbounded` and the `terminate` and `stack-depth` rows: 1.5.8c (D-304).
- Relational field invariants: declined by D-308. They are kept by a module's sealed fields and its functions'
  contracts.
- Bounds written for a count's sake: declined by D-309.
- NIKOS's disposition and the cycle's close: 1.5.8d.

## 3. Questions for the user — ALL SETTLED 2026-09-19, the day each was asked, first-hand to this seat

- **S-88 → D-308**, a struct field's `limit<Rules>`: "as far as the limit thing, I am in agreement with your
  recommendation. It sounds like it extends limit into an even more capable construct than the one I originally
  imagined. Lets ratify that."
- **S-89 → D-309**, the residue kept guarded, and **S-90 → D-310**, a certain constant overflow refused: "those
  recommendations sound fine to me as well. Lets ratify those too."
- **S-91 → D-311**, `uint64`'s upper half by bit operations, after the D-037 → D-210 history was set out for him:
  "that all sounds fine and now I do remember it. I think that means your recommendation is a go from me."
- **S-92 → D-312**, the wrapping family, his own question: "I agree. the % family makes the most sense to me too
  as it's actually describing what will happen, not just a nifty shorthand way of doing a thing."
- **S-93 → D-313**, `sealed`: "Lets go with A. As far as keyword, I think sealed is fine. … i'd rather overlap
  some than come up with some word that doesn't accurately represent what is happening."
- **S-94 → D-314**, `hidden` and checked `l[i]`: "hidden sounds fine to me. it says what it does. i like it".

## 4. What lands (each step a cumulative prefix under a full harness, D-228; the library listener is told before each language change lands)

0. **This plan and the seven decisions.** DECISIONS.md: D-308…D-314, and dated notes on D-148 and D-210.
   OPEN_DECISIONS: S-88…S-94 settled, and DEF-70…DEF-74 recorded (DEF-75 and DEF-76 are recorded by their steps).
   Also a dated note in LEXICAL_REFERENCE §6.2; 1.5.8.md's dated note; the 1.5 README's row; CLAUDE.md's status.
   GATE: the harness green. Nothing in the compiler changes.
1. **`sealed` and `hidden` (§2.1).** GATE:
   - the harness green;
   - the new rejection files in both runners;
   - every program's emission byte-identical to the previous step's, since this step refuses and generates
     nothing new;
   - the keyword measurement recorded, with any collision renamed.
1b. **`List` (§2.2).** GATE:
   - the bridging refresh at stage 2 == stage 3, installed with its STAMP;
   - the harness green WITH the refreshed snapshot;
   - the three probes refused;
   - `list_ops.npk` and `list_index_oob.npk`;
   - the compiler's build time and guard census before and after, recorded;
   - `nitpick.obligations` re-recorded if it moved (the sweep changes the compiler's own functions).
2. **The constants (§2.3).** GATE:
   - TYPE-076's rejection file;
   - `constant_fold.npk`'s values and its IR assertion;
   - the census's drop recorded;
   - the library listener told before the commit lands, with the two library sites' fix (`~0u64`).
3. **The `overflow` rows (§2.4).** GATE:
   - `nitpick.obligations` re-recorded, with the kinds and counts in the record;
   - every verify program's lines exact;
   - both runners' belts count −4110;
   - the kinds-agree rule in both runners;
   - DEF-75 fixed, with a planted self-check case each way;
   - the residue and the speed recorded.
4. **The wrapping family (§2.5).** GATE:
   - the known-answer programs at both optimisation levels;
   - TYPE-078's file;
   - the "no row" verify test;
   - the prelude's `fnv_mix` decision recorded with its measurement.
5. **`bounds` and `cast-range` (§2.6).** GATE: the manifests re-recorded; both runners' tables and belts; the new
   verify tests.
   - *[Refined 2026-09-19 while step 5 was being written.]* The `bounds` rows make DEF-82 likelier to meet real
     code (an element write inside a `defer` is an ordinary shape), and the tree still has none: every `defer` in
     `src/`, `npkg/`, `lib/` and `tools/` is a free, an unwatch or a capture release, with no guarded operation
     inside. The design to implement: the obligation table holds each row's FIELDS rather than its rendered text
     (it is written at the end of the compilation, `obl_write_dir`, so nothing else has read it yet), the encoder
     tags a row recorded inside a `defer` body with that statement's id, and the emitter — which knows how many
     times it wrote each body — multiplies those rows' `traps` by the count once the function is lowered. The
     elision answers stay computed before the lowering, as they must.
   - *[Added 2026-09-19 at step 3.]* **DEF-82**: a `defer` body's guard has one row and is written once per exit,
     so the belts count one trap where the build holds one per exit. The fix: a function's rows are written after
     its body is lowered, each row inside a `defer` body carrying the body's emission count as its traps (the
     elision answers stay computed before the lowering). A verify test puts a guard in a `defer` at two exits and
     holds both belts to it. Step 5 adds the `bounds` rows, and an element write in a `defer` is the likeliest
     shape.
6. **Field limits and the lengths (§2.7).** GATE:
   - the floor's rows (`runtime/npkrt.obligations` re-recorded, `tcb_floor.py --write` before and after);
   - the D-303 sweep;
   - `oom.npk` deterministic;
   - the compiler's discharge against step 3's baseline;
   - the library listener told (the ceiling and the two primitives' traps).
7. **The docs and the close.**
   - CLAUDE.md: the status, and gotchas for `sealed`/`hidden`, `l[i]`, TYPE-076 and the wrapping family.
   - VERIFICATION_REFERENCE §2 (field limits), §7b (the three kinds live), §8 (the registrations).
   - MEMORY_REFERENCE (`List`), OP_REFERENCE, TYPE_REFERENCE, BUILTIN_REFERENCE (the two primitives, the new
     `List` functions), TCB.md.
   - The 1.5 README row.
   - `1.5.8c.md` planned execution-grade.

## 5. Decisions taken at planning (each with its reason)

- **K-1 — live memory-safety holes first** (steps 1 and 1b before any row). A fact about a length is worthless
  while a program can write the length, and the holes are exploitable today.
- **K-2 — `List` is its own step (1b).** It needs a bridging refresh and a ~1,700-site sweep. The mechanism lands
  first (step 1), tested on user types and the built-in headers, so the sweep's step changes one thing.
- **K-3 — the built-in headers are sealed by the member typer**, not by a declaration. No module declares them,
  so no module's code may write them, the prelude included.
- **K-4 — a generic instance's declaring module is its template's.** An instance is the template at a type, not
  a new declaration.
- **K-5 — `$$i` of a sealed field is a read; `@`, `$$m` and a `Self->` receiver are writes.** It follows the
  capability each hands out, D-286's reading of the three.
- **K-6 — a struct literal is a write.** It sets every field. A struct with a sealed or hidden field is built by
  its module's functions, which is where its invariant is kept.
- **K-7 — the folder computes at the declared width and never traps.** Its answer is the language's answer at
  compile time, so it must be D-210's. A width it cannot represent declines to fold rather than guess.
- **K-8 — TYPE-076 reaches exactly what the folder folds.** A run-time value is the guard's business.
- **K-9 — the overflow row's site key is the guard's own.** The belts pair rows with traps by (symbol, code,
  group), so a row keyed anywhere else is a red belt. Negation's site 0 is corrected rather than matched.
- **K-10 — elision keeps the intrinsic and assumes its negated bit (D-218.9).** `nsw` would license deleting the
  guard's reason; the assume states it.
- **K-11 — `cast-range` joins `ASSUME_KINDS` at step 3**, with `overflow`. It already elides into an assume, and
  its absence was a gap in both belts.
- **K-12 — the kinds-agree check gains "every guarded kind has a trap code" in both runners.** The three kinds'
  absence from `TRAP_OF_KIND` was invisible to every instrument.
- **K-13 — D-309's speed measurement is recorded, never a gate.** A wall-clock is never a verdict, and the
  instruction counts are the stable half.
- **K-14 — the wrapping tokens are generated from LEXICAL_REFERENCE's region**, like every token, because a
  token list with two homes drifts.
- **K-15 — `fnv_mix` adopts `*%` on measurement.** FNV is modular by definition, so the operator states the truth
  (D-309 allows a bound or spelling that is true).
- **K-16 — `oom.npk` lowers its own `RLIMIT_AS`.** Under a 2^47 ceiling a large request is a bad request, and
  `mmap` with overcommit may grant anything below it. The address-space limit makes the refusal deterministic.
- **K-17 — a limited field's read is a fact wherever it is read.** Every write is checked or proven, the field
  has no address, and a sealed field has no writer outside its module. The read is therefore true by
  construction. A `wild` write through a raw pointer is the author's opt-out.
- **K-18 — the length primitives trap `OutOfBounds`, not a new identity.** A length beyond the address space is
  a view out of bounds. `OutOfBounds` is armed in nearly every program already, and a new universal identity
  would add to the library workbench's re-pin sweep (145 `failsafe`s at pin 3d15ac9 name neither of step 0's
  two).

## Execution record (appended as steps land)

### Step 0 — the plan and the seven decisions (2026-09-19)

*Written at step 1: step 0's harness ran on a tree whose record section was empty.*
- **What landed:**
  - this plan;
  - DECISIONS D-308…D-314, with dated notes on D-148 and D-210;
  - OPEN_DECISIONS S-88…S-94 SETTLED with the user's words, and DEF-70…DEF-74 recorded (DEF-75 and DEF-76 at their
    own steps, as §4 says);
  - a dated note in LEXICAL_REFERENCE §6.2;
  - 1.5.8.md's dated note;
  - the 1.5 README's 1.5.8b row PLANNED;
  - CLAUDE.md's status, and its stale decision count (240 → 314).
- No compiler, floor, manifest or ladder byte moved.

### Step 1 — `sealed`, `hidden`, and the headers sealed by definition (2026-09-19)

- **The keywords.** `"sealed" | "hidden"` join LEXICAL_REFERENCE §4's `ModuleKeyword` (beside `pub`, since both are
  about module ownership), with a production comment. `gen_tables.py` regenerated the token tables (252 kinds, 168
  keywords).
- **The measurement (§2.1), exhaustive over code lines and the strings a test parses:**
  - `src/`: a local `sealed` (`exhaust.npk`, now `after_wildcard`) and a field `sealed` (`bindings.npk`, now
    `sealed_any`).
  - Seven tests named an inline module `hidden`, now `nested` (same length, so every pinned column holds):
    `mod_private_qualified`, `error_arms`, `mod_not_value` (types), `mod_private_use` (modules), and the programs
    `inline_mod`, `mod_qualified` and `error_arm_forms`.
  - Five files named a function or a local `hidden`, now `secret`: the visibility fixture `tests/modules/vis/lib.npk`
    with `private_import.npk` and `private_name.npk`, and the unit tests `imports.npk` (the name as a string it
    asserts on) and `symbols.npk` (in embedded source it parses).
  - The docs' examples: CLAUDE.md's, MODULE_REFERENCE §2.2 and §3's, the modules README's row. A dated note on D-273
    records the rename.
  - The first survey missed `error_arm_forms.npk`, and the IR comparison below caught it: the new compiler refused
    it where the old one accepted it. The survey was then redone over every `.npk` in the tree, code lines and
    parsed strings both.
  - The library listener measured its 170 files: zero identifiers, zero header writes, no `OwnedFd` or `RGuard` at
    all.
- **The rule (type_stmt.npk, beside TYPE-071's `place_fixed`):**
  - The helpers are `struct_field_decl`, `struct_field_quals`, `struct_outside` and `struct_decl_name`.
  - "Outside" is D-313's line as recorded in its landing note: the struct's home scope (`symtab_home_scope`), and not
    `scope_is_inside` it. That is the private member's line (`ns_visible`).
  - One write walk, `place_sealed_access`. It asks each member access in a place: a sealed field this code is
    outside, a compiler-known header, or a read hold's view. It continues into a VALUE and stops at a pointer, slice
    or handle base.
  - `refuse_sealed_write`/`report_sealed` sit at every write form: the assignment statement (both spellings, asked
    only of a target that typed, D-240), `@`, `$$m` (not `$$i`), a `Self->` receiver's implicit address, a stateful
    operation, and `move`/`pass` out of an owning field (`refuse_move_out_of_sealed`, `refuse_move_out_of_limited`'s
    twin). The struct literal asks the field directly.
  - `refuse_hidden_touch` sits at `member_type`'s struct arm (every access), a call through a function-valued field
    (`v.cb(1)`; `(v.cb)(1)` is an access), the struct literal, and the struct pattern.
  - `refuse_field_quals` covers a local, a parameter, a module binding, a cast target (`=>` and `=> dyn`), and both
    qualifiers on one field.
- **A code the plan did not allot: `NITPICK-TYPE-081`**, the qualifier off a field or both on one. The plan said
  "refused where the other field-only qualifiers are", and there are none: `move` is the one position-bound
  qualifier, and it is parameter-only (MOVE-004, in the bindings analysis). A `for` binding takes no qualifier at all
  (its parser reads a type first), so it has no case.
- **Found on the way, fixed in this step:**
  - **DEF-77.** `RGuard.value` was read-only only as an assignment's direct target. `g.value.x = 5`, `@g.value.y` and
    `$$m g.value.x` were accepted: writes through a shared hold.
  - **DEF-78.** `@f.value` and `$$m f.value` on an `OwnedFd` were accepted.
  - Both are DEF-72's class: a read-only member refused in one spelling only.
  - The walk now asks both at every write form. `RGuard` keeps TYPE-007. `OwnedFd.value` joins the headers sealed by
    definition as TYPE-079, and `stream_rules.npk`'s expectation moved from 007 to 079 with a dated comment.
  - The two special cases in the assignment check are gone, so there is one mechanism.
- **Measured before the harness:**
  - The new checker over `src/npkc.npk`, `npkg/main.npk` and every tool: exit 0, no new code.
  - Over every file under `tests/`: no new code outside the new files and `stream_rules.npk`.
  - The prelude writes no header (its two `l.cap = …` are `List`'s own, inside the prelude).
  - **Every program's IR, old compiler (main's `35ad9e1`) against new, over the 402 programs of
    `tests/backend/programs/` and `tests/verify/`:** 398 byte-identical. The three renamed programs are identical
    after `nested` is read back as `hidden`. `src/npkc.npk` and `npkg/main.npk` differ because their own source did.
- **Tests** (each refusal pinned to its line by `expect-error-at:`, so a form that stops refusing fails the unit):
  - `tests/types/rejection/sealed_fields.npk`: 13 write forms, with reads, a `$$i` claim, a copy, a write through a
    sealed pointer field and the module's own writes passing.
  - `hidden_fields.npk`: 17 touches, 4 of them one literal's four fields.
  - `header_writes.npk`: 40. It covers each kind's headers under `=`, `+=`, `@` and `$$m`, `OwnedFd.value`, and
    DEF-72's `lenwrite` probe.
  - `field_qual_position.npk`: 5.
- **Docs:**
  - TYPE_REFERENCE §9.1.1 (new);
  - DECISIONS: dated landing notes on D-313 and D-314, notes on D-056 (DEF-77) and D-185 (DEF-78), and D-273's
    rename note;
  - OPEN_DECISIONS: DEF-72 FIXED, DEF-77 and DEF-78 recorded FIXED, DEF-73/74's schedule corrected to step 1b;
  - CLAUDE.md: the status, and the reserved-word row.

### Step 1b — `List`: `l[i]`, its fields qualified, its checked operations, the sweep, the bridging refresh (2026-09-19)

- **`l[i]` (§2.2).**
  - The checker types an index on the compiler-known `List<T>` as its element, through `list_elem_type` (type_layout,
    `items`' pointee bound through the instance by `struct_field`).
  - The emitter lowers it in `addr_of_place`'s own arm: the list's address, then the index, then `count` and `items`
    loaded AFTER the index is evaluated. The guard is the slice's (`OutOfBounds`, one unsigned compare against
    `count`), and D-286's alias guard wraps the address as it wraps every one.
  - Reads go through `addr_of`, and assigning over an OWNING element drops the old value, as a managed array's does
    (`ir_stmt`'s overwrite rule).
  - `l[lo...hi]` ranges into a checked `T[]` view (`type_index`'s range arm, `emit_range_slice`), because three of
    the compiler's own byte views over a sink's `List<int8>` took `items` raw.
- **TYPE-082 (settled as built, raised with the user): `p[i]` on a pointer to an array, a slice or a `List` is
  refused; the element is `(<-p)[i]`.**
  - D-098 auto-dereferences only where there is one reading, and pointer indexing gives these pointees two.
  - Measured before the rule: zero sites in the tree.
  - The sweep produced 66 in `npkg/`, each caught by the rule and rewritten. Without it, each would have compiled as
    pointer arithmetic over lists.
- **The prelude.** `List<T>` is `{ hidden wild T->:items; sealed int64:count; sealed int64:cap; }`, and six checked
  operations sit beside `list_push`/`list_reserve`: `list_pop`, `list_truncate`, `list_clear`, `list_insert`,
  `list_remove` and `list_swap_remove`. An index outside the list is `!!! OutOfBounds`, the guard's identity. An
  element leaving is moved to the caller, or into a temporary its statement drops (D-246).
- **The classification (§2.2's "measured by classifying every direct `.count` write first"):**
  - The fields were qualified in a throw-away build with the diagnostic bound (256) lifted, and the new checker
    listed every refusal over `src/npkc.npk`, `npkg/main.npk`, every tool and every test.
  - That gave 2,149 sites: 2,008 `items` touches (2,005 element accesses, 3 raw byte views) and 141 `count` writes
    (77 `= 0`, 42 `= mark`, 21 `= count − 1`, 1 `= count + 1`). There were no `cap` writes.
  - Every shape had an operation; none was raised.
- **The sweep** (`.internal/handoff_s11/sweep_158b1b.py`, then `deref_158b1b.py`):
  - 2,011 element accesses became `l[i]` or, through a pointer, `(<-p)[i]`.
  - 77 became `list_clear`; 42 became `list_truncate(@l, mark)`.
  - 21 became `list_pop`: six merged with the read of the last element above them, fifteen as
    `discard(raw list_pop(…))`.
  - The one `count + 1` was `tokenlist_split_shr`'s hand-written insert, whose shift loop wrote `l.v[count]`, past
    the end; it became `list_insert`.
  - The three byte views became `x.v[lo...hi].ptr`.
  - Two faults of the scripts, found and fixed on the way:
    - two list accesses in one member chain share the checker's reported column, so the deduplicated site list held
      one of each pair (the bridge found the leftovers, and the script ran again over what it reported);
    - the `count` rule's base pattern stopped at an index segment, and wrote `f.blocks[b].drop list_clear(@succs);`.
      A search for any rewrite glued to a path found that one site.
- **The refresh** (bootstrap/seed/README.md's bridging variant):
  - Hop 1: the committed snapshot built the tree with `src/` UNSWEPT into the bridge, 25,381,288 bytes of IR.
  - Hop 2: the bridge built the swept tree. Stage 2 == stage 3: 25,707,020 bytes, sha256
    `4974aba2f7159d7b443d42d33e32e7b6eaec532e22cc21d4abbe7b79607ab039`, 3,239 defines, no absolute site path. It is
    installed with its STAMP.
  - **One deviation from §2.2, with its reason.** Hop 1 carried the QUALIFIED fields too, not only `l[i]` and the
    operations, so the only difference between the hops is `src/` unswept versus swept. The bridge's embedded
    prelude is then the final one, and the bridge itself refuses any leftover site in the swept tree. The old
    snapshot never reads the embedded prelude's text, so hop 1 compiles either way.
- **Found by the new check on its first run: DEF-79.**
  - The first stage-2 compiler exited 3 at once, having trapped `OutOfBounds` in `ast_init` (`gdb` on `npk_trap`).
  - Since 1.4.7 step 2, `ast_init` had stored the NONE declaration into slot 0 of an EMPTY `decls` list, past
    `count`, so the first real declaration was pushed over it at DeclId 0, the id `decl_is_none` reads as absent.
  - The NONE node is pushed now, as every sibling array's is.
- **Found by the sweep's `npkg/` build:** a view through `<-p` was refused as "a view of a temporary" (BORROW-012).
  - The escape analysis's `view_root` treated a deref root as a temporary on purpose ("fails closed").
  - `(<-p).x` is `p.x`'s storage, so the walk now continues into the operand, and a deref of a temporary pointer
    still fails closed.
- **Measured:**
  - **Guard census**, the compiler's own emission: `OutOfBounds` 13 → 765; `IntOverflow` 2,250 → 2,248 (the
    hand-written `count ± 1` moved into the operations).
  - **The compiler's own build at `-O0`**, CPU seconds, two runs each, under a shared load:
    - before 59.19 / 59.16;
    - after 70.92 / 69.61;
    - step 1's compiler alone 59.21 / 59.38 (its member checks cost nothing measurable).
  - **Attributed by two throw-away variants:**
    - `list_truncate` as a bare store: 69.80 / 69.89, so not the drop loop;
    - no list guard: 61.00 / 60.68, so about 9 s (15%) is the bounds guard itself at `-O0`, and about 1.6 s the
      extra loads and the new drops.
  - **At `-O2`** (`opt -O2` and `llc -O2` of each snapshot, each compiler over its own tree): before 15.25 / 15.10,
    after 17.04 / 17.01, about 12%. The optimizer removes some guards and proves few bounds.
  - Step 5's `bounds` rows are what let the verified build elide the guards they prove.
  - **`npkg verify` on the refreshed tree:** 441 obligations (275 discharged, 138 open, 0 budget, 22 unencoded, 6
    checker). `nitpick.obligations` matches, so no row moved: the sweep's new guards carry no rows until step 5.
    The verified compiler rebuilds itself byte-identically, and the floor's 388 rows match.
  - **The churn probe:** `list_ops_churn.npk`'s five hundred rounds peak at 10,764 bytes, equal to one round's
    (`tests/cost/list_ops.toml`, bound 2).
- **Tests:**
  - `tests/backend/programs/list_ops.npk`: every operation, `l[i]` read, write and compound, `(<-p)[i]`, ranges, and
    an owning `List<string>` overwritten, popped, inserted, removed and cleared. It exits 0 at `-O0` and after
    `opt -O2`.
  - `list_index_oob.npk`: DEF-74's probe written `a[i]`, exit 94, `OutOfBounds`.
  - `list_ops_churn.npk`/`list_ops_once.npk` with `tests/cost/list_ops.toml`.
  - `tests/types/rejection/list_fields.npk`: 12 refusals pinned, including DEF-73's `a.cap = 100`.
  - `index_pointer.npk`: 4 TYPE-082 refusals, with the `(<-p)[i]` and scalar-pointer forms passing.
  - The step-1 rejection files re-pin byte-identically under the new compiler.
- **The library listener** measured zero on every count: its containers are its own `Vec`s, with no prelude `List`
  anywhere, no function named after the six operations, and no index on a pointer to an indexable value.

### Step 2 — the constants: exact folding, TYPE-076, the emitter's fold, and the folder's other operations (2026-09-19)

- **The folder** (`type_resolve.npk`):
  - `+ - *` and negation compute EXACTLY at the operation's width through `fold_exact`. That covers `exact_s64`
    (CERT's pre-checks, no `MIN / -1` ever computed), `exact_u64` for `uint64` bit patterns, and a range check at
    every narrower width.
  - A width past 64 bits folds inside the 64-bit window only, and a result beyond it is `EX_UNKNOWN` and declines.
  - The folder reports TYPE-076 where the operation's type is written, and never traps.
- **The checker** (`type_expr.npk`, `check_const_overflow`): at every plain integer `+ - *` and negation, the
  OPERANDS are folded and the exact result computed at the NODE's type.
  - That is how an unsuffixed pair meets the width its context gives it (`int8:x = 100 + 100;`).
  - One sentence (`const_overflow_msg`) from both reporters, so identical reports collapse (D-240).
  - An operand whose fold already reported says nothing more.
  - `is_const_initialiser` asks `fold_gave_up`, so a `fixed` initialiser that overflowed is not also "not a
    constant". The first run reported both, TYPE-076 and TYPE-035, at one line.
- **The emitter** (`emit_const_fold`, DEF-70): a `+ - *` or negation whose NODE folds, to a value that fits its plain
  integer type, is emitted as the constant, with no intrinsic, no guard and no row. The emitter's unit test
  `ir_expr.npk` pins `2 * 8` as `i64 16` and `-1` as `-1`, with no instruction in the function body.
- **Found and fixed with it, DEF-80: the folder's other operations were not the machine's.** The first build of the
  checker's operand folding trapped `ShiftRange` inside the compiler's own folder, on a prelude expression shifting
  a `uint256` constant by 200 (the amount bounded by the type's width, the shift done in `int64`). Reading the
  folder beside it found:
  - a narrow `<<` not truncated;
  - a narrow unsigned `~` not masked;
  - `uint64` division, remainder, right shift and comparison done signed;
  - `MIN / -1` and `MIN % -1` trapping the compiler;
  - a cast reading a `uint64` bit pattern as another type's value.

  Each is the machine's answer now: `fold_width`, `fold_to_width`, `fold_value_fits`. A constant `MIN / -1` is
  TYPE-004, as a constant division by zero is. Found through the record: see the next item.
- **`BASIS_TEXT`/`PRIME_TEXT`** (`derive_gen.npk:201-207`) were used by nothing. `BASIS_TEXT` spelled the basis
  `0u64 - 3750763034362895579u64`, D-037's wrap. Both are removed.
- **A near miss, recorded for the next reader.**
  - `constant_fold.npk`'s first run compared the prelude's `fnv_offset()` with the textbook FNV-1a basis, since the
    prelude's comment named it, and failed: the high words differ (`0xCBF5DAE4` against `0xCBF29CE4`).
  - The record settles it. D-190 made `0xCBF5DAE484222325` the ecosystem's basis on purpose, one constant for every
    derived identity (D-179's error codes, the interface hash, the derived `Hash`), and `sdk/npkdrv.h` says so.
  - The constant was right and the prelude's comment was stale. The comment is corrected, and the test holds
    `fnv_offset()` to D-190's value built with bit operations.
  - A "fix" to the textbook value would have moved every error code, every interface hash (breaking every driver)
    and every derived hash.
- **Measured:**
  - **The census, the compiler's own emission:** all 85 constant checked operations gone (84 negated literals, one
    pair; the plan's reconciliation, exactly), none left. `IntOverflow` guards 2,248 → 2,187, and `with.overflow`
    calls 2,308 → 2,247: the 85 removed, net of the 25 guarded operations step 2's own folder code adds (`exact_s64`
    9, `fold_exact` 5, `fold_value_fits` 5, `exact_u64` 4, `fold_to_width` 2).
  - **The collateral scan:** `src/`, `npkg/`, every tool and every test pass the new rules. No existing file reports
    a new TYPE-076, and no rejection file's code set moved.
  - **`nitpick.obligations` re-recorded (D-040, the commit that changes what is proven): 368 → 390 rows.**
    - Six rows went with `fold_binary`'s old body, and 28 arrived in the folder's new code, all of them in
      `type_resolve`: 12 `shift-range` retained, where a computed shift is bounded by a width the solver does not
      know to be at most 64; 5 `div-zero` elided and 3 retained; 4 `div-min` elided and 2 retained; 2 `shift-range`
      in the bit-vector tier.
    - `npkg verify`: 464 obligations (285 discharged, 151 open, 0 budget, 22 unencoded, 6 checker), against 441
      (275, 138) before.
    - The verified compiler rebuilds itself byte-identically, and the floor's 388 rows match.
- **Tests:**
  - `tests/types/rejection/constant_overflow.npk`, 9 refusals pinned: `127i8 + 1i8`, a `fixed` operand plus one,
    `100 + 100` in an `int8`, an `int32` sum past 2^31−1, `0 - MIN`, `-(1i32 << 31i32)`, a `comptime` product past
    2^63, `0u8 - 1u8`, and `0u64 - 1u64` in a `fixed` initialiser. The wide `int128` case declines and is accepted.
  - `constant_division.npk`: 4 refusals, three `MIN / -1` shapes and a division by zero.
  - `tests/backend/programs/constant_fold.npk`: fifteen folded constants against their run-time twins, the D-311
    spellings, and `fnv_offset` against D-190's constant.
  - `ir_expr.npk`'s new case.
- **Docs:** OP_REFERENCE (constant expressions beside D-210's note), DECISIONS dated landing notes on D-310 and
  D-311, OPEN_DECISIONS DEF-70/71 FIXED and DEF-80, this record, CLAUDE.md's status and a gotcha.

### Step 3 — the `overflow` rows, their elision, and what the residue is (2026-09-19)

- **The rows** (`smt_encode.npk`): one per `IntOverflow` guard, at the guard's own site — a binary node's, a
  compound assignment's target, a negation's own node (the emitter passed 0 there, and the site is the unary node's
  id). A `simd` integer operation is ONE row over the lanes' conjunction, because the guard is one any-lane trap. An
  integer `.sum()` is ONE row at the site with N−1 traps, its goal every partial sum in range, because the scalar
  core guards each fold step there. A node the folder writes as its constant (D-310, step 2) has neither guard nor
  row. The goal is `smt_range_axiom` over the result; it is a hypothesis after the site, and inside a proposition it
  belongs to the proposition (DEF-33). An operand the walk cannot express makes the row `unencoded`.
- **The elision** (`ir_expr.npk`): a discharged row's branch becomes `xor` of the intrinsic's overflow bit and one
  `call void @llvm.assume`. The intrinsic stays, and the vector guard elides the same way over its any-lane test.
- **Both runners' tables.** `TRAP_OF_KIND["overflow"] = "-4110"`; `ASSUME_KINDS` gains `overflow` AND `cast-range`
  (the gap §1 predicted: D-306 gave `cast-range` a guard that elides into an assume, and neither runner counted it).
  `check_obligation_kinds_agree` gains the rule the three kinds lacked, with an npkg twin (`kinds_have_traps`):
  every kind the catalogue marks guarded and producing rows has a trap code, or is a bypass kind. `limit-subsume`
  is the one bypass kind with no code of its own — its guard is the callee's entry, which the bypass belt counts —
  and the first run of the rule found exactly that, correctly.
- **DEF-75**: npkg's `read_rows` fails a malformed line with `ERowsMalformed` where it skipped one in silence.
  Both self-checks plant three lines: a well-formed row, an eleven-field row and an unknown role.
- **THE TABLE HOLDS WHAT THE EMISSION HOLDS** (D-262's rule, carried to the obligations; `obl_keep_held`). The walk
  covers every function of the module graph, and the trim drops a prelude function nothing references. Its rows
  guarded nothing in the artifact, and the solver decided them anyway. Measured on a small verify program: 168
  functions decided to emit 14, and 260 of its 267 seconds went to one function it never references
  (`flt_bits_shortest`). The compiler's own manifest loses exactly the rows of 145 prelude functions, the 213
  `err-exit` rows of 1.5.4b among them.
- **Found by the belt, fixed here: DEF-81, a soundness hole.** A guard inside a loop's invariant had a row in the
  ENTRY's context alone, and the emitter elided it on that row although the head's one check runs at every visit.
  The back edge and each `continue` record the guard's rows now, the emitter elides only when every row of a guard
  is discharged, and `rows.txt` carries each row's clause context in a twelfth field so both belts count GUARDS. A
  guard inside a check that is not emitted at all counts as neither a trap nor an assume, which a discharged
  `ensures` seam needed too. `inv_inner_guard.npk` and `ens_inner_guard.npk` hold both; five planted cases in each
  self-check hold the belt, the defect's own shape among them.
- **Found by writing DEF-75's self-check, fixed here: DEF-84.** An index through a value no place holds — a call's
  result — or through a `Result`'s `.value` was admitted by the checker and refused by the emitter as EMIT-002.
  A `Result`'s stored members are places now, and a base rooted in a temporary is spilled to an entry-block slot
  and read there. `index_temporary.npk` covers every shape (inside a coroutine and an `await`'s operand too), and
  `tests/cost/index_temporary.toml` holds the temporaries dropped: 50,000 statements peak at the one statement's 8
  live bytes, where a planted spill that took ownership peaked at 400,000 and the unit failed it.
- **Found by step 1b's first harness, fixed here: DEF-83.** An explorer control's finding seed spins to the shim's
  step budget by design, "about 30 s"; the per-seed net was 60 s in both runners, and under load the finding seed
  read "hung" and the control read blind. The control seeds run under 300 s now — a hang net, never a verdict.
- **Recorded, scheduled for step 5: DEF-82.** A `defer` body's guard has one row and is written once per exit, so
  the belts would count one trap where the build holds two. It fails closed, and no `defer` in the tree holds a
  guard today.
- **The manifest** (`nitpick.obligations`, re-recorded under D-040): **390 → 2,198 lines, 2,439 rows — 1,273
  discharged, 1,161 open, 0 budget, 0 unencoded, 5 checker.** The verified compiler rebuilds itself
  byte-identically and the floor's 388 rows are unmoved.
  - **The gate holds.** Of the rows in both manifests, none moved its verdict. No held function's discharged count
    fell for any kind.
  - 294 lines left: 213 `err-exit` and the rest of 145 prelude functions the emission does not hold, and rows whose
    HASH moved because an `overflow` fact after its site joined their cone (they arrive again in the same
    functions, with the same verdicts).
  - Three functions' `div-min` lines split (9 → 12, 5 → 6, 1 → 2), all discharged: the manifest is a SET, identical
    problems had collapsed into one line each, and the overflow facts tell them apart.
- **The verify suite**: 83 of 105 programs name their own `overflow` rows, each read against its line.
  - Five new tests for the shapes: a discharged row whose guard is gone, an open one whose guard stays and FIRES
    (exit 93), the lane form with `.sum()`, negation, and the compound forms.
  - `prove_lemma.npk` is bounded now. Its claim `d * d + 1 > 0` was proven for every `d` while `*` had no row; a
    proposition holds only where its evaluation does not trap (DEF-33), and at `d = 50000` the multiplication
    overflows. The branch bounds `d`, and the claim is total inside it.
  - `divz_shift_lit.npk` carries one `budget` row: `a + b` discharges and the `- 6` after it does not, though the
    query is the same shape. Its guard stays (D-309).
  - `simd_compound_rows.npk`'s comment said the `.sum()` and the subtraction had no row "until 1.5.8"; both are
    `open` rows now.
- **D-309's residue, measured** (1,124 open rows of 2,217 in the compiler's own emission; 1,093 discharged):
  | shape | open | discharged |
  |---|---|---|
  | a name plus or minus a constant (a counter, an index, an offset) | 278 | 884 |
  | a sum or difference of two unknowns | 300 | 70 |
  | the prelude's numeric cores | 270 | 5 |
  | a field, an element or a call's result plus or minus a constant | 200 | 93 |
  | a doubling or a scaling by a constant | 46 | 32 |
  | a product of two unknowns | 30 | 3 |

  That is planning's prediction, measured: what discharges is a counter or an index the path conditions bound; what
  stays is a length or a field nothing states a bound for, a counter the loop does not bound, and the prelude's
  arithmetic cores (270 rows, decided over opaque operands). The share proven is 49% (1,093 of 2,217), against the
  planning prototype's 49% (1,158 of 2,343). The full list is regenerated by
  `meta/roadmap/1.5/tools/residue.py` (open rows with their file, line, shape and source text).
- **The speed, measured three ways** (the plain compiler `build/npkc` against the verified one
  `build/verify/npkc`, each compiling `src/npkc.npk` from the manifest root):
  | | plain | verified (elided) |
  |---|---|---|
  | wall clock, three runs | 71.32, 70.03, 69.89 s | 70.27, 71.02, 70.62 s |
  | `NPK_HEAP_STATS` peak live | 126,636,045 B | 126,636,045 B |
  | allocations | 11,328,186 | 11,328,186 |
  | `IntOverflow` traps in its own emission | 2,307 | 1,126 |
  | `llvm.assume` in its own emission | 11 | 1,244 |
  | binary | 9,057,632 B | 8,873,088 B |
  | instructions (callgrind, one compile) | 632,818,358,986 | 631,634,446,879 |

  Both compilers emit byte-identical IR for `src/npkc.npk`. The 1,181 elided guards are **1,183,912,107
  instructions of 632,818,358,986 — 0.19% of the compile**, about a million executions per guard site, and
  **the wall clock does not move**:
  the difference is inside the run-to-run spread of a 70-second compile. The guards are predictable branches, and
  the elided build's gain is in instructions and in 184 KB of binary, not in time. Recorded, never gated (K-13).
  (The allocator's `allocated` differs by three bytes between the two runs: the two binaries' own paths, which the
  runtime copies out of `argv`.)

### Step 4 — the wrapping family `+% -% *%` (2026-09-19)

- **The tokens** (`gen_tables.py`'s `OP_NAMES`, LEXICAL_REFERENCE §5): `+%`, `-%`, `*%`, `+%=`, `-%=`, `*%=`, six
  kinds and their text, regenerated into `token_kind.npk`, `token_name.npk` and `operators.npk` (258 kinds, 64
  operators). The longest match makes `a +% b` one token, and `a + %b` was never a program: `%` starts no
  expression.
- **The parser**: the binary three at their trapping twins' precedence (`+%`/`-%` additive, `*%` multiplicative), so
  the spelling of the overflow rule never moves an expression's shape; the compound three beside `+=`.
- **The checker** (`type_wrap_arith`, one function both spellings call): the operands are plain integers
  (`intN`/`uintN`) or `simd` integer lanes, and every other kind is `NITPICK-TYPE-078` NAMING THE KIND AND THE
  REASON — a twisted value's ERR would be laundered into a number, the ternary kinds saturate at the balanced
  bound, `frac` is exact or ERR, `dim256` carries a unit, `complex` computes per component, a float is IEEE. After
  the gate the ordinary arithmetic typing runs, so an unsuffixed literal takes the other side's type and a
  mismatched pair reads as `+`'s would.
- **The emitter**: `arith_name` maps the three to `add`/`sub`/`mul`, and `ovf_family_of` answers -1 for them, so
  the guarded path is not taken — no intrinsic, no branch, no trap, no row. The vector form is the same
  instruction at vector type. REACH arms nothing, and the comment beside its two lists says so.
- **The folder**: a constant wrap folds WITH the wrap and never reports TYPE-076. It computes in `uint128`
  (`wrap64`), because the compiler's own `+ - *` trap and a wrap is exactly the case that would trap; two 64-bit
  operands cannot overflow 128 bits, and the subtraction adds 2^64 first so nothing underflows. A width past the
  folder's 64-bit window declines, as every other operation does there.
- **The encoder**: `wrap_to_word` is one function — `(mod t 2^N)`, and for a signed width the two's-complement
  `ite` — used by the wrapping family AND by D-279's `<<` form, which had the same eight lines inline. No row and
  no guard: the operator's meaning IS the wrap.
- **The prelude's FNV step adopted it** (§2.5's condition, measured on the probe `.internal/fnvcmp.npk`, both
  spellings side by side in one program):
  - before: `zext` to `i128`, `llvm.umul.with.overflow.i128`, an overflow branch, a trap block, `trunc` — twelve
    lines of body and a RETAINED `overflow` row in every hashing program;
  - after: `%t5 = mul i64 %t4, 1099511628211` — one instruction, no row;
  - the value is identical, which the probe checks at run time.

  `intern.npk`'s copy of the same step keeps the `uint128` spelling: `src/` is bounded by what the SNAPSHOT can
  compile (D-205), and no snapshot parses `*%` yet. Both comments say so, and both compute `(h ^ v) * prime`
  modulo 2^64, which is what D-179's error identity needs. **Owed: the adoption of `*%` in `intern.npk` at the
  next snapshot refresh** — 1.5.8b step 7's record carries it forward if no earlier step refreshes.
- **Found by step 4's own full harness, and it is about the TESTS, not the feature: a rejection file whose
  expectations do not parse is skipped in SILENCE.** `wrap_kinds.npk` wrote its sixteen expectations as
  end-of-line comments (`discard(t +% 1tbb32);   // expect-error-at:14 NITPICK-TYPE-078`), and both runners'
  readers take an expectation only from a `//` comment that is the WHOLE line. So the file parsed as having no
  expectations, both runners classified it as a FIXTURE ("a file with no `expect-error:` is a fixture another
  one imports"), and sixteen refusals ran zero times. Nothing noticed except `check_codes_tested`, which caught
  it only because TYPE-078 was a NEW code with no other case — a malformed file asserting an EXISTING code would
  have been invisible. **Both runners now refuse a file that spells `expect-error` after code**, by name, at the
  point where the walk would skip it — one function per runner (`stray_expectation_lines`, `stray_expectations`),
  and BOTH SELF-CHECKS HOLD THE PAIR `stray-expectation` / `stray-expectation-control` over the same two texts:
  the planted malformed line is flagged by line number, and a fixture whose leading comment mentions the word
  (`header_sibling.npk`'s own first line) is not. The tree had exactly those two files mentioning `expect-error` without parsing
  one.
- **Tests:**
  - `tests/types/rejection/wrap_kinds.npk`: sixteen refusals, each a `// expect-error:` line of its own — `tbb32`, `tfp64`, `tryte`, `nyte`, `frac64`,
    `dim256<Hz2>`, `complex<flt64>`, `flt64`, a float `simd`, `bool`, and a compound `*%=` on a twisted target.
  - `tests/backend/programs/wrap_ops.npk`: `int8`, `uint64`, `int128` and a `simd<int32, 4>`, across MIN and MAX,
    with the operand computed where the case tests the instruction and literal where it tests the folder; the
    `int128` extremes are CONSTRUCTED (D-148 refuses spelling them). Sixteen cases.
  - `wrap_fnv1a.npk`: the published FNV-1a 64 digests of "", "a" and "foobar", with the textbook basis built from
    bit operations (D-311).
  - `wrap_splitmix64.npk`: the first five outputs from seed 0, against the reference sequence.
  - `tests/verify/wrap_no_row.npk`: the `*%` site has NO row, and a `prove` over the wrapped value discharges —
    the encoder's model is exact — while the addition after it discharges too.
- **The manifest** (re-recorded, D-040): 2,439 → 2,444 rows, **1,278 discharged, 1,161 open, 0 budget, 0
  unencoded**. The gate holds: of the 2,177 rows in both, none moved its verdict, and no function's discharged
  count fell. The five new rows are the FOLDER's own `uint128` arithmetic in `wrap64`, all discharged — the
  operands are widened `uint64`s, so the solver bounds them. The rest of the difference is hashes moving in the
  four functions step 4 touched (`fold_binary`, `op_match`, `enc_expr`, and `int_form_shift`, whose one row
  followed its body into `wrap_to_word`).

### Step 5 — the `bounds` and `cast-range` rows (2026-09-19)

- **`bounds`, the row** (`record_bounds`, `record_bounds_range`): `0 <= i` and `i < len` at every checked element
  access, and ONE row for a range slice's `lo <= hi && hi <= len` — the emitter's own guard, which tests both in
  one `and`, so the discharge removes one branch and leaves one `llvm.assume`. The goal is a hypothesis after the
  site (every continuing path passed the compare), and inside a proposition it is the proposition's (DEF-33). A
  pointer's element has no bound at all (D-038: pointers are thin, and the emitter guards nothing there), so no
  row either.
- **A CONTAINER'S LENGTH IS ONE TERM** (`len_term`, `place_len_term`): `.len` on a slice, a string, a cstring or a
  buffer and `.count` on a `List` read off a BINDING are `(|npk.len| base)` — one symbol per binding, minted on
  first use, invalidated with the binding and cleared at a reassignment. That is what makes a loop prove its
  body: in `for (int64:i in 0i64...xs.len)` the bound and the length the row compares against are the same
  symbol. A fixed array's length is its type's constant, so a literal index discharges outright.
- **`cast-range`, the row** (`record_cast_range`): the goal is the emitter's own guard read back as a
  proposition — the two ORDERED compares against the same `double` bounds, from ONE function. `cast_bounds.npk`
  is a new leaf module holding both renderings (`cast_lo_const`/`cast_hi_const` for the LLVM text,
  `cast_bound_smt`/`cast_lo_smt_pred` for the SMT text), so the check and the row cannot drift apart; a `flt32`
  operand is widened for the comparison exactly as the emitter widens it.
- **The elision**: `emit_bounds_guard` is one function for all five callers (array, slice, buffer, `List`, and the
  digit extraction's index), and the range slice and the cast have their own; each asks `llctx_elided` with the
  site the encoder recorded and writes `llvm.assume` over the same compare it would have branched on.
- **Both runners**: `bounds` → `-4099` and `cast-range` → `-4117` in each trap table, both added to the assume
  kinds. **The belt found the second one for us**: `npkg`'s `trap_of_kind` had no `bounds` row and
  `kinds-agree` refused the recording run by name before any solver ran — the catalogue marks a kind guarded and
  producing rows, and a runner with no code for it is a belt that cannot count.
- **Found and fixed here, the site key** (measured by the same belt): the range slice asked `llctx_elided` with
  the INDEX EXPRESSION's node while the encoder records the row at the RANGE node (the index expression's rhs).
  The key never matched, so `bounds_proven.npk`'s row read `discharged` while its guard stayed in the verified
  build — "2 `llvm.assume` for 3 elided guards, 1 `-4099` trap for 0 retained". Nothing but the assume-versus-
  guard belt could have seen it: the manifest was green.
- **DEF-82 fixed** (a `defer` body's guard is one row and several traps): the row carries the body's statement id,
  the emitter reports each copy it writes, and the table computes the tenth field as one copy's traps times the
  copies. `tests/verify/defer_guard_copies.npk` holds both directions and FAILS against the pre-fix accounting,
  measured twice — with the counting disabled ("3 assumes for 1 elided guard") and with the pre-fix default of
  one copy ("3 assumes for 2 elided guards; 2 `-4099` traps for 1 retained"), which is DEF-82's own shape.
- **Tests**: `tests/verify/bounds_proven.npk` (a loop over `xs.len`, a range slice, a literal index into a fixed
  array — three rows, all discharged, every guard gone), `bounds_open.npk` (a parameter index nothing bounds: the
  row is open, the guard fires, exit 94), `cast_range_proven.npk` (a bounded float), `cast_range_open.npk` (a
  float through a call, so the walk has no term for it), `defer_guard_copies.npk`.
- **Two more found by measurement, both in this step's own code, both fixed here:**
  - **An element WRITE had a guard and no row.** `addr_of_place` is one function for a read and a write, so
    `t.haspt[id] = 1i32` is guarded exactly as `x = t.haspt[id]` is — and the encoder recorded the row only on
    the expression path. 71 of the compiler's own 756 `-4099` traps were unaccounted, and the belt refused the
    recording run. The assignment walk records the target's row now, at the same site key. The CLAUDE.md rule
    ("a rule written for one spelling of a construct is owed to the other") caught by an instrument rather than
    by reading, for the second time this subcycle.
  - **The length symbol's sentinel was 0, and `sym_new` hands out ids from 0.** A function whose first named
    term was a length re-minted its length symbol at every read, so two reads of `xs.len` named two symbols and
    no row over them could close. Found by instrumenting the memo after a probe showed `len.s.1` and `len.s.2`
    in a program with two plain reads and no write between them. The sentinel is −1 now. DEF-69's rule, which
    this tree learned the hard way at 1.5.8 step 3c: a sentinel is a value the domain never produces.
  - **A third, found by the gate itself: a shared symbol's range axiom must live in the ROOT region.** A
    hypothesis is in a row's cone only when its region DOMINATES the row's, and until this step every length
    read minted its own symbol and pushed its own axiom right where it was used. One symbol read from two loops
    carries its axiom in the FIRST loop alone — so `nio_normalize` and `path_relative` lost two discharged
    `overflow` rows each (`i + 1` is in range BECAUSE `i < len <= MAX`), with strictly stronger facts around
    them. A range axiom is a fact about a symbol's SORT, true on every path, so it is pushed at region 0 now.
    The gate is what caught it: the rows' hashes had moved, so "no shared row moved its verdict" was green, and
    the per-(symbol, kind) discharged counts were not.
- **The manifest** (re-recorded, D-040): 2,444 → **3,040 rows — 1,136 discharged, 1,177 open, 0 budget, 722
  `unencoded`, 5 checker** (3,282 obligations before the set collapses identical problems). **The gate holds**:
  of the 1,920 rows in both manifests NONE moved its verdict, and the one (symbol, kind) pair whose discharged
  count fell is `pow2_double_signed`, whose four rows are unmoved under `cast_bounds`'s name — the function this
  step re-homed. The floor's manifest did not move.
- **The `bounds` rows, by shape: 831 — 79 discharged, 30 open, 722 `unencoded`.** The residue is the honest
  answer to what this kind can prove today, and its shape is one thing: **a `List` is address-taken by every
  `push`, so DEF-14's rule (a name a pointer may write is never NAMED) leaves it no length term.** 722 of the
  compiler's own accesses are `List` elements, and the guard stays on every one — D-309's rule, measured. What
  discharges is a container whose length is stable and named: a slice parameter looped over its `.len`, a fixed
  array's constant, a range slice's pair, an index a `limit<Rules>` bounds (`limit_element.npk` discharges 7).
  **`cast-range` has no row in the compiler's own build at all** — nothing in `src/` casts a float to an
  integer — so that kind's coverage is the verify suite's two programs.
- **Found by step 5's own first full harness, fixed in it: a `simd` LANE ACCESS had a guard and no row.**
  `v[i]` on a `simd` is answered by its own arm in the encoder — D-282's, which returns the LANE's term for a
  numeral index — and that arm returns before the general element arm where step 5 put `record_bounds`. So three
  verify programs carried `-4099` traps the belts could not account for (`simd_lanes`, `simd_div_open`,
  `simd_div_rows`), and the parity stage failed with them. The row is recorded in the lane arm now, against the
  LANE COUNT, which is the type's own constant — so a numeral index discharges outright, and all three
  programs' rows are `discharged`. THE THIRD site-key lesson of this step, and the third one only a belt could
  see. **One row of the compiler's own manifest moved with it, and not for the reason I first predicted**: I
  argued the manifest could not move because `simd` values appear only in `lib/nvec.npk`, which `src/` does not
  import. That was true and beside the point — the new `record_bounds` call is IN `enc_expr`, so `enc_expr`'s
  OWN `bounds` row changed problem text and hash. Re-recorded under D-040 in this commit: 3,040 rows before and
  after, that one hash moved, `open` before and `open` after, no verdict moved anywhere and no function's
  discharged count fell. A prediction about which PROGRAMS gain rows says nothing about which of the compiler's
  own rows move when the compiler's own source is what changed.
- **Recorded for 1.6, not a defect**: proving a mutable container's index needs a frame condition ("nothing in
  this region writes `l`"), which is an effect analysis, not a solver query. That is leg B's abstract
  interpretation over the emitted IR, where an interval domain plus "no store to this object in the loop" is
  the standard shape. Named in OPEN_DECISIONS so it is not rediscovered.
