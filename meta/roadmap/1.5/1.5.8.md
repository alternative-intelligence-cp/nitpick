# 1.5.8 — The runtime's uncontrolled stops, closed: a float cast that was poison, a stack overflow that killed with no `failsafe`, a frame that could jump its guard, and the last net for every other fault (D-305…D-307; DEF-58…DEF-64)

**PLANNED 2026-09-18 by `nitpick-compiler_s11` on `e3bf48c` (1.5.7's close), measured first, and APPROVED the
same day by the user, first-hand to this seat: "go with all four" — S-84…S-87, asked in plain text with a
recommendation each, SETTLED under their recommendations and recorded at step 0 as D-304…D-307.** Before the
four, he had answered the question 1.5.7's close left open — whether `terminate` has a surface: "As far as
decreases, I'm not sure really what it does but if we need it for verification I say we add it" — and chosen the
model: "so 1.5.7 went ok with Opus and I think 1.5.8 will too. Lets proceed with Opus".

## 0. What 1.5.8 is now

The 1.5 README's row gave 1.5.8 "overflow obligations + close-out"; its 2026-09-17 note corrected that to the
FIVE kinds VERIFICATION_REFERENCE §7b assigns it — `overflow`, `bounds`, `cast-range` (each a guard to elide),
`terminate` and `stack-depth` (none) — and to a language question. Planning measured before it wrote, and the
measurements found that three of the five kinds sit on top of UNCONTROLLED STOPS the language exists to forbid:
the guard `cast-range` was to elide did not exist and its absence was LLVM poison (DEF-58); the stack
`stack-depth` was to bound killed the process with no `failsafe` when it ran out (DEF-59) and, on a spawned
thread, could be jumped by one large frame into another mapping (DEF-60). A proof about a guard that does not
exist proves nothing, and a row about a stack whose exhaustion is a crash is a row about a crash. So the work is
four subcycles, in the order the user ratified ("DEF-58 first, because it's live undefined behaviour. Then the
stack check … Then the overflow, bounds and cast proofs … Then `decreases` and the loop sweep. Then the cycle
close."):

| subcycle | what | file |
|---|---|---|
| **1.5.8** (this file) | the four identities and one snapshot refresh; `CastRange` (D-306, DEF-58) and the 128-bit conversions (DEF-61); the stack check (D-305, DEF-59, DEF-60); the last net (D-307); three instrument defects (DEF-62…DEF-64) | `1.5.8.md` |
| **1.5.8b** | the guarded kinds' rows: `overflow` (D-210 §4's promise — 2,250 guards in the compiler alone), `bounds`, `cast-range`, their elision, the compiler's own manifest re-recorded | `1.5.8b.md`, planned at this file's close |
| **1.5.8c** | `decreases` / `unbounded` (D-304): the surface, the checks, the `terminate` rows, the `stack-depth` rows, the sweep of ~925 loops, the refusal | `1.5.8c.md`, planned at 1.5.8b's close |
| **1.5.8d** | the cycle's close: NIKOS's disposition executed (D-217 as annotated), the docs, `done/1.5/` | `1.5.8d.md` |

Each later file is planned execution-grade by the seat that closes its predecessor — the person who has just
learned what a subcycle taught is the one best placed to plan the next. Every DECISION all four need is already
taken (D-304…D-307): what is left to their plans is measurement and mechanism, never a language question.

## 1. Where it starts (measured 2026-09-18 on `e3bf48c`; this machine, nothing else running)

- **The guard census** (`nitpick-compiler_s10`, `.internal/handoff_s10/guard_sites_e3bf48c.txt`): the compiler
  under test's own emission of `src/` holds 2,519 guard-trap call sites — `IntOverflow` 2,250 (89.3%) in 790
  functions (the top ten hold 11%: a long tail, so 1.5.8b's discharges must come from general facts), `DivByZero`
  71, `DivOverflow` 67, `DeadlineExceeded` 51, `EnsuresViolated` 38, `ShiftRange` 22, `OutOfBounds` 15, one each
  of `TbbErr`, `BorrowOverlap`, `Unreachable`, `BadStep`, `LimitViolated` — and 123 `npk_raise` calls (a
  program's own `?!`, D-285). **No cast guard exists anywhere.** `smt_kinds.npk`'s header says "casts trap
  (D-148)" — a claim the lowering never kept.
- **DEF-58, the poison.** `emit_cast`'s float→int arm (`src/backend/ir/ir_expr.npk`, "FLOAT CASTS (0.9.4)") is
  a bare `fptosi`/`fptoui`; `emit_simd_cast`'s float→int arm likewise. LLVM: "If the value cannot fit in ty2, the
  result is a poison value." Probe (`flt64:f = 3000000000.0; int32:i = f =>! int32; if (i < 0i32) { exit 3i32; }
  if (i > 0i32) { exit 4i32; } exit 5i32;`): **exit 3 at the pinned `-O0`, exit 9 after `opt -O2`** — `main`'s
  whole body folded to `unreachable`, and the binary ran into the next function. The same probe over a runtime
  value (`scale * 3000000000.0`, `scale` from `argv.len`) exits 3 at both levels: the hazard is whatever the
  optimiser can see. The twisted families' ENTERING arms (`emit_tbb_cast`, `emit_tfp_cast`, `emit_tern_cast`)
  carry "ord + strict range guard; the select keeps fptosi off poison" since 1.3.2 — the rule written for one
  spelling and never owed to the other.
- **DEF-61, the libcalls.** `flt64 =>! int128` and `int128 =>! flt64` are `CAST_LOSSY` (admitted) and lower to
  `fptosi`/`sitofp` at `i128`, which `llc` turns into compiler-rt calls at both levels: quickemit refuses the
  link — "needs symbol(s) the project does not provide: __fixdfti, __floattidf -- a zero-dependency violation".
  Loud, never silent, but the language admits what the backend cannot build. `i256` conversions are expanded
  inline by LLVM 20 — and routing `i128` through `i256` is NOT enough: `opt -O2` narrows `sitofp (sext i128 to
  i256)` back to `sitofp i128` and `__floattidf`/`__floatuntidf` return (measured; the `fptosi`-to-`i256` + `trunc`
  direction survived `opt -O2`).
- **DEF-59, the uncontrolled stack stop.** The floor installs exactly one signal action, SIGUSR1's
  (`npkrt.ll`'s `npk_start`, `rt_sigaction(10, …)`, flags SA_RESTORER alone). A stack overflow is SIGSEGV with the
  kernel's default action: no `failsafe`. Measured on the compiler itself, `build/npkc src/npkc.npk`: `ulimit -s
  8192` ok, `4096` ok, **`2048` exit 139, `1024` exit 139** — the compiler needs between 2 and 4 MiB of the
  kernel's stack, and past it dies uncontrolled. The main thread runs on the KERNEL's stack, so how deep a program
  may recurse is the SHELL's decision today.
- **DEF-60, the stack clash.** `llc -O0 -stack-size-section` over `build/npkc.ll`: 3,192 functions; 456 frames
  above 1 KiB, **151 above 4,096 bytes**, 69 above 8 KiB, 29 above 16 KiB, 2 above 64 KiB — the largest
  `npk.ir_expr.emit_expr_kind` at 120,904 bytes, then `call_builtin` 82,104 and `emit_arith_value` 54,952
  (the median frame is 88 bytes). A spawned thread's stack (`npk_thread_start`) is "guard page + 2 MiB in one
  mapping" — ONE page of guard. An overflow that enters a frame larger than the guard moves the stack pointer PAST
  it and writes into whatever mapping `mmap` placed below: silent corruption, not a crash. (The main thread's
  kernel stack has the kernel's 1 MiB guard gap; the floor's thread stacks do not.)
- **LLVM's split-stack prologue, under the pinned `llc`** (`"split-stack"` function attribute; both levels):
  `leaq -FRAME(%rsp), %r11; cmpq %fs:0x70, %r11; ja ok; movl $FRAME, %r10d; movl $ARGS, %r11d; callq
  __morestack; retq` — the EXACT frame compared BEFORE `subq $FRAME, %rsp`. A frame of 256 bytes or less compares
  `%rsp` itself; a leaf with no frame emits no check (LLVM's 256-byte slack, `kSplitStackAvailable`). Written
  inline after the parameter list (`define i64 @f(i64 %n) "split-stack" {`) it is accepted and survives `opt -O2`
  (moved into an attribute group). The emitter writes NO function attributes today; nine `define` sites
  (`emit_program.npk` 427, 1393, 1997, 2243, 2256; `ir_func.npk` 514, 696, 902; `ir_rules.npk` 279).
- **The linker.** ld.lld refuses any link that mixes split-stack objects with objects lacking
  `.note.GNU-split-stack` ("mixing split-stack objects requires a definition of __morestack_non_split"), and with
  that symbol defined it REWRITES every split-stack function that calls into such an object: the short prologue's
  compare becomes `stc; nopl`, so EVERY call takes the slow path — gccgo's segmented-stack protocol, where
  `__morestack_non_split` allocates a new segment and runs the function there. A trapping `__morestack` cannot
  serve that. **With the floor object carrying `.note.GNU-split-stack` (a `module asm` section) and both
  `__morestack` and `__morestack_non_split` defined, ld.lld leaves every prologue alone at `-O0` and `-O2`**
  (measured on a two-object program: the caller's `cmpq %fs:0x70, %rsp` intact).
- **The floor's own stack use** (`llc -O0 -stack-size-section` over `runtime/npkrt.ll`, its call graph from the
  IR): 175 defines, every one sized; the deepest chain **1,032 bytes** (`npk_frame_free`, `npk_arena_alloc`),
  the largest frame 328 (`npk_park_sleep`); two cycles, both through the trap route (`npk_mono_now → npk_trap`,
  `npk_exit → npk_trap` — a failure there re-enters `npk_trap`, whose re-entry rule exits 70); two indirect calls
  (`npk_step` → a resume function, which is emitted and so checked; `npk_wildx_call` → JIT code, `wildx`'s
  opt-out).
- **Thread-local storage** (the survey of `npkrt.ll`): `%npk.tls = type { ptr, ptr, ptr, ptr, i32 }` — self 0x00,
  exec 0x08, root 0x10, resume 0x18, tid 0x20; 40 bytes. The main thread's block is a raw 40-byte mapping
  (`npk_tls_boot`: `npk_hmap(40)`, `arch_prctl(ARCH_SET_FS)`); a spawned thread's is `npk_alloc_internal(40)` —
  size class 48, slot stride 64 with a 16-byte header — **so `%fs:0x70` on a spawned thread is a NEIGHBOURING
  slot's header**. Everything before `arch_prctl` runs with a zero `%fs` base: `npk_start`, `npk_tls_boot`,
  `npk_hmap`, `npk_sys6`, `npk_reg_claim`, and in the explored build the shim's first entry. None of them is
  emitted code.
- **The trap route** (D-291, DEF-57): `npk_trap` freezes (`store atomic @npk_frozen`), claims `@npk_in_failsafe`
  by `cmpxchg`, the re-entering holder exits 70 (`hard`), every other loser parks; the winner's `run` block calls
  `npk_stop_others` (SIGUSR1 by `tgkill` to every registered thread, then a futex wait on `@npk_stopped` under the
  join deadline), `npk_driver_kill_all`, then `call i32 @npk_failsafe(i32 %code)` and `npk_exit`. All of it on the
  trapping thread's current stack; D-292's region (`@npk_fs_region`, 1 MiB) is heap for `failsafe`'s allocations,
  not a stack. The SIGUSR1 handler (`npk_stop_handler`, noreturn: count, wake, park forever) "runs on the
  interrupted thread's own stack" — so a thread near its limit that receives SIGUSR1 needs room for the kernel's
  signal frame, whose size is the machine's (`AT_MINSIGSTKSZ`).
- **The explorer** (`runtime/explore/npkx.ll`, its C reference, `npkg/explore.npk`): the shim refuses no syscall
  number — `sigaltstack` (131), `arch_prctl` (158), `mprotect` (10), `munmap` (11) each cost one step and run for
  real; new `@npk_sys6(` call sites are routed and counted automatically. It installs every `rt_sigaction` for
  real as well as remembering it, and runs virtual signals by a direct call on the target's current stack; its
  comment "the real action is installed too, and never fires: no real signal is ever sent" becomes false the day
  a fault action exists. `module asm` passes through untransformed and uncounted — which already hides one
  syscall from the census: `rt_sigreturn` (15), the stop handler's restorer, has no kernel-effect row and is not
  among TCB.md §4b's "27 numbers" (DEF-64).
- **The two instruments the identity survey caught.** `npkg/verify.npk`'s list of trap codes the verified build's
  belt counts (`:653-662`) holds nine codes and not `-4116` — the Python twin counts every `TRAP_OF_KIND` value,
  so 1.5.5 step 2 added `disjoint` to one runner's belt only (DEF-62). `npkg/floor_smt.npk:3252-3254` maps
  `@npk_heap_bad` / `@npk_heap_badreq` / `@npk_heap_oom` to −4103 / −4103 / −4104 where the floor traps −4102 /
  −4104 / −4103 — no clause reads `trap_code` today, so nothing is wrong yet (DEF-63).
- **The failsafe census** (for the sweep): 552 `func:failsafe` in the tree — `src/` 1, `npkg/` 1, `tools/` 5,
  `tests/` 545 — of which 481 carry a `pick` over their `Error` (one each, four spaces deep, `(*)` last) and 71 do
  not (61 of `types/rejection/`'s 75, six of `modules/rejection/`, and four more); 442 of the 481 take two new arm
  lines before `(*)` mechanically and 39 need a hand edit (two arms packed on a line, `(*)` sharing a line, a pick
  or a failsafe on one line, `(*)` the only arm). The 98 `tests/verify/` files each name `failsafe-post discharged
  N`, N the `exit`s in the failsafe. Both runners' self-checks carry failsafe TEXT (`npkg/selfcheck.npk:59, 524,
  530`; `selfcheck.py:52, 981, 998`) and the cost wrapper is one text in two places (`npkg/suites.npk:1481`,
  `harness.py:4437`). REACH runs only after resolution and typing report nothing, so the four suites refused
  before it (`types/`, `modules/`, `expansion/`, `derive/` rejection) are not REACH's. Outside the tree:
  `nitpick-libs` 135, `nitpick-apps` 7 — theirs, and FORECAST F5 told them.
- **REACH** (`reach.npk`'s `reach_settle`): four identities are unconditional (`Unreachable`, `HeapOom`,
  `HeapBadRequest`, `WildLeak`), the rest armed by what the walk saw; a missing arm is REACH-002 (REACH-001 is a
  `failsafe` with no `pick`); an arm naming an identity nothing reaches is accepted (the check walks the
  REACHABLE set). `reach_add_system` does nothing, silently, if the name is not in the prelude.
- **Codes:** the prelude's system family ends at `BorrowOverlap = 4116`; the checker's last code is TYPE-071.
- **The snapshot** (`bootstrap/seed/STAMP`) is 1.5.4b step 0's: it knows `ShiftRange` and not `BorrowOverlap`
  (1.5.5 did not refresh).

## 2. The design

### 2.1 Four identities, one refresh (step 0)

`CastRange = 4117`, `StackExhausted = 4118`, `DecreasesViolated = 4119`, `MachineFault = 4120` join the
prelude's system family together, each with its one-line comment, so ONE snapshot refresh carries all four and no
later step needs another for an identity (K-1). Nothing arms them at step 0: each arms with its mechanism — `CastRange`
at step 1, `StackExhausted` at step 2, `MachineFault` at step 3, `DecreasesViolated` at 1.5.8c.

**Every `failsafe` REACH checks names `(StackExhausted)` and `(MachineFault)` from step 0** (K-2) — before they
are reachable, because REACH accepts an arm nothing reaches and because the two will be reachable from EVERY
program: one sweep now instead of two later. Written unqualified (`types/rejection/error_arms.npk` refuses a
qualified system name), exiting what the `(*)` arm exits unless the file gives each identity its own code, in
which case the next two unused codes of that file. The four pre-REACH rejection suites are not swept (their
verdicts are decided before REACH, and an edit moves their expected positions: `release_then_exit.npk`'s
`expect-error-at: 35:5`). `analysis/rejection/failsafe_exit.npk`'s arm above `(*)` is its test case (`exit 0i32 -
1i32`) and is not copied. `conformance/nf_twin` changes identically in both twins (their IR is compared byte for
byte). Each `tests/verify/` file's `failsafe-post discharged N` becomes N+2, and each runner's self-check failsafe
texts gain the two arms with their counts (10 → 12, 9 → 11) in both runners at once.

**The refresh is the bridging variant** (`bootstrap/seed/README.md`, K-3): the prelude gains names that roots the
SNAPSHOT builds must use — `src/npkc.npk` and the five `tools/` roots. Hop 1 (never committed): the final tree
minus those six files' new arms; the committed snapshot builds it into a bridge compiler whose embedded prelude
has the four names. Hop 2: the final tree; the bridge builds it, stage 2 == stage 3, stage 2 installed with a STAMP
naming this commit's own tree. The refresh also carries `BorrowOverlap` into the snapshot, which 1.5.5 never did.

### 2.2 `CastRange` and the 128-bit conversions (step 1; D-306, DEF-58, DEF-61)

**The guard** (both the scalar arm of `emit_cast` and `emit_simd_cast`'s lanes): the operand widened to `double`
(`fpext`, exact) and tested with ordered compares — `fcmp ord`, then `fcmp ogt d, LO` and `fcmp olt d, HI`:

| bound | signed N-bit target | unsigned N-bit target |
|---|---|---|
| LO | `fcmp ogt d, -2^(N-1) - 1` while that is a `double` (N ≤ 53); `fcmp oge d, -2^(N-1)` while that is (N ≤ 1024 — no `double` lies strictly between the two); past `double`'s range, `fcmp ogt d, -inf` | `fcmp ogt d, -1.0` |
| HI | `fcmp olt d, 2^(N-1)` while that is a `double` (N ≤ 1024); past it, `fcmp olt d, +inf` | `fcmp olt d, 2^N` while that is a `double` (N ≤ 1023); past it, `fcmp olt d, +inf` |

The result is one `i1`; the elision test `llctx_elided(ctx, SITE_EXPR(), site, OK_CAST_RANGE())` is written like
the shift guard's (it answers false until 1.5.8b records rows); otherwise `br i1 ok, label %cr.ok, label %cr.bad`,
the bad block `npk_chain_reset(i32 SITE)`, `npk_trap(i32 -4117)`, `unreachable` — `emit_shift_guard`'s exact shape.
A `simd` operand compares lane-wise at vector type and folds the lanes to one `i1` by the extract chain the other
any-lane guards use (no reduction intrinsic: nothing becomes a libcall). REACH arms `CastRange` where the walk sees
a cast whose source is a float (or a `simd` of floats) and whose target is a plain integer (or a `simd` of them) —
the twisted families' entering casts keep their own ERR-absorbing guards and arm nothing new.

**The conversions** (K-4): at N ≤ 64 the instruction itself (`fptosi`/`fptoui`/`sitofp`/`uitofp`, native on
x86-64 or expanded inline). At N > 64 the emitter lowers BOTH directions by hand, in integer arithmetic, and never
writes an LLVM conversion at a width the target lacks — `opt -O2` proved it will narrow an `i256` route back to an
`i128` libcall, so no route through LLVM's own wide conversion is trusted:
- float → N-bit integer (after the guard, so the truncation fits): `bitcast` the `double` to `i64`; the biased
  exponent `e` and the significand `m` (the implicit bit restored); `k = e − 1075`; `m` zero-extended to `iN`, then
  `shl k` for `k ≥ 0` (the guard bounds `k < N − 53`) or `lshr −k` for `k < 0` (0 when `−k ≥ 64`: every shift
  amount is held below its width, D-277's rule); the sign applied by `sub 0, x`.
- N-bit integer → float: the magnitude (a signed operand's `sub 0, x` under a `select`, computed in the unsigned
  view so `MIN`'s magnitude fits); `lz = ctlz(mag)` (an intrinsic LLVM lowers inline at every width —
  measured at implementation, and the `-O2` leg's zero-dependency scan holds it); zero and
  `bits − lz ≤ 64` take `uitofp i64` directly; otherwise `s = bits − lz − 64`, `t = trunc(mag >> s)` with the
  STICKY bit — `(mag & (2^s − 1)) != 0` — OR'd into `t`'s lowest bit, `uitofp i64 t` (rounds once: `t` carries
  eleven bits past the significand, so the sticky bit decides ties exactly as the full value would), then scaled
  by `2^s` as an exact multiply by a power-of-two constant for `s` within the format's exponent range and as the
  infinity past it (an `s` that puts the value at or above `2^1024` — `2^128` for `flt32` — is infinite under
  round-to-nearest); the sign reapplied. Known-answer vectors from Python's exact rationals over the edges (the
  powers of two, the halfway points either side of each rounding boundary, `MIN`, `MAX`, `2^53 ± 1`,
  `2^64 ± 1`) at `int128`, `uint128` and `int256`, both floats.

### 2.3 The stack (step 2; D-305, DEF-59, DEF-60)

**Emitted code.** Every `define` the compiler writes carries `"split-stack"` after its parameter list — all nine
sites, the prelude's bodies, the drop bodies and the generated helpers included (one rule: every emitted function).
A `define` never runs before the thread's limit word exists: the first emitted call on a thread is `main` (after
`npk_start`'s setup) or a resume function (after `npk_thread_entry`'s).

**The floor** carries `.note.GNU-split-stack` in a `module asm` section, and defines `__morestack` (enters the
trap route as `StackExhausted`, −4118) and `__morestack_non_split` (traps `Unreachable`, −4102: reachable only
through ld.lld's cross-split rewrite, which the note rules out and the closed-world link (D-206) cannot admit).
Both are `module asm` stubs that realign the stack to 16 and call into IR; neither returns. The floor's functions
never carry the attribute: they run before `%fs` exists, inside signal handlers on signal stacks, and inside the
trap route.

**The note is a claim, and a belt holds it** (K-8): "the floor's stack use fits in the reserve" — from
`llc -O0 -stack-size-section` over the floor and its call graph (`floor_shared.npk`'s `floor_calls`, the
harness's `calls_of`), the deepest chain of floor frames, plus one repetition of each trap-route cycle (the
re-entry rule bounds it), plus the 256-byte leaf slack and the 128-byte red zone of an emitted leaf above it, must
be at most a QUARTER of the reserve (16 KiB); a chain that reaches an indirect call stops there (a resume function
is emitted and checks itself; JIT code is `wildx`'s, TCB.md §5). `floor-stack-reserve` in both runners, with a
planted self-check case over a toy floor whose chain is known.

**Every thread's stack is the floor's, one mapping each** (K-6), lowest address first:

| region | size | main thread | spawned thread | failsafe |
|---|---|---|---|---|
| guard (PROT_NONE) | 4 KiB | yes | yes | yes |
| signal stack | 64 KiB | yes | yes | — |
| guard (PROT_NONE) | 4 KiB | yes | yes | — |
| reserve (below the limit word) | 64 KiB | yes | yes | yes |
| usable | — | 8 MiB | 2 MiB (unchanged) | 1 MiB |

`npk_start` maps the main thread's stack and the failsafe stack through `npk_hmap` (so the explorer's virtual
address space places them deterministically), sets both guards with `mprotect`, writes the limit word, registers
the signal stack (`sigaltstack`, 131), installs SIGUSR1 with SA_ONSTACK added, and SWITCHES to the new stack
through a `module asm` trampoline that calls the rest of startup (`npk_start_main`: argv, `main`, `npk_exit`) and
never returns — `argv`, `envp` and `auxv` stay where the kernel put them. `npk_thread_start` maps the larger
mapping, writes the CHILD's limit word and signal-stack base into the child's TLS block BEFORE the clone (K-7), and
`npk_thread_entry` registers the child's signal stack as its first act (a routed step after `npkx_begin` in the
explored build). The kernel checks a signal stack against the machine's minimum (`sigaltstack` fails with ENOMEM
below it): a failure is a startup trap, so the assumption that 64 KiB holds the machine's signal frame is CHECKED
on every machine that runs a program (K-10).

**The TLS block grows to 120 bytes** (K-7): `%npk.tls = type { ptr, ptr, ptr, ptr, i32, i32, ptr, [8 x i64], i64 }`
— the five fields as today, a pad, the signal-stack base at 0x28, reserved words, and the LIMIT at 0x70, where
LLVM's prologue reads it. Spawned blocks come from `npk_alloc_internal(120)` (class 128); the main thread's from
the same raw mapping. The spec's `%npk.tls` rows and the shared-state belt's field lists (`floor.py`,
`floor_shared.npk`) move with it.

**`failsafe` runs on its own stack** (K-9): `npk_trap`'s `call i32 @npk_failsafe(i32 %code)` becomes a call of
`@npk_failsafe_on_stack(i32 %code)`, an IR function whose body calls the `module asm` trampoline
`npk_fs_switch_call`: save the frame and the limit word, load `@npk_fs_stack_top` into `%rsp` and
`@npk_fs_stack_limit` into `%fs:0x70`, call `npk_failsafe`, restore both, return its result. The trampoline sits
one call below `npk_trap`, so `npk_trap` keeps its class ("specified; modelled") and every line the `trap-route`
model and its controls quote except that one call. An overflow INSIDE `failsafe` reaches `__morestack` against
the failsafe stack's limit, `npk_trap` finds the holder re-entering, and the process exits 70. The stop walk and
the driver kill still run on the trapping thread's stack, inside the reserve the belt bounds.

**REACH** adds `StackExhausted` to the unconditional four. `analysis/rejection/missing_failsafe.npk`'s comment
("six") and REACH-003's listed identities move with it.

**The explorer.** `runtime/explore/npkx.ll` gains the same note (a program's own `sys` calls reach the shim
directly since X-20, so emitted code calls into it); the C reference's build (`build.sh`, `hashcmp.sh`) links a
note object beside it. Step counts move (new steps at startup and at every thread's start): the D-303 sweep is run
after the step and the numbers quoted; DEF-57's kept seed 371 (`trap_one_failsafe.npk`) and the `within:` bounds of
the controls are RE-FOUND, not kept blindly (K-11).

**Tests.** `stack_exhausted.npk` (unbounded recursion on the main thread → `StackExhausted`), `stack_exhausted_thread.npk`
(the same on a spawned thread), `stack_bigframe_thread.npk` (DEF-60's shape: a recursive function with a 64 KiB
local array on a thread → `StackExhausted` at the prologue, where it used to jump the guard), `stack_failsafe_overflow.npk`
(a `failsafe` that recurses without end → exit 70), `stack_deep_fits.npk` (a recursion using ~6 MiB on the main
thread exits 0 — no false positive near the budget), `stack_budget_own.npk` (the program lowers its own
`RLIMIT_STACK` through `sys` to 64 KiB and still recurses 6 MiB deep: the budget is the floor's). Every one at both
optimisation levels, as every program is.

### 2.4 The last net (step 3; D-307)

Four more actions from `npk_start` — SIGSEGV (11), SIGBUS (7), SIGILL (4), SIGFPE (8) — each `rt_sigaction` with
SA_ONSTACK | SA_SIGINFO | SA_RESTORER | SA_NODEFER (`0x4C000004`: 0x08000000 | 0x00000004 | 0x04000000 |
0x40000000; SIGUSR1's word becomes `0x0C000000`, SA_RESTORER | SA_ONSTACK), one handler `npk_fault_handler(i32, ptr, ptr)` → `npk_trap(i32 -4120)`, noreturn. Signal
stacks are per thread and registered by step 2, so step 3 is the actions, the handler, REACH's unconditional
`MachineFault`, and the evidence:
- **The spec:** a section for `npk_fault_handler` (trusted where its path is the kernel's), the kernel-effect rows
  (row 13 already covers an `rt_sigaction` with a null old action), TCB.md's regions regenerated.
- **The `trap-route` model gains a FAULT step** enabled at every pc of a task and of the executor (an asynchronous
  entry into `npk_trap`, which the model's `trap` — firing only from pc 7 — cannot express today), with a control
  that removes SA_NODEFER's re-entry and must reach a two-failsafe or blocked state.
- **The explorer:** real faults are real (the shim installs the action and the kernel delivers it); the shim's
  comment and VERIFICATION_REFERENCE §10 amended; a fault INSIDE the shim — which is not reentrant — is a shim
  defect, so the shim marks its own code and its first point entered from a fault handler while marked exits 97
  with a line naming it (K-13).
- **Tests:** `machine_fault.npk` (a `wild` store through a null pointer → `MachineFault`), `machine_fault_thread.npk`
  (the fault on a spawned thread: the holder's `failsafe`, every other thread parked), `machine_fault_failsafe.npk`
  (a `failsafe` that faults → exit 70).

### 2.5 The instruments, fixed where found

- **DEF-62** (step 0): `npkg/verify.npk`'s counted codes gain `-4116`, and the list is DERIVED from
  `trap_of_kind` as the Python twin derives its own from `TRAP_OF_KIND`, so a kind added to one table cannot be
  missing from the count (K-5).
- **DEF-63** (step 0): `floor_smt.npk`'s three heap trap codes corrected to the floor's (−4102, −4104, −4103).
- **DEF-64** (step 2, which adds `module asm` stubs of its own): the floor's syscall census reads `module asm`
  too — a `movl $N, %eax` … `syscall` pair in a `module asm` block is a syscall site with a kernel-effect row like
  any other — so `rt_sigreturn` (15) gets its row and TCB.md §4b counts it.

### 2.6 What this subcycle does not do

It writes no obligation row: `cast-range`, `overflow` and `bounds` rows are 1.5.8b's, `terminate` and
`stack-depth` 1.5.8c's. No runner's kind-to-code table gains −4117 before 1.5.8b (a table entry with no rows
behind it would count every guard as unaccounted — K-5). No `decreases`, no `unbounded`: 1.5.8c.

## 3. Questions for the user — ALL SETTLED 2026-09-18, the day they were asked (the header quotes him)

Each was asked in plain text with its recommendation, the alternatives stated; each is settled under the
recommendation and recorded at step 0.
- **S-84 → D-304 — `decreases`**: a clause of `while`/`when` and of functions; checked in every build
  (`DecreasesViolated`); required on every `while`/`when` as `decreases E` or the new keyword `unbounded`;
  optional on recursion (the stack check is its net). Declined: optional everywhere; required on recursion too.
- **S-85 → D-305 — the stack**: the split-stack prologue on every emitted function, `StackExhausted`, `failsafe`
  on its own stack, the check never elided. Declined: nothing; a bigger stack.
- **S-86 → D-306 — float→int `=>!`**: NaN, ±∞ or out of range traps `CastRange`. Declined: saturation.
- **S-87 → D-307 — the last net**: SIGSEGV/SIGBUS/SIGILL/SIGFPE on the signal stack → `MachineFault` →
  `failsafe`, a fault inside `failsafe` → exit 70; what stays uncontrolled recorded in TCB.md.

D-304's three consequential rules (TYPE-073/074/075) were written into its text for the user to read before
anything lands; they are 1.5.8c's to implement.

## 4. What lands (each step a cumulative prefix under a full harness, D-228)

0. **The decisions and this plan; the four identities; the sweep; the refresh; DEF-62, DEF-63.** DECISIONS.md
   D-304…D-307 and D-142's registry annotated through 4120; OPEN_DECISIONS S-84…S-87 SETTLED, DEF-58…DEF-64
   recorded; the 1.5 README's 1.5.8 row split into four; the prelude's four lines, `gen_tables.py`; the two arms
   in every `failsafe` REACH checks (§2.1); the two self-checks and the cost wrapper; the bridging refresh. GATE:
   the harness green with the refreshed snapshot installed; `nitpick.obligations` re-recorded only if it moved;
   no behaviour anywhere changes (nothing is armed).
1. **`CastRange` and the conversions (§2.2).** GATE: the new programs exit identically at both levels (the -O2 leg
   is the proof the poison is gone), DEF-58's probe traps `CastRange` at both, the wide-conversion vectors agree
   bit for bit, the `-O2` leg's zero-dependency scan silent.
2. **The stack (§2.3), DEF-64.** GATE: every program at both levels; the new belt in both runners with its planted
   case; the floor's spec, models and TCB regions current (`tcb_floor.py --write` before and after `npkg verify
   --floor-only --record`); the D-303 sweep; `ulimit -s 1024; build/npkc src/npkc.npk` succeeds (DEF-59's measurement
   inverted).
3. **The last net (§2.4).** GATE: as step 2; the model's fault step and its control.
4. **The snapshot refresh that carries the stack check into the BUILDER, the docs, the close.** A one-hop refresh
   (no prelude change): until it lands, `build/npkc` — built by the snapshot — runs on the floor's stacks without
   prologue checks of its own. CLAUDE.md (the status; gotchas: every emitted function checks its stack, the floor
   does not and proves its reserve; a `failsafe` names the two universal identities; a float cast to an integer
   can trap), VERIFICATION_REFERENCE §7b (`cast-range`'s text), §9 (the floor's reserve belt, the note), §10 (real
   faults), MEMORY_REFERENCE (the stacks), BUILD_REFERENCE (the belt, the note), TCB.md (§5: the reserve argument,
   the JIT exception, D-307's residue), the 1.5 README row; `1.5.8b.md` planned execution-grade.

## 5. Decisions taken at planning (each with its reason)

- **K-1 — the four identities are declared together at step 0**, each armed with its own mechanism later. One
  refresh instead of three, and REACH never claims a reachability before the mechanism that makes it true lands.
- **K-2 — the universal arms go in at step 0, everywhere REACH checks.** REACH walks the reachable set, so an arm
  ahead of its identity is accepted; sweeping once instead of at steps 2 and 3 halves the churn of 480 files, and
  the arms' presence changes no verdict until the identities arm. The pre-REACH rejection suites are not touched:
  their verdicts come first and their positions are asserted.
- **K-3 — the bridging refresh at step 0, not at step 2.** Step 2 arms `StackExhausted` for every program, the
  `selfhost` stage then demands the arm in `src/npkc.npk`, and only a snapshot that knows the name can build that
  file — so the refresh must precede step 2, and step 0 is where the names are born.
- **K-4 — above 64 bits, float↔integer conversions are hand-lowered in both directions.** Measured: `opt -O2`
  narrows the `i256` route back to an `i128` libcall. A lowering that depends on what the optimiser declines to do
  is the prototype's lesson (llvm-optimisation-can-remove-guarantees); integer arithmetic has no libcall to become.
- **K-5 — no runner table learns −4117 before its rows exist; `npkg`'s counted codes are derived, not listed.** The
  belts compare retained traps per kind with the manifest's retained rows: a kind in the table with no rows behind
  it fails every program that has the guard. DEF-62 is what a hand list does.
- **K-6 — every stack is the floor's; the sizes and the layout of §2.3.** 8 MiB main (2–4× what the compiler needs
  to compile itself, the conventional default), 2 MiB threads (unchanged), 1 MiB `failsafe` (D-292's region's size),
  64 KiB signal stacks and reserve (sixty times the floor's deepest chain; above every x86 signal frame including
  AMX's), 4 KiB guards (the prologue compares before it allocates, so the guard is a backstop for floor code, which
  the belt bounds far below it, and for `wildx` code, which is the author's). One mapping per thread, so freeing a
  thread's stack frees its signal stack.
- **K-7 — the limit word at `%fs:0x70`, written by the parent before the clone.** LLVM reads that offset; the
  child's first emitted instruction may be a prologue, and `%fs` is set by the clone itself (CLONE_SETTLS).
- **K-8 — the floor carries the note and PROVES it.** Without the note ld.lld rewrites every caller of floor code
  into the slow path (measured). The note is a claim about the floor's stack use, and a claim with no check is the
  next false clause (1.5.6c): the reserve belt is the check.
- **K-9 — the switch to the `failsafe` stack is one call below `npk_trap`.** A direct call of a `module asm` symbol
  makes a floor function `asm` class (`floor.npk`); an IR wrapper keeps `npk_trap` specified and modelled, and keeps
  the lines the `trap-route` controls quote.
- **K-10 — the kernel's `sigaltstack` check is the check of the signal-frame assumption.** Below the machine's
  minimum the call fails with ENOMEM, and the floor traps at startup: the assumption is tested on every machine that
  runs a program, with no `auxv` parsing to get wrong.
- **K-11 — explorer step counts move and the kept seeds are re-found.** A seed kept for a window (DEF-57's 371) is
  a schedule, and a schedule shifts when a step is added before it; the directed control (`preempt-at:`) is the
  stable regression and the seed is re-searched.
- **K-12 — the stack budget is tested from inside a program** (`stack_budget_own.npk` lowers its own
  `RLIMIT_STACK` through `sys`), not by lowering a runner's limit: `llc`, `opt` and `z3` inherit a runner's limits.
- **K-13 — a fault inside the shim is the shim's defect, reported by the shim.** The shim is not reentrant; a
  handler entering it while it runs cannot be explored, only named.

## Execution record (appended as steps land)

### Step 0 — the decisions, the four identities, the sweep, the bridging refresh, DEF-62, DEF-63 (2026-09-18)

- **The decisions:** DECISIONS.md D-304…D-307, each with its alternatives declined, D-304's three consequential
  rules (TYPE-073/074/075) written into its text; D-142's registry annotated through 4120; OPEN_DECISIONS S-84…S-87
  SETTLED with the user's words and DEF-58…DEF-64 recorded; the 1.5 README's 1.5.8 row split into four; CLAUDE.md's
  status. FORECAST F5 (and its correction: a missing arm is REACH-002, not REACH-001) told the library listener before
  anything landed.
- **The identities:** `CastRange` 4117, `StackExhausted` 4118, `DecreasesViolated` 4119, `MachineFault` 4120, in the
  prelude's system family; `gen_tables.py` regenerated `prelude_source.npk` (`--check` current). Nothing arms them.
- **The sweep (K-2):** every `failsafe` REACH checks names `(StackExhausted)` and `(MachineFault)` — 463 roots:
  447 by a script (`.internal/handoff_s11/sweep_arms.py`: two lines above a lone `(*)` arm, its exit value and its
  `{` column copied; idempotent), 16 by hand (nine `tests/accept/` files and six programs with packed or one-line
  arms — the two arms inserted before `(*)` on its line — and `analysis/rejection/failsafe_exit.npk`, whose arm
  above `(*)` is its test case and is not copied). The three failsafes with no `pick` are untouched
  (`failsafe_nopick.npk` asserts REACH-001; `whole_grammar.npk` and `resolvable.npk` are not REACH's). The 98
  `tests/verify/` files' `failsafe-post discharged N` are N+2 — cross-checked against the exits each failsafe holds;
  `failsafe_post.npk`'s own is 9 → 11 (its one computed exit is the `open` row). Both runners' self-check failsafe
  texts and counts (10 → 12, 9 → 11) and the cost wrapper, held byte-identical between the runners by extracting
  and comparing them.
- **DEF-62:** `npkg/verify.npk`'s counted codes and the report's kinds are derived from `trap_of_kind` over the
  catalogue (`OK_COUNT`, `ok_name`) — `counted_trap_codes`, and `kinds_of_trap` as the join of the kinds sharing a
  code — so `-4116` is counted and a kind added to the table cannot be missing from the count.
- **DEF-63:** `floor_smt.npk`'s `trap_code_of` reads −4102, −4104, −4103 for `@npk_heap_bad`, `@npk_heap_badreq`,
  `@npk_heap_oom`. No hash moved: `runtime/npkrt.obligations` matches.
- **The bridging refresh** (`.internal/handoff_s11/refresh_bridge.sh`, run from the worktree root): the committed
  snapshot built the tree minus `src/npkc.npk`'s two new arms into a bridge (the only file the builder reads that
  names them — the tools are compiled by the NEW snapshot in the harness); the bridge built the final tree; stage 2
  == stage 3, 24,855,019 bytes, sha256 `b72cf63911825b6a4c7ba9182418c992c70338ddc471e418cb46a4c262428a8d`, installed
  with its STAMP; no absolute site path. Four minutes, hop to hop. The refresh also carries `BorrowOverlap` (1.5.5),
  which the previous snapshot (`8050b1ab…`, 1.5.4b step 0's) predated.
- **Measured before the harness:** quickemit through the new snapshot runs `wide_div_guard.npk` (21) and
  `tests/verify/shift_range.npk` (41) as they expect; `npkg verify` — "441 obligation(s): 275 discharged, 138 open,
  0 budget, 22 unencoded, 6 checker; nitpick.obligations matches; the verified compiler rebuilds itself
  byte-identically" (the compiler's two new `exit 3i32` arms share their row's hash), and "381 obligation(s): 374
  discharged … runtime/npkrt.obligations matches"; the ladder: `npkrt.o` unmoved (`c7da7711…`), the builder, its
  object, the emission (`b72cf639…`, the snapshot's own bytes now — the builder IS this tree's compiler), `npkc.o`
  and `npkc` moved.
- **The record's own lesson, again:** the first harness of this step was launched with this section empty and was
  stopped at twenty minutes to add it (1.5.7's s8 learned the same: the record is part of the tested tree).
