# 1.5.2f — A bare type parameter is move-only in the body that names it (D-264) — PLANNED (2026-09-05; execution-grade; ratified the same day)

> Scope: OPEN_DECISIONS S-40 (the library workbench's O-N19, DEF-23), ratified
> 2026-09-05 as **D-264**. Two steps, two commits, each under a FULL harness
> on a cumulative prefix (D-228). What it leaves open is S-41 (a borrowing
> `pick` binding form), the user's.

## 1. Where you are

`main` is the 1.5.2e close: the prelude's `List<T>` on managed storage
(D-263), `.len` on a fixed array lowering (DEF-22). The snapshot is 1.5.1b's
close (D-205: `src/` adopts nothing here; the rule refuses nothing in it —
measured).

## 2. What this subcycle builds

**Step 1 (D-264).** `type_owns_for_move(e, ty)` in `type_expr.npk`: what
drops owns, and so does a `TY_PARAM` or `TY_SELF`; `require_move_if_owning`
uses it and reports the parameter case with its own text (TYPE-046); the
lending-pick binding check in `type_stmt.npk` asks the same question; the
derive generator's `dv_refusal` refuses a `DVC_PARAM` payload under the four
binding traits as it refuses a `DVC_STRING` payload (DERIVE-006). The seven
sites measured in §4 respelled `move T:v` / `move(v)`. Tests:
`tests/types/rejection/generic_owning_copy.npk` (the bare element copy, the
stored parameter, the lending-pick binding: TYPE-046 ×3),
`tests/derive/rejection/derive_generic_payload.npk` (DERIVE-006 ×2),
`tests/backend/programs/generic_owning_move.npk` (the spelling, at `string`
and at `int32`, exit 0); `derive_generic.npk`'s `Opt<T>` section derives
`Hash`/`ToString`/`Debug`, which bind nothing.

**Step 2.** The docs: TRAITS_REFERENCE §3.2 (a `T` is move-only in the body),
§2.5 (a `T` payload under the four binding traits refuses), MEMORY_REFERENCE
(D-183's rule and the parameter), D-264's landed note, S-40 landed, the 1.5
README row, ROADMAP's 1.5 row, CLAUDE.md's status and a gotcha; `check_refs.py`
at 62; the workbench notified.

## 3. Decisions taken at planning

- **L-1 — The rule lives in one predicate.** `type_owns_for_move` is the one
  place "does this type own, for the move-only rule" is answered; the three
  sites that asked `type_drops` directly ask it.
- **L-2 — The generator refuses what the checker would refuse.** A generated
  lending `pick` over a `T` payload is the same copy; refusing it at the derive
  (DERIVE-006, D-259's re-homing) is the honest report. The derives that bind
  nothing keep generating.
- **L-3 — Seven respellings, no rewrites.** Each site stores a by-value `T:v`
  into an owning slot; `move T:v` and `move(v)` is the D-183 spelling for a
  concrete owning type and the same for a `T`.

## 4. Measurements (2026-09-05, on `f6dfce9` with the rule in place)

The compiler, `npkg`, the tools, `lib/` but `ntensor.npk`, and
`tests/conformance`, `tests/frontend`, `tests/accept`, `tests/verify` refuse
nothing new. Seven sites: `generic_enum.npk:31` (`wrapg`, a constructor
payload), `generic_list.npk:47` (an element) and its `pair_of` caller,
`impl_target.npk:36` (a field), `thread_pool.npk:54` (a channel send),
`lib/ntensor.npk:54,107` (matrix and tensor cells). After the respelling and
the generator's refusal: zero files with a TYPE-046 or a DERIVE-006 in any
positive suite; `derive_generic.npk`'s `Eq`/`Ord`/`Clone` over `Opt<T>` was
the hole in generated form and is DERIVE-006 now.

## 5. What the executor should know

- `move(x)` SPENDS `x` (MOVE-001 on a later read), at a scalar too — a test
  that moves a scalar reads the copy, not the source.
- Reserved words that bit this subcycle: `on`, `end`, `old`.
- Stop every background waiter before reporting done.

## Execution record (appended as steps land)

### Ratification (2026-09-05)

The user, on S-40: *"the recommendation for the new decision sounds fine to
me. lets ratify it"*. Entered in DECISIONS as **D-264**; S-40 reads settled;
S-41 (a borrowing `pick` binding form) recorded as the question it leaves.
