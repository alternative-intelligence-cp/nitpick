# 1.5.8d — the cycle's close: two leads decided, the snapshot refreshed, the docs synced, the folder archived

> Planned 2026-09-24 by `nitpick-compiler_s13` at the 1.5.8c close (step 5), on
> `d7a8092` plus step 5's own commit, execution-grade for the seat that runs it
> after the hand-off ritual (a fresh session, briefed by message). Two questions
> wait on the user before step 0 (§3); nothing else in this file waits on anyone.
> The precedent is `../done/1.4/1.4.9.md`, and this file follows its order —
> the declaration of what the cycle produced, the refresh, the doc sync, the
> archive, one full harness, one commit, the briefing — with one addition the
> 1.4 close did not have: two compiler leads are owed a disposition AT this
> close, and a compiler change lands BEFORE the refresh or not in this cycle,
> because the refreshed snapshot is the emission of the closed cycle's source
> (D-203) and 1.6 reads the encoder as the close leaves it.

## 0. What 1.5.8d is

The 1.5 README's row: *NIKOS's disposition executed per D-217-as-annotated;
C-19 closed by D-233 (no external gate remains); the docs synced; the cycle
to `done/1.5/`.* What that means, measured (§1): the first two are records,
not work — `[verify.nikos]` already refuses by name and C-19 closed on
2026-09-01 — so the close's substance is the refresh, the doc sync, the
archive, the run, and the two leads: **E-5** (the reach analysis's bound-call
over-approximation, owed a disposition "at 1.5.8d's close" by its own trigger
text, OPEN_DECISIONS §4) and **E-6** (a by-value aggregate has no value term,
found by 1.5.8c step 5's measurement, OPEN_DECISIONS §4). Each is a question
for the user (§3), and the recommendation differs: E-6 lands as step 0, E-5
is decided out.

## 1. Where it starts (measured 2026-09-24 on `d7a8092`, 1.5.8c step 4b's tree)

- **The tree.** HEAD is 1.5.8c step 5's commit on `d7a8092`; every stage
  green (52/52), `parity` 1,705 verdicts agreeing, npkc and the verified
  compiler byte-identical; `nitpick.obligations` 5,861 rows (`npkg verify`
  6,118 obligations: 2,859 discharged, 2,428 open, 1 budget, 825 unencoded,
  5 checker); the floor's 388 (381 discharged, 7 budget). Every landing of
  this cycle was pushed as it landed (`land_step.sh`'s rule since 1.5.8b), so
  the per-cycle push of 1.4.9's step 6b is one commit here, not fifty.
- **The two `done/`-reading instruments, dry-run with 1.5 archived** in a
  scratch copy (the 1.4.9 method): `check_rung_names_open_cycle` — 0
  failures (the five `iv_rung`/`pv_rung` calls left in
  `src/backend/ir/ir_expr.npk` each pass a rung TEXT through from a
  refusal record and name no cycle); `check_decisions_current` — 0
  candidates. So the archive moves nothing in the compiler and demands no
  annotation in DECISIONS.md; the run will confirm it.
- **Living citations of `meta/roadmap/1.5/`** that follow the move (the 1.3
  and 1.4 precedent: living documents follow, settled DECISIONS text stays):
  CLAUDE.md 30, VERIFICATION_REFERENCE 12, OPEN_DECISIONS 23,
  TRAITS_REFERENCE 1, `tests/cost/README.md` 1, `tests/verify/README.md` 1,
  `tests/modules/rejection/header_missing.npk` 1, `nitpick.toml` 1 — and
  seven code comments (`bootstrap/harness/harness.py`,
  `bootstrap/harness/explore.py`, `npkg/explore.npk`, `npkg/verify.npk`,
  `runtime/explore/npkx.ll`, `src/frontend/analysis/alias.npk`,
  `src/backend/smt/smt_encode.npk`), each a same-line path in a comment.
  DECISIONS.md's 37 stay as written. No code comment cited
  `meta/roadmap/1.4/` at the 1.4 close, so this is the first close that has
  to decide the code-comment case: it decides RE-POINT (P-3 below).
- **The tools folder** `meta/roadmap/1.5/tools/` (the loop census, the sweep
  tool and its reading record, the loop and group dumps, `residue.py`,
  `model_bfs.py`, `add_headers.py`, and `explore_prototype/` — the explorer's
  C reference shim, its `hashcmp.sh` sweep, `build.sh`, `sweep.sh`,
  `transform.py`, a README) moves with the folder. `hashcmp.sh` takes its
  root and output directory as arguments and assumes no path; the README's
  own instructions cite the folder by name and are re-pointed with the rest.
  Nothing in the harness, `npkg` or the tests EXECUTES anything under the
  folder (measured: the only references are comments and CLAUDE.md's
  instructions), so the move changes no run.
- **NIKOS.** `nitpick.toml` carries `[verify.nikos] domain = "interval"`;
  `npkg/manifest.npk` admits exactly that key; `npkg verify` prints
  `[verify.nikos] declared, not run -- the engine is 1.6.0's gate's (D-217,
  D-233)`. The disposition D-217-as-annotated asks for is already the
  tooling's behaviour; the close records that it held through the cycle and
  that 1.6.0's bring-up gate names the engine (IKOS — the fork NIKOS was —
  against Clam/Crab, by measurement).
- **C-19.** Closed by D-233 on 2026-09-01 (OPEN_DECISIONS §4's row, the 1.5
  README's "Watch for"); nothing external gates this cycle's exit. Recorded,
  not worked.
- **The snapshot.** `bootstrap/seed/stage1.ll` is 1.5.8c step 2's one-hop
  refresh (sha256 `30b4f135…`, 26,843,449 bytes; STAMP source-commit at that
  refresh), the emission of step 1's `src/` — five landings ago. D-203: the
  snapshot refreshes at cycle closes, with the push, from the FINAL tree.
- **E-5's measurement** (1.5.8b step 6b, unchanged since): four tests pay one
  `TbbErr` arm each for the bound-call fan-out (derive and dispatch tests
  whose generics never instantiate at a twisted type); no program in the
  tree, the libraries or the apps has paid more than an arm.
- **E-6's measurement** (1.5.8c step 5, `residue.py` over the compiler's
  emission): of 499 open `terminate` row sites, 79 read a field of a by-value
  aggregate and 116 put a `pure never fails` call over one in the measure —
  195 rows with ONE cause, an aggregate value that has no term, against 240
  that read through a pointer (E-4's, untouched here) and 64 open on their
  merits. `state_copy`'s file shows the shape: `src.count` read as `|_.2|` in
  the condition and `|_.3|` in the measure.
- **E-6's one open question, MEASURED at planning** (2026-09-24, the
  compiler under test at `d7a8092`, `quickemit`): does a callee's write to a
  BY-VALUE aggregate parameter reach the caller's binding? `meta/roadmap/1.5/
  tools/e6_probe_byval.npk` (a copyable struct `P{int32:a}` passed plain, the
  callee assigning `p.a = 9i32` and returning it) exits **19**: the caller's
  `p.a` is still 1, the callee wrote its own copy. `e6_probe_own.npk` (an
  OWNING struct `Q{string:s; int32:a}` passed plain — a lend under D-004 — the
  callee assigning `q.a`) is ACCEPTED by the checker and exits **19** as well:
  the lend copies the header, and the callee's write lands in that copy. So a
  plain argument is value semantics for the caller's binding in both cases,
  and §2.1's step (4) stands as written: a plain argument bumps no version.
  (Observed on the way, not a defect of this plan's: a scalar write to a lent
  owning parameter is accepted and goes nowhere the caller can see; whether
  the checker should say so is a question for the library era's S-37 class,
  recorded here and nowhere else.) The executor re-runs both probes on its
  own tree as step 0's first act (P-2) — the answer is a fact about the
  calling convention, cheap to re-establish.

## 2. The design

### 2.1 E-6 — an unescaped by-value aggregate carries a term (S-97; step 0 if ratified)

**What.** In the encoder (`src/backend/smt/smt_encode.npk`), a binding of
aggregate type (a struct or an enum, a by-value local or parameter) whose name
is NOT escaped (`is_escaped(e, nm)` false: the address never taken by `@`,
`$$i`, `$$m` or an implicit `Self->` receiver — the set DEF-14 already
computes before the parameter loop) gets a versioned Int-sorted symbol of its
own, `s.k`, exactly as a scalar local does (`bind_find`/`version_next`). It is
an IDENTITY term: nothing is asserted about its value, and it is never
compared with a number. Then:

1. **A field read** `s.f` of a scalar field (an integer, a `bool`, a `char`, a
   pointer, a flag word) is the uninterpreted function
   `(|npk.f.<TYPEID>.<field>| s.k)` — one function symbol per (struct type
   id, field name), declared through `sym_new_fun` as `|npk.len|` is, of sort
   `(Int) <field sort>`; a nested read `s.a.b` composes (`(|f.b| (|f.a|
   s.k))`), and the inner aggregate needs no symbol of its own. A `List`
   field's `count` keeps the length symbol it has today (`place_len_term` on
   a bare name; through a field it is `(|npk.len| (|f.list| s.k))` — the same
   uninterpreted-function shape, and what lets `for (i in 0...s.items.count)`
   bound `s.items[i]`). A field of a twisted, float or `simd` kind takes the
   same function with its sort; a field whose value the fragment does not
   hold (a `dyn`, a channel) stays opaque per read as today. A limited field's
   rule (D-308) is asserted over the APPLIED term at each read, where today it
   is asserted over the fresh opaque one (`assert_field_rule`, unchanged in
   shape).
2. **A write to a field** `s.f = v` (an assignment through any path of `s`, a
   compound `s.f += v`, a `move`/`pass` out of an owning field, a write to a
   nested field `s.a.b = v`) bumps the version to `s.k+1` and states
   `(= (|f.f| s.k+1) <v's term>)` (or nothing, when `v` has no term) and, for
   every OTHER field `g` of the same struct type, `(= (|f.g| s.k+1) (|f.g|
   s.k))` — the update frame, bounded by the type's field count (a nested
   write frames the inner aggregate's siblings through the composed
   functions). A whole assignment `s = expr` bumps the version and states the
   per-field equalities when `expr` is a struct literal with terms, nothing
   otherwise.
3. **A loop head's havoc, a `pick` arm's merge, a `move` of `s`** bump the
   version through the same paths that bump a scalar's (`invalidate`,
   `invalidate_all`, the merge's `ite` chains — an aggregate version merges as
   an identity choice, `(ite c s.j s.k)`, which is what keeps a field read
   after an `if` equal on both arms when neither wrote it).
4. **A call argument.** Passing `s` PLAIN to a call changes nothing: the
   callee's parameter is its own binding — MEASURED at planning (§1's last
   bullet: both probes exit 19, the copyable and the owning case alike; the
   probes are committed beside the tools as `e6_probe_byval.npk` and
   `e6_probe_own.npk`). The executor re-runs them first (P-2); if either ever
   answers 99, a plain argument becomes a version bump at the call (sound),
   and the closing power drops to the loops whose body calls nothing with
   `s`. `@s` as an argument is already an escape (never named).
5. **A `pure never fails` call** in a contract or a measure over such an
   aggregate then applies the uninterpreted function to `s.k` (1.5.3's
   encoding, unchanged), so two reads of `raw item_member_count(ast, ld)`
   between which nothing writes `ld` are one term.

**Where** (the sites, each read before it is edited): `enc_expr`'s
`ExprMemberAccessExpr` arm (the field read — today `term_opaque` after
`assert_field_rule`); the assignment and compound-assignment walkers where a
field target invalidates today; `enc_param_bind`/`enc_param_entry` (a
parameter's first version; an aggregate parameter gets `s.1` where today it
gets nothing); the local-declaration walker (`P:s = P{…}` — the literal's
field facts); `invalidate`/`invalidate_all` (loops, calls that may write
memory — a by-value aggregate is NOT invalidated by a call unless (4) says
so); the merge after `if`/`when`/`pick`; the escape set's computation
(unchanged: it already lists every address-taken name).

**Soundness.** No alias to an unescaped by-value binding exists (D-004: a
pointer to a local needs `@`), and the callee's copy is the callee's — under
(4)'s measured answer. The pointee of a pointer (`x.count` with `Reach->:x`)
is NOT covered: any callee holding the pointer may write it (E-4, 1.6 leg B).

**The gate.** (i) The probe of (4) recorded first. (ii) `tests/verify/
terminate_field.npk`: a by-value struct's field as a loop's bound — its
`terminate` and `overflow` rows `discharged`, the check's compares assumes;
the same loop over the struct THROUGH A POINTER — `open`, the check kept; a
field written in the body between the reads — `open`; a `pure never fails`
call over the struct in the measure — `discharged`; each row named. (iii) The
manifest re-recorded (D-040): the 195 sites of §1 discharge, or the record
says which did not and why, read one by one; the gate over the shared rows
(`manifest_gate.py`, step 5's manifest against this one) moves NO verdict
and no (symbol, kind) discharged count falls. (iv) Every program's emission
byte-identical except where a guard was elided (the elided guards are the
rows that moved: count them, they are the difference). (v) The harness green,
the belts (an assume per elided guard), `parity` green.

### 2.2 E-5 — the bound-call fan-out (S-98; recommended: decided out)

DEF-86 made a trait-method callee reach every impl of its trait, because a
generic body is walked once and its impl is decided per instantiation. The
narrowing on offer: for a receiver whose type is a bound parameter `T` inside
generic function `G` (or a method of a generic impl), read `G`'s
instantiations (`FnInstTable`, `fninst_at`: `decl`, the argument window
`args_start/args_count` against the parameter window `params_start/
params_count`; a generic impl's through `InstanceTable` and the impl's target
pattern by `family_unify`), bind `T` per instance to its concrete argument,
and reach only `find_method(impls, bounds, types, ast, interns, concrete,
mname, 0).decl`; an argument that is itself a parameter of an ENCLOSING
generic follows that generic's instances in turn (a chain bounded by the
table); a chain reaching no concrete type, a `dyn` receiver and a `Self`
inside a trait's default body keep every impl. Sound; about 150 lines in
`reach_callee` (`src/frontend/analysis/reach.npk`), which would also need
`FnInstTable->` and `InstanceTable->` (`f.fninsts`, `f.inst` in
`pipeline.npk`) and the current function (`x.cur_fn` set in `reach_fn`).

**Why decide it out.** The over-approximation is in the SOUND direction (an
arm the program cannot enter costs a line; the direction it protects against
is a raise no arm names); its whole measured cost is four test files with one
arm each, unchanged since it was measured at 1.5.8b step 6b; and the
narrowing is a generic-chain walk inside a SAFETY analysis whose defect mode
— reaching too few impls — is exactly the hole DEF-86 closed. A precision
change with no safety gain inside the analysis that guards `failsafe`'s
exhaustiveness should wait for a program that pays for its absence in more
than an arm. Decided out means: the lead's row is closed with this
paragraph, the design above stays beside it, and the re-open trigger is a
measured cost — a library or app root that must name an identity its
instantiations can never raise in more than one arm. If the user prefers it
in, it is step 0b, after E-6, under the same gate shape (every rejection file
reports the same set; the four tests' `TbbErr` arms come off and REACH-002
does not ask for them back; the harness green).

### 2.3 The refresh (step 1)

`bootstrap/seed/README.md` "Refreshing it", verbatim in shape, in a script
that echoes each step and refuses to install on a failed comparison
(`refresh_onehop.sh` in the s11 scratch is the shape; scratchpad
intermediates, never `/tmp`): from the tree root, `src/npkc.npk` spelled
RELATIVELY (D-236: an absolute argument embeds the machine's path in the site
tables and the `repro` stage refuses it); the builder is the COMMITTED
snapshot, it compiles the current `src/` to stage1.new, stage1.new's
compiler builds stage 2, stage 2's builds stage 3, `cmp stage2 stage3` silent,
STAGE 2 installed, sha256 and byte count and `git rev-parse HEAD` into
`STAMP`. Then the prologue census the s13 refresh printed (`define` count,
every one `"split-stack"`, zero absolute site paths). The refresh is landed
ONLY under a full harness run with the refreshed snapshot installed (the
README's rule since 1.5.1b step 5c: the fixpoint is not the proof, the run
is), and that run is step 3's. If step 0 lands, the refresh follows it (the
snapshot is the emission of the final `src/`); if not, it refreshes from step
5's tree, which is `d7a8092`'s `src/` — the emission moves either way, since
`src/` changed at steps 3, 4 and 4b after step 2's refresh.

### 2.4 The doc sync, item by item (step 2)

Each item: the file, the change, the reason. Nothing under `done/` and no
settled decision text is rewritten — dated annotations only. Applied by ONE
script that asserts every anchor matches exactly once before writing
(1.4.9's method), anchors being sentences, never line numbers.

- **CLAUDE.md** — the status line to "cycle 1.5 COMPLETE"; the "WHAT REMAINS
  OF CYCLE 1.5" sentence to the close's record (the declaration, the refresh's
  numbers, S-97/S-98's answers, what the close synced, that 1.6.0 is next
  with `meta/roadmap/1.6/README.md` the map); the tail sentence "**Read the
  map, the catalogue and the manifest, not a summary of them**" stays; every
  `meta/roadmap/1.5/` citation re-pointed at `meta/roadmap/done/1.5/` (30);
  the "Building and testing" paragraph's `residue.py` path likewise.
- **meta/roadmap/1.5/README.md** — the 1.5.8d row marked DONE with the
  record; a closing note under the title (the 1.4 README's shape: closed
  2026-09-2x at 1.5.8d, `done/1.5/`); a new section **"What cycle 1.5
  taught"** — the full account, each lesson with the incident behind it,
  drawn from the twenty-seven subcycle records' "found on the way" paragraphs
  and CLAUDE.md's 1.5 gotchas. The material, gathered here so the executor
  does not re-read the cycle: (1) MEASURE BEFORE ATTRIBUTING — the prelude's
  +0.75 s was the bindings analysis (1.5.2d), the sweep's +21 s was
  `tt_instance` (4b), `npk_hardware_concurrency`'s 1008 was an unzeroed
  buffer (1.5.6b), 1.5.8's three uncontrolled stops were found by trying
  them; (2) A PROPOSITION HOLDS ONLY WHERE ITS EVALUATION DOES NOT TRAP
  (DEF-33 — a guard's fact pushed from inside a clause proved the clause
  from itself, twice); (3) A FIXTURE THAT CLAIMS A PROPERTY IS EVIDENCE OF
  NOTHING UNTIL SOMETHING DECIDES IT (the runner self-check's toy model,
  commented safe for a cycle and unsafe in two steps; every model read TWICE
  since D-295); (4) A SPEC CLAUSE IS A HYPOTHESIS ABOUT THE CALLER and nothing
  checks the callers no row covers — two were false for a legal caller, found
  by READING (1.5.6c), then EXECUTED under the explorer (1.5.7 step 5); (5)
  CORRECT BY ACCIDENT, again — DEF-14 (a stable symbol for an escaped local),
  DEF-81 (a guard elided on the first visit's proof), DEF-93 (a `pub` that was
  a node index's parity), DEF-69 (a "none" of 0 that was stdin), the
  five-second join that slept its whole deadline; (6) A CONTROL PROVES SIGHT
  OF ITS OWN DEFECT ONLY THROUGH THAT DEFECT'S ROUTE (DEF-57: two directed
  controls went blind when the defect they had been reaching their verdicts
  through was fixed); (7) A RULE WRITTEN FOR ONE SPELLING IS OWED TO THE
  OTHER (the `pick` expression form typed no arm binding for four subcycles);
  (8) A WHOLE-TREE SWEEP OVER TEST FILES IS CHECKED BY THE REJECTION SUITES
  AND BOTH SELF-CHECKS BEFORE ITS HARNESS (1.5.8c step 3's six reds); (9) A
  RED UNDER LOAD IS READ, NOT RE-RUN — a deadline is a hang net (DEF-85), a
  literal `/tmp` path is shared (DEF-91), a wedged solver reads as a hang
  (S-71); (10) A REWRITTEN FUNCTION'S ROWS LEAVE WITH IT, so the manifest
  gate reads (symbol, kind) counts and not row identity alone (4b); (11) THE
  `--only` FILTER REACHES THE UNIT TESTS ALONE, so the belts run in-process
  before a harness (the s12/s13 practice, `inproc_checks.py`); (12) THE
  BLUEPRINT TEST FOR A NEW SYMBOL — `+% -% *%` says what happens to the data,
  D-312 — and for a new keyword the tree is measured for the identifier
  first (`unbounded` cost one rename, `sealed`/`hidden` fourteen).
- **meta/roadmap/ROADMAP.md** — the 1.5 row closed with the cycle's record in
  the 1.4 row's style (the kinds live, the manifest's size, the floor's
  evidence, the explorer, the residue by cause and where each goes); a compact
  **"What cycle 1.5 taught"** after "What cycle 1.4 taught"; the 1.6 row's
  "Map: `1.6/`" pointer stays.
- **meta/roadmap/OPEN_DECISIONS.md** — §4's heading annotated ("cycle 1.5
  closed 2026-09-2x"); E-5 and E-6 rows carrying S-97/S-98's answers and their
  landings; §2e's S-37 stays open for the library era, as the user said; §2f's
  heading (the workbench's defects, "scheduled as 1.5.1b") annotated closed —
  every DEF up to DEF-93 carries FIXED or a named owner (measure: `grep -c
  'FIXED' §2f` against the DEF count; any DEF without a disposition is listed
  in the record with its owner); the `meta/roadmap/1.5/` citations
  re-pointed (23).
- **meta/specs/VERIFICATION_REFERENCE.md** — its opening note ("`limit`,
  contracts, `prove`, overflow and the rest follow in 1.5.1–1.5.8") gets one
  dated line: every kind of §7b's catalogue that 1.5 owns is live at the
  close (the catalogue's "landed" column is the proof); the 12 citations
  re-pointed.
- **meta/specs/TRAITS_REFERENCE.md**, `tests/cost/README.md`,
  `tests/verify/README.md`, `tests/modules/rejection/header_missing.npk`,
  `nitpick.toml`'s comment — the citations re-pointed (1 each).
- **The seven code comments** (§1) re-pointed on their own line (P-3): the
  emission is byte-identical by construction (a comment is not in the IR and
  the line count does not move), measured by the emission comparison before
  the run.
- **meta/specs/TCB.md** — nothing generated moves (no floor, spec or model
  edit in the close); if step 0 lands, `tcb_floor.py --write` is NOT owed (the
  encoder is not the floor). Recorded as checked.
- **meta/roadmap/1.6/README.md** — a "start here" note for the 1.6.0 executor
  in the shape 1.4.9 added to the 1.5 README: what 1.5 left it — every
  obligation kind live with its residue by cause (E-4's 825 `unencoded`
  `bounds` rows and the 240 pointer-read `terminate` rows are leg B's frame
  problem; the `open` `overflow` residue by shape is `residue.py`'s), TCB.md's
  enumerated floor for leg A's model, the D-218.9 `llvm.assume` channel, the
  explorer as a standing instrument, the snapshot refreshed at the close.
- **meta/NOTICES.md** — row 48's commit cell (1.5.8c step 5's sha), row 49
  for this close (and 49's commit cell is the NEXT seat's: the cell for the
  commit a notice announces is filled in the landing after it, since the docs
  are committed in the same commit — the rule since 1.5.8c step 5).
- **bootstrap/seed/README.md** — nothing (the refresh follows its text).
- **BUILD_REFERENCE §6** — nothing (1.4.9's declaration line stands; the close
  adds no criterion).

### 2.5 The archive (step 3)

`__pycache__` under `meta/roadmap/1.5/tools/` removed first (ignored,
untracked, not an archive); `git mv meta/roadmap/1.5 meta/roadmap/done/1.5`
with this file inside and its execution record complete up to the run; the
two `done/`-reading instruments run standalone on the REAL tree after the move
(0 and 0, as the dry run says); then the full harness (§2.6). The living
citations were re-pointed at step 2 so that the tree between the two commits
is never half-moved: step 2 and step 3 are ONE commit (P-4).

### 2.6 The run, the commit, the push, the briefing (step 3)

One full harness on the final tree with the refreshed snapshot installed and
1.5 archived, `--verdicts` written for the record; `repro` (the STAMP
matches, zero absolute site rows, cwd-independent, llc deterministic),
`selfhost`, `parity` green; `check_decisions_current` printing nothing with
1.5 under `done/`. ONE commit in the house style with the numbers in its
message; `git push` (the landing script's rule); `origin/main == main`
checked. Then notice 49 to the library listener (the six ladder rows — the
builder rows MOVE at a refresh, so all six carry previous digests — the
manifest numbers, the harness block, and "next: 1.6.0"). Then the briefing
for 1.6.0 by message to the fresh idle session the user starts (the ritual:
state, the reading order — CLAUDE.md's status, `done/1.5/README.md`'s "What
cycle 1.5 taught", `ORCHESTRATION.md`, `bootstrap/seed/README.md`,
`1.6/README.md`'s batch — the gotchas not already in the memory files, an
invitation to ask; questions answered before this seat closes).

### 2.7 What the close does NOT change

- No test, no harness code, no floor byte, no spec or model. The instruments
  were current at 1.5.8c; the close's measurement is the run. Anything the run
  finds is a defect escalated under D-228 R6, not a close item.
- `src/` moves ONLY at step 0 (E-6, if ratified) and, if S-98 goes the other
  way, step 0b — each under its own harness before the refresh; the doc-sync
  commit touches `src/` only in seven comment lines (P-3).
- The harness does not retire: D-206 — both runners run through 1.6 as well,
  retirement under `meta/SWITCH.md`.
- The prelude's impl bodies emitted whether reached or not (the 1.5 README's
  "Watch for") is the emission policy's owner's, 1.6's; not a close item.
- E-4 (the `bounds` and pointer-read `terminate` residue) is 1.6 leg B's by
  D-309 and the 1.5.8b step 5 note; the close hands it over in the 1.6 README's
  note and does not touch it.

## 3. Questions for the user (each blocks step 0 only; nothing else waits)

The rule these rest on, recapped: D-309 (2026-09-19) says the rows nothing
proves stay guarded, measured and reported, and NO BOUND IS WRITTEN INTO THE
TREE TO CLOSE A ROW — the residue is read by shape, honestly, and a way to
prove more is a compiler change, never a source edit that helps the solver.
D-203 says the snapshot refreshes at cycle closes from the final tree. D-233
(2026-09-01) put the abstract-interpretation evidence in 1.6 over the emitted
IR, and E-4 (1.5.8b step 5) sent the frame-condition residue there. So a
proof-power change in the ENCODER belongs to this cycle if it lands before the
refresh, and to 1.6 only if it needs 1.6's tools.

- **S-97 — E-6, a by-value aggregate as a term (§2.1). Recommendation: land
  it, as step 0 of this close.** It is proof power inside the encoder — the
  kind of change D-309 leaves room for — with a measured target (195 of the
  compiler's 499 open `terminate` sites, plus a share of the 1,554 open
  `overflow` rows of the "sum or difference of two unknowns" shape), a
  soundness argument that rests on the language's own aliasing rule, and a
  probe that decides its one open question (a plain argument's write) before
  a line is written. The alternative — leaving it to 1.6 leg B — would have
  an abstract interpreter recover from the IR a fact the frontend already
  holds by name, which is backwards, and it would put a language-level
  precision rule under a tool 1.6.0 has not yet chosen. If the probe answers
  that a plain argument's write reaches the caller, the design's step (4)
  makes a call a version bump and the recommendation stands with the smaller
  payoff, recorded.
- **S-98 — E-5, the bound-call fan-out (§2.2). Recommendation: decide it
  out, with the design kept and a measured re-open trigger.** The
  over-approximation is sound, its measured cost is four test arms, and the
  narrowing is a generic-chain walk inside the analysis that guards
  `failsafe`'s exhaustiveness — the direction DEF-86 just closed. A safety
  analysis should grow more precise when a program pays for its imprecision,
  and none has. "Decided out" here means closed on the record with the
  trigger stated, which is the house pattern for a lead nothing needs yet.

## 4. What lands (each step under a full harness, D-228)

0. **E-6, if S-97 says so** (§2.1): the probe and its recorded answer; the
   encoder; `tests/verify/terminate_field.npk`; the manifest re-recorded and
   gated; the docs (VERIFICATION_REFERENCE §4b's measurement bullet gains the
   after-numbers; §8's registrations if a sort or a function symbol shape is
   new; OPEN_DECISIONS E-6 → LANDED; D-309 gets a dated note that the encoder
   grew and no bound was written). GATE: §2.1's five items; the harness green.
   0b. **E-5, only if S-98 says in** (§2.2). GATE: as written there.
1. **The refresh** (§2.3) from the final `src/`: stage2 == stage3, installed
   with its STAMP; the prologue census recorded. Committed WITH step 2/3 (the
   refresh is validated by the same run).
2. **The doc sync** (§2.4), one script, every anchor asserted.
3. **The archive, the run, the commit, the push, the notice, the briefing**
   (§2.5, §2.6). GATE: every stage green with the refreshed snapshot and 1.5
   archived; `repro`, `selfhost`, `parity` green; `check_decisions_current`
   empty; `origin/main == main`; notice 49 sent; the briefing sent and its
   questions answered; the tree clean and nothing running.

## 5. Decisions taken at planning (each with its reason)

- **P-1 — a compiler lead lands BEFORE the refresh or not in this cycle.**
  The refreshed snapshot is the closed cycle's emission (D-203); a `src/`
  change after it would leave the committed builder one step behind the
  source it builds, which is the state 1.5.8c step 2 existed to end.
- **P-2 — the probe precedes the encoder change** (§2.1 (4)). The soundness
  of E-6 rests on one fact about the language's calling convention that the
  planning seat did not measure; a design that assumes it would be the
  cycle's recurring defect (1.4's lesson 1) in its last step.
- **P-3 — code comments citing the moved folder are re-pointed, on their own
  line.** A stale path in a comment is a document that disagrees with the
  tree, the failure mode the user has been burned by repeatedly; the cost is
  seven lines, none in the IR, and the emission comparison proves it.
- **P-4 — the doc sync and the archive are one commit.** A tree in which the
  citations point at `done/1.5/` while the folder is still `1.5/` (or the
  reverse) is a state no reader should be able to check out.
- **P-5 — the refresh comes from the FINAL `src/`**, after step 0 or 0b if
  they land, so the STAMP's source-commit is the tree whose `src/` the
  snapshot is — the 1.4.9 rule ("`src/` does not change in the close, so the
  snapshot IS the emission of that commit's source"), kept by ordering.
- **P-6 — "What cycle 1.5 taught" is written from the records, with the
  incident behind each line** (the 1.4 README's shape), and the compact form
  in ROADMAP repeats only the rules that generalise. §2.4 lists the material
  so the executor writes, not re-derives.
- **P-7 — the briefing goes by message to a fresh session the user starts**,
  and this seat answers its questions before closing (the ritual since
  1.4.7; the user's drift control, kept even when continuing in place would
  be cheaper).

## Execution record (appended as steps land)
