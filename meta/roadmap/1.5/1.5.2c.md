# 1.5.2c — Generic enums, and a `pick` over an `Optional` refused by name — PLANNED (2026-09-05; execution-grade; ratified the same day as D-260 and D-261)

> Scope: the two findings 1.5.2b recorded for the user (OPEN_DECISIONS DEF-19,
> DEF-20), ratified as recommended on 2026-09-05 ("lets go with your
> recommendations on those") — **D-260**: a `pick`'s selector may not be an
> `Optional`, refused by name; **D-261**: generic enums are IN the language,
> a family exactly as a generic struct is. Scheduled before 1.5.3 (contracts
> live) because 1.5.3 lowers contracts over enums and D-261 changes what an
> enum instance IS to every pass that reads a payload type.
>
> Written in the 1.5.2 shape: the plan, the decisions taken at planning with
> their reasons (L-1…L-8), the measurements (from 1.5.2b's probes, re-run
> here), the steps, then the record appended as each step lands. Three steps,
> three commits, each under a FULL harness on a cumulative prefix (D-228).
> Nothing is pending on the user.

## 1. Where you are (the tree at open, 2026-09-05)

- HEAD is 1.5.2b's close (`4806025`, pushed). The snapshot is 1.5.1b's
  close-out refresh. **Nothing here needs a refresh**: `src/`, `lib/`, `npkg/`
  and `tools/` declare no generic enum and write no `pick` over an `Optional`
  (measured: `rg "^\s*(pub )?enum:[A-Za-z_]+<"` finds nothing; every `pick`
  over an `Ordering?` in the tree is spelled `?? Ordering.Equal`).
- The harness asserts 56 tests, 27 verified programs, 141 obligations (116
  discharged, 25 open), parity at 1075 verdicts, a rung count of 4. None of
  these moves here except the test count and the parity count (L-7).
- The interfaces this plan reads and never re-derives: `bind_instance` /
  `struct_field` (`type_layout.npk`), `type_enum_bindings` /
  `type_pick_bindings` (`type_stmt.npk`), `namespace_member` /
  `type_ctor_named` / `check_ctor_args` / `infer_from_args` / `unify_into` /
  `all_solved` (`type_members.npk`), `subst_instance` (`type_generic.npk`),
  `variant_payload_slot_at` / `variant_info` / `emit_ctor` (`ir_expr.npk`),
  `emit_pick` (`ir_stmt.npk`), the drop generator's enum arm
  (`emit_program.npk`), `emit_instance_header` (already admits TY_ENUM).

## 2. What this subcycle builds, in one picture

```
enum:Opt<T> = { Some(T); None; };                 // a TEMPLATE, as struct:Box<T> is
Opt<int32>:a = Opt.Some(3i32);                    // the instance from the ANNOTATION
Opt<int32>:n = Opt.None;                          // ...which a payload-less variant needs
Opt<string>:s = Opt.Some(string_concat("a", "b")); // owns; dropped at scope exit
pick (move(s)) { (Opt.Some(v)) { … v: string … }, (Opt.None) { … } }
int64:len = (pick (move(s)) { (Opt.Some(v)) { give v.len; }, (Opt.None) { give 0i64; } });
func:wrap<T> = Opt<T>(T:v) { pass Opt.Some(move(v)); }   // the instance from the RETURN
Opt<int32>:i = Opt.Some(7i32) …                   // or INFERRED from the payload: `Some(T)` against int32

Ordering?:o = a.partial_cmp(b) ?! E9;
pick (o) { (Ordering.Less) { … } }                // REFUSED, TYPE-065: an Optional has no arms
pick (o ?? Ordering.Equal) { (Ordering.Less) { … } }   // the spelling
```

## 3. Decisions taken at planning (each with its reason)

- **L-1 — The `Optional` refusal sits where the frac and complex refusals sit.**
  `type_stmt.npk`'s `StmtPickStmt` arm types the selector and refuses a frac
  (D-198) and a complex (D-199) by kind before the arms are read; the
  `Optional` arm joins them, and `type_pick_expr` (`type_expr.npk`) gains the
  same test on `pickx_selector` — the expression form was admitted the same
  way. TYPE-065 `TYPE_PICK_OPTIONAL` in `type_codes.npk`; one report, the arms
  not walked (their patterns have no selector to be read against). The message
  names both spellings the language has (`??`, `== NIL`). `check_codes_tested`
  wants the case: `tests/types/rejection/pick_optional.npk`, statement and
  expression forms, one TYPE-065 each at the `pick`.
- **L-2 — One binding for every payload read (D-261).** `bind_instance`
  (`type_layout.npk`) becomes `pub` with its comment saying struct OR enum (it
  reads the declaration and the argument window, both kind-agnostic); each of
  the five payload-resolution sites wraps its `resolve_type` in
  `bind_instance(r, enum_type)` / restore: the layout's enum arm
  (`ensure_layout`), `type_enum_bindings`, `check_ctor_args`,
  `variant_payload_slot_at` (which the emitter's construction, `pick`
  binding and drop body all read — one fix, three consumers), and
  `emit_ctor`'s single-payload path, which resolves `vi.payload_ty` by hand
  today and moves onto `variant_payload_slot` so there is ONE payload-type
  rule in the emitter. `variant_info` keeps `payload_ty` as the NODE for the
  callers that read only the tag and count.
- **L-3 — The emitter substitutes the enum type through the specialization at
  its two entries.** `emit_ctor`'s `etid` and `emit_pick`'s `sel_tid` come
  from the recorded type, which inside a generic body is `Opt<T>`; `ll_type`
  substitutes internally for the LLVM text but `variant_payload_slot` reads
  the tid it is handed, so both entries call `subst_emitted_type` when the
  type mentions a parameter (the `type_mentions_param` guard the method-call
  path uses). This is what makes `pick (self)` inside `impl:<T: Eq>:Opt<T>:Eq`
  bind `int32` payloads at `Opt<int32>` — and it is why D-258's generator was
  already written with bare variant patterns and constructors.
- **L-4 — The instance of a constructor or a bare reference: expected, then
  inferred, then refused (D-261).** New `enum_instance_for(e, decl, name, n,
  start, count)` in `type_members.npk`, read by `namespace_member` (the bare
  `Opt.None`) and by the method-call path's constructor branch (`Opt.Some(…)`):
  a non-generic declaration is its bare type as today; for a generic one, the
  expected type wins when it is a TY_ENUM instance of the same declaration;
  otherwise the constructor infers — each payload node resolved under the
  declaration's OWN window (`bind_own_generics`, so `T` is the parameter
  type) and unified against the typed argument by `unify_into` (an argument
  that `takes_type_from_context` skipped, as `infer_from_args` skips it),
  then `all_solved`'s refusal (TYPE_CANNOT_INFER naming the parameter and
  the declaration; the message says to annotate the binding) and
  `make_instance` (`type_generic.npk`, factored from `subst_instance`'s tail:
  `mangle_instance` + `tt_instance` + `inst_record`, so the instance is
  judged by `check_one_instance` like an annotated one). The bare reference
  of a generic enum with no expected instance is TYPE_CANNOT_INFER too:
  `Opt.None` alone names no `T`, and its common spelling
  (`Opt<int32>:o = Opt.None;`) supplies it.
- **L-5 — Patterns are read against the selector, never typed as values.**
  Measured: the checker does not type a value pattern's expression (no
  `pat_value` read in `type_stmt.npk`/`type_expr.npk`; the emitter's
  `pattern_const` and the exhaustiveness pass read them), so `(Opt.None)` in
  pattern position needs no instance and L-4's refusal cannot fire there.
  Nothing to build; recorded so the executor does not add a pattern typing
  that would.
- **L-6 — Payload-less generic enums are `i32` like any other.**
  `enum_has_payload` and `ll_type`'s payload-less shortcut read the
  declaration; `Tag<T> = { A; B; }` is a legal but pointless template, and
  `Tag<int32>` lowers to `i32`. Nothing to build; a test says so.
- **L-7 — What must not move.** `nitpick.obligations` (141 rows: no new
  guard, no new walked kind), the rung count (4), `selfhost` (nothing in
  `src/` declares a generic enum or picks on an `Optional`), the snapshot.
  The test count and the parity count move by the new units only.
- **L-8 — Order.** The refusal first (its own commit: a checker rule with one
  test), generic enums second (the substantive change, one commit — binding,
  instance, emitter, tests — because a partial landing leaves a program that
  types and does not lower, which is the shape this subcycle exists to end),
  the docs last.

## 4. Measurements the plan rests on (2026-09-05, on `4806025`)

- **DEF-19 repro** (eight lines, `.internal/probes_152b/`-style): `func:pc =
  Ordering?(int32:a, int32:b) never fails { if (a < b) { pass Ordering.Less; }
  pass Ordering.Equal; };` and `pick (o) { (Ordering.Less) { exit 0i32; }, (*)
  { exit 3i32; } }` over `Ordering?:o = raw pc(1i32, 2i32);` — `tools/check`
  accepts, `npkc` answers `NITPICK-EMIT-002 …:5:16`. `emit_pick` has no
  `TY_OPTIONAL` arm (`rg TY_OPTIONAL ir_stmt.npk` is empty) and switches on
  the selector as its own discriminant.
- **DEF-20 repro**: `enum:Opt<T> = { Some(T); None; };` + `Opt<int32>:o =
  Opt.Some(3i32);` + a `pick` — TYPE-001 at the payload `T` (2:22, raised by
  the LAYOUT's payload resolution, `type_layout.npk:510`, when the annotation
  `Opt<int32>` asks for its size), TYPE-007 "expected `Opt<int32>`, found
  `Opt`" at the constructor (`namespace_member`/`type_ctor_named` build the
  bare enum with `tt_enum(decl, name, 0, 1)`), TYPE-007 "expected
  `<unresolved>`, found `int32`" at the payload (`check_ctor_args` resolved
  `T` unbound).
- **Every payload-type read in the tree** (`rg variant_payload_start`):
  `type_stmt.npk:498` (`type_enum_bindings`), `type_layout.npk:510` (the
  layout), `type_members.npk:1062` (`check_ctor_args`), `ir_expr.npk:9053`
  (`variant_payload_slot_at`) and `:9113` (`variant_info`'s node), plus the
  generator's text (`derive_gen.npk`, no resolution). Five readers, one
  binding to add.
- **The struct precedent**: `struct_field` binds the instance
  (`bind_instance`) before resolving a field's type; `resolve_named_generic`
  interns an enum instance with `tt_instance(kind = TY_ENUM, …)` exactly as a
  struct's; `subst_instance` substitutes TY_ENUM instances; `type_mentions_
  param` walks TY_ENUM arguments; `emit_instance_header` emits a TY_ENUM
  instance's header; `check_one_instance` judges any instance's bounds. The
  kind was half-carried everywhere but where a payload is read.
- **Inference precedent**: `infer_from_args` + `unify_into` + `all_solved`
  solve a generic call's parameters from its arguments; `unify_into` already
  handles TY_PARAM leaves, the one-operand shapes and instances of one
  declaration (`same_instance_decl` admits TY_ENUM).

## 5. The steps, in order, each one commit under a full harness

Protocol: D-228 as 1.5.2b ran it — each step in a detached worktree under
`.internal/wt/` (absolute paths; `mkdir -p .internal` inside it), validated
as a cumulative prefix by a full harness with `--verdicts`, landed in order
by cherry-pick with the record appended; a red prefix stops the line.

### Step 0 — D-260: a `pick` does not select on an `Optional` (L-1)

- `type_codes.npk`: `TYPE_PICK_OPTIONAL` = `NITPICK-TYPE-065`, commented.
- `type_stmt.npk` (`StmtPickStmt`): after the frac and complex arms, an
  `Optional` selector reports TYPE-065 and returns; `type_expr.npk`
  (`type_pick_expr`): the same on the selector before the arms.
- `tests/types/rejection/pick_optional.npk`: the statement form and the
  expression form (a `pick` used as a value with `give`), each one TYPE-065
  with `expect-error-at` on the `pick`; the file's comment carries the two
  spellings.
- Acceptance: full harness green; `check_codes_tested` green; no other test
  moves (every `pick` over an `Ordering?` in the suite already writes `??`).

### Step 1 — D-261: generic enums live (L-2 … L-6)

- `type_layout.npk`: `bind_instance` public, comment widened; the enum arm of
  `ensure_layout` binds the instance around each payload's `resolve_type`.
- `type_stmt.npk`: `type_enum_bindings` binds the instance around its
  `resolve_type`.
- `type_generic.npk`: `pub func:make_instance(r, kind, decl, IdSpan:args,
  Span:at)` — the tail of `subst_instance` factored (mangle, intern, record).
- `type_members.npk`: `enum_instance_for` (L-4); `namespace_member` and the
  constructor branch of `type_method_call` call it instead of `tt_enum(…)`;
  `check_ctor_args` binds the instance around the payload resolution; the
  inference helper resolves payload nodes under `bind_own_generics` and
  unifies with `unify_into`; `all_solved`'s message for a constructor says
  "annotate the binding" rather than "write the turbofish" (a constructor has
  no turbofish position — its parameters are the enum's).
- `ir_expr.npk`: `variant_payload_slot_at` binds the instance around
  `resolve_type` (through `bind_instance` on the `tr` it is handed);
  `emit_ctor` substitutes `etid` and reads the single payload's slot through
  `variant_payload_slot`; `ir_stmt.npk`: `emit_pick` substitutes `sel_tid`.
- Tests. `tests/backend/programs/generic_enum.npk`: `Opt<int32>` constructed
  from the annotation and inferred from the payload, picked with a binding and
  as an expression (`give`), a payload-less `Opt.None` from the annotation;
  `Opt<string>` owning its payload, consumed by `pick (move(s))` and dropped
  at scope exit (exit 0 under D-151's leak check); `Opt<Opt<int32>>` nested;
  `Either<L, R>` two parameters, both variants; a generic enum as a field of a
  generic struct (`Box<Opt<T>>`); `Tag<T>` payload-less (L-6); a generic
  function returning `Opt<T>` from `Opt.Some(move(v))` (the instance from the
  return). `derive_generic.npk` regains the section 1.5.2b dropped: `Opt<T>`
  under `Eq`/`Ord`/`Clone` with `Opt<int32>` values. `tests/types/rejection/
  generic_enum_infer.npk`: a bare `Opt.None` with nothing to infer from, and
  `Opt.Some(5)` with an unsuffixed literal — TYPE_CANNOT_INFER each; a payload
  of the wrong type against the annotated instance — TYPE-007; the messages
  say annotate. `tests/frontend/type_generic.npk`: a `gx` case with an enum
  instance whose bound fails (`Opt<Point>` under `struct:… <T: Pr>`-style
  `enum:Opt<T: Pr>`) — TYPE-017 through `check_one_instance`.
- Before the harness: `quickcheck` over `src/npkc.npk` and `npkg/main.npk`
  (nothing in the tree declares a generic enum, so nothing should move) and
  `quickemit` over every `tests/backend/programs/*enum*.npk` and
  `derive_*.npk`.
- Acceptance: full harness green; `selfhost`, `repro`, `parity` green; 141
  obligations unchanged; every pre-existing enum program at its exit.

### Step 2 — the docs, the decisions, the close

- TYPE_REFERENCE §9.3: a generic enum is a template; the instance rule; the
  layout per instance; the constructor's instance (expected, inferred,
  refused). §11.1 (`Optional`): no `pick` selects on one (D-260, TYPE-065).
  CONTROL_REFERENCE §1.2 (`pick`): the selector may not be an `Optional`.
  TRAITS_REFERENCE §3.3: an enum instance is judged as a struct's is; §2.5's
  derive text: a generic enum derives as a generic struct does.
- DECISIONS: D-260 and D-261's landed notes; D-258's note gains "the generic
  enum test returned at 1.5.2c". OPEN_DECISIONS: DEF-19/DEF-20 read landed.
  `1.5.2b.md`'s DEF-20 mention points here. The 1.5 README row → DONE;
  ROADMAP's 1.5 row; CLAUDE.md's status and its gotcha list (a generic enum's
  payload-less variant needs the annotation; a `pick` over an `Optional`
  spells `??`).
- Acceptance: `check_decisions_current` reports nothing new; `check_refs.py`
  holds its baseline of 62; the record complete; `origin/main == main`.

## 6. What the executor should know (gotchas, carried and found)

- A full harness is about 75 minutes alone; stdout is block-buffered, judge
  liveness by pid. `cd` EXPLICITLY at the start of every command — 1.5.2b lost
  three commands to a working directory that had drifted to the main tree.
- `quickcheck` rebuilds the checker each run (~1.5 min); `quickemit` rebuilds
  `npkc` when `src/` changed (~4 min) and is cached after.
- A probe's `failsafe` names `HeapOom`, `HeapBadRequest`, `Unreachable`,
  `WildLeak` (and `IntOverflow`, `DivByZero`, `DivOverflow` where arithmetic
  or division appears), or REACH-002 stops it before the rule under test.
- Reserved words that read like locals here: `any`, `on`, `in`, `mod`, `fd`,
  `fails`, `end`, `unit`, `error`, `buffer`, `raw`, `move`, `old`, `pure`,
  `result`, `limit`, `trit`/`nit`, the flag families. 1.5.2b lost a build to
  `any` and a test run to `on`.
- Strings are move-only owners (TYPE-046); an owning payload is bound only by
  a consuming `pick (move(v))` (D-216), which is what `Opt<string>`'s test
  writes.
- Both self-checks share one case set by NAME; nothing here touches them.
- `check_codes_tested` wants a case for TYPE-065 (step 0's file).
- The user's client flags security-attack vocabulary and the column-trimming
  shell command spelled c-u-t; keep both out of prose and commands.
- Stop every background waiter before reporting done.

## 7. Acceptance for the subcycle

Three commits, each under a full harness with every stage green (`selfhost`,
`undef-ban`, `repro`, `absent-fact`, `cost`, `object`, `verify` with
`nitpick.obligations` byte-identical, `parity`); the new rejection and
program cases exact in both runners; the two repros of §4 re-run at the
close — the `pick` refused TYPE-065 at the user's line, the generic enum
program running to its answer; the docs synced; this record complete;
`origin/main == main` after the push.

## Execution record (appended as steps land)

### Ratification (2026-09-05)

The user, on the 1.5.2b report's two findings: *"lets go with your
recommendations on those. I don't see anything wrong with them."* Entered in
DECISIONS as **D-260** (DEF-19: a `pick` does not select on an `Optional`)
and **D-261** (DEF-20: generic enums are in the language); OPEN_DECISIONS'
two rows read settled. Every step may start; the order is L-8's.

### Step 0 — LANDED (2026-09-05): D-260, a `pick` does not select on an `Optional` (L-1)

`TYPE_PICK_OPTIONAL` is `NITPICK-TYPE-065` (`type_codes.npk`), reported at the
SELECTOR before the arms are read, in the statement form (`type_stmt.npk`, the
`StmtPickStmt` arm, beside the frac and complex refusals D-198 and D-199 put
there) and in the expression form (`type_pick_expr`, which had no selector rule
at all). The message names the two spellings: reach the value with `??`
(`pick (o ?? default) { … }`) or test it with `== NIL`. The arms are not walked
once the selector is refused — there is no selector to read them against, and
a second report about one mistake is what D-240 forbids.
`tests/types/rejection/pick_optional.npk` pins both forms at their positions
(19:5 and 22:15) and accepts the `??` form; every other rejection file reports
exactly what it did (D-237's exact-set rule, both runners). The compiler and `npkg` check clean under the rule (no
`pick` over an `Optional` exists in `src/`, `npkg/`, `lib/` or `tools/`).
Nothing else moved: no lowering changed, `nitpick.obligations` did not move.

**Harness (`533fcb2`, prefix q0, 2026-09-05, run side by side with the later
prefixes): every stage green -- `ok 56 test(s) passed`, 217 real-backend
programs at -O0 and under opt -O2, 2 module objects, 27 verified programs;
`verify` 141 obligations, 116 discharged, 25 open, 0 budget, 0 unencoded,
`nitpick.obligations` matches, 116 guards elided, the verified compiler
rebuilds itself byte-identically; `parity` 1077 verdicts agree between the two
runners (1075 at the 1.5.2b close plus `pick_optional.npk`'s two), `npkc` and
the verified compiler byte-identical; `selfhost`, `undef-ban`, `repro`,
`absent-fact`, `nf-inert` green; `cost` 6 units, 0 designed failures confirmed
(`self.toml` peak 13,362,864,105 bytes under its 23,258,956,650 ceiling).**

### Step 1 — LANDED (2026-09-05): D-261, generic enums live (L-2 … L-6)

**As planned.** `bind_instance` (`type_layout.npk`) is `pub` and binds a struct's
OR an enum's declaration window to the instance's arguments; the four payload
readers the plan enumerated go through it — `ensure_layout`'s enum arm (each
instance its own size, alignment and owning bit), `type_enum_bindings` (a
binding over `Opt<string>` is a `string`), `check_ctor_args` (`Opt<int32>.Some`
expects an `int32`, TYPE-007 against `2i64`) and the emitter's
`variant_payload_slot_at`, which construction, `pick` binding and the generated
drop body share (reworked with a `done` flag so the binding is restored on
every path). `enum_instance_for` (`type_members.npk`) supplies a constructor's
or a bare variant's instance: the expected type when `expected_value` is an
instance of the same declaration (an annotation, a parameter, a return, a
payload, a field, a `give`), else inferred from the payload arguments with the
generic call's own `unify_into` over the variant's payload nodes resolved under
`bind_own_generics` (now `pub`), skipping an argument that takes its type from
context (D-108), else `NITPICK-TYPE-022` naming the parameter with the advice
to annotate — the payload-less `Opt.None` bare has nothing to infer from and
says so. A variant that does not exist returns the bare type so the caller's
"no member" stays the one report (D-240). The instance is built by
`make_instance` (`type_generic.npk`: the tail `subst_instance` always had,
factored — mangle, intern, RECORD) so `check_instantiations` judges an inferred
instance exactly as an annotated one. In the emitter, `emit_ctor` substitutes a
generic body's enum type through the specialization before reading the slots
(the single-payload path reads `variant_payload_slot` instead of resolving the
node by hand), and `pick_sel_tid` (`ir_stmt.npk`) does the same for the three
readers of a selector's type — `emit_pick` and both frame-slot scans.

**Deviations, and what the measurement missed.** (1) The plan's "five readers,
one binding to add" keyed on `variant_payload_start`; two more readers took
`variant_info`'s `payload_ty` NODE and resolved it by hand in the enum's home
scope — `bind_payload`'s single-binding path and `scan_pick_binds`' mirror of
it (`ir_stmt.npk`) — and the first generic program found them as TYPE-001 at
the declaration's `T` plus EMIT-002 at the arm. Both read
`variant_payload_slot` now; `payload_ty` has no resolver left in the backend.
The 1.4.7 lesson again: an enumeration keyed on one token misses the family
that spells the same read another way. (2) `finish_layouts` (D-227's sweep)
completes the table for every interned type after typing, so an instance
minted only by inference (`pick (Opt.Some(big))`, `Opt<int64>`) has its layout
when `enum_payload_slot` reads the recorded size — measured (the program runs),
not assumed. (3) `type_generic.npk`'s `gx` fixture judges the instances a
BODY records, not a parameter's (the pipeline's `check_instantiations` catches
`Opt<Point>:o` in a signature — probed through `tools/check`, TYPE-017 at the
parameter — the fixture's flow does not), so its three cases were written in
the fixture's own shape, a body-local `Opt<Point>:o;` as its `b1` writes
`Box<Point>:b;`.

**Found on the way, fixed in the step: the `pick` EXPRESSION form typed no arm
binding.** `type_pick_expr` walked each arm with `check_stmt` and typed the
`give`, and never called `type_pick_bindings` or `check_pick_owning_binds` —
the statement form's prelude since 1.0.9c and 1.4.3b. A bound `t` was an
unknown the checker asked nothing of: `give t.nope` was ACCEPTED and reached
the emitter as EMIT-002 (reproduced on a plain `enum:OptP = { Some(Pt); None; }`,
so this is not D-261's), `give t` of a struct where an `int32` was expected
passed the fit check against nothing, and an owning payload bound in a LENDING
pick expression was copied out unrefused — the 1.4.3b hole, open in one of the
two spellings while the record said it was closed. The statement form's whole
prelude — the frac, complex and `Optional` refusals, the consuming selector and
its meaningless-move refusal, the binding types, the owning-binding refusal —
is ONE function now, `type_pick_rules` (`type_stmt.npk`, `pub`), called by
both forms with their own arm window and span; `type_pick_bindings` and
`check_pick_owning_binds` take a window instead of a statement node.
`tests/types/rejection/pick_expr_bindings.npk` pins the four rules that became
live for the expression form (TYPE-019, TYPE-007, TYPE-046 twice);
`pick_optional.npk`'s two positions are unchanged. The compiler, `npkg` and
the tools check clean under the shared rules: no expression-form pick in the
tree binds an owning payload or reads a member its payload lacks.

**Tests.** `tests/backend/programs/generic_enum.npk` (exit 0: the annotated
constructor and `Opt.None`, the inferred `pick (Opt.Some(big))`, a consuming
pick over `Opt<string>` whose binding drops at the arm's end and a second
instance dropped at scope exit, `Opt<Opt<int32>>` with a `give Opt.None` typed
from the first arm, `Either<int32, string>` both ways, `Box<int32>` holding an
`Opt<T>` field, payload-less `Tag<int32>` as an `i32` tag, `wrapg<T>` returning
`Opt<T>` — so D-151 asserts nothing leaked); `derive_generic.npk` regains the
`Opt<T>` section 1.5.2b dropped (`Eq`, `Ord`, `PartialOrd`, `Clone` over
`Opt<int32>`, and an owning `Opt<string>` cloned and consumed) and still exits
42; `tests/types/rejection/generic_enum_infer.npk` (TYPE-022 for the bare
`Opt.None` and for `Opt.Some(5)`, TYPE-007 for `Opt.Some(2i64)` against
`Opt<int32>`); `tests/frontend/type_generic.npk` ge1..ge3 (TYPE-017 for
`Opt<Point>` under `enum:Opt<T: Pr>`, clean with the impl, TYPE-017 for the
inferred `Opt.Some(q)`). Every existing pick and enum program keeps its exit
(`async_pick`, `enums_pick`, `pick_move`, `pick_tail`, `struct_pick`,
`derive_ord_enum`, `derive_payload`, `enum_drop`, `enum_int_cast`,
`enum_payloads`). Measured across every program the tree compiles as a unit (the backend programs, the frontend unit tests and the conformance suite): the compiler at the 1.5.2b close and the step-1 compiler emit BYTE-IDENTICAL IR for 245 of 255, and every one of the 10 that differ compiles changed source — `derive_generic.npk` and `type_generic.npk` themselves, and the eight frontend unit tests that import the changed `src/frontend/` modules. A non-generic enum binds an empty window, as D-261 said it would, and nothing else moved. `nitpick.obligations` did not move (L-7).

**Harness (`fe78c5d`, prefix q0+q1, 2026-09-05, run side by side with the other
prefixes): every stage green -- `ok 56 test(s) passed`, 218 real-backend
programs at -O0 and under opt -O2 (`generic_enum.npk` the 218th), 79 type
rejection files (`generic_enum_infer.npk` and `pick_expr_bindings.npk` the two
new), 2 module objects, 27 verified programs; `verify` 141 obligations, 116
discharged, 25 open, 0 budget, 0 unencoded, `nitpick.obligations` matches, 116
guards elided, the verified compiler rebuilds itself byte-identically;
`parity` 1083 verdicts agree between the two runners, `npkc` and the verified
compiler byte-identical; `selfhost`, `undef-ban`, `repro`, `absent-fact`,
`nf-inert` green; `cost` 6 units, 0 designed failures confirmed (`self.toml`
peak 13,428,902,379 bytes under its 23,258,956,650 ceiling -- half a percent
above step 0's, the new frontend code's own working set).**
