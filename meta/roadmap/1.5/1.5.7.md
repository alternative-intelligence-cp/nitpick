# 1.5.7 — The schedule-exploration harness (D-212, G-5): every synchronization step of the floor under a seeded scheduler, virtual time, and oracles that can SEE a lost wakeup

**Planned 2026-09-17 by `nitpick-compiler_s7` on 1.5.6c's close, measured first with a throw-away prototype, and
APPROVED the same day by the user, first-hand to that seat: "so, I think i am good with all your recommendations. The
only thing i'm not positive about is the C shim you mentioned. What exactly is it's purpose? If it's just an extra
layer of verification for us that doesn't get shipped then i don't see a problem with it." — S-77 and S-78…S-83 are
SETTLED under their recommendations, S-83 on the condition he stated, which is exactly what §3 says: the C shim is a
behavioural reference the hand-written IR is held to, built by nothing that ships and by no gate. Recorded at step 0
by `nitpick-compiler_s8`: S-77 as D-297 (its own landing before step 0, `fa7b5a9`), S-78…S-83 as D-298…D-303 in
DECISIONS.md, the six rows SETTLED in OPEN_DECISIONS §2e with those words, and §3 below rewritten as settled.
Everything in §1 was measured with the prototype (a C shim built with clang, OUTSIDE every gate and never for the
tree; the zero-dependency rule governs the artifact, and this plan's shim is hand-written IR) — the prototype is
kept under `meta/roadmap/1.5/tools/explore_prototype/` by D-303. No language surface changes in this subcycle: no
keyword, no refusal, no builtin. The floor's bytes do not move (the explored floor is GENERATED from the real one at
test time).** *[2026-09-18, step 4: they moved once. The explorer's first floor find, DEF-57, was fixed in
step 4's landing; its record says what moved.]*

D-212 asks for "the minimal harness of the r6 recipe": a mocked-primitive build of the runtime, a centralized
PCT-seeded scheduler stepping one synchronization operation at a time, a virtualized reactor, and seed-replay —
complementing `// stress:`, never replacing it. Three things are owed to it from earlier subcycles: E-3's instrument
half (the I/O wake path's first EXECUTABLE evidence), the models' controls as the explorer's negative controls, and
1.5.6c's six invariants as executable assertions — the place where what the floor's spec ASSUMES of its callers is
tested rather than argued.

## 1. Where it starts (measured 2026-09-17 on `c609350`; this machine, five harnesses running beside it)

- **The seam is a TEXT TRANSFORM of the real floor, and its totality is a count.** A scheduling point before every
  atomic step line (`atomicrmw`, `cmpxchg`, `fence`, `load atomic`, `store atomic` — `line_is_step`'s own definition,
  `npkg/floor_model.npk`) and every `@npk_sys6(` CALL routed to the shim: **141 sites = 78 atomic (6 `cmpxchg`, 8
  `atomicrmw`, 39 `load atomic`, 25 `store atomic`) + 63 syscall sites over 27 numbers**, in 63 of the floor's 175
  functions — exactly the census, so "no step escapes" is an equality both runners can hold. Thread lifecycle is four
  hooks: before and after the `npk_clone_raw` call, first in `npk_thread_entry`, before its `ret`.
- **Real clone'd threads under a BATON work with the floor as it is.** One thread runs; the rest sit in a real private
  futex wait inside the shim. `%fs`, the per-thread constants and the clone trampoline stay the real ones.
- **Only what BLOCKS needs virtualizing:** futex WAIT/WAKE (virtual queues, longest-blocked first), `epoll_pwait` (a
  real poll with timeout 0 under the baton; a blocked waiter is re-probed only after another thread stepped — so
  eventfd, pipes and the one-shot registrations stay REAL and deterministic), `clock_gettime` (a VIRTUAL CLOCK: +1 µs
  per read; a jump to the earliest pending deadline when nobody can step). Thread exit: the kernel's CLEARTID write
  and wake are invisible to virtual futexes — a joiner blocked on a word holding the tid of a thread that has ENDED
  does a real wait for the kernel's clear and returns 0. Everything else passes through after its point.
- **EXACT REPLAY:** the same seed gives the same step count and the same schedule hash (`mutex_basic`, seed 1 twice:
  1,314 steps, one hash); different seeds differ. Decisions depend on the seed, slot indices and step counts — never
  on tids, addresses or wall time.
- **RATE:** `mutex_basic`, 300 schedules of ~1,300 steps in 0.83 s (~360/s); the plain build takes 4.97 s for 40 runs.
  Virtual time makes every deadline path instantaneous: a schedule is ~45× CHEAPER than a stress run.
- **THE SWEEP: 35 of the 44 `// stress:` programs explore correctly, 100 seeds each, 0.1–4 s per program.** The other
  9 need fixtures or argv (the six driver programs, `extern_c_driver`, `extern_stub`, `proc_tool`): real CHILD
  PROCESSES, which a virtual clock cannot coexist with (§2.8).
- **The trap route needs VIRTUAL SIGNALS, and they work:** record the handler at `rt_sigaction`; `tgkill` marks the
  target; the target runs the handler in its own context at its next grant, and a virtually BLOCKED target is made
  runnable (a signal interrupts a blocking call). `trap_two_threads`, `windup_thread`, `trap_stops_runner`,
  `failsafe_alloc`: 0 of 30 → 100 of 100.
- **PCT NEEDS A FAIRNESS RULE AND ORDERED PRIORITY BANDS.** A thread that never blocks ("write a byte until another
  thread stops me") starves everyone behind it under strict priorities, and virtual time never advances because it
  never reads the clock: a hang in REAL time. The rule that fixed it: after F consecutive steps while another thread
  could step, the runner drops below everything — and the bands are ordered *initial > change-point > demotion, each
  demotion below every earlier one*. (The first attempt tied the demoted threads and livelocked differently; the dump
  that showed it: two threads at change-point priorities 1 and 2, READY forever behind a spinner whose 700 demotions
  were all still above them.)
- **A LOST WAKEUP IN THIS RUNTIME IS MOSTLY *LATENESS*, WHICH AN EXIT CODE CANNOT SEE — and virtual time hides it
  completely.** Every wait carries a deadline (D-071), so an executor that misses its wake sleeps until the next
  deadline and the program still exits right, late. Planted: `npk_ch_wake_one` stamps the task due and never rouses
  its executor (r6's "absorbed notification", tokio#2057). With a QUIESCENCE ORACLE — *virtual time may jump only
  when no blocked thread's executor holds a task stamped due* — the explorer says `LOST WAKE` at seed 1, 142 steps,
  naming both threads and their deadlines. (§1's last bullet has the comparison numbers.)
- **A MODEL'S CONTROL IS NOT ALWAYS A FLOOR BUG.** `park-unpark`'s `drop-second-sweep` applied to the floor (the second
  `npk_sl_wake_due` of `npk_step` deleted): 0 failures in 1,800 explored schedules and 360 stress runs — and reading
  says why: `npk_sl_earliest` reads the stamp the sweep would have found, so the park's deadline is already past and
  it returns at once. The floor defends that window twice; the model mutates one of the two. So each control is
  MEASURED against the floor, and the ones that are not floor bugs are recorded with their reason (§2.6).
- **THE SPEC'S HYPOTHESES, EXECUTED, WOULD HAVE FOUND E-1's FALSE CLAUSE ON THE FIRST CALL** (two checks written by
  hand into the prototype to measure §2.4's idea; the 35 programs, one schedule each): `npk_small_free` was called
  5,276 times — the clause 1.5.6 wrote (unconditional apartness of the chunk, its neighbours and the partial head) was
  FALSE ON 5,276 OF 5,276 CALLS, and 1.5.6c's `apart-when` clause on none; `npk_rq_push`'s three-way apartness held on
  1,424 of 1,424 calls. Reading found that clause after a year; a checker at the symbol's entry finds it in the first
  millisecond of any program that frees a small block — and it checks the callers no row covers.
- **A shallow planted bug for scale:** `futex-mutex`'s `store-release` (`npk_mx_unlock`'s exchange → a plain store, no
  wake): explorer `DEADLOCK` at seed 1, 268 of 300 seeds, 0.9 s; stress 29 of 40 runs fail (exit 91) in 59 s.
- **THE COMPARISON THAT DECIDES S-81** — the absorbed notification planted (`npk_ch_wake_one` stamps and never
  rouses), the same buggy floor under three instruments (explorer: 100 seeds; stress: 10 plain runs):

  | program | explorer + the oracles | explorer, exit code only | `// stress:` |
  |---|---|---|---|
  | `thread_root_wake` | 100/100 `LOST WAKE`, seed 1, 0.2 s | 100/100 (exit 43: its own `DeadlineExceeded`) | 10/10 bad, 230 s |
  | `channel_threads` | 99/100, seed 1, 0.3 s | 35/100, first at seed 2 | 3/10 bad, 5.5 s |
  | **`nested_wait`** | **100/100, seed 1, 0.2 s** | **0/100** | **0/10 — every run exits right, 1.5 s late** |
  | `channel_cross` | 0/100 | 0/100 | 0/10 (the path is not exercised) |

  `nested_wait` is the case: the bug is there on every schedule and NOTHING but the oracle sees it. On the clean
  floor both oracles are silent over 35 programs × 100 seeds (no false positive), and they turn the shallow control's
  bare `DEADLOCK` into `LOST FUTEX WAKE (a waiter's word changed and nobody woke it)`.

## 2. The design

### 2.1 The explored build is the REAL floor, transformed
`npkg/explore.npk` (and `tools/explored.npk`, the harness's entry to the same functions, snapshot-built as
`tools/floorspec.npk` is — named `explored` at step 0 because a program cannot hold two modules named `explore`,
1.5.2d's `prelude_names` lesson): text in, text out. Before every step line `call void @npkx_point(i32 SITE)`; every
`@npk_sys6(` call becomes `@npkx_sys6(` (same signature); `@npkx_prespawn()` / `@npkx_spawned(i64 tid)` around the
clone call; `@npkx_begin()` first in `@npk_thread_entry`, `@npkx_end()` before its `ret`; the declares appended; a
`sites.txt` (site, function, kind, text) beside it. `module asm`, `@npk_sys6`'s own body and declarations are left
alone. **THE TOTALITY BELT, both runners:** the number of step lines in `runtime/npkrt.ll` (the model belt's
`line_is_step`) equals the number of points plus routed calls in the transformed text, and no step line in the output
lacks its point — `explore-step-escapes`, by name. ONE transformer (as the floor has one translator); the belt is the
second reader, and it is a count.

### 2.2 The shim: `runtime/explore/npkx.ll`, hand-written LLVM IR
Test infrastructure, linked into explored test binaries only, under the floor's own belts (the `undef` ban, D-173
and the stack rule, the zero-dependency scan). NOT Nitpick, by construction and not by taste: the shim needs mutable
module state (D-211 gives a program none), runs INSIDE the floor's critical sections (so it may not allocate, trap
or call the floor), and must not itself be transformed. Contents, each measured in the prototype: 64 slots (state,
tid, priority, futex word, virtual deadline, wake reason, pending signal, executor); `npkx_point`; `npkx_sys6` (futex
WAIT/WAKE/WAIT_BITSET, `epoll_pwait`, `clock_gettime`, `rt_sigaction`, `tgkill`, `exit`, `exit_group`; the rest pass
through after a point); the lifecycle hooks; the scheduler — PCT (depth d, d−1 change points over k steps) with the
three ordered priority bands and the fairness bound F, or a uniform random walk; the virtual clock; xorshift64* from
the seed; an FNV schedule hash; the verdicts (§2.3), each printed with the seed and every slot's state, and ended
with `exit_group(97)`. Parameters by environment (`NPKX_SEED`, `NPKX_POLICY`, `NPKX_D`, `NPKX_K`, `NPKX_FAIR`,
`NPKX_BUDGET`, `NPKX_TRACE`), read once from `/proc/self/environ`.

### 2.3 The verdicts — an exit code is not enough
Per seed: the program's exit equals its `// expect-exit:`; and none of: **`DEADLOCK`** (nobody can step, no deadline
pending, a thread lives); **`LOST-WAKE`** (at quiescence — before virtual time may jump — a blocked thread's executor
holds, on its sleeper list, a frame stamped due: the models' `wake-before-sleep`, `absorbed-notification` and
`spent-marker`, read off the REAL state); **`LOST-FUTEX-WAKE`** (at quiescence a virtual waiter's word no longer
holds the value it waited on: every futex protocol of the floor is "wait while the word is v; whoever changes it
wakes"); **`STEP-BUDGET`** (a livelock the fairness rule did not break). A verdict is a red run by name, with the
replay line (`NPKX_SEED=… NPKX_K=… <binary>`). The quiescence oracles read three offsets of `%npk.exec`/`%npk.hdr`
(sleeper head, `qnext`, `wake_at`): the shim's copy of them is held to the floor's struct types by a belt.

### 2.4 The spec's hypotheses, EXECUTED (1.5.6c's TCB.md §4d, tested instead of argued)
For every section that has rows and a caller assumption (31 today), the transformer emits, at the symbol's entry in
the explored floor, a call of a GENERATED checker (`@"npkx.req.<symbol>"`, IR text written by the tool from the
spec's own S-expressions): each `requires` with no free symbol, each range's address-space bound, and each pair
fact of `(objects …)`/`(views …)` — with `apart-when`'s condition and the null/empty rule exactly as
`objects_facts` states them — evaluated over the ENTRY state (parameters, `load8/16/32/64`, `+ - * mod ite`,
comparisons, the per-thread constants by their own calls). A false one is the verdict **`ASSUMPTION <symbol>:
<clause>`**. A clause the generator cannot evaluate (a free symbol: `npk_small_free`'s sorted-table `requires`) is
LISTED BY NAME in the stage's output and in TCB.md — never skipped in silence. This is what turns §4d's two "NOT
PROVED" columns into "checked at every call of every explored schedule", for untranslated floor callers and emitted
code alike.

### 2.5 Units, markers, the stage
A program opts in with **`// explore: N`** (N seeds; optional `d=…`), or out with **`// explore: no <reason>`**; a
belt in both runners holds every `// stress:` program to one or the other (`explore-unmarked`). `stage = "explore"`
in `nitpick.toml`'s `[[test]]` table (D-238). Per unit: compile, transform the program's IR too (§2.7), `llc`, link
against the explored floor and the shim; a measuring run (seed 0, no change points) gives k; seeds 1…N under PCT;
**the first seed is run TWICE and the two schedule hashes must agree** (`explore-replay-differs`: nondeterminism in
the explored build is a defect of the instrument); one verdict line per unit for `parity`. A seed that ever failed
is committed beside the marker as `// explore-seed: S` and runs first, forever.

### 2.6 Negative controls: the instrument must FIND what is planted
`runtime/explore/controls/<name>.ctl`: a text substitution on `runtime/npkrt.ll` (the exact old lines, the new
lines), a program, the verdict it must produce and within how many seeds. Both runners build the patched floor and
REQUIRE the verdict (`explore-control-blind` otherwise — the models' rule, for the explorer). Landed with the
mechanism: `store-release` (DEADLOCK, `mutex_basic`, seed 1) and `no-rouse` (LOST-WAKE, `thread_root_wake`, seed 1).
Then ALL NINETEEN model controls are walked: each becomes a control, or is recorded in
`runtime/explore/controls/README.md` as NOT A FLOOR BUG with the reason and the measurement (`drop-second-sweep` is
the first: `npk_sl_earliest` sees the stamp) — which is also a finding about the model, recorded in its file.

### 2.7 Program-level atomics
`atomic<T>` lowers inline in the PROGRAM's IR; the same transformer runs over each unit's emitted IR (points before
its atomic lines; its own `@npk_sys6` calls routed). The shim's module is never transformed.

### 2.8 What is NOT explored, and says so (TCB.md, generated where it can be)
Programs with real child processes (a virtual clock cannot coexist with a real child's real time: the nine, each
`// explore: no real child processes`); WEAK MEMORY — the baton serializes, so every atomic is explored as
`seq_cst` and an ordering bug in a `release`/`acquire` pair is invisible here (r6: loom's territory; the models and
D-290's shared-state belt are what speak to it); the clone trampoline and the asm bottom; schedules beyond the
seeds — PCT's guarantee is 1/(n·k^(d−1)) per run for a depth-d bug, which the stage prints per unit from its
measured n and k.

## 3. Questions for the user — ALL SETTLED 2026-09-17, the day they were asked (the header quotes him)
Each was asked in plain text with its recommendation and the alternative stated; each is settled under the
recommendation and recorded in DECISIONS.md at step 0.
- **S-78 → D-298 — the shim's form: hand-written LLVM IR in `runtime/explore/`** (D-203's form for exactly this kind
  of code; §2.2 says why Nitpick cannot express it: no mutable module state under D-211, and code that runs inside the
  floor's critical sections may not allocate, trap or call the floor). The alternative, keeping exploration out of the
  tree until it can be Nitpick, was declined: the language will not gain mutable module state for a test harness's
  sake, and D-212's evidence would wait on a change nobody wants.
- **S-79 → D-299 — explicit markers and the belt**: `// explore: N` or `// explore: no <reason>`, and
  `explore-unmarked` in both runners holds every `// stress:` program to one or the other. "Every `// stress:`
  program is explored" implicitly was declined: the nine that cannot be would live in a skip list in a runner, the
  next stale list.
- **S-80 → D-300 — 1,000 seeds per unit per run.** MEASURED: the 35 programs × 1,000 seeds = 35,000 schedules, all
  correct, both oracles silent, in 298 s with four harnesses running beside it (≈ 5 minutes per runner) — against 100
  (≈ 30 s). Per-run cost is not the constraint here; coverage is.
- **S-81 → D-301 — `LOST-WAKE` / `LOST-FUTEX-WAKE` are RED even when the exit code is right**: it is the only way a
  late wake is ever seen (§1's table — `nested_wait`'s defect is found on every schedule by the oracle and by nothing
  else). Reporting without failing was declined.
- **S-82 → D-302 — §2.4 (the spec's hypotheses executed) inside this subcycle**, step 5: it is where 1.5.6c's
  sixteenth acceptance gets its test. Its own subcycle after was declined.
- **S-83 → D-303 — the planning prototype's C shim is kept in the tree, OUTSIDE every gate, as the IR shim's
  BEHAVIOURAL REFERENCE** (`meta/roadmap/1.5/tools/explore_prototype/`, beside `model_bfs.py`, with a header saying
  what it is and that nothing builds it into anything): the hand-written IR is held to it SCHEDULE HASH FOR SCHEDULE
  HASH over the units at step 1 — which turns "port 250 lines of C into ~900 lines of IR by hand" from a review into
  a measurement. The user's condition — "if it's just an extra layer of verification for us that doesn't get
  shipped" — is exactly its status: it ships nowhere, no gate builds it, and the harness never invokes a C compiler
  for it (the fixture drivers of 1.1.13c are the precedent for C as test tooling outside the TCB, D-149). The
  alternative — no C file in the repository, the IR validated by the units and the negative controls alone — was
  stated and declined by his answer.

## 4. What lands (each step a cumulative prefix under a full harness, D-228)
**Before step 0 — S-77, its own landing — LANDED 2026-09-18 as `fa7b5a9` (D-297):** the hang net in both runners
is `120 + 10·checks + 60·B` seconds per file, B the number of that file's rows the COMMITTED manifest records as
`budget` (a `--record` run, a verify test and a planted self-check case have no manifest to trust and take the larger
bound for every row: B = checks); `harness.z3_verdicts` and `npkg/verify.npk`'s `z3_decide`, no hang-net case
existed in either self-check and each gained two (`hang-net`, `hang-net-untrusted`, one planted text), 1.5.0.md's
P-13 amended by a dated note, VERIFICATION_REFERENCE §8's dated paragraph, BUILD_REFERENCE, OPEN_DECISIONS S-77
SETTLED and LANDED, DECISIONS.md D-297. No verdict moved; `npk_small_free` is decided under a net of 610 s where it
was 250 (13 rows, 6 `budget`), `npk_int_to_string` under 260 where it was 200, no other file's net moved. Then:
0. The transformer + the totality belt (both runners) + self-check cases on planted IR; this plan lands with it,
   and so do the settled S-78…S-83 (the decisions, OPEN_DECISIONS, the S-83 prototype copied to
   `meta/roadmap/1.5/tools/explore_prototype/` with its header).
1. The shim (futex, epoll, clock, lifecycle, PCT + fairness, DEADLOCK/STEP-BUDGET) + the `explore` stage in both
   runners + markers on every `// stress:` program + the marker belt + the replay check. GATE: the 31 signal-free
   programs at N seeds in both runners, `parity` agreeing unit for unit; the shim under the floor's belts.
2. Virtual signals: the four trap-route programs join.
3. The quiescence oracles + the control mechanism + the two measured controls.
4. The nineteen model controls, walked.
5. The spec's hypotheses, executed (§2.4).
6. Program-level atomics (§2.7).
7. Docs (VERIFICATION_REFERENCE's new section, TCB.md, BUILD_REFERENCE §7.1's row, CLAUDE.md) and the close; E-3's
   instrument half closed by a dated note.

## 5. Decisions taken at planning (each with its reason)
- **X-1 — the explored floor is a TRANSFORM of the real one, never a second floor.** A mock floor that re-implements
  the primitives explores the mock. The transform adds calls and renames one callee; every instruction the artifact
  runs is the instruction explored (at `seq_cst` — X-8).
- **X-2 — the shim is hand-written IR** (S-78). Nitpick cannot express it: no mutable module state (D-211), and code
  that runs inside the allocator's and the scheduler's critical sections may not allocate, trap or call the floor.
- **X-3 — virtualize only what BLOCKS** (futex WAIT/WAKE, `epoll_pwait`, the clock) and the two things the kernel
  does behind the floor's back (CLEARTID at thread exit, signal delivery). Everything else is the real syscall after
  a point: eventfd, pipes and one-shot epoll registrations stay real and are deterministic under the baton. Measured:
  35 programs run unmodified.
- **X-4 — PCT (r6: a per-run guarantee of 1/(n·k^(d−1)) for a depth-d bug), d = 3 by default, with three ORDERED
  priority bands and a fairness bound F = 4,096** (measured: without them two programs hang in real time). A uniform
  random walk stays available by environment for comparison; it is not what the stage runs.
- **X-5 — k comes from a measuring run** (seed 0, no change points) per unit, so the change points fall inside the
  schedule and nobody maintains a number by hand.
- **X-6 — verdicts have NAMES and an exit code is not enough** (S-81): measured — a lost wakeup in this runtime is
  lateness, and virtual time hides lateness completely.
- **X-7 — the replay check is a standing belt, not a debugging aid:** the first seed of every unit runs twice and the
  hashes must agree. A nondeterminism source that creeps into the explored build is found the day it arrives.
- **X-8 — what the baton cannot see is SAID, not implied:** weak-memory behaviour (every atomic is explored as
  `seq_cst`), real child processes, the asm bottom, and everything past the seeds. TCB.md carries the sentence; the
  stage prints PCT's bound per unit from its measured n and k.
- **X-9 — one transformer, and a COUNTING belt as its second reader** in both runners. (1.5.6b's lesson was a model
  with one reader; the answer there was a second implementation because the semantics were rich. Here the property
  that matters — no step escapes — is an equality of two counts.)
- **X-10 — a model's control is MEASURED against the floor before it is a negative control** (`drop-second-sweep` is
  not a floor bug), and what is not one is recorded with its reason: it is a fact about the model.
- **X-11 — a failing seed is kept:** `// explore-seed: S` beside the marker, run first on every run. A schedule that
  found a defect once is the cheapest regression test the project will ever own.
- **X-12 — the explorer COMPLEMENTS `// stress:`** (D-212's words): stress runs the real clock, the real kernel
  scheduler and weak memory on real cores; the explorer runs the interleavings stress never reaches. Neither marker
  implies the other's removal.
- **X-13 (added at step 1, 2026-09-18) — the ADDRESS SPACE is virtual, as time is.** The port of the shim to IR
  found that the C prototype did not replay exactly under machine load: `npk_chunk_new` over-maps 128 KiB and trims
  to 64 KiB alignment, so whether its pre-trim `munmap` happens depends on the alignment of `mmap`'s answer — an
  ADDRESS decided a step count, and with it every later PCT change point and the schedule hash (`channel_owning`
  seed 1: 2,648 steps in one run of eight, 2,649 in the rest, either shim, under a twelve-process load). §1's "exact
  replay" had been measured on one program whose mappings happened to align every time. Both shims now place an
  anonymous private mapping with no hint at a bump pointer (16 TiB, below every randomized mapping and above the
  static image; 64 KiB-aligned so the allocator's trims are the same on every run and every machine) with
  `MAP_FIXED_NOREPLACE`; a collision moves the pointer 1 GiB and retries, deterministically; a hinted, fixed or
  file-backed mapping passes through. The floor has exactly two anonymous `mmap` sites (`npk_hmap`, the `wildx` map),
  both with a null hint. The lesson for X-7: the replay belt (one seed twice, back to back) would not have caught
  this reliably — the two runs would have seen the same alignment more often than not — and a sweep under LOAD did;
  D-303's hash-for-hash measurement is what surfaced it, on the second day of the port.
- **X-14 (added at step 4, 2026-09-18) — the kernel never sleeps past an EXPIRED deadline, and neither does the
  shim.** A futex wait whose absolute timeout is already due (`FUTEX_WAIT_BITSET` with a timepoint at or before
  now; a relative timeout of zero) queues, arms an hrtimer that has already expired, and returns `ETIMEDOUT` at once
  — it waits for nothing else. Both shims blocked such a wait VIRTUALLY until quiescence, where the LOST-WAKE oracle
  read it as a sleeper: `park-unpark`'s `drop-second-sweep` measured 3 LOST-WAKEs in 100 seeds on
  `channel_threads` that were the SHIM's, not the floor's — `npk_sl_earliest` reads a due stamp (`wake_at` = 1) as
  a deadline of 1 ns, so an executor whose second sweep is dropped sleeps for no time at all on the kernel. Both
  shims now return `-110` before parking when the deadline is at or before virtual now; the C reference is amended
  (its third block marked), the D-303 sweep re-run (34 of 34 agree), and ONE clean program's schedules moved
  (`windup_drop`, 2 of 20 seeds: a wait with a past deadline the clean floor does issue).
- **X-15 (added at step 4) — DIRECTED controls: a change point at a PLACE.** Three model controls plant
  interleavings whose windows are one scheduling point wide inside runs of 100,000 steps (`old-arbitration`,
  `old-loser-exits`: the trap route's spins are 100,000 clock reads under virtual time; `link-without-cas`: two
  growers at ONE capacity boundary with a push inside the other's, depth 3). Blind PCT's bound there is
  1/(n·k^(d−1)) ≈ 10⁻⁸ per seed, and 0 of 100 seeds found any of them. A control may now say `preempt-at:
  @function <instruction text prefix>` (up to four): both runners resolve each against the patched floor's
  `sites.txt` (exactly one `atomic` row, else `explore-control-site-unmatched`/`-site-kind` by name) and the shim
  (`NPKX_PREEMPT1..4`) demotes the arriving thread below every other at EVERY arrival at that site — a change point
  that fires at a place instead of a step count, in the same band as PCT's own demotions so PCT's change points
  still act after it. A directed site takes effect BEFORE the named instruction (the point precedes the step), so a
  control names the first step AFTER its window. The two kinds are stated in the README: a BLIND control measures
  the instrument's POWER (PCT finds it); a DIRECTED one measures its SIGHT (told where to preempt, the oracle or the
  program's answer sees it). The three deep controls are found at seed 1 (two) and 4 (one) when directed. The unit
  stage never sets a directed site; the C reference carries the same four words (D-303).
- **X-16 (added at step 4) — the fairness bound is JITTERED.** A thread that has run 4096 consecutive steps while
  another could run is demoted; a FIXED bound resonates with a periodic thread, which then rests at the same phase
  on every slice: `failsafe_alloc`'s churner (lock, unlock, one flag read — period three) rested before its LOCK in
  27 of 30 seeds and before the flag read in 3, never before the unlock — never holding the mutex the
  `heap-mutex-in-failsafe` control needed it stopped inside, so the control was blind (0 of 100) for a reason that
  was the shim's. When a thread reaches the bound, 0..63 further steps are drawn from the seed's stream; liveness is
  4096 + 63 at most, determinism is untouched (the draw is seeded), and the same control finds its deadlock in 36 of
  100 seeds from seed 1. Both shims; the D-303 sweep re-run after it.
- **X-17 (added at step 4) — a control may name LATENESS, and explores with the MEASURED k.** `verdict: late N`
  is met when the shim's virtual run time at exit (`vrun=`, the clock's advance since the start, a new field of
  the trace line) is at or past N nanoseconds — the class D-301 names, which no exit code and no stamp oracle can
  see: `drop-keep-path` overwrites the due mark with the deadline, the task sleeps to its 2 s recv deadline,
  `channel_threads` still exits 100, and the clock says 2,000,532,000 where the clean floor says 606,000.
  `verdict: wrong-exit` (any exit but the program's own, a signal included, or any shim verdict) and `verdict:
  exit N` are the program's own answer; a shim word stays as before. A control's seeds now run at the schedule's
  MEASURED length (a seed-0, depth-1 run first, as a unit's — the verdict line carries `steps=` when the planted
  defect fires there), not the prototype's fixed k = 2000, which wasted every change point past a short program's
  last step; and a control may carry several `old:`/`new:` pairs, applied in order, each exactly once.
- **X-18 (added at step 5) — a SPEC control.** The control mechanism plants defects in the floor's text; a caller
  hypothesis lives in the spec's, and D-302's checkers are generated from it, so `spec-old:`/`spec-new:` pairs
  plant a FALSE HYPOTHESIS the same way (exactly once, the patched spec beside the patched floor in the run's
  build directory, the tree untouched) and the verdict is the shim's `ASSUMPTION`. The committed
  `unconditional-apartness.ctl` is 1.5.6's clause that 1.5.6c found false by reading, found by the checker on the
  27th step of `drop_string`: the mechanism's proof that it sees what D-302 promised.
- **X-19 (added at step 5) — a thread's END is settled before anyone else steps.** The kernel clears a thread's
  `CHILD_CLEARTID` word in `mm_release`, AFTER the thread's last virtual step, racing whoever holds the baton next
  — and `npk_thread_join` reads that word before it waits, so whether the clear had landed decided a step count:
  `shared_arena_spawn` seed 1 took 784 steps or 787, in EITHER shim, when the two binaries were run alternately
  under a full environment (2 of 60 alternations; never in 80 runs of one binary alone, never under `env -i`;
  the sweep after step 5's checkers is what surfaced it, on a program that agreed 20 of 20 at step 4). The X-7
  replay belt runs one seed twice back to back and had not caught it; D-303's alternating sweep did — X-13's
  lesson a second time. The transformer now hands the clone's third argument, the ctid word, to
  `npkx_spawned(i64 %r, i64 %ctid)`; the shim keeps it per slot (a sixteenth field, the slot 96 bytes); an
  ending thread queues (tid, word) before it gives the baton away, and the next holder of the baton — in
  `npkx_run_pending`, wherever a thread comes to hold it — waits, really and with the kernel's SHARED wake,
  until every queued word reads cleared, then steps. A thread's end is one event of the virtual schedule; 60 of
  60 alternations agree afterwards. Both shims; the prototype's `transform.py` hands the word too.
- **X-20 (added at step 6) — a unit's OWN IR is transformed by the same transformer, in its PROGRAM MODE.**
  `atomic<T>` and `atomic_from_ptr` lower inline in the program, and the `sys` builtin calls `@npk_sys6` from it:
  steps the floor's transform never sees. `explore_transform_program` applies the same two rules to a unit's
  emitted IR — a point before every atomic step line of its defines, every `@npk_sys6(` call routed — and nothing
  else: a program defines no thread entry and calls no clone, and it has no spec. Its sites are numbered from
  `PROGRAM_SITE_BASE` = 1,000,000, a range that the floor's sites (numbered from 0) and a routed syscall's
  identity (`n | 2^31`, the shim's) can never reach, so one schedule hash reads both modules' points without
  ambiguity. A unit's function names are read off the RAW line, because the emitter quotes them and the code part
  blanks a string's contents (the first cut named every function `@"`). The counting belt holds the program's
  text too (`explore_totality_of("program", …)`, the harness's `check_totality(…, what="program")` and
  `check_sites(…, base=PROGRAM_SITE_BASE)`); the harness transforms through the tool (`explored --program IN OUT
  SITES`), npkg in-process, as for the floor; the shim's module is never transformed. The census at step 6: no
  explored program's own IR held an atomic step (an `atomic<T>` borrow cannot cross a spawn, D-180, so a
  threaded atomic is `wild` storage reached through `atomic_from_ptr`, the Bridge's shape), and seven held 23
  `sys` calls (read, write, close, pipe2, memfd_create, getpid, lseek, and library wrappers' computed numbers).
  Those calls had run as REAL syscalls with no scheduling point before them; none is one the shim virtualizes, so
  routing them adds a point and changes no answer.
- **X-21 (added at step 6) — a PROGRAM control.** A defect in the PROGRAM's own synchronization is planted the
  way X-18 plants a false hypothesis: `program-old:`/`program-new:` pairs over the SOURCE of the file `program:`
  names (exactly once each), the patched source (same basename, which the module's name must match) written
  into the control's build directory and compiled from there. A block line is taken verbatim and must begin
  with two spaces, so only indented source lines can be planted, and a program with a relative `use` cannot be
  planted, since its imports would not resolve from the build directory (a compile error, loud). The committed
  `atomic-lost-update.ctl` splits `atomic_threads`' `fetch_add` into a `load` and a `store`: found at seed 1.
- THE PROTOTYPE IS THE BEHAVIOURAL SPEC of the shim until the IR exists: `meta/roadmap/1.5/tools/explore_prototype/`
  (`transform.py`, `npkx.c`, `build.sh ROOT PROGRAM OUT [FLOOR.ll]`, `sweep.sh ROOT OUT SEEDS`; D-303 — outside every
  gate, built by nothing that ships). Build a unit by hand:
  `npkc prog.npk > prog.ll`, `llc` with the pinned flags, `ld.lld -static prog.o npkrt.explore.o npkx.o`; run with
  `NPKX_SEED=… NPKX_TRACE=1` under `(ulimit -n 1024; …)` and with stdin from `/dev/null`.
- A checker or an oracle that needs one of the floor's `internal` globals gets its ADDRESS as an argument from the
  call the transformer inserts — the inserted call is inside the floor's module and can name it; the shim cannot.
- The spec's integers are mathematical: evaluate a hypothesis in i128 (`2^64` is a spec literal), zero-extending
  parameters and loads; `mod`/`div` on i128 call `__umodti3`/`__udivti3`, which the floor exports. A load whose
  address is below 4096 reads as 0 (the null page is never mapped: a list walked past its null end — `npk_ch_wait_
  unlink`'s nine waiters — must not fault the checker; the spec's own convention is that a null range is no object).
- The three priority bands must stay ORDERED (initial > change-point > demotion, each demotion below the last) — the
  first fairness patch tied the demoted threads and livelocked; the dump format that showed it (every slot's state,
  priority, last site, pending signal) is worth keeping in the IR shim's verdict output.
- A thread the explorer never schedules again after `tgkill` still runs its handler: the target of a virtual signal
  is made runnable if it was virtually blocked, and the handler runs in ITS context at its next grant.
- `printf` with IR text in a shell script dies on `%a`/`%r` having written nothing; a background waiter that
  `pgrep -f`s a pattern its own command line contains waits for itself, and a `pkill -f` kills itself (use a
  character class: `x_dee[p]`). Both cost this planning session a command.
- Real time under exploration is a smell: a run that takes seconds is a thread spinning without reading the clock
  or blocking (the fairness rule's case) — dump it with a small `NPKX_BUDGET`, do not wait for it.
- (Step 0's lessons.) A helper named `site_line` is refused: it is a builtin's name (D-294) — the snapshot, older than
  the rule, compiled `tools/explored.npk` with it and the compiler under test refused `npkg`. A program cannot hold two
  modules of one basename (the `prelude_names` lesson), so the tool is `explored`. The plan's record must be IN the
  tree the harness runs on (D-228): write the record, then launch.

## Execution record (appended as steps land)

### Step 0 — the transformer, the totality belt, the plan and the decisions (2026-09-18)

**What landed.** `npkg/explore.npk`, the ONE transformer (X-1, X-9): `explore_transform` walks the floor's raw lines
byte for byte and puts `call void @npkx_point(i32 SITE)` before every atomic step line — the census's own predicate,
`line_is_step` minus the trampoline — routes every `@npk_sys6(` CALL to `@npkx_sys6(`, brackets the `@npk_clone_raw(`
call (a statement that spans lines) with `@npkx_prespawn()`/`@npkx_spawned(i64 %r)`, puts `@npkx_begin()` first in
`@npk_thread_entry` and `@npkx_end()` before its `ret`, leaves `module asm`, `@npk_sys6`'s own body and every
declaration alone, appends the six declares, and writes `sites.txt` (`N<TAB>function<TAB>kind<TAB>text`); a missing
anchor is a problem by name, never a silent no-op. `explore_census` (the step lines of a floor, by `line_is_step`
over its defines' bodies) and `explore_totality` (the count equality, every atomic step of the output with its point
IMMEDIATELY before it, no `@npk_sys6(` call left unrouted — `explore-step-escapes`), and `explore_totality_current`
over the tree. `tools/explored.npk` (`--emit DIR` writes `npkrt.explore.ll` and `sites.txt`; bare, the belt) is
snapshot-built as `tools/floorspec.npk` is. THE HARNESS COUNTS AND WRITES NO TRANSFORM: `bootstrap/harness/explore.py`
(`census` by the model belt's `_STEP_LINE`, `check_totality`, `check_sites`) and `harness.check_explore_totality`,
which builds the tool, runs it over the real floor, counts, holds the site map to the text, and assembles the explored
floor under the pinned `llc` — printed as the `explore` line. `npkg verify` runs `explore_totality_current` beside the
floor belts on both of its paths (`verify_run`, `floor_leg`). Self-checks, both runners, ONE planted floor and TWO
literals: `explore-transform` (the transformer's text and sites equal the literals — in Python through the built tool
over a synthetic root, in npkg through the module), `explore-step-escapes` (the literal with one point deleted fails by
name), `explore-step-escapes-control` (the literal passes; Python also holds the sites). The plan (this file, from the
approved draft: the header quoting the user, §3 settled, the S-77 paragraph recorded), D-298…D-303 in DECISIONS.md,
S-78…S-83 SETTLED in OPEN_DECISIONS §2e with his words, the prototype under `meta/roadmap/1.5/tools/explore_prototype/`
with a README and amended headers (D-303), CLAUDE.md's status, the 1.5 README's row and ROADMAP's.

**MEASURED at this tree (`runtime/npkrt.ll` unmoved since 1.5.6b step 0):** 141 step lines = 78 atomic (6 `cmpxchg`,
8 `atomicrmw`, 39 `load atomic`, 25 `store atomic`) + 63 `@npk_sys6(` calls, in 63 of the floor's 175 functions — the
census of §1 exactly; the Nitpick transformer's `sites.txt` equals the prototype's `transform.py` output number for
number, function for function, kind for kind, and the two explored floors differ only by the prototype's two
measurement hooks (`npkx_chk_small_free`, `npkx_chk_rq_push`), their three declares (with `npkx_trap`'s) and the
declares block's comment line; the explored floor assembles under
the pinned `llc -O0`. Tampering by hand: one point deleted, and one call unrouted, each fails both readers by name
(the Python belt over the tool's output; the Nitpick belt in the self-check).

**Departures from the plan, recorded:** the tool is `tools/explored.npk`, not `tools/explore.npk` (two modules cannot
share a basename in one program); the prototype's `npkx_trap` hook is not carried (the plan's §2.1 lists four
lifecycle hooks and the trap route is explored like everything else — §1's virtual signals); a helper first named
`site_line` was renamed at the compiler's refusal (D-294: a builtin's name).

**THE GATE:** the full harness on this tree (its paragraph in the landing commit); `npkg verify --floor-only` with the
belt on both paths, 379 rows matching, no verdict moved. No floor byte, no manifest, no ladder row, no language
surface.

### Step 1 — the shim, the `explore` stage, the markers, the replay check (2026-09-18)

**What landed.** `runtime/explore/npkx.ll`, the shim in hand-written LLVM IR (D-298), ported function for function
from the C reference: 64 slots (the state, tid, priority, futex word, virtual deadline, wake reason, executor, the
value waited on) reached through a byte-offset table because a struct index in a GEP must be constant; `npkx_point`;
`npkx_sys6` (futex WAIT/WAIT_BITSET blocking virtually while the word holds the expected value, EAGAIN otherwise, a
word holding an ENDED thread's tid waited for really because the kernel's CLEARTID clear and its SHARED wake are
invisible to the virtual queues; WAKE/WAKE_BITSET the longest-blocked first; `epoll_pwait` a real poll with timeout
0 under the baton, re-probed only after another thread stepped, the virtual deadline fixed once; `clock_gettime` the
virtual clock; `exit_group` the trace; `exit` the thread's end; everything else the real syscall after a scheduling
point); the lifecycle hooks (`npkx_prespawn`/`npkx_spawned` handing the baton to the child through a registration
futex, `npkx_begin`/`npkx_end`); PCT with the three ORDERED priority bands and the fairness bound (initial random ≫
change-point 2^30 + rank ≫ demotions from 2^20 downward), xorshift64 from the seed, the FNV schedule hash over
(slot, site); the verdicts `DEADLOCK`, `STEP BUDGET` and `MMAP`, each printing the seed, the steps and every slot's
state before `exit_group(97)`; parameters read once from `/proc/self/environ` (the floor's `npk_environ` is EMPTY at
the shim's first call, which `npk_start` makes before the slice is stored); `NPKX_TRACE=1` prints the seed, steps,
hash, clock and thread count at exit, `NPKX_TRACE=2` every step as `me what`. THE STAGE, both runners
(`stage_explore`; `npkg/explore_stage.npk` + `run_explore`): the explored floor and the shim built once per run — the
shim under the floor's belts (the `undef` ban, D-173's hoisted allocas, the stack rule, the zero-dependency scan)
and assembled by the pinned `llc` — then per unit compile, link, the measuring run (seed 0, depth 1 → k), the seeds
(`// explore-seed: S` first, then 1…N) under `NPKX_SEED/K/D/TRACE=1` and nothing else in the environment, each run
held to the program's exit and to no verdict of the shim, and the first seed run again with the same hash demanded
(`explore-replay-differs`); every failure carries its replay line; the per-unit line prints n, k, d and PCT's per-run
bound `1/(n·k^(d−1))` (X-8). THE MARKERS: 44 `// stress:` programs — 30 `// explore: 1000`, 10 `// explore: no real
child processes -- …`, 4 `// explore: no the trap route needs virtual signals -- 1.5.7 step 2 marks it …`; THE MARKER
BELT (`explore-unmarked`, D-299) in the stage of both runners, held by three planted headers in both self-checks
(`explore-unmarked`, `explore-marked-no`, `explore-marked-n`). `nitpick.toml`'s `[[test]]` entry `explore`
(`stage = "explore"`, `path = "tests/backend/programs"`, after `runtime`). The C reference AMENDED once (X-13) with
the amendment marked in `npkx.c` and recorded in the prototype's README; `hashcmp.sh` beside it is D-303's
measurement, run by hand outside every gate.

**MEASURED. D-303, hash for hash:** the IR shim against the amended C reference over every explorable program, 20
seeds each, under a twelve-process CPU load: **30 of 30 signal-free programs agree on every seed** — the same exit,
step count, schedule hash and verdict word. The four trap-route programs differ (a real `tgkill` breaks the baton —
step 2's virtual signals), and `driver_spawn_fail` — no `argv:`, no fixture, so the prototype's sweep had counted it
among its 35 — forks a real child whose exit it reads in real time and agreed on 12 of 20 seeds in EITHER shim: it is
the tenth `explore: no real child processes`, by the plan's own §2.8 rule; §1's 35 = these 30 + the four + it. THE
STAGE over all 44 programs by the harness's own code path: 30 explored at 1,000 seeds each in 93 s on an idle
machine, every replay holding, 14 not explored with their reasons printed; `mutex_basic` 4 threads, k = 1,345;
`channel_owning` 2 threads, k = 2,883; `temp_await` 1 thread, k = 6,442.

**Found on the way.** X-13 (above): the address-space hole, in both shims, by the hash sweep under load — a per-step
diff (`NPKX_TRACE=2`) of a 2,648-step and a 2,649-step run of one seed showed the extra step to be a `munmap` by
thread 0 at step 22, and the floor's one `munmap` site is `npk_chunk_new`'s trim. Two refusals of the compiler under
test: `fails` is a keyword (the second word of `never fails`) — a struct field named `fails` made the struct-literal
lookahead miss and the parse fail two hundred columns later; `XRun` was already a name of `floor_explore.npk`
(RESOLVE-008 names both exporters). The C prototype's `npkx_trap` hook is not carried (the trap route is explored
like everything else once signals are virtual).

**Departures from the plan, recorded:** the four trap-route programs carry a dated `explore: no` until step 2
(the plan's gate for this step names the 31 signal-free programs; there are 30, `driver_spawn_fail` being the
tenth real-child program); `NPKX_TRACE=2` and the `threads=` field on the trace line are additions to the
prototype's interface, made for the diff that found X-13 and for X-8's printed bound.

**THE GATE:** the full harness on this tree with the `explore` stage in both runners and `parity` agreeing unit for
unit; no floor byte, no manifest, no ladder row, no language surface.

### Step 2 — virtual signals: the four trap-route programs join (2026-09-18)

**What landed.** The shim's signals (`runtime/explore/npkx.ll`): `rt_sigaction` remembers the handler it installs
(`@npkx_sig_handler`, one per signal number; the real action is installed too and never fires, since no real signal
is ever sent); `tgkill` marks the target's `pending_sig` and makes a virtually BLOCKED target runnable with the
reason `SIGNAL` (a signal interrupts a blocking call), an unknown tid answering ESRCH; and `npkx_run_pending` runs
a pending handler in the TARGET's own context at its next grant — after every scheduling point, at a child's first
grant, and after a virtual futex or epoll wait returns — so the stop handler's own atomic steps are explored like
any others and it parks forever on a word nothing writes, a virtual wait with no deadline. A futex wait a signal
interrupted returns 0, the reference's behaviour, not EINTR: every futex protocol of the floor re-checks its word.
The four markers flipped: `trap_two_threads`, `trap_stops_runner`, `windup_thread`, `failsafe_alloc` say
`// explore: 1000`.

**MEASURED. D-303, hash for hash:** the four against the C reference, 20 seeds each, with step 1's full harness
running beside — all four agree on every seed (exit, steps, hash): `trap_two_threads` 41, `trap_stops_runner` 43,
`windup_thread` 42, `failsafe_alloc` 45. With step 1's thirty, 34 of 34 explorable programs agree; the tenth
real-child program stays out by §2.8.

**THE GATE:** the full harness with the four in the `explore` stage of both runners and `parity` agreeing; no floor
byte, no manifest, no ladder row, no language surface.

### Step 3 — the quiescence oracles, the control mechanism, the two measured controls (2026-09-18)

**What landed.** THE ORACLES in the shim (D-301): `npkx_lost_wake_oracle`, read at QUIESCENCE — at the top of
`advance` (before virtual time may jump) and before a `DEADLOCK` is declared — **`LOST-FUTEX-WAKE`** (a `B_FUTEX`
slot whose word no longer holds the value it waited on: every futex protocol of the floor is "wait while the word
is v; whoever changes it wakes") and **`LOST-WAKE`** (a blocked slot's executor holds, on its sleeper list, a frame
stamped due: `%npk.exec` field 2 → the head, each `%npk.hdr`'s field 8 `wake_at` == 1, field 7 `qnext` the walk,
bounded at 100,000). The three offsets are NAMED CONSTANTS of the shim (`@npkx_off_sl_head` 16, `@npkx_off_qnext`
48, `@npkx_off_wake_at` 56) and **the belt `explore-oracle-offsets`** in both runners computes the floor's own
layout from its `%npk.exec = type {…}` / `%npk.hdr = type {…}` lines (x86_64: `ptr` 8/8, `iN` at a power-of-two
alignment capped at 8, arrays, nested structs) and holds the constants to it — a shim reading a word the floor
moved fails by name before anything runs. `NPKX_ORACLE=0` turns the oracles off, for comparison only. THE CONTROL
MECHANISM (X-10): `runtime/explore/controls/<name>.ctl` — `program:`, `verdict:`, `within:` and the `old:`/`new:`
line blocks; both runners apply the substitution to `runtime/npkrt.ll` (the `old` lines must occur EXACTLY ONCE,
`explore-control-unmatched` otherwise; an unreadable file is `explore-control-malformed`), transform the patched
floor through the ONE transformer, count it, assemble it, link the program against it and the shim, and run seeds
1…within under the prototype's measuring parameters (k = 2000, d = 3) until the named verdict appears — a control
the explorer never reaches the verdict of is **`explore-control-blind`**, the models' rule for the explorer; the
patched floor never leaves the run's build directory. The two controls the prototype measured: `store-release`
(`npk_mx_unlock` releases and never wakes; `mutex_basic`; `LOST-FUTEX-WAKE`) and `no-rouse` (the absorbed
notification, `npk_ch_wake_one` stamps the task due and never rouses the owner; `thread_root_wake`; `LOST-WAKE`),
each a one-line substitution unique in the floor, with the directory's README (the model controls step 4 walks are
recorded there as `.ctl`s or as not-a-floor-bug with the measurement). Self-checks, both runners: the offset belt
tampered (`qnext` at 40) and clean; a control without its `verdict:`; `old` lines that occur twice (`ret void`); and
END TO END through the real mechanism — a control that plants NOTHING (a unique comment line replaced by itself)
is `explore-control-blind` within 2 seeds, and the committed `store-release` is found within 3. `hashcmp.sh`
compares the verdict WORD letters only (the C reference prints `LOST WAKE (…)`, the shim `LOST-WAKE (…)`).

**MEASURED (the shim in the tree, the floor unmoved).** Planted by hand before the mechanism existed, ten seeds
each: `thread_root_wake` under `no-rouse` — `LOST-WAKE` at seed 1, 10 of 10; `channel_threads` 10 of 10;
**`nested_wait` 10 of 10** — the case §1 named, where the exit code and forty stress runs see nothing; `mutex_basic`
under `store-release` — `LOST-FUTEX-WAKE` at seed 1, 9 of 10. THE CLEAN FLOOR, oracles on: all 34 explorable
programs × 20 seeds = 680 runs, **0 verdicts** — no false positive. Through the harness's own path: both controls
found at seed 1.

**THE GATE:** the full harness with the oracles live in every explored run, the two controls in the `explore`
stage of both runners and `parity` agreeing; no floor byte, no manifest, no ladder row, no language surface.

### Step 4 — the nineteen model controls, walked (2026-09-18)

**What landed.** The control grammar grew, both runners (`bootstrap/harness/explore.py` + `harness.py`,
`npkg/explore.npk` + `explore_stage.npk`): SEVERAL `old:`/`new:` pairs per control, applied in order, each `old`
occurring exactly once in the text as it stands (`explore-control-unmatched` names the pair); the VERDICT may be the
program's own answer — `wrong-exit` (any exit other than its `expect-exit:`, a signal included, or any shim verdict),
`exit N` — or its LATENESS, `late N` (the shim's virtual run time at exit, a new `vrun=` field of the trace line, at
or past N ns: the class D-301 names, the only lens for a mark OVERWRITTEN rather than left unread; X-17); a control's
seeds run at the schedule's MEASURED length (a seed-0, depth-1 run first, as a unit's; the verdict line carries
`steps=` when the planted defect fires there — the harness's first measuring run said "no schedule trace" for exactly
that, fixed in both by reading the steps off either line); and DIRECTED controls (X-15): `preempt-at: @function
<instruction text prefix>` (up to four), resolved by both runners against the patched floor's `sites.txt` to exactly
one `atomic` row (`explore-control-site-unmatched`, `explore-control-site-kind` by name), passed to the shim as
`NPKX_PREEMPT1..4`, where `npkx_count_step` demotes the arriving thread below every other at every arrival — in the
same band as the fairness demotions, so PCT's own change points still act after it. THE SHIM, three amendments,
each mirrored in the C reference and swept (D-303): X-14 (a futex wait whose absolute deadline is at or before
virtual now returns `-110` before parking — the kernel arms an hrtimer that has already expired; the shim had
parked it until quiescence, where the oracle read it as a sleeper: 3 false LOST-WAKEs in 100 seeds under
`drop-second-sweep`; one clean program's schedules moved, `windup_drop`, 2 of 20 seeds), X-15 (the four directed
sites), X-16 (the fairness bound jittered by 0..63 steps drawn from the seed's stream when a thread reaches 4096 —
a fixed bound resonated with `failsafe_alloc`'s period-three churner, which rested before its LOCK in 27 of 30 seeds
and never inside the mutex; `heap-mutex-in-failsafe` went from 0 of 100 to 36 of 100). Self-checks, both runners,
four new cases on planted text: `explore-control-two-pairs` (the second pair's `old` is a line the first wrote),
`explore-control-verdicts` (the four forms judged identically), `explore-control-sites` (one site resolved to its
number; three rows, none, and a `sys6` row refused by name), and the existing `-found` case now runs through the
measuring run. FOUR PROGRAMS written for shapes no explored program had, each explored in its own right from now on:
`io_ready_declined` (a watch the kernel declines resumes at once), `shared_arena_race` (every slot re-read behind a
two-party barrier), `trap_one_failsafe` (a `failsafe` that marks a pipe and finds a second `failsafe`'s mark),
`reactor_arm_race` (an executor arming its reactor while a channel waker on another thread rouses it). TWELVE
CONTROLS in `runtime/explore/controls/` (ten new: `wait-expecting-one`, `drop-keep-path`, `drop-due-stamp`,
`drop-duenow`, `index-read-then-write`, `link-without-cas` (directed), `no-stop`, `old-loser-exits` (directed),
`old-arbitration` (directed), `heap-mutex-in-failsafe`), the README rewritten (the grammar, blind and directed, the
nineteen in a table, the eight records), and dated notes in `park-unpark.model` and `channel-table.model` (a model
row's hash is over its canonical S-expression, so the manifest does not move).

**MEASURED (the final shim, 100 seeds per control unless said; the README carries every number).** Found from
seed 1 or 2: `store-release` 95, `wait-expecting-one` 95, `no-rouse` 100, `drop-due-stamp` 100, `drop-duenow` 100,
`no-stop` 100, `old-loser-exits` 100 (directed; 0 blind), `old-arbitration` 100 (directed; 0 blind),
`heap-mutex-in-failsafe` 36 (0 before X-16); `drop-keep-path` 62 of 1000, first at 5 (`late`: 2,000,491,000 ns
where the clean floor's range is 205,000–606,000); `index-read-then-write` 51 of 500, first at 24;
`link-without-cas` 27 of 100 directed at the reserve and the store, first at 4 (0 of 500 blind, 0 of 500 on a
four-filler variant; a found seed spins to the STEP BUDGET, about 30 s). NOT A FLOOR BUG, five: `drop-second-sweep`
and `clear-after-sweep` (the stamp-as-deadline in `npk_sl_earliest`, a second line the model has no deadline to
represent: 0 in 200 LOST-WAKE and 0 in 1000 `late` on `channel_threads`), `drop-epoll-recheck` (the order argument:
arm → clear → store → read; 0 in 1000 on `reactor_arm_race`, where `no-rouse` is 84 of 200),
`count-before-pointer` and `drop-recheck` (a handle is minted by the open that publishes its slot; a reclaim is the
scope exit, once; 0 in 300 on each of four channel programs). NOT OBSERVABLE, one: `free-before-unwatch` (a write
into freed memory; the order is the prelude's `defer`, the property the model's). NOT EXPLORABLE, two: the
driver-registry pair (real-child programs, §2.8). D-303 after the three amendments: 38 of 38 explorable programs
agree hash for hash with the C reference on 20 seeds (34 + the four new); `driver_spawn_fail` stays the real-child
exception.

**DEF-57, FOUND BY THIS STEP'S OWN HARNESS AND FIXED IN ITS LANDING: the explorer's first floor defect.** The
first full harness of the step (s8, `9c8baf2`) was green on every unit but one: `trap_one_failsafe` exited 96
under the explorer at seed 371, where 41 is expected. The trace (NPKX_TRACE=2) was two instructions wide.
`npk_trap` stores `@npk_frozen` BEFORE it claims the failsafe holder by cmpxchg. The late thread trapped first and
was preempted at its claim. The early thread's executor, starting its first step, read the flag in `npk_step`, and
the `frozen:` block, older than D-291, answered by calling `npk_trap(-4102)`: it entered the arbitration with a
code of its own and WON. `failsafe` ran with `Unreachable`; the program's failsafe mapped that to 96, which it
shared with `IntOverflow`, and a variant with the arms split exited 72. Seeds 370 and 372 exit 41, 40 stress runs
never reached the window, and `trap_two_threads` has the same shape but its 1,000 seeds did not land there. The
`trap-route` model was blind to it: it had no error code, and every bad predicate it had stayed unreachable. THE
FIX (`nitpick-compiler_s10`, the seat's successor, who took the step over at this point) keeps `npk_trap` and
D-291's text as they are (the freeze first, (4)'s order). The `frozen:` block now reads the holder word: a watcher
PARKS (D-291's "any other loser", signalled and counted by the winner's stop walk), and the holder itself takes
the re-entry exit 70 through the route's own arm. No program can drive the executor from `failsafe` (it is never
`async`, TYPE-043), but a holder that parked would end nothing, and the fix first proposed at the hand-off (claim
before publishing, every watcher parks) would have parked it. THE MODEL gained the codes `ca`/`cb` (1 a fault of
the thread's own, 2 the frozen flag's `Unreachable`), the bad predicates `wrong-error` (a failsafe running with
code 2) and `holder-parks`, the step `refrozen` (the holder's own failsafe meeting the flag), and two controls:
`frozen-traps` (the old block) reaches `wrong-error` at depth 6, and `frozen-parks-holder` (the first-proposed
fix) reaches `holder-parks` at depth 6. Both readings agree: 201 reachable states (105 before), all inside K 14 /
D 6, and neither bad predicate is reachable. THE PROGRAM keeps the schedule (`// explore-seed: 371`, X-11) and gives
`Unreachable` its own exit, 72. THE EXPLORER'S CONTROL for it is a record, not a `.ctl` (the README says why): the
window is one point wide between two arrivals at ONE site, the claim, and a directed site demotes each arrival
below every thread already there, so the trapper that arrived first always claims first. Blind, the unit's own
run met it first at seed 371, and a seed-pinned control would go blind at the first unrelated change to the
route's step count. The model's `frozen-traps` is the standing negative control: both readings decide it on every run.
AND TWO OF THIS STEP'S DIRECTED CONTROLS HAD BEEN FOUND THROUGH DEF-57. On the fixed floor, `old-arbitration` and
`old-loser-exits` as first directed were BLIND (exit 41 on 10 of 10 seeds each). Their "second trapper" had been
the other thread before its task started, trapping `Unreachable` on the flag, i.e. DEF-57's window. Both now also
name the first trapper's frozen store, so it yields BEFORE publishing the flag and the other thread's task starts
and traps for real. Measured on the fixed floor, 100 seeds each, from seed 1: exit 77 on every seed (two
`failsafe`s) and exit 70 on every seed (the planted loser arm). The other ten controls were re-run on the fixed
floor through the harness's own code path and found at their recorded seeds. The lesson is the instrument's: a
control proves sight of its OWN defect only when the finding schedule goes through it, and nothing had looked at
the route these two took.

**THE GATE:** the full harness with twelve controls in the `explore` stage of both runners, 38 explored programs,
`parity` agreeing; no manifest of the compiler's and no language surface. The FLOOR moved (DEF-57): its bytes
(the `npkrt.o` ladder row and every binary linked against the floor), `runtime/npkrt.obligations` (the
`trap-route` model's rows, re-recorded in this commit, D-040) and TCB.md's generated regions.

### Step 5 — the spec's caller hypotheses, executed (2026-09-18)

**What landed.** `npkg/explore_req.npk`, THE GENERATOR (D-302; the one generator, X-9): for every section of
`runtime/npkrt.spec` that has rows (the translator's own rule — an `ensures`, `ensures-trap`, `frame` or `loop`
and no `boundary`) and assumes something of its caller (`requires`, `objects`, `views`), a CHECKER in LLVM IR text,
`@"npkx.req.<symbol>"`, taking the symbol's own parameters, evaluating each clause over the ENTRY state in the
spec's mathematical integers — i128, every register read UNSIGNED as the translator reads it; a parameter through
`ptrtoint`/`zext`, an aggregate parameter's leaf `a.0` through `extractvalue`, `exec`/`tls` by their own calls
(`npk_exec()`, `npk_tls_self()`), a global by its ADDRESS; `+ - *` exact, `mod` by a power of two the low bits (the
Euclidean remainder, of a negative value too), `ite` a `select`, `and64`/`or64`/`xor64` over operands wrapped as the
translator wraps them, `s8`/`s32`/`s64` the signed reading, a `loadW mem a` through an address below the null page
redirected to a zero word of the checker's own (the spec's convention that a null range is no object; no branch, no
phi) — and calling `@npkx_assumption` with `<symbol>: <clause>` when a clause is false: the shim's verdict
`ASSUMPTION <symbol>: <clause>`, both shims. The objects facts are `objects_facts`' exactly (each range in the
address space; each object apart from every object before it; a view apart from every object and no view; a null or
empty range no object; `apart-when` guarding its pairs). The transformer (`explore_transform(floor, spec)`) places
the call as the symbol's FIRST INSTRUCTION — after `entry:` where the entry block is labelled; three of the floor's
are not, and one header spans two lines — appends the checkers, and writes `assumptions.txt` beside `sites.txt`: one
row per clause, `checked` or `listed` with the reason (a free symbol, a body register, `mem2`/`result`, a form
outside the entry vocabulary): LISTED BY NAME, never skipped in silence. The tool (`tools/explored.npk`), both
stages, the belt in `npkg verify` and the control runner all pass the spec; a SPEC CONTROL (X-18: `spec-old:`/
`spec-new:` pairs, the same substitution over `runtime/npkrt.spec`, the patched spec beside the patched floor in the
run's build directory) plants a false hypothesis and expects `ASSUMPTION`. THE BELT, both runners
(`explore-assumptions`): the harness's second reader (`explore.assumption_facts`) classifies every clause again by
the generator's rules and holds `assumptions.txt` to it line for line; npkg's twin (`explore_assumptions`) counts
the facts each eligible section yields against the rows; both hold the explored text to one call of each checked
symbol's checker as its first instruction, the checker defined, and no such call anywhere else. The stage prints
`D-302: N caller hypotheses checked at every call of M symbol(s), K listed by name` and each listed clause under it;
TCB.md §4d's generated region (both generators, held to one literal in both self-checks) gained the sentence and the
column `executed at every explored call` (`N of M` per section) and the listed clauses under the table. SELF-CHECKS,
both runners: `explore-assumptions` (the real floor and spec: clean; a row's status flipped and a checker's call
removed each fail by name) and `explore-control-spec` (the committed `unconditional-apartness.ctl` found within two
seeds). Found on the way: the harness's `define_headers` read one line, so the three defines whose parameter lists
continue on a second line (`npk_string_concat` among them) had no header — `check_spec`'s free-symbol check and the
new second reader skipped them in silence; it joins continued lines as `parse_floor` does.

**MEASURED.** 31 sections, 240 hypotheses: 236 CHECKED at every call of every explored schedule, 4 LISTED — the
sorted-table `requires` of `npk_chtab_find`, `npk_lg_find`, `npk_small_free` and `npk_small_check`, each naming the
free symbol `j` (the case D-302 named). Six programs run clean against the checked floor (mutex_basic,
channel_threads, thread_root_wake, io_ready_basic, failsafe_alloc, drop_string): no ASSUMPTION on the clean spec.
The spec control: 1.5.6's unconditional apartness planted back, `drop_string` reports `ASSUMPTION @npk_small_free:
(apart ((load64 mem (+ npk_cls_part (* 8 (load64 mem (+ (- ip (mod ip 65536)) 8))))) 65536) ((- ip (mod ip 65536))
65536))` at step 27, seed 1, both shims word for word. D-303: the sweep re-run with the checkers in the explored floor
both shims link found `shared_arena_spawn` disagreeing on 3 of 20 seeds — a REAL RACE in both shims, not the
checkers (X-19, the settled end, above); after it 60 of 60 alternations agree and the sweep is re-run once more.
A checker is straight-line loads and i128 arithmetic — no atomic step, no syscall — so the schedule hashes of every
program that never raced its joins are what they were.

**THE GATE:** the full harness with the checkers in every explored run, the spec control in the `explore` stage of
both runners, TCB.md's region current in both, `parity` agreeing; no floor byte, no manifest, no ladder row, no
language surface.

### Step 6 — the program's own steps (2026-09-18)

**What landed** (`nitpick-compiler_s10`, on step 5's tree; X-20, X-21). THE PROGRAM MODE of the one transformer,
`explore_transform_program` (`npkg/explore.npk`): a unit's emitted IR through the same two rules as the floor's —
a point before every atomic step line of its defines, every `@npk_sys6(` call routed — and nothing else, its sites
numbered from `PROGRAM_SITE_BASE` = 1,000,000, the two shim declares appended; `explore_totality_of(what, …)` names
the module in the count's finding ("the program holds N step line(s) …"), `explore_totality` unchanged for the
floor. The tool's `explored --program IN.ll OUT.ll SITES.txt` transforms one unit and runs the belt over the pair.
BOTH RUNNERS, every unit and every control's program: emit, transform (`explored_program_object` in each — the
harness through the tool, npkg in-process), the counting belt (and, in the harness, the site map from the base),
`llc`, link the TRANSFORMED object against the explored floor and the shim. THE PROGRAM CONTROL (X-21):
`program-old:`/`program-new:` pairs in both control readers (npkg's reader now keys a pair by kind — floor, spec,
program — where it held one bool), applied to the named program's source, the patched source written into the
control's build directory under its own basename and compiled from there. THE PROGRAM, `atomic_threads.npk`
(`// stress: 40`, `// explore: 1000`): two threads add 1, 200 times each, by `atomic_from_ptr(cell).fetch_add` over
`wild` storage (an `atomic<T>` borrow cannot cross a spawn, D-180), and exit 1 on a total short of 400. THE
CONTROL, `atomic-lost-update.ctl`: the `fetch_add` split into a `load` and a `store`. SELF-CHECKS, both runners,
two cases on planted text: `explore-transform-program` (the program mode's text and sites over a planted unit —
a quoted name, an atomic, a `sys` call, a second function — equal to one literal, and the pair passes the
counting belt; the harness through the tool's `--program`, npkg through the module) and `explore-control-program`
(program pairs read as their own kind, applied to the source and never the floor; a `program-new:` closing a floor
`old:` refused by name). `hashcmp.sh` transforms each program's IR through the tool too (D-303's sweep must show
both shims the same points), and requires the tool in OUT by name.

**MEASURED.** The census over the 39 explored programs' own IR: no atomic step in any (the one `atomic<T>` program,
`atomic_ops`, is single-threaded and not explored), 23 `@npk_sys6(` calls in seven (`io_ready_basic` 3,
`reactor_arm_race` 1, `streams_pipe` 1, `sys_typed` 5, `text_pipe` 1, `trap_one_failsafe` 3, `trap_stops_runner`
9: read, write, close, pipe2, memfd_create, getpid, lseek and library wrappers' computed numbers). Those had run as
REAL syscalls with no scheduling point before them. None is futex, epoll or clock, so routing adds a point and
changes no answer. `atomic_threads`: 3 threads, k 517, the transformed IR three points (the `fetch_add`, main's
`store`, main's `load`). Through the harness's own code path on this tree: all 39 explored units (the new one with
them) at 1,000 seeds each, every replay holding, 357 s; all fourteen controls found at their recorded seeds, the
new one at seed 1. The control's premise, 100 seeds each at the measured k: the planted source linked with its IR
UNTRANSFORMED, 100 of 100 exit 0 (k 115 — each loop runs from one floor step to the next with no point inside
it); TRANSFORMED, 55 of 100 exit 1, the first at seed 1 (k 917). FOUND ON THE WAY: the first cut read a unit's
function names off the code part, which blanks a string's contents, and the emitter quotes nearly every name, so
every row of a program's `sites.txt` said `@"`; the program mode reads the name off the raw line now
(`define_symbol_raw`), and the self-check's literal holds a quoted name for that reason. D-303, re-swept with
each program's own IR transformed for both shims (`hashcmp.sh` through the tool): 39 of 39 explorable programs
agree hash for hash on 20 seeds, `atomic_threads` among them; `driver_spawn_fail` stays the real-child exception.
Both self-checks green, the two new cases in each.

**THE GATE:** the full harness with every unit's own IR transformed, the program control in the `explore` stage
of both runners, the two new self-check cases in both, `parity` agreeing; no floor byte, no manifest, no ladder
row, no language surface.
