# 1.5.2g — The ladder reports its digests; the emission is the cross-machine identity claim (D-265) — PLANNED (2026-09-06; execution-grade; ratified the same day)

> Scope: OPEN_DECISIONS S-42 (the library workbench's first CI finding),
> ratified 2026-09-06 as **D-265**. Two steps, two commits, each under a FULL
> harness on a cumulative prefix (D-228). It decides nothing new about the
> language; it changes what `npkg` reports and what the docs claim.

## 1. Where you are

`main` is the 1.5.2f close (D-264) plus S-42's record (`3d15ac9`). The
snapshot is 1.5.1b's close. `lib/nhash.npk` already holds the project's one
SHA-256 (1.5.0, D-218.8), used by the compiler for obligation identity and by
`npkg` for the z3 binary pin; `tests/backend/programs/sha256_kat.npk` holds
its known answers. The ladder (`npkg/build.npk`) produces `build/npkrt.o`,
`build/builder.o`, `build/builder`, `build/npkc.ll`, `build/npkc.o` and
`build/npkc` (and `build/npkc.opt.ll` when `[build] opt-level` is 2) and
today reports only `ok  built build/npkc`. The harness never runs
`npkg build`; its `parity` stage runs `npkg test --verdicts` (which runs the
same ladder first) and byte-compares `build/npkc` with its own.

## 2. What this subcycle builds

**Step 1 (D-265 §3).** `ladder_digests(Ctx->)` in `npkg/build.npk`: for each
intermediate the ladder produced, in ladder order, one line
`  sha256      <64 hex>  <bytes> B  <path relative to the root>` (the
`stage_text` shape, label `sha256`), the digest by `sha256_hex` over the
file's bytes, the count the file's length; a file that cannot be read back
is reported by name in the same position rather than skipped, since a
missing line is the silent failure. `main.npk` prints the lines after the
`builder` stage line on EVERY ladder run — `build`, `test` and `verify` alike
— because a report that exists in one mode and not another is the kind of
context-dependence the blueprint philosophy refuses. The harness's `parity`
stage parses the `  sha256 ` lines from `npkg test`'s stdout and compares
each to `hashlib.sha256` over the same file under `build/`, failing by name
on a missing line, an unknown path, or a digest or byte count that differs;
the runner self-check is not extended (the lines are not verdicts). Test:
the cross-check IS the test, and it runs on every full harness.

**Step 2.** The docs: BUILD_REFERENCE §5's bullet names D-265 and the ladder's
report, and the `npkg` table's `build` row says what the tool prints; D-265's
landed note; S-42's row reads landed; the 1.5 README row and ROADMAP's 1.5 row
read DONE; CLAUDE.md's status reads as the close; `check_refs.py` at 62; the
workbench told that its notices may quote the ladder's lines verbatim.

## 3. Decisions taken at planning

- **L-1 — Every ladder run reports, no flag.** A `--digests` flag would be a
  mode, and the report costs one read and one hash per intermediate (about
  45 MB, well under a second). The same lines in every run also mean the
  parity stage sees them without running the ladder a second time.
- **L-2 — The cross-check lives in the harness's `parity` stage**, which is
  the one place both an `npkg` run's stdout and an independent hash are at
  hand. When the harness retires (`meta/SWITCH.md`), the check moves into
  `npkg`'s own self-check as a comparison against `sha256_kat.npk`'s
  vectors over the same code path; recorded here so the retirement does not
  drop it.
- **L-3 — The unreadable case is a line, not an omission.** The report's
  consumer is a person comparing two machines; a shorter list on one side
  reads as "a different ladder", which is the wrong diagnosis.
- **L-4 — `npkc.opt.ll` rides only when it exists.** `[build] opt-level` is
  0 for the compiler's own build; under 2 the optimised text is a real
  intermediate and is reported in its position (between `npkc.ll` and
  `npkc.o`).

## 4. Order of work

1. Worktree `g1` from `main`: `ladder_digests` + the `main.npk` print + the
   harness cross-check; `quickemit --keep npkg/main.npk` and a hand run of
   `p_main_npk build` to read the lines and compare with `sha256sum` by hand
   before the full harness. Full harness.
2. Worktree `g2` = g1 + the docs. Full harness (side by side).
3. Land both by cherry-pick with their records appended here, push, tell the
   workbench.

### Step 1 — LANDED (2026-09-06): D-265 §3, the ladder reports its digests

**As planned.** `digest_line` and `ladder_digests` in `npkg/build.npk`: one
`  sha256      <64 hex>  <bytes> B  <root-relative path>` line per intermediate
the ladder produced, in ladder order (`npkrt.o`, `builder.o`, `builder`,
`npkc.ll`, `npkc.opt.ll` under `opt-level = 2`, `npkc.o`, `npkc`), the digest
by `lib/nhash.npk`'s `sha256_hex` over the file's bytes, an unreadable file a
line naming it (L-3); `main.npk` prints them after the `builder` stage line on
every ladder run (L-1). The harness's `check_ladder_digests` (beside the
`build/npkc` byte-compare in `check_parity`) holds each line to `hashlib` over
the same file under the root and fails a missing, malformed, foreign or
differing line by name (L-2). Measured before the harness: the six lines equal
`sha256sum`'s digests and `stat`'s byte counts (the emission `05457db4…`,
21,514,197 B — unchanged from the 1.5.2f close, since `src/` is unchanged);
the check passes on the real output, reports two failures on a renamed line
and six on an output without lines.

**Harness (`72cb414`, prefix g1, 2026-09-06, run side by side with the docs
prefix): every stage green -- `ok 56 test(s) passed`, 223 real-backend
programs at -O0 and under opt -O2, 81 type and 10 derive rejection files;
`verify` 141 obligations, 116 discharged, 25 open, 0 budget, 0 unencoded,
`nitpick.obligations` matches, 116 guards elided, the verified compiler
rebuilds itself byte-identically; `parity` 1099 verdicts agree between the two
runners, `npkc` and the verified compiler byte-identical, and the ladder's six
`sha256` lines from `npkg test` held to an independent digest of each file
(the new cross-check's first run, silent); `selfhost`, `undef-ban`,
`prelude-trim` (101 prelude functions kept), `repro`, `absent-fact`,
`nf-inert` green; `cost` 6 units, 0 designed failures, `self.toml` at a peak
of 115,169,069 under its ceiling of 225,827,260.**
