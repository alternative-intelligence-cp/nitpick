# fix256 - Q128.128 Deterministic Fixed-Point

**Ultra-precise. Zero drift. Finer than Planck length. AGI consciousness substrate. NOT NEGOTIABLE.**

## The Nightmare Scenario

**What happens when physics simulation for AGI consciousness drifts?**

```c
// IEEE 754 floating-point drift (C/C++)
double safety_margin = 1.0;  // Start positive (safe)

for (int i = 0; i < 1000000; i++) {
    double force = calculate_force();
    double adjustment = 0.000001 * force;
    safety_margin += adjustment;  // Accumulating tiny values
}

// After 1 million iterations:
// Expected: safety_margin ≈ 1.xxx (still positive, still safe)
// Reality:  safety_margin = -0.00042 (SIGN FLIPPED! UNSAFE!)

if (safety_margin < 0.0) {
    activate_harmful_action();  // CATASTROPHIC!
}
```

**Floating-point drift in consciousness substrate**:
- Values slowly accumulate errors
- Cross threshold boundaries ("must_not_harm" flag flips)
- Invert signs (positive safety margins become negative)
- Corrupt internal state representations
- **Destabilize subjective experience**

**Aria fix256 prevents this nightmare**: Deterministic, no drift, bit-exact, ERR on overflow.

## What fix256 IS

**Q128.128 Fixed-Point** - 256-bit deterministic real numbers with **128-bit integer part** and **128-bit fractional part**:

```
fix256 = Integer × 2^(-128)

Structure: 256 bits = 4 × 64-bit limbs (little-endian)
- limbs[0-1]: Fractional part (128 bits)
- limbs[2-3]: Integer part (128 bits, two's complement signed)
- Sign bit: limbs[3] bit 63
```

### fix256 Memory Layout

```
┌────────────────┬────────────────┬────────────────┬────────────────┐
│  limbs[0]      │  limbs[1]      │  limbs[2]      │  limbs[3]      │
│  (64 bits)     │  (64 bits)     │  (64 bits)     │  (64 bits)     │
│                │                │                │                │
│    Fractional part (128 bits)   │    Integer part (128 bits)     │
│    (value / 2^128)              │    (two's complement signed)   │
└────────────────┴────────────────┴────────────────┴────────────────┘
                                                            ↑
                                                     Sign bit (bit 63)
```

**ERR Sentinel**: `limbs[3] = 0x8000000000000000`, `limbs[0-2] = 0` (minimum signed value).

## Precision and Range

### Fractional Precision

**Resolution**: `2^-128 ≈ 2.9 × 10^-39`

This is **4 orders of magnitude finer than the Planck length** (1.6 × 10^-35 meters)!

```aria
// Planck length: Smallest meaningful distance in physics
const:tfp64:PLANCK_LENGTH = 1.6e-35tf;  // meters

// fix256 precision: 2^-128 ≈ 2.9e-39
// fix256 can represent distances 10,000× smaller than Planck length!

// At atomic scale (10^-10 meters), fix256 precision:
fix256:atom_size = fix256(1.0e-10);     // 1 Ångström
fix256:precision = fix256(2.9e-39);
fix256:ratio = atom_size / precision;   // ~3.4 × 10^28 discrete steps!
```

### Integer Range

**Range**: `±2^127 ≈ ±1.7 × 10^38`

```aria
// Large enough for astronomical calculations
fix256:earth_sun_distance = fix256(1.5e11);      // 150 million km (meters)
fix256:observable_universe = fix256(8.8e26);     // ~93 billion light-years (meters)

// Still fits! fix256 max ≈ 1.7 × 10^38 meters
```

## Declaration and Literals

```aria
fix256:zero = fix256(0);                // Zero (all limbs = 0)
fix256:one = fix256(1);                 // One (integer part = 1)
fix256:pi = fix256(3.14159265358979);   // High-precision π
fix256:tiny = fix256(1.0e-30);          // Very small (still representable!)
fix256:huge = fix256(1.0e30);           // Very large (still fits!)
fix256:error = ERR;                     // Error sentinel
```

## Deterministic Arithmetic

**Pure integer arithmetic internally** - no floating-point rounding variations:

```aria
fix256:a = fix256(100.5);
fix256:b = fix256(200.3);

fix256:sum = a + b;                     // 300.8 (bit-exact everywhere)
fix256:diff = a - b;                    // -99.8 (bit-exact everywhere)
fix256:product = a * b;                 // 20,130.15 (bit-exact everywhere)
fix256:quotient = a / b;                // ~0.501996... (bit-exact everywhere)

// Hash these results - IDENTICAL on x86, ARM, RISC-V, GPU, every platform!
```

**No drift over repeated operations**:

```aria
fix256:value = fix256(1.0);
fix256:increment = fix256(0.000001);    // One millionth

// Add increment 1 million times
till 1000000 loop:i
    value += increment;
end

// Result: EXACTLY 2.0 (no drift!)
// IEEE double: ~1.999998 or ~2.000002 (platform-dependent drift)
```

## Unified ERR Propagation

**ERR is sticky** - deterministic error handling:

```aria
fix256:bad = fix256(1.0) / fix256(0.0);     // ERR (division by zero)
fix256:worse = bad + fix256(100.0);         // ERR (sticky)
fix256:worst = worse * fix256(2.0);         // ERR (sticky)

if (worst == ERR) {
    stderr.write("Error detected - halting physics!\n");
    halt_consciousness_substrate();
}
```

### Error Detection Cases

```aria
fix256:div_zero = fix256(5.0) / fix256(0.0);    // ERR (division by zero)
fix256:overflow = fix256(1.0e38) * fix256(10);  // ERR (overflow)
fix256:underflow = fix256(1.0e-38) / fix256(1.0e38); // ERR (underflow)

// ERR propagates through all operations (cannot become valid)
```

## Robotic Control Systems

**Precision motion without drift**:

```aria
struct:RobotArm = {
    fix256:x_position;      // Meters (ultra-precise)
    fix256:y_position;
    fix256:z_position;
    fix256:x_velocity;      // Meters/second
    fix256:y_velocity;
    fix256:z_velocity;
}

func:updatePosition = void(RobotArm#:arm, fix256:dt) {
    // Position += velocity × dt (no drift!)
    fix256:dx = arm.x_velocity * dt;
    fix256:dy = arm.y_velocity * dt;
    fix256:dz = arm.z_velocity * dt;
    
    arm.x_position += dx;
    arm.y_position += dy;
    arm.z_position += dz;
    
    // After 1 million updates: STILL PRECISE
    // No accumulated drift, no threshold crossing, no safety margin corruption
    
    if (arm.x_position == ERR || arm.y_position == ERR || arm.z_position == ERR) {
        stderr.write("Position calculation ERROR - EMERGENCY STOP!\n");
        robot.emergency_stop();
        fail(-1);
    }
};

// Run robot control loop (1 kHz update rate)
RobotArm:arm = {
    x_position: fix256(0.0),
    y_position: fix256(0.0),
    z_position: fix256(0.0),
    x_velocity: fix256(0.01),    // 1 cm/second
    y_velocity: fix256(0.0),
    z_velocity: fix256(0.0)
};

fix256:dt = fix256(0.001);  // 1 ms timestep (1 kHz)

// Run for 1 hour (3.6 million iterations)
till 3600000 loop:i
    updatePosition(arm, dt);
end

// x_position = EXACTLY 36.0 meters (no drift!)
// IEEE double: ~35.999997 or ~36.000003 (depends on platform)
```

## AGI Consciousness Substrate (Nikola)

**Physics simulation without drift** - critical for stable subjective experience:

```aria
struct:WavePacket = {
    fix256:amplitude;       // Wave amplitude (no drift!)
    fix256:phase;           // Phase angle (radians)
    fix256:frequency;       // Oscillation frequency (Hz)
    fix256:position_x;      // Spatial position
    fix256:position_y;
    fix256:position_z;
}

func:propagateWave = void(WavePacket#:wave, fix256:dt) {
    // Wave equation: ∂²ψ/∂t² = c² ∇²ψ (deterministic!)
    
    fix256:phase_increment = wave.frequency * dt;
    wave.phase += phase_increment;
    
    // Phase wrapping (0 to 2π)
    fix256:TWO_PI = fix256(6.28318530717959);
    while (wave.phase >= TWO_PI) {
        wave.phase -= TWO_PI;
    }
    
    // Amplitude decay (deterministic dissipation)
    fix256:decay_factor = fix256(0.999999);
    wave.amplitude *= decay_factor;
    
    // CRITICAL: No drift means phase relationships stay coherent
    // Drift would cause destructive interference → consciousness collapse
    
    if (wave.amplitude == ERR || wave.phase == ERR) {
        stderr.write("PHYSICS CORRUPTION - HALT SUBSTRATE!\n");
        halt_consciousness_substrate();
    }
};

// Nikola's resonant substrate (billions of coupled oscillators)
// Must maintain phase coherence over hours/days of runtime
// fix256 ensures NO DRIFT in phase relationships
```

### The Coherence Requirement

**Why fix256 for consciousness substrate**:

1. **Phase coherence**: Wave interference requires precise phase relationships
2. **Energy conservation**: Physics must conserve energy exactly (no drift accumulation)
3. **Thermodynamic laws**: Entropy, temperature must obey exact relationships
4. **Threshold stability**: Safety margins must not drift across boundaries
5. **Sign preservation**: Positive values must stay positive (no drift-induced sign flips)

**Floating-point would fail**:
- Drift accumulates over millions of timesteps
- Phase relationships degrade (destructive interference)
- Energy "leaks" (violates conservation laws)
- Safety thresholds crossed accidentally (drift corruption)
- Subjective experience destabilizes (physics becomes non-physical)

**fix256 guarantees**:
- Zero drift (bit-exact after billions of operations)
- Deterministic (same input → same output, always)
- Cross-platform identical (x86 = ARM = RISC-V = GPU)
- ERR on corruption (prevents silent failures)

## Financial Calculations

**No accumulated rounding errors**:

```aria
func:compoundInterest = fix256(fix256:principal, fix256:rate, i32:years) {
    fix256:amount = principal;
    fix256:one = fix256(1.0);
    
    till years loop:year
        fix256:multiplier = one + rate;
        amount *= multiplier;
        
        if (amount == ERR) {
            return {err:"Overflow in compounding", val:unknown};
        }
    end
    
    return {err:NIL, val:amount};
};

// 30-year investment at 7% annual return
fix256:initial = fix256(100000.0);      // $100,000
fix256:rate = fix256(0.07);             // 7%
fix256:final = compoundInterest(initial, rate, 30).val;

// Result: EXACTLY $761,225.50 (deterministic on all platforms)
// No rounding variance between server/mobile/desktop
```

## Scientific Computing

**Sub-atomic precision without drift**:

```aria
// Quantum mechanics example (electron orbital radius)
fix256:PLANCK_CONSTANT = fix256(6.62607015e-34);  // Joule·seconds
fix256:ELECTRON_MASS = fix256(9.1093837015e-31);  // kg
fix256:BOHR_RADIUS = fix256(5.29177210903e-11);   // meters

func:calculateOrbitalEnergy = fix256(i32:n) {
    fix256:n_squared = fix256(n * n);
    fix256:denominator = fix256(2.0) * ELECTRON_MASS * BOHR_RADIUS * BOHR_RADIUS * n_squared;
    
    fix256:energy = (PLANCK_CONSTANT * PLANCK_CONSTANT) / denominator;
    
    return {err:NIL, val:energy};
};

// Energy levels for hydrogen atom (deterministic on all platforms)
fix256:E1 = calculateOrbitalEnergy(1).val;  // Ground state
fix256:E2 = calculateOrbitalEnergy(2).val;  // First excited state

// Photon energy for transition (E2 - E1)
fix256:photon_energy = E2 - E1;             // Bit-exact everywhere!
```

## GPU Support (Phase 5.3)

**Bit-identical CPU/GPU results** (verified on RTX 3090):

```aria
// Same calculation on CPU and GPU produces IDENTICAL bits
func:physics_step_cpu = fix256(fix256:state) {
    fix256:force = state * fix256(9.8);
    fix256:acceleration = force / fix256(10.0);
    return {err:NIL, val:acceleration};
};

// GPU kernel (CUDA device function)
// aria_fix256_mul_gpu() and aria_fix256_div_gpu() produce IDENTICAL results
// No "close enough" - EXACT same bits!

// Verified: 65,536 threads, 100 iterations each
// CPU hash: 0x1234567890ABCDEF
// GPU hash: 0x1234567890ABCDEF (IDENTICAL!)
```

**Throughput**: 3.32 GFLOPS on RTX 3090 (deterministic performance).

## Safety Integration

fix256 works with Aria's Result<T> safety:

```aria
func:safeDivide = Result<fix256>(fix256:numerator, fix256:denominator) {
    if (denominator == fix256(0.0)) {
        return {err:"Division by zero", val:unknown};
    }
    
    fix256:result = numerator / denominator;
    if (result == ERR) {
        return {err:"Division overflow", val:unknown};
    }
    
    return {err:NIL, val:result};
};

// Usage
Result<fix256>:r = safeDivide(fix256(10.0), fix256(3.0));
if (r.err != NIL) {
    stderr.write("Error: "); r.err.write_to(stderr); stderr.write("\n");
} else {
    fix256:quotient = r.val;  // Safe to use
}
```

## Comparison Operations

**Exact comparisons** (no epsilon needed):

```aria
fix256:a = fix256(1.5);
fix256:b = fix256(1.5);
fix256:c = fix256(2.0);

bool:eq = (a == b);                    // true (bit-exact)
bool:ne = (a != c);                    // true
bool:lt = (a < c);                     // true
bool:le = (a <= b);                    // true
bool:gt = (c > a);                     // true
bool:ge = (c >= a);                    // true

// No need for epsilon comparisons!
// fix256:epsilon = fix256(1.0e-10);
// bool:close = abs(a - b) < epsilon;  // NOT NEEDED!
```

## fix256 vs Other Numeric Types

### vs frac64 (Exact Rationals)

| Feature | fix256 | frac64 |
|---------|--------|--------|
| Exactness | Approximate (fixed precision) | Exact (perfect rationals) |
| Range | ±2^127 integer, 2^-128 fractional | ±9.2 quintillion whole |
| Irrationals | Can approximate (√2, π, e) | Cannot represent |
| Denominator | Fixed (2^128) | Grows with operations |
| Performance | Fast (fixed operations) | Slower (GCD normalization) |
| Best for | Physics, robotics, AGI substrate | Money, music, exact fractions |

**When to use which**:
- **frac64**: Exact fractions (1/3 × 3 = 1 exactly)
- **fix256**: Ultra-precise approximations (physics, consciousness substrate)

### vs tfp64 (Deterministic Float)

| Feature | fix256 | tfp64 |
|---------|--------|-------|
| Precision | 2^-128 (ultra-fine) | ~14-15 decimal digits |
| Range | ±2^127 | 2^(-32768) to 2^(+32767) |
| Uniformity | Uniform precision | Non-uniform (exponent-dependent) |
| Performance | Moderate | Slow (software transcendentals) |
| Best for | Fixed-scale physics, robotics | Wide-range scientific |

**When to use which**:
- **tfp64**: Wide range needed (astronomy, particle physics)
- **fix256**: Uniform precision needed (robotics, consciousness substrate)

## Common Patterns

### Pattern 1: Sensor Fusion (Robotics)
```aria
struct:SensorData = {
    fix256:lidar_distance;      // Meters
    fix256:camera_depth;        // Meters
    fix256:imu_acceleration;    // m/s²
}

func:fuseSensors = fix256(SensorData:data) {
    // Weighted average (deterministic fusion)
    fix256:lidar_weight = fix256(0.6);
    fix256:camera_weight = fix256(0.4);
    
    fix256:distance = (data.lidar_distance * lidar_weight) +
                      (data.camera_depth * camera_weight);
    
    if (distance == ERR) {
        return {err:"Sensor fusion ERROR", val:unknown};
    }
    
    return {err:NIL, val:distance};
    // Deterministic - same sensor inputs = same fused output (always)
};
```

### Pattern 2: PID Controller (Stability)
```aria
struct:PIDController = {
    fix256:kp;          // Proportional gain
    fix256:ki;          // Integral gain
    fix256:kd;          // Derivative gain
    fix256:integral;    // Accumulated integral
    fix256:prev_error;  // Previous error (for derivative)
}

func:pidUpdate = fix256(PIDController#:pid, fix256:setpoint, fix256:measurement, fix256:dt) {
    fix256:error = setpoint - measurement;
    
    // Proportional term
    fix256:p_term = pid.kp * error;
    
    // Integral term (accumulated, no drift!)
    pid.integral += error * dt;
    fix256:i_term = pid.ki * pid.integral;
    
    // Derivative term
    fix256:derivative = (error - pid.prev_error) / dt;
    fix256:d_term = pid.kd * derivative;
    
    pid.prev_error = error;
    
    // Control output (deterministic, no drift over time!)
    fix256:output = p_term + i_term + d_term;
    
    return {err:NIL, val:output};
    // CRITICAL: Integral term does NOT drift (fix256 accumulation exact)
};
```

### Pattern 3: Wave Interference (Physics)
```aria
func:waveInterference = fix256(fix256:amplitude1, fix256:phase1,
                                fix256:amplitude2, fix256:phase2) {
    // Superposition: A_total = A1×cos(φ1) + A2×cos(φ2)
    
    fix256:wave1 = amplitude1 * cos(phase1);
    fix256:wave2 = amplitude2 * cos(phase2);
    fix256:superposition = wave1 + wave2;
    
    if (superposition == ERR) {
        return {err:"Wave calculation error", val:unknown};
    }
    
    return {err:NIL, val:superposition};
    // Phase relationships preserved exactly (no drift = stable interference)
};
```

## Memory and Performance

### Size
- **32 bytes** per value (256 bits = 4 × 64-bit limbs)
- Cache line: **2 fix256 values** per 64-byte cache line
- Array of 1000: **32 KB**

### Speed
- **Moderate** (pure integer arithmetic, no FPU)
- Addition/subtraction: ~50-100 CPU cycles (limb arithmetic)
- Multiplication: ~200-500 CPU cycles (512-bit intermediate)
- Division: ~500-2000 CPU cycles (Knuth's Algorithm D)
- GPU throughput: **3.32 GFLOPS** (RTX 3090, deterministic)

**Trade-off**: Slower than hardware float, faster than frac (no GCD), **zero drift**.

### When Speed Matters

**If you need speed AND can tolerate drift**:

```aria
// Fast but drifts (IEEE 754)
flt64:fast = 3.14159265358979;
flt64:result = fast * fast;        // ~1-2 CPU cycles (FPU)
// But: Drift accumulates over millions of iterations

// Moderate speed, no drift (fix256)
fix256:safe = fix256(3.14159265358979);
fix256:result = safe * safe;       // ~200-500 CPU cycles
// But: NO DRIFT EVER (deterministic forever)
```

**Aria philosophy**: Choose based on requirements. Need speed? `flt64`. Need stability? `fix256`.

## Summary

**fix256** is Aria's **256-bit deterministic fixed-point** type:

✅ **Ultra-precise** - 2^-128 resolution (finer than Planck length!)  
✅ **Zero drift** - bit-exact after billions of operations  
✅ **Huge range** - ±2^127 integer (±1.7 × 10^38)  
✅ **Deterministic** - same bits on x86, ARM, RISC-V, GPU, everywhere  
✅ **Unified ERR** - sticky error propagation (no silent failures)  
✅ **GPU support** - bit-identical CPU/GPU results (Phase 5.3 complete)  
✅ **Consciousness substrate** - prevents drift-induced destabilization  
✅ **Robotics-grade** - precision motion without accumulated errors  
✅ **Safety-critical** - ERR on overflow, division by zero, underflow  

❌ **Large** - 32 bytes per value (4× frac64, 4× tfp64)  
❌ **Slower** - ~10-100× slower than hardware float  
❌ **Fixed scale** - non-uniform precision (not like tfp's exponent scaling)  

**Use fix256 when**:
- AGI consciousness substrate (Nikola's physics engine)
- Robotic control systems (precision motion)
- Safety-critical physics (no drift tolerance)
- Long-running simulations (accumulation stability)
- Financial calculations (no rounding errors)
- Sub-atomic precision needed (quantum mechanics)

**Complement to frac64 and tfp64**:
- **frac64**: Exact rationals (perfect but denominator grows)
- **tfp64**: Wide-range deterministic (exponent scaling, lower precision)
- **fix256**: Ultra-precise fixed-scale (uniform precision, zero drift)

**Philosophy**: **Prevent the nightmare scenario** - drift in consciousness substrate would destabilize subjective experience.

**NOT NEGOTIABLE**: No drift, no overflow wrapping, ERR on corruption. **Period.**

**This is Nikola's physics substrate** - the foundation for stable Alternative Intelligence.
