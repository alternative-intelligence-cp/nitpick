// expect-exit: 42

func:failsafe = int32(tbb32:err) {
    exit 7;
};

func:main = int32() {
    (int32)(int32):identity = int32(int32:x) { pass x; };
    int32:base = 40;
    int32:r = raw identity(base);
    exit r + 2;
};
