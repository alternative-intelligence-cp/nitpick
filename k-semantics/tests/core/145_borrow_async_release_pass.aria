// expect-exit: 54
// v0.25.7 BORROW-014 repeated await on same input value
async func: bump = int32(int32:n) { pass n + 1; };
func:failsafe = int32(tbb32:err) { exit 7; };
func:main = int32() {
    int32:x = 10;
    int32:a = await bump(x);
    int32:b = await bump(x);
    int32:c = await bump(x);
    int32:d = await bump(x);
    exit a + b + c + d + x;
};
