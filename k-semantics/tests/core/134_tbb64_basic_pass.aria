// expect-exit: 15
// tbb64 declaration and subtraction within range
func:failsafe = int32(tbb32:err) { exit 1; };
func:main = int32() {
    tbb64:x = 100;
    tbb64:y = 85;
    tbb64:z = x - y;
    if (z == 15) { exit 15; }
    exit 99;
};
