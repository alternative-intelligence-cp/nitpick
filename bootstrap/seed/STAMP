source-commit: this commit (the bridging refresh at 1.5.8b step 1b, bootstrap/seed/README.md's two-hop variant: the committed snapshot built the tree with `src/` UNSWEPT -- `l[i]`, `l[lo...hi]`, TYPE-082, the prelude's checked `List` operations and its qualified fields already in, `src/` still reaching into `items` -- into the bridge; the bridge built this tree, whose `src/` indexes every `List` as `l[i]` (2,011 sites) and changes one only through the checked operations (136); stage 2 == stage 3; the previous snapshot was b7585e715ea5f24fc701b86ded63bedea7d5b9e232179efdad6b9aa989b8c1c3, 1.5.8 step 4's one-hop refresh)
toolchain: llvm 20.1.2
sha256: 4974aba2f7159d7b443d42d33e32e7b6eaec532e22cc21d4abbe7b79607ab039
bytes: 25707020
