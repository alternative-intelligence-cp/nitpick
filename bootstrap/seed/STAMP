source-commit: this commit (the bridging refresh, 1.5.8 step 0: `CastRange`, `StackExhausted`, `DecreasesViolated` and `MachineFault` enter the prelude, and the compiler's own `failsafe`, the tools' and every root REACH checks name `StackExhausted` and `MachineFault`, which the previous snapshot's embedded prelude could not resolve -- the snapshot and the src/ it builds land together; the refresh also carries `BorrowOverlap`, 1.5.5's, which the previous snapshot predated; the previous snapshot was 8050b1ab98f2b3f22b492fab4d32a735d31f4ea4502bad3a4710fce721e07393)
toolchain: llvm 20.1.2
sha256: b72cf63911825b6a4c7ba9182418c992c70338ddc471e418cb46a4c262428a8d
bytes: 24855019
