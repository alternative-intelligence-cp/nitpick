source-commit: this commit (the one-hop refresh at 1.5.8 step 4, the subcycle's close: the snapshot carries LLVM's split-stack prologue on all 3,202 of its defines (D-305), so the builder and every tool it compiles check their own stacks against the thread's limit word as a program does; it also carries steps 1-3c's emission, the `CastRange` guard (D-306) and the conversions past 64 bits (DEF-61) among it; `src/` is step 3c's, unchanged by this commit; the previous snapshot was b72cf63911825b6a4c7ba9182418c992c70338ddc471e418cb46a4c262428a8d, 1.5.8 step 0's bridging refresh)
toolchain: llvm 20.1.2
sha256: b7585e715ea5f24fc701b86ded63bedea7d5b9e232179efdad6b9aa989b8c1c3
bytes: 25208600
