// Aria Compiler Test Suite - Working Features
// Tests all implemented parser and codegen features

// Test 1: Variable declarations with different memory strategies
int64:x = 42;
wild int64:heap_val = 100;
stack int64:stack_val = 200;
const int64:constant = 999;

// Test 2: Arithmetic operations
int64:a = 10;
int64:b = 5;
int64:sum = a + b;
int64:difference = a - b;
int64:product = a * b;
int64:quotient = a / b;
int64:remainder = a % b;

// Test 3: Comparison operations
bool:equal = (a == b);
bool:not_equal = (a != b);
bool:less = (a < b);
bool:greater = (a > b);
bool:less_equal = (a <= b);
bool:greater_equal = (a >= b);

// Test 4: Logical operations
bool:flag1 = true;
bool:flag2 = false;
bool:and_result = flag1 && flag2;
bool:or_result = flag1 || flag2;
bool:not_result = !flag1;

// Test 5: If/else statements
if (x > 50) {
    int64:positive = 1;
} else {
    int64:negative = 0;
}

// Test 6: While loop
int64:counter = 0;
while (counter < 10) {
    counter = counter + 1;
}

// Test 7: For loop with array
for item in [1, 2, 3, 4, 5] {
    int64:val = item;
}

// Test 8: When expression
int64:result = when {
    x == 0 then 100;
    x < 10 then 200;
    x >= 10 then 300;
    else 400;
end;

// Test 9: Assignment operators
int64:var = 100;
var = var + 10;
var = var - 5;
var = var * 2;
var = var / 3;

// Test 10: Nested expressions
int64:complex = ((a + b) * (a - b)) / 2;

// Test 11: Break and continue
while (true) {
    if (counter > 20) {
        break;
    }
    counter = counter + 1;
    if (counter % 2 == 0) {
        continue;
    }
}

// Test 12: Exotic types
trit:balanced_ternary = 1;
tryte:ternary_byte = 100;
nit:nonary_digit = 4;
nyte:nonary_byte = 255;

// Test 13: Multiple variable types
int8:tiny = 127;
int16:small = 32000;
int32:medium = 2000000;
int64:large = 9000000000;
uint8:byte_val = 255;
uint32:unsigned_val = 4000000000;

// SUCCESS: All working features compiled!
