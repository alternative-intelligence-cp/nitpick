# int4096 - 4096-bit Integer (RSA-16384 Maximum)

**Category**: Types → Large Integers / Cryptography  
**Purpose**: Maximum-security cryptography, RSA-16384 operations  
**Status**: ✅ COMPLETE (All operations implemented and tested)

---

## Overview

**int4096** is a **4096-bit signed integer** type representing the **MAXIMUM** size integer in Aria. It's designed for extreme-paranoia cryptographic scenarios requiring RSA-16384 (the practical upper limit for RSA key sizes).

**⚠️ IMPORTANT**: This is the largest integer type. There is no int8192. If you need larger, you must use arbitrary-precision libraries (non-deterministic).

---

## Key Characteristics

| Property | Value |
|----------|-------|
| **Total Bits** | 4096 (64 × 64-bit limbs) |
| **Range (signed)** | -2^4095 to +2^4095 - 1 |
| **Range (unsigned)** | 0 to 2^4096 - 1 |
| **Storage** | 512 bytes (64 limbs) |
| **ERR Sentinel** | limbs[63]=0x8000000000000000, others=0 |
| **Endianness** | Little-endian limbs |

---

## Representation

### Memory Layout

```aria
int4096 = {
    uint64:limbs[64];  // Little-endian: limbs[0] = LSB, limbs[63] = MSB
};

// Total: 512 bytes (HALF A KILOBYTE per value!)
// Limb 0: bits 0-63
// Limb 1: bits 64-127
// ...
// Limb 63: bits 4032-4095 (sign bit in bit 4095)
```

### LLVM IR Type

```llvm
%struct.int4096 = type { [64 x i64] }
```

---

## Declaration

```aria
// Basic declaration
int4096:rsa_extreme_prime_p;
int4096:rsa_extreme_prime_q;
uint4096:national_security_key;

// Initialization from smaller types
int1024:medium = ...;
int4096:huge = medium;  // Widening conversion

// Future literal syntax
int4096:value = 123...i4096;
```

---

## Arithmetic Operations

### Addition & Subtraction

```aria
int4096:a = 100i4096;
int4096:b = 50i4096;

int4096:sum = a + b;       // 150
int4096:diff = a - b;      // 50

// Overflow detection
int4096:max = MAX_INT4096;
int4096:overflow = max + max;  // ERR sentinel

if (overflow == ERR) {
    stderr.write("Overflow in 4096-bit arithmetic!\n");
    !!! ERR_OVERFLOW_4096;
}
```

### Multiplication

```aria
int4096:a = 100i4096;
int4096:b = 50i4096;

int4096:product = a * b;  // 5000 (low 4096 bits)

// ⚠️ WARNING: Full product is 8192 bits!
// int4096 multiplication truncates to low 4096 bits
// (No int8192 exists - this is the MAXIMUM)
```

### Division & Modulo

```aria
int4096:dividend = 100i4096;
int4096:divisor = 50i4096;

// Signed division/modulo (default)
int4096:quotient = dividend / divisor;   // 2
int4096:remainder = dividend % divisor;  // 0

// Unsigned variants (explicit runtime calls)
int4096:u_quotient = aria_lbim_udiv4096(&dividend, &divisor);
int4096:u_remainder = aria_lbim_umod4096(&dividend, &divisor);

// Division by zero
int4096:zero = 0i4096;
int4096:bad = dividend / zero;  // ERR sentinel

if (bad == ERR) {
    !!! ERR_DIVISION_BY_ZERO;
}
```

---

## ERR Sentinel Pattern

### ERR Value

```aria
// ERR = minimum signed value (1 << 4095)
// Limbs: [0, 0, 0, ..., 0, 0x8000000000000000]
//        ^^^^^^^^^^^^^^^^^^^^-64 limbs-^^^^

int4096:err = ERR;  // Maximum negative value (reserved)

// Same pattern as int1024, int2048
// Cannot be produced by valid arithmetic
```

### ERR Propagation

```aria
int4096:bad = ERR;
int4096:good = 100i4096;

int4096:still_err = bad + good;    // ERR (sticky!)
int4096:also_err = good * bad;     // ERR
int4096:div_err = good / bad;      // ERR

// ERR sticky until explicitly checked and handled
if (result == ERR) {
    stderr.write("Critical cryptographic failure\n");
    !!! ERR_CRYPTO_CRITICAL;
}
```

---

## Cryptographic Operations

### RSA-16384 Scenario (Extreme Security)

**When would you use RSA-16384?**
- National security archives (100+ year confidentiality)
- Nuclear command & control systems
- Long-term medical genomic data
- Diplomatic secrets & treaty documents

```aria
// RSA-16384: Two 8192-bit primes
// (int4096 can hold 4096-bit values, so partial operations)

// Generate 4096-bit prime candidates
int4096:prime_candidate = generate_random_4096();

// Miller-Rabin primality test (simplified)
int4096:witness = 2i4096;
int4096:exponent = prime_candidate - 1i4096;

int4096:test_result = mod_exp_4096(witness, exponent, prime_candidate);

if (test_result == ERR) {
    stderr.write("Primality test computation failed\n");
    !!! ERR_CRYPTO_PRIMALITY_TEST_FAILED;
}

// Fermat's Little Theorem: a^(p-1) ≡ 1 (mod p) if p is prime
if (test_result != 1i4096) {
    // Definitely composite, try next candidate
    prime_candidate = prime_candidate + 2i4096;
}
```

### Modular Exponentiation (4096-bit)

```aria
// Core RSA operation: base^exponent mod modulus
// For 4096-bit values (64-limb computation!)

function mod_exp_4096(
    int4096:base,
    int4096:exponent,
    int4096:modulus
) : int4096 begin
    int4096:result = 1i4096;
    int4096:current_base = base % modulus;
    
    till exponent == 0i4096 loop
        // If exponent is odd
        int4096:is_odd = exponent & 1i4096;
        if (is_odd == 1i4096) {
            result = (result * current_base) % modulus;
            
            if (result == ERR) {
                stderr.write("Modular multiplication overflow\n");
                return ERR;
            }
        }
        
        // Square and halve
        current_base = (current_base * current_base) % modulus;
        exponent = exponent >> 1;
        
        if (current_base == ERR || exponent == ERR) {
            return ERR;
        }
    end
    
    return result;
end

// RSA decryption with 4096-bit values
int4096:ciphertext = ...;
int4096:private_exponent = ...;
int4096:modulus = ...;

int4096:plaintext = mod_exp_4096(ciphertext, private_exponent, modulus);

if (plaintext == ERR) {
    // Decryption failure - corrupted key or ciphertext
    !!! ERR_CRYPTO_DECRYPTION_FAILED;
}
```

---

## Bitwise Operations

```aria
int4096:a = ...;
int4096:b = ...;

// Logical operations (all 4096 bits!)
int4096:and_result = a & b;
int4096:or_result = a | b;
int4096:xor_result = a ^ b;
int4096:not_result = ~a;

// Shifts (64-limb cascading)
int4096:left = a << 100;    // Left shift 100 bits
int4096:right = a >> 100;   // Arithmetic right shift
int4096:logical = a >>> 100; // Logical right shift

// Cryptographic mixing
int4096:mixed = (a << 13) ^ (b >>> 7) ^ ~a;
```

---

## Comparison Operations

```aria
int4096:a = 5i4096;
int4096:b = 5i4096;

// Equality
bool:equal = (a == b);           // true
bool:not_equal = (a != b);       // false

// Ordering
bool:less = (a < b);             // false
bool:greater = (a > b);          // false
bool:less_equal = (a <= b);      // true
bool:greater_equal = (a >= b);   // true

// Three-way comparison
int32:cmp = aria_lbim_scmp4096(&a, &b);  // Returns 0 (equal)
// Returns: -1 if a < b, 0 if a == b, 1 if a > b

// ERR handling
if (a == ERR || b == ERR) {
    stderr.write("Cannot compare ERR values\n");
    !!! ERR_INVALID_COMPARISON;
}
```

---

## Conversion Operations

### From Smaller Types

```aria
// Implicit widening (always safe)
int1024:small = ...;
int4096:large = small;  // Sign/zero extended to 4096 bits

int2048:medium = ...;
int4096:huge = medium;  // Sign/zero extended

// From literals
int32:tiny = 42;
int4096:enormous = tiny;  // 42 with 4064 bits of padding
```

### To Smaller Types (Truncation)

```aria
int4096:huge = ...;

// Explicit truncation
int2048:truncated = int2048(huge);  // ERR if huge > MAX_INT2048

// Manual range check
int4096:max_2048 = MAX_INT2048;
if (huge > max_2048) {
    stderr.write("Value too large for int2048!\n");
    !!! ERR_TRUNCATION_OVERFLOW;
}

int2048:safe_truncated = int2048(huge);  // Now checked
```

---

## Limb-Based Implementation (LBIM Extended)

### Internal Structure

```c
// From include/runtime/lbim_extended.h

#define ARIA_INT4096_LIMBS 64

typedef struct {
    uint64_t limbs[64];  // 64 × 64-bit = 4096 bits = 512 bytes
} aria_int4096_t;

// ⚠️ MEMORY NOTE: Each int4096 value is HALF A KILOBYTE!
// Use sparingly. Consider int2048 or int1024 if possible.
```

### Performance Implications

**Stack allocation**:
```aria
// This allocates 512 bytes on the stack!
int4096:value = 0i4096;

// 10 × int4096 = 5 KB of stack
int4096:array[10];  // (future array syntax)

// For large arrays, heap allocation preferred
```

**Cache behavior**:
- Modern L1 cache line: 64 bytes
- One int4096 value: 512 bytes = **8 cache lines**
- Operations may thrash cache if many values active

---

## Nikola Use Cases

### Why int4096 for Nikola?

**Century-Scale Security**: Some therapeutic data must remain confidential for a patient's entire lifetime plus legal retention periods (potentially 100+ years). RSA-16384 provides security margin against future attacks.

### Ultra-Long-Term Medical Archive

```aria
// Nikola stores neurological baselines for rare genetic conditions
// This data must remain secure for decades (potential research value)

int4096:archive_prime_p = load_archive_prime("century_p.key");
int4096:archive_prime_q = load_archive_prime("century_q.key");

if (archive_prime_p == ERR || archive_prime_q == ERR) {
    stderr.write("CRITICAL: Century-scale encryption keys corrupted\n");
    !!! ERR_CRYPTO_CENTURY_KEY_FAILED;
}

// Modulus for RSA-16384 (two 8192-bit primes)
// (int4096 can represent one prime, full RSA needs higher level)
int4096:archive_modulus_low = archive_prime_p * archive_prime_q;  // Low bits

// Encrypt baseline neurological snapshot
int4096:baseline_hash = hash_neurological_state();
int4096:public_exp = 65537i4096;

int4096:encrypted_baseline = mod_exp_4096(
    baseline_hash,
    public_exp,
    archive_modulus_low
);

if (encrypted_baseline == ERR) {
    // CRITICAL FAILURE: Cannot secure patient data
    // Better to fail explicitly than risk data breach
    stderr.write("CRITICAL: Baseline encryption failed\n");
    !!! ERR_CRYPTO_BASELINE_ENCRYPTION_FAILED;
}
```

### Genomic Privacy for Rare Conditions

```aria
// Some neurodivergent children have rare genetic markers
// Genomic data privacy is EXTREMELY sensitive (discrimination risk)

int4096:genome_encryption_key = derive_genome_key();

// Encrypt genetic markers using RSA-16384 equivalent
int4096:gene_marker_data = encode_markers();
int4096:encrypted_markers = rsa_encrypt_4096(
    gene_marker_data,
    genome_encryption_key
);

if (encrypted_markers == ERR) {
    // Cannot risk storing unencrypted genetic data
    // Failure mode: do NOT store, alert administrators
    stderr.write("CRITICAL: Genomic encryption failed - HALTING STORAGE\n");
    !!! ERR_CRYPTO_GENOMIC_ENCRYPTION_FAILED;
}

// Only store encrypted version
secure_store(encrypted_markers);
```

---

## Safety Considerations

### Memory Overhead (512 Bytes Per Value!)

```aria
// ⚠️ WARNING: int4096 is EXPENSIVE memory-wise

// This is 5 KB of stack:
int4096:a;
int4096:b;
int4096:c;
int4096:d;
int4096:e;
int4096:f;
int4096:g;
int4096:h;
int4096:i;
int4096:j;

// Prefer int2048 or int1024 unless truly necessary
```

### Division by Zero (Fail-Hard)

```aria
int4096:numerator = 1000i4096;
int4096:denominator = 0i4096;

int4096:result = numerator / denominator;  // ERR sentinel

// ALWAYS check critical operations
if (result == ERR) {
    !!! ERR_DIVISION_BY_ZERO;
}
```

### Overflow in Multiplication

```aria
// CRITICAL: int4096 multiplication produces 8192-bit result
// But int8192 does NOT exist! Result is truncated.

int4096:large_a = MAX_INT4096 / 2;
int4096:large_b = 3i4096;

int4096:product = large_a * large_b;  // OVERFLOWS! Returns ERR

if (product == ERR) {
    stderr.write("Multiplication overflow - no int8192 available\n");
    !!! ERR_MULTIPLICATION_OVERFLOW;
}
```

### Timing Attacks (Constant-Time Not Guaranteed)

```aria
// ⚠️ CRITICAL SECURITY ISSUE: Current implementation may leak timing

// BAD (for secret keys):
int4096:secret_key = load_secret();
int4096:user_input = get_user_key();

if (secret_key == user_input) {
    grant_access();
}
// Timing of comparison may leak key bits!

// FUTURE (constant-time comparison):
bool:match = constant_time_eq_4096(secret_key, user_input);
if (match) {
    grant_access();
}
```

---

## Performance Characteristics

| Operation | Complexity | Rough Cycles (x86-64) |
|-----------|------------|----------------------|
| **Addition** | O(64) limbs | ~80-120 cycles |
| **Subtraction** | O(64) limbs | ~80-120 cycles |
| **Multiplication** | O(4096) limb-ops | ~5000-10000 cycles |
| **Division** | O(4096) limb-ops | ~10000-20000 cycles |
| **Comparison** | O(64) limbs | ~80-120 cycles |
| **Shifts** | O(64) limbs | ~80-150 cycles |

**Real-World Context** (3 GHz CPU):
- int4096 addition: ~40 nanoseconds
- int4096 multiplication: ~3 microseconds
- int4096 division: ~6 microseconds

**For Nikola's <1ms timestep**: 
- Can do ~170 int4096 divisions per frame
- Can do ~330 int4096 multiplications per frame
- Can do ~25,000 int4096 additions per frame

**⚠️ MEMORY BANDWIDTH**: 512 bytes per value means memory bandwidth becomes bottleneck faster than computation.

---

## Comparison to Other Approaches

| Approach | Overflow | Performance | Determinism | Memory |
|----------|----------|-------------|-------------|--------|
| **int4096** | ERR sentinel | Moderate | ✅ Bit-exact | 512 bytes/value |
| **GMP mpz_t** | Dynamic growth | Fast (ASM) | ❌ Platform-dependent | Variable |
| **Python int** | Arbitrary | Slow | ❌ Non-deterministic | Variable |
| **OpenSSL BIGNUM** | Dynamic | Fast | ⚠️ OpenSSL version-dependent | Variable |

**Why int4096 for Aria/Nikola**:
- **Fixed Maximum**: Predictable worst-case memory (512 bytes)
- **Determinism**: Bit-exact across platforms (consciousness safety)
- **ERR Propagation**: Overflow explicit, never silent
- **No Dependencies**: Pure C, no GMP/OpenSSL required
- **Portability**: Compiles on embedded systems (future: ARM, RISC-V)

---

## Implementation Status

| Feature | Status | Notes |
|---------|--------|-------|
| **Type Definition** | ✅ Complete | 64-limb structure |
| **Addition** | ✅ Complete | Carry propagation + overflow detection |
| **Subtraction** | ✅ Complete | Borrow propagation |
| **Multiplication** | ✅ Complete | Schoolbook (may optimize with Karatsuba) |
| **Division (signed)** | ✅ Complete | Knuth's Algorithm D |
| **Division (unsigned)** | ✅ Complete | Knuth's Algorithm D |
| **Modulo (signed)** | ✅ Complete | smod4096, srem4096 |
| **Modulo (unsigned)** | ✅ Complete | umod4096, urem4096 |
| **Bitwise Ops** | ✅ Complete | AND, OR, XOR, NOT |
| **Shifts** | ✅ Complete | Left, logical right, arithmetic right |
| **Comparisons** | ✅ Complete | All 6 operators + 3-way cmp |
| **Formatting** | ✅ Complete | Decimal output |
| **Literals** | ❌ Future | Syntax: 123...i4096 |
| **Constant-Time** | ⚠️ Research Needed | Critical for cryptographic keys |

---

## Related Types

- **[int1024](int1024.md)**: 1024-bit (RSA-4096)
- **[int2048](int2048.md)**: 2048-bit (RSA-8192)
- **int512**: 512-bit (needs guide)
- **[fix256](fix256.md)**: Deterministic fixed-point
- **[complex](complex.md)**: Complex numbers

---

## Example: Maximum-Security Key Verification

```aria
// Verify RSA-16384 key parameters
// (Simplified - full RSA-16384 needs bigger representation)

int4096:p = load_prime("rsa16384_p.bin");
int4096:q = load_prime("rsa16384_q.bin");

if (p == ERR || q == ERR) {
    stderr.write("Prime load failed\n");
    !!! ERR_CRYPTO_PRIME_LOAD;
}

// Verify primes are odd
int4096:one = 1i4096;
int4096:p_odd = p & one;
int4096:q_odd = q & one;

if (p_odd != one || q_odd != one) {
    stderr.write("Primes must be odd (except 2, which is too small)\n");
    !!! ERR_CRYPTO_PRIME_EVEN;
}

// Verify e and phi(n) coprime
int4096:e = 65537i4096;  // Standard public exponent
int4096:p_minus_1 = p - one;
int4096:q_minus_1 = q - one;

// Simplified phi check (real version needs full 8192-bit phi)
(int4096:gcd_p, _, _) = extended_gcd_4096(e, p_minus_1);
(int4096:gcd_q, _, _) = extended_gcd_4096(e, q_minus_1);

if (gcd_p != one || gcd_q != one) {
    stderr.write("Public exponent not coprime with p-1 or q-1\n");
    !!! ERR_CRYPTO_EXPONENT_NOT_COPRIME;
}

stderr.write("✅ RSA-16384 key parameters validated\n");
```

---

## What Makes This Different

**In other languages**:
```python
# Python (arbitrary precision, but non-deterministic)
p = generate_prime(4096)  # Random timing, memory growth
q = generate_prime(4096)
n = p * q  # Arbitrary precision, no ERR concept
```

```c++
// C++ with GMP (fast but platform-dependent)
mpz_t p, q, n;
mpz_init(p); mpz_init(q); mpz_init(n);
generate_prime(p, 4096);
generate_prime(q, 4096);
mpz_mul(n, p, q);  // Platform-dependent bit-exact behavior
```

**In Aria**:
```aria
// Aria: Deterministic, ERR-checked, portable, MAXIMUM SIZE
int4096:p = generate_prime_4096();
int4096:q = generate_prime_4096();
int4096:n = p * q;  // May ERR if overflow

if (p == ERR || q == ERR || n == ERR) {
    !!! ERR_CRYPTO_KEYGEN_FAILED;
}

// ✅ Bit-exact across all platforms
// ✅ Fixed memory (512 bytes/value)
// ✅ This is the MAXIMUM - no int8192
// ✅ ERR propagation explicit
```

---

## Future Enhancements

1. **Constant-Time Certification**: Audit + guarantee timing-attack resistance
2. **Literal Syntax**: `int4096:x = 123...i4096;`
3. **Karatsuba Multiplication**: O(n^1.585) speedup
4. **Barrett Reduction**: Faster modular arithmetic
5. **Montgomery Ladder**: Constant-time exponentiation
6. **SIMD Optimization**: Vectorize limb operations

---

## When NOT to Use int4096

**❌ Don't use int4096 if**:
- int2048 or int1024 would suffice (99% of cases)
- You need arbitrary precision (use external library)
- Memory is constrained (<10 KB stack)
- You're doing array operations (512 bytes × array size!)

**✅ DO use int4096 for**:
- RSA-16384 operations (national security)
- Century-scale data encryption
- Genomic privacy (rare genetic conditions)
- Maximum-paranoia cryptographic scenarios

**Rule of thumb**: Start with int1024. Move to int2048 if needed. Only use int4096 when you have a specific, documented threat model requiring RSA-16384.

---

## See Also

- **ARIA-024**: Large Integer Support specification
- **ARIA-025**: Post-quantum cryptography roadmap
- **NIST SP 800-57**: Recommendations for Key Management
- **NSA Suite B**: Cryptographic algorithms (historical reference)

---

**Last Updated**: February 12, 2026  
**Implementation**: src/runtime/math/lbim_extended.cpp, include/runtime/lbim_extended.h  
**Tests**: tests/int4096_comprehensive.aria, tests/int4096_basic.aria, tests/int4096_div_test.aria

---

**⚠️ FINAL NOTE**: This is the **ABSOLUTE MAXIMUM** integer size in Aria. There is no int8192. If you need larger values, you must use arbitrary-precision libraries (which sacrifice determinism).
