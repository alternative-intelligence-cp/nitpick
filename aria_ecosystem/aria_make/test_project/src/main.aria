// main.aria - Main program that uses utils
// Test incremental build

use utils;

func:failsafe = void(int32:err_code) {
    // Error handling
};

func:main = int32() {
    int64:result = utils.add(10, 20);
    return 0;
};
