[package]
name = "aria-console"
version = "0.2.0"
authors = ["Aria Language Project"]
license = "MIT"
description = "Virtual 8-bit console address map and frame scheduler — 16-bit memory-mapped address space, pure Aria"
keywords = ["console", "virtual", "memory-map", "frame-scheduler"]
categories = ["graphics"]

[dependencies]

[build]
type = "library"
entry = "src/aria_console.aria"
