[package]
name            = "aria-http"
version         = "0.2.2"
description     = "HTTP client library for Aria — GET, POST, headers, status codes via libcurl"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/aria"

[dependencies]

[build]
link_libraries = ["aria_http_shim", "curl"]
