[package]
name = "aria-str"
version = "0.1.0"
description = "High-level string manipulation library for Aria"
author = "Aria Ecosystem"
license = "MIT"

[lib]
src = "src/aria_str.aria"

[dependencies]
# Pure Aria — no external dependencies
