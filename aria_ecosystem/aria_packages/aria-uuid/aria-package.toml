[package]
name = "aria-uuid"
version = "0.2.0"
authors = ["Aria Language Project"]
license = "MIT"
description = "128-bit UUID arithmetic utilities — pack/unpack, hi/lo split, comparison, and version extraction"
keywords = ["uuid", "identifier", "uint128"]
categories = ["utilities"]

[dependencies]

[build]
type = "library"
entry = "src/aria_uuid.aria"
