[package]
name            = "aria-log"
version         = "0.2.2"
description     = "Structured logging library for Aria — levels, timestamps, formatted output"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/aria"

[dependencies]

[build]
link_libraries = ["aria_log_shim"]
