[package]
name = "aria-clamp"
version = "0.2.0"
authors = ["Aria Language Project"]
license = "MIT"
description = "Min, max, clamp, abs, and sign for int64 and uint64"
keywords = ["clamp", "min", "max", "abs", "int64", "uint64"]
categories = ["math", "utilities"]

[build]
type = "library"
entry = "src/aria_clamp.aria"
