[package]
name = "aria-display"
version = "0.2.0"
authors = ["Aria Language Project"]
license = "MIT"
description = "Cross-platform terminal display library — ANSI/termios TUI primitives, cursor control, raw mode via C shim"
keywords = ["display", "terminal", "ansi", "tui", "console", "ffi"]
categories = ["graphics"]

[dependencies]

[build]
type = "library"
entry = "src/aria_display.aria"
link_libraries = ["aria_display"]
