[package]
name = "aria-endian"
version = "0.2.0"
authors = ["Aria Language Project"]
license = "MIT"
description = "Byte-swap and rotation utilities — endian conversion, circular bit shifts on uint64"
keywords = ["endian", "byte-swap", "rotation", "bits"]
categories = ["utilities"]

[dependencies]

[build]
type = "library"
entry = "src/aria_endian.aria"
