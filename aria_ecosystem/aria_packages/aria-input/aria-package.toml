[package]
name = "aria-input"
version = "0.2.0"
authors = ["Aria Language Project"]
license = "MIT"
description = "Raw keyboard input for Aria virtual console — key detection with SNES-style button mapping via C shim"
keywords = ["input", "keyboard", "console", "gamepad", "ffi"]
categories = ["utilities"]

[dependencies]

[build]
type = "library"
entry = "src/aria_input.aria"
link_libraries = ["aria_input"]
