[package]
name = "aria-zigzag"
version = "0.2.0"
authors = ["Aria Language Project"]
license = "MIT"
description = "Zigzag encoding/decoding — maps signed integers to unsigned for efficient varint serialization"
keywords = ["zigzag", "encoding", "varint", "serialization"]
categories = ["algorithms", "data"]

[dependencies]

[build]
type = "library"
entry = "src/aria_zigzag.aria"
