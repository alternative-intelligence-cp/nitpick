[package]
name            = "aria-regex"
version         = "0.2.2"
description     = "Regular expression library for Aria — POSIX extended regex"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/aria"

[dependencies]

[build]
link_libraries = ["aria_regex_shim"]
