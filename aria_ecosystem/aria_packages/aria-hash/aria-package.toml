[package]
name = "aria-hash"
version = "0.2.0"
authors = ["Aria Language Project"]
license = "MIT"
description = "Non-cryptographic hash functions — FNV-1a, djb2, bit-mixing for hash tables, checksums, and seeding"
keywords = ["hash", "fnv", "djb2", "checksum", "fingerprint"]
categories = ["algorithms"]

[dependencies]

[build]
type = "library"
entry = "src/aria_hash.aria"
