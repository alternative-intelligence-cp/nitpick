// Test: Fixed primitive - should fail compilation on reassignment

func:main = int32() {
    // Test: Fixed primitive - should fail compilation on reassignment
    fixed int32:x = 10i32;
    x = 20i32;  // ERROR: Cannot reassign fixed variable 'x'
    
    // This line should never execute
    pass x;
};
