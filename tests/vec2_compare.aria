// Test vec2 with comparison
func:main = int32() {
    // Test vec2 construction
    vec2:pos = vec2(1.0, 2.0);
    
    // Test component access
    flt64:x = pos.x;
    
    // Test comparison
    if (x != 1.0) {
        int32:tmp1 = 1;
        pass(tmp1);
    }
    
    // Return 0
    int32:result = 0;
    pass(result);
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
