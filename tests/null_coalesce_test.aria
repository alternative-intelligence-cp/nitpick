// Test ?? (null coalescing) operator with working @ operator
func:main = int32() {
    // Test 1: Non-null pointer should return dereferenced value
    int32:x = 42i32;
    int32->:ptr = @x;
    
    // TODO: Once <- dereference is implemented, this will work:
    // int32:result = ptr ?? 99i32;  // Should be 42 (dereferenced from ptr)
    
    // For now, test that ptr is not null using ?? with a default
    // The ?? operator should check if ptr is null, but we can't dereference yet
    
    print("Null coalescing test - @ operator works!\n");
    pass(0i32);
};


func:failsafe = void(int32:err_code) {
    // Error handling
};
