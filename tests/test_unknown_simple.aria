// Simple test: unknown literal basic functionality
// Phase 5.1: Core unknown literal

func:main = int32() {
    // Test 1: Assign unknown to a variable
    int32:x = unknown;
    
    // Test 2: Compare unknown with unknown (should be equal)
    if (x == unknown) {
        pass(42i32);  // Returns 42
    } else {
        pass(1i32);   // Returns 1 (failure)
    }
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
