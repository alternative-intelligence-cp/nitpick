// Test basic int512 operations
// Part of Phase 3.1: Extended Precision Integers
// NOTE: int512 may have platform-specific limitations

func:main = int32() {
    // Test int512 basic declaration
    int512:a = 100;
    int512:b = 200;
    
    // Very simple arithmetic to avoid platform issues
    int512:sum = a + b;
    
    // Simple comparison
    bool:works = (sum == 300);
    
    if (!works) {
        int32:one = 1;
        pass one;
    }

    int32:zero = 0;
    pass zero;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
