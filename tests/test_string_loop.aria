// Test string reassignment in loop

func:main = int32() {
    string:result = "";
    int64:count = 3;
    
    // Simple loop that builds a string
    while (count > 0) {
        string:digit = "X";
        result = string_concat(digit, result);
        count = count - 1;
    }
    
    // result should now be "XXX"
    pass(0);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
