// Minimal test to see how unknown pattern is parsed

func:main = int32() {
    int32:x = 42i32;
    
    pick(x) {
        (unknown) {
            pass(100i32);
        },
        (*) {
            pass(1i32);
        }
    }
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
