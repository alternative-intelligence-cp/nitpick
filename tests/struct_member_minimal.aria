/// Minimal test for struct member access

struct:Point = {
    int32:x;
    int32:y;
};

func:main = int32() {
    stack Point:p = Point{x: 10, y: 20};
    int32:val = p.x;
    pass(val);
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
