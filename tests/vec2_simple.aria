// Minimal vec2 test to isolate issues
func:main = int32() {
    // Test vec2 construction
    vec2:pos = vec2(1.0, 2.0);
    
    // Test component access
    flt64:x = pos.x;
    
    // Return 0
    int32:result = 0;
    pass(result);
};

func:failsafe = void(int32:err_code) {
    // Error handling
};
