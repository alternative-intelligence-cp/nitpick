// Test fall statement in pick
// Tests explicit fallthrough with labeled cases

func:main = int32() {
    int8:status = 5;
    
    pick(status) {
        (5) {
            print("Five");
        },
        (*) {
            print("Other\n");
        }
    }
    
    pass(0);
};
