// Test fall statement in pick
// Tests explicit fallthrough with labeled cases

func:main = int32() {
    int8:status = 5;
    
    pick(status) {
        (*) {
            print("Matched any value\n");
        }
    }
    
    pass(0);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
