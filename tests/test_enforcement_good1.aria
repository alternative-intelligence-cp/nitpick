// Test Phase 1: Proper .is_error check before .value access
func:get_number = int64() {
    pass(42i64);
};

func:main = int32() {
    Result<int64>:r1 = get_number();
    
    // Check .is_error before accessing .value - should work!
    if (r1.is_error == false) {
        int64:x = r1.value;
    }
    
    pass(0i32);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
