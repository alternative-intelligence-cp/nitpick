// Test: Template literals with complex expressions
// Expected: Compiles and runs

func:main = int32() {
    int64:a = 5;
    int64:b = 3;
    
    // Nested expressions
    string:msg1 = `Result: &{a * b + 2}`;
    
    // Multiple interpolations with text between
    string:msg2 = `The values &{a} and &{b} when added give &{a + b}`;
    
    // Comparison expressions
    string:msg3 = `Is &{a} greater than &{b}? &{a > b}`;
    
    pass(0);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
