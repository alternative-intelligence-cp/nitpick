// Minimal NIL test
func:main = int32() {
    int64?:opt = 100;
    
    if (opt == NIL) {
        return 1;  // Error: should not be NIL
    }
    
    return 0;  // Success
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
