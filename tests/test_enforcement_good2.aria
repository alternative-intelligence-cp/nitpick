// Expected: COMPILER ERROR
// Test Phase 1.5: wild modifier allows .is_error modification
func:get_number = int64() {
    pass(42i64);
};

func:main = int32() {
    wild Result<int64>:r1 = get_number();

    // With wild modifier, can modify .is_error - should work!
    r1.is_error = false;

    free(r1);
    exit(0);
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
