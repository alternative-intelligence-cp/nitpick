// Imports beta, which imports alpha back. A CYCLE, and it is legal (D-086):
// `use` names a namespace and imports no initialisation order, so there is
// nothing to sequence.
use "./beta.npk".*;

pub func:alpha_fn = int32() { pass 2i32; };
pub struct:Alpha = { int32:x; };
