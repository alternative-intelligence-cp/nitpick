// Reached by `mod:nested;` in root.npk, which searches for `nested.npk` OR
// `nested/mod.npk` (MODULE_REFERENCE 1.1). Only this one exists, so there is no
// ambiguity to report.
pub func:nested_fn = int32() never fails { pass 4i32; };
