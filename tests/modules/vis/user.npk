// Imports middle. `middle_own` and the re-exported `relayed` arrive; `shared`
// does not, because middle imported it with a plain `use`.
mod:user;
use "./middle.npk".*;
pub func:user_fn = int32() never fails { pass 5i32; };
