// expect-error: NITPICK-RESOLVE-005
//
// A `use` naming a file that is not there. Relative paths do NOT fall back to
// the dependency roots -- the first character decides the class and there is no
// retry (BUILD_REFERENCE 3).
use "./definitely_absent.npk".*;
