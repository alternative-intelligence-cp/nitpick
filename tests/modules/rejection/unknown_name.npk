// expect-error: NITPICK-RESOLVE-002
//
// A name that exists nowhere. The diagnostic says which scope it was looked up
// in, because "cannot find x" alone leaves the reader to guess whether they
// misspelled it or forgot an import.
func:f = int32() never fails { pass nowhere_at_all; };
