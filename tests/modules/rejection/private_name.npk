// expect-error: NITPICK-RESOLVE-003
//
// `hidden` exists in lib.npk and is private. This says PRIVATE, not "not found":
// different mistakes, and the reader can act on exactly one of them. A note
// points at the declaration so they can go and decide whether it should be pub.
mod:private_name;
use "../vis/lib.npk".hidden;
