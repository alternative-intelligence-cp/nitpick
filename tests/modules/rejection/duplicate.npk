// expect-error: NITPICK-RESOLVE-001
// expect-error-at: 7:1
//
// Two declarations of one name in one scope. An error on the SECOND with a note
// at the first, so the reader lands on the line they just wrote.
func:twice = int32() never fails { pass 1i32; };
func:twice = int32() never fails { pass 2i32; };
