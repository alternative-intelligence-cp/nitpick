// Range-based for loop with type annotations

// Inline int64_toString for explicit conversion
func:int64_toString = string(int64:value) {
    if (value == 0) { pass("0"); }
    bool:is_negative = false;
    int64:work_value = value;
    if (value < 0) { is_negative = true; work_value = -value; }
    string:result = "";
    int64:remaining = work_value;
    while (remaining > 0) {
        int64:digit = remaining % 10;
        remaining = remaining / 10;
        string:digit_char = "";
        if (digit == 0) { digit_char = "0"; }
        if (digit == 1) { digit_char = "1"; }
        if (digit == 2) { digit_char = "2"; }
        if (digit == 3) { digit_char = "3"; }
        if (digit == 4) { digit_char = "4"; }
        if (digit == 5) { digit_char = "5"; }
        if (digit == 6) { digit_char = "6"; }
        if (digit == 7) { digit_char = "7"; }
        if (digit == 8) { digit_char = "8"; }
        if (digit == 9) { digit_char = "9"; }
        result = string_concat(digit_char, result);
    }
    if (is_negative) { result = string_concat("-", result); }
    pass(result);
};

// int32_toString for int32 loop variables
func:int32_toString = string(int32:value) {
    // Cast to int64 and use int64_toString
    int64:value64 = value;
    string:result = int64_toString(value64);
    pass(result);
};

func:main = int32() {
    drop(println("Testing type-annotated range-based for loops:"));
    
    // With int64 type annotation (explicit)
    drop(println("int64:i in 1..3:"));
    for (int64:i in 1..3) {
        string:i_str = int64_toString(i);
        drop(println(i_str));
    }
    
    // With int32 type annotation
    drop(println("int32:j in 10...13:"));
    for (int32:j in 10...13) {
        string:j_str = int32_toString(j);
        drop(println(j_str));
    }
    
    pass(0);
};

func:failsafe = void(int32:err_code) {
    // Error handling
};
