/**
 * Ultra-simple test: Just one template string interpolation
 */

func:failsafe = int32(int32:err_code) {
    pass(err_code);
};

func:main = int32() {
    int32:x = 42;
    
    // Just the interpolation, nothing else
    drop(println(`x = &{x}`));
    
    pass(0);
};
