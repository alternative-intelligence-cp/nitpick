/**
 * Test: String interpolation control (WORKS - workaround)
 * Issue: Template literal first works fine
 */

func:failsafe = int32(tbb32:err) { exit(1); };

func:main = int32() {
    int32:x = 42;
    
    // This order works (template first):
    drop(println(`Test 1: x = &{x}`));          // OK
    drop(println("Test 2: Plain text"));        // OK
    
    exit(0);
};
