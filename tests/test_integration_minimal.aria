func:test_minimal = NIL() {
    // Test variables
    nit:accumulator = 3;
    nit:increment = 2;
    nit:new_value = accumulator + increment;
    
    nit:expected_nit_err = -128;
    if (new_value != expected_nit_err) {
        drop(print("Test running"));
    }
};

func:main = int32() {
    drop(test_minimal());
    pass(0);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
