// Simpler const test - find the segfault

fixed int64:SIZE = 10;

func:main = int32() {
    // Test 1: Just use the const
    int64:x = SIZE;
    
    int32:zero = 0;
    exit zero;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
