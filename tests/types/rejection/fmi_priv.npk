// A FIXTURE for mod_file_private.npk, loaded by its `mod:fmi_priv;`: one
// public member and one private one. Not a test of its own.
mod:fmi_priv;
pub func:open_fn = int32() never fails { pass 1i32; };
func:secret = int32() never fails { pass 2i32; };
