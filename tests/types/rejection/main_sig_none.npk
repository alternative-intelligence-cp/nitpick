// expect-error: NITPICK-TYPE-083
//
// D-089 §4: the signature is FIXED, not optional -- a program that ignores the
// command line writes `cstring[]:_~argv`, never an empty parameter list.
mod:main_sig_none;

func:main = int32() { exit 0i32; };
func:failsafe = int32(Error:_~e) { exit 1i32; };
