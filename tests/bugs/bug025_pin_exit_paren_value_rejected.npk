// BUG-025: Parenthesized exit values preserve pinned by-value rejection.
// Expected: error ARIA-016

func:failsafe = int32(tbb32:err) {
    exit 7;
};

func:main = int32() {
    int32:value = 11;
    int32->:pinned = #value;
    exit(value);
};
