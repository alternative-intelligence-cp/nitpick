// BUG-040: Dynamic-index array element borrow initializers stay rejected for now.
// Expected: compile failure because $$m requires addressable literal-index storage

func:failsafe = int32(tbb32:err) {
    exit 7;
};

func:main = int32() {
    int32[2]:arr = [10, 20];
    int32:i = 0;
    $$m int32:left = arr[i];
    exit arr[0];
};
