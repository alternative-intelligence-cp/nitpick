// BUG-177: early exit() with unfreed wildx leaks must be flagged (BORROW-003, v0.25.1).
// Expected: ARIA-014 ("Memory leak on early exit").

func:failsafe = int32(tbb32:err) { exit 7; };

func:main = int32() {
    wildx int8->:p = alloc(64);
    if (1 == 1) {
        exit 0;
    }
    free(p);
    exit 0;
};
