// bug419: v0.31.2.10 D-23 — pick on Optional<T> without NIL arm or
// wildcard is rejected. Expected diagnostic mentions NIL and the
// `Optional<T> requires NIL handling or wildcard *` hint.

func:failsafe = int32(tbb32:err) { exit 99; };

func:main = int32() {
    int32?:opt = 42i32;
    pick (opt) {
        (42i32) { exit 0; }
    }
    exit 1;
};
