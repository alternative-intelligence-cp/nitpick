// bug386 — v0.31.2.2 D-15: NULL assigned to a pointer target via
// assignment statement is accepted (positive control for bug384).
//
// Expected: exit 0

func:failsafe = int32(tbb32:err) { exit 1; };

func:main = int32() {
    int32->:ptr = NULL;
    ptr = NULL;
    exit 0;
};
