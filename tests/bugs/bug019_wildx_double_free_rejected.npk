// BUG-019: wildx allocations should be covered by double-free checks.
// Expected: error ARIA-022

func:failsafe = int32(tbb32:err) {
    exit 7;
};

func:main = int32() {
    wildx int8->:buffer = alloc(64);
    free(buffer);
    free(buffer);
    exit 1;
};