// bug385 — v0.31.2.2 D-15: NIL assigned to an optional target via
// assignment statement is accepted (positive control for bug383).
//
// Expected: exit 0

func:failsafe = int32(tbb32:err) { exit 1; };

func:main = int32() {
    int32?:opt = NIL;
    opt = NIL;
    exit 0;
};
