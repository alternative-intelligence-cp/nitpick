// v0.31.1.5 bug368 — `obj` keyword retired, now usable as plain identifier
// Expected: exit 42
// Regression for AUDIT_v0.31.1.4 D-10 Option A
func:failsafe = int32(tbb32:err) { exit 1; };

pub func:main = int32() {
    int32:obj = 42i32;
    exit obj;
};
