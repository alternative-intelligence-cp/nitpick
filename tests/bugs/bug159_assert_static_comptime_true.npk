// v0.24.4 (COMPTIME-008): assert_static of a literal-true comptime expression
// must short-circuit through the Z3 verifier as PROVEN.
pub func:main = int32() {
    assert_static comptime(true);
    exit 0;
};
func:failsafe = int32(tbb32:err) { exit 1; };
