// Test lambda expressions
// Lambda syntax: returnType(params) { body }
// Immediately invoked: returnType(params){body}(args)

print("Testing lambdas...");

// Immediately invoked lambda (IIFE style - like JavaScript!)
int8:result = int8(int8:a, int8:b) {
    return a + b;
}(10, 20);

print("Lambda test complete!");
