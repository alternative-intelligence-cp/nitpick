// Test lambda with immediate invocation INSIDE a function
// This should work since we have an insertion point

func:testLambdas = void() {
    print("Testing lambda inside function...");
    
    // Immediately invoked lambda in variable assignment
    int8:result = int8(int8:a, int8:b) {
        return a + b;
    }(10, 20);
    
    print("Lambda result:");
    print(result);
}

func:main = int8() {
    testLambdas();
    return 0;
}
