/**
 * Test: Defer Executes on Normal Return
 * Expected: COMPILES SUCCESSFULLY
 * Why: defer block must execute when function returns normally
 *
 * From TODO: "Test defer executes on normal return"
 */

func:main = int32() {
    wild int8@:p = alloc(1024);
    defer { free(p); }

    // Normal execution path
    // defer should execute before return
    return 0;
};
