/**
 * Test: TBB Valid Value Construction
 * Expected: COMPILES SUCCESSFULLY
 * Why: Valid range values should be constructible
 *
 * From TODO: "tbb8_from_int(127) - OK (max valid value)"
 */

func:main = int32() {
    // These should work - valid range
    tbb8:max_val = tbb8_from_int(127);    // OK: max valid value
    tbb8:min_val = tbb8_from_int(-127);   // OK: min valid value
    tbb8:zero = tbb8_from_int(0);         // OK: zero

    return 0;
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
