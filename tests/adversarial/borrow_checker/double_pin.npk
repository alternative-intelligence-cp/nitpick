/**
 * Test: Cannot Pin Twice
 * Expected: COMPILER ERROR - variable already pinned
 */

func:main = int32() {
    int32:x = 10;

    wild int32->pin1 = #x;  // First pin
    wild int32->pin2 = #x;  // ERROR: Cannot pin already pinned variable

    return 0;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
