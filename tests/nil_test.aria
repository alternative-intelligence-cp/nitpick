// Test NIL keyword recognition
func:test_nil = int64() {
    // Placeholder - will use NIL when parser supports it
    int64:x = 42;
    pass(x);
};

func:main = int32() {
    int64:result = raw(test_nil());
    exit(0);
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
