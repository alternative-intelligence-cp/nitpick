// A FIXTURE for inline_imports.npk, loaded by the `mod:ii_other;` written
// INSIDE its inline module `m` -- relative to the FILE, the only path an
// inline module has (D-276, 1.5.4d). Not a test of its own.
mod:ii_other;
pub func:h = int32() never fails { pass 11i32; };
