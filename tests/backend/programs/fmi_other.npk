// A FIXTURE for mod_file_import.npk, loaded by mod_file_import_lib.npk's
// `pub mod:fmi_other;` and reached from the main file through that file's
// wildcard import (D-274 with D-273 §4). Not a test of its own.
mod:fmi_other;
pub func:h = int32() never fails { pass 11i32; };
