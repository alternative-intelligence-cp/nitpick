// The imported half of `global_export.npk` -- a fixture, not a program (no
// `main`): two module bindings, one exported and one not.
mod:global_export_lib;

pub fixed int32:GX = 3i32;
pub fixed string:GNAME = "three";
fixed int32:HIDDEN = 100i32;

pub func:hidden_value = int32() never fails { pass HIDDEN; };
