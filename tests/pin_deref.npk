/// Regression test: local pin references can be dereferenced for stable reads.
/// Expected process exit: 42

func:main = int32() {
    int32:value = 42;
    int32->:pin = #value;
    int32:got = <-pin;
    exit got;
};

func:failsafe = int32(tbb32:err) {
    exit 99;
};
