// Expected: COMPILER ERROR
// Simple test of Result parameter passing

func:get42 = int32() {
    pass(42i32);
};

func:extract = int32(Result<int32>:r) {
    // Properly check error before accessing value
    if (r.is_error) {
        pass(0i32);
    }
    pass(r.value);
};

func:main = int32() {
    Result<int32>:result = get42();
    int32:value = extract(result) ? 0i32;
    pass(value);  // Should return 42
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
