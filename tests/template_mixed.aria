// Test: Template literals with mixed types
// Expected: Compiles and runs

func:main = int32() {
    string:name = "Alice";
    int64:age = 30;
    flt64:height = 5.7;
    bool:active = true;
    
    string:profile = `User: &{name}, Age: &{age}, Height: &{height}ft, Active: &{active}`;
    
    exit(0);
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
