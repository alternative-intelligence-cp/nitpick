// test_rules_minimal.aria — v0.2.41 Minimal Rules test
extern func:puts = int32(string:s);

Rules:r_lt_100 = {
    $ < 100
};

func:failsafe = int32(tbb32:err) {
    drop puts("FAILSAFE");
    exit 1;
};

func:main = int32() {
    drop puts("Rules declaration parsed OK");
    exit 0;
};
