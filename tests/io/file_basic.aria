// Test basic file I/O functionality
// Phase 4.2: File I/O Builtins

func:main = int64() {
    // Test 1: Write a file
    string:content = "Hello from Aria file I/O!";
    int64:write_result = writeFile("/tmp/aria_test.txt", content); //this should fail, assigning raw result to int64, no unwrap,  result object == {err:errCode OR NIL, val:returnVal OR NIL}
    
    if (write_result != 0) {
        print("Failed to write file");
        pass(1);
    }
    print("File written successfully");
    
    // Test 2: Read the file back
    string:read_content = readFile("/tmp/aria_test.txt"); //this should fail, assigning raw result to string, no unwrap,  result object == {err:errCode OR NIL, val:returnVal OR NIL}
    print(read_content);
    
    // Test 3: Check if file exists
    bool:exists = fileExists("/tmp/aria_test.txt");//this should fail, assigning raw result to bool, no unwrap,  result object == {err:errCode OR NIL, val:returnVal OR NIL}
    if (!exists) {
        print("File exists check: FAIL");
        pass(1);
    }
    print("File exists check: PASS");
    
    // Test 4: Get file size
    int64:size = fileSize("/tmp/aria_test.txt"); //this should fail, assigning raw result to int64, no unwrap,  result object == {err:errCode OR NIL, val:returnVal OR NIL}
    if (size < 0) {
        print("Failed to get file size");
        pass(1);
    }
    print("File size retrieved successfully");
    
    // Test 5: Delete the file
    int64:delete_result = deleteFile("/tmp/aria_test.txt");//this should fail, assigning raw result to int64, no unwrap,  result object == {err:errCode OR NIL, val:returnVal OR NIL}
    if (delete_result != 0) {
        print("Failed to delete file");
        pass(1);
    }
    print("File deleted successfully");
    
    // Test 6: Verify file no longer exists
    bool:still_exists = fileExists("/tmp/aria_test.txt");//this should fail, assigning raw result to bool, no unwrap, result object == {err:errCode OR NIL, val:returnVal OR NIL}
    if (still_exists) {
        print("ERROR: File still exists after deletion");
        pass(1);
    }
    print("File deletion verified");
    
    print("All file I/O tests passed!");
    pass(0);
};

func:failsafe = void(int32:err_code) {
    // Error handling
};
