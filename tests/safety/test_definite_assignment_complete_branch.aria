// Test 4: Assignment in both branches (should compile)
// Variable assigned in all paths should be OK

func:main = int8() {
    int64:x;
    bool:condition = true;
    
    if (condition) {
        x = 42;
    } else {
        x = 100;
    }
    
    print(x);  // OK: x is definitely assigned (both branches assign it)
    int8:ret = 0;
    pass(ret);
};
