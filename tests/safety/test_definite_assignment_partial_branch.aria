// Expected: COMPILER ERROR
// Test 3: Assignment in only one branch (should ERROR)
// Variable assigned in then-branch but not else-branch

func:main = int32() {
    int64:x;
    bool:condition = true;
    
    if (condition) {
        x = 42;
    }
    // else branch doesn't assign x
    
    print(x);  // ERROR: x is possibly uninitialized (else path didn't assign)
    int8:ret = 0;
    pass ret;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
