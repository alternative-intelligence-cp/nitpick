// Test 5: Assignment in loop (should ERROR)
// Loop might not execute, so variable is only MAYBE_ASSIGNED

func:main = int8() {
    int64:x;
    int64:i = 0;
    
    while (i < 10) {
        x = i;
        i = i + 1;
    }
    
    print(x);  // ERROR: x is possibly uninitialized (loop might not execute)
    int8:ret = 0;
    pass(ret);
};
