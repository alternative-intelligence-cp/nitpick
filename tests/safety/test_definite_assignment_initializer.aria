// Test 6: Declaration with initializer (should compile)
// Variable initialized at declaration is always assigned

func:main = int32() {
    int64:x = 42;
    // OK: x is initialized at declaration — use in comparison
    if (x == 42i64) {
        pass(0i32);
    } else {
        pass(1i32);
    }
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
