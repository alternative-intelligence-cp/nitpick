func:main = int32() {
    trit:zero = 0;   // Should work - literal
    trit:pos = 1;    // Should work - literal  
    trit:neg = -1;   // Fails - unary expression!
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
