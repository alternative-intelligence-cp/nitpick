/**
 * Test: %assign arithmetic expressions
 * Expected: COMPILES SUCCESSFULLY
 * Verifies: %assign evaluates numeric expressions and stores result
 */

%assign BASE 10
%assign OFFSET 5
%assign TOTAL BASE+OFFSET

func:main = int32() {
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    exit 1;
};
