/**
 * Test: %push / %pop context stack
 * Expected: COMPILES SUCCESSFULLY
 * Verifies: %push creates a context; %pop removes it without crashing
 */

%push section_a

%pop section_a

func:main = int32() {
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    exit 1;
};
