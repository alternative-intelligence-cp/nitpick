// GPU/PTX Backend - Phase 3 Test: NVVM Kernel Metadata
// Minimal test with single GPU kernel

// Simple GPU kernel
func:gpu_simple = NIL(int32:n) {
    int32:x = n * 2;
};

// Required failsafe
func:failsafe = NIL(int32:err_code) {
    // Empty for testing
};
