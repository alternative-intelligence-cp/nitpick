// Test balanced ternary types - minimal (Session 15)
// Expected: Basic trit operations work

func:main = int32() {
    // Trit: Single balanced ternary digit (-1, 0, 1)
    trit:zero = 0;
    trit:positive = 1;
    trit:negative = -1;
    
    // Test that values match expected
    if (zero == 0) {
        pass(42);  // Success
    }
    
    pass(1);  // Failure
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
