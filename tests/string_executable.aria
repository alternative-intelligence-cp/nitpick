// Simple string test that can be executed
// Tests basic string operations with visible output

func:main = int64() {
    // Test string_length - should return 5
    int64:len = string_length("Hello");
    
    // Test with different lengths
    int64:len2 = string_length("World!");
    int64:len3 = string_length("");
    
    // Return the first length as exit code
    pass(len);
};
