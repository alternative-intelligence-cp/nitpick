// Test that pass() and fail() work with plain return types

func:get_success = int32() {
    pass(42i32);
};

func:main = int32() {
    int32:value = raw(get_success());
    pass(value);  // Should return 42
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
