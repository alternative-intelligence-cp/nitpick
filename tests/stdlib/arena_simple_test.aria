/**
 * Arena Allocator Simple Test - Phase 4.2
 * Very simple test to verify arena basics work
 */

func:main = int32() {
    // Just verify we can compile and run
    // We'll add actual allocator tests incrementally
    
    int64:result = 0;
    exit(0);  // was: pass(result)
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
