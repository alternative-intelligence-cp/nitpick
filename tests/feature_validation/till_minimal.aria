// Minimal till test without using $
func:main = int32() {
    int64:sum = 0;
    
    // Just iterate 5 times
    till(5, 1) {
        sum = sum + 1;
    }
    
    // sum should be 5
    exit 0;  // was: pass(sum)
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
