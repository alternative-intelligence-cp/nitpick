// Minimal till test without using $
func:main = int64() {
    int64:sum = 0;
    
    // Just iterate 5 times
    till(5, 1) {
        sum = sum + 1;
    }
    
    // sum should be 5
    pass(sum);
};
