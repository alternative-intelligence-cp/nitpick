// Simple assignment test

func:test_assignment = int32() {
    int32:x = 10;
    x = 20;
    x = 30;
    pass(x);
};

func:main = int32() {
    int32:r = test_assignment() ? -1i32;
    pass(r);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
