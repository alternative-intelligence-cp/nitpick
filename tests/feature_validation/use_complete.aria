// Comprehensive use test
// Tests: various import forms

// Simple module import
use std.io;

// Selective imports
use std.collections.{array, map, set};

// Wildcard import
use math.*;

// File path imports
use "./utils.aria" as utils;
use "../shared/crypto.aria" as crypto;

// Nested module imports
use network.http.{get, post};
use network.tcp;

func:main = int32() {
    pass(42);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
