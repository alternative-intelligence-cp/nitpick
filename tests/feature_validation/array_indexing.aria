// Array indexing test
func:main = int32() {
    // Create array
    int64[3]:arr = [10i64, 20i64, 30i64];
    
    // Read first element
    int64:first = arr[0];
    
    // Read second element  
    int64:second = arr[1];
    
    // Read third element
    int64:third = arr[2];
    
    // Simple check - if first element is 10, return 42
    int64:ten = 10;
    if (first == ten) {
        int32:success = 42;
        pass(success);
    }
    
    // Otherwise return 1
    int32:fail_code = 1;
    pass(fail_code);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
