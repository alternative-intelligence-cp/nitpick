// Expected: COMPILER ERROR
// Test: Atomic<T> type constraints - wrong number of type arguments
// Expected: "atomic<T> requires exactly 1 type argument"

func:test_zero_args = int32() {
    stack atomic<>:bad;  // No type argument
    return 0;
};
