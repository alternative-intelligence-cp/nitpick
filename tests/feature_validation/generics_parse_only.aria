// Minimal test: Just parse generic functions (no execution needed yet)

// Test 1: Basic generic identity function
func<T>:identity = *T(*T:value) {
    pass(value);
};

// Test 2: Generic max function
func<T>:max = *T(*T:a, *T:b) {
    if (a > b) {
        pass(a);
    }
    pass(b);
};

// Main - just exit 0 for now
func:main = int32() {
    exit(0);
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
