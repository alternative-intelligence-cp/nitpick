// Minimal array test with explicit types
func:main = int32() {
    // Array with int64 elements (default for literals)
    int64[]:arr = [1, 2, 3];
    int32:result = 42;
    pass(result);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
