// Assignment with arithmetic

func:counter = int32() {
    int32:count = 0;
    count = count + 1;
    count = count + 2;
    count = count + 3;
    pass(count);  // Should return 6
};

func:main = int64() {
    pass(counter());
};

func:failsafe = void(int32:err_code) {
    // Error handling
};
