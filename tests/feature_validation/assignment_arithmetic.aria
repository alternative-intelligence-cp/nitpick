// Assignment with arithmetic

func:counter = int32() {
    int32:count = 0;
    count = count + 1;
    count = count + 2;
    count = count + 3;
    pass(count);  // Should return 6
};

func:main = int32() {
    int32:r = counter() ? -1i32;
    pass(r);
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
