// Test module type checking

// Valid imports
use std.io;
use math.{sin, cos};

// Valid module with valid identifier name
mod valid_module {
    pub func:test = int64() {
        pass(42);
    };
}

func:main = int64() {
    pass(0);
};
