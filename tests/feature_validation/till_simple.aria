// Simple till test that uses $ variable
// ✅ $ variable works — parser, type checker, and codegen all handle it

func:main = int32() {
    int64:sum = 0;
    
    // Count from 0 to 4 (5 iterations)
    till(5, 1) {
        sum = sum + $;
    }
    
    // sum should be 0 + 1 + 2 + 3 + 4 = 10
    exit(0);
};

func:failsafe = int32(tbb32:err) {
    exit(1);
};
