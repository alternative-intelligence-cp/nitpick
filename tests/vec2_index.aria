// Test vec2 with index access
func:main = int32() {
    // Test vec2 construction
    vec2:pos = vec2(1.0, 2.0);
    
    // Test component access
    flt64:x = pos.x;
    
    // Test index access
    flt64:first = pos[0];
    
    // Return 0
    int32:result = 0;
    pass(result);
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
