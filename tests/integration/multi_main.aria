// Main file that would use utils (in a real implementation)
// For now, just has its own main function

func:main = int32() {
    // In a full implementation, we would:
    // use("./utils.aria");
    // int8:result = add(5, 3);
    // print(result);
    
    // For now, just return success
    print("Multi-file test");
    pass(0i32);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
