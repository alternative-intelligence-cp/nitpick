// Test: Type system integration for new numeric types
// Purpose: Verify type registration and basic variable declarations

func:test_all_new_types = tbb32() {
    // Frac types - exact rational arithmetic
    frac8:f8 = 0;
    frac16:f16 = 0;
    frac32:f32 = 0;
    frac64:f64 = 0;
    
    // TFP types - twisted floating point
    tfp32:t32 = 0;
    tfp64:t64 = 0;
    
    // Vec9 type - 9D toroidal vector
    vec9:v = 0;
    
    // TMatrix type - sentinel-aware matrix
    tmatrix:m = 0;
    
    // TTensor type - 9D toroidal tensor
    ttensor:t = 0;
    
    pass(0);
};

func:main = int32() {
    pass(0);
};
