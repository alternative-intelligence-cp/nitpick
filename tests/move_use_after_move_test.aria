// Test 2: Use-after-move detection (should FAIL)
func:test_use_after_move = int32() {
    int32:x = 42;
    int32:y = move(x);  // x is now moved
    int32:z = x;  // ERROR: Should detect use-after-move
    
    int32:zero = 0;
    pass(zero);
};
