// Test explicitly positive and negative nit values

func:failsafe = int32(tbb32:err) { exit 1; };

func:main = int32() {
    nit:positive = 2;    // Should work
    nit:negative = -2;   // Test if this works in variable init
    
    println("Both worked!");
    exit 0;
};
