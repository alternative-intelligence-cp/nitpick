// Test if void functions can be called without unwrap

pub func:test_void = NIL() {
    drop println("Hi");
};

func:failsafe = int32(tbb32:err) { exit 1; };

func:main = int32() {
    raw test_void();
    exit 0;
};
