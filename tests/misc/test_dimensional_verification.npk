// P1-5 Dimensional Analysis: Phase 3 Verification
// This file tests that dimensional types are recognized by the type checker
// Even though we can't compile yet (need constructors), we can verify NO errors
// about "Unknown generic type" or "not numeric"

// For now, these are just syntax examples documenting what SHOULD work
// Once constructors are available in Phase 4:

// fix256<Joules>:energy = fix256_from_int(100);
// fix256<Meters>:distance = fix256_from_int(10);
// 
// // Dimensional algebra:
// fix256<Newtons>:force = energy / distance;  // J/m = N (should work!)
// fix256<Meters>:bad = energy + distance;      // ERROR: J + m (should fail!)

// Minimal test just to check compilation
func:main = int32() {
    return 0;
};

// Required failsafe  
func:failsafe = int32(tbb32:err) {
    exit 1;
};
