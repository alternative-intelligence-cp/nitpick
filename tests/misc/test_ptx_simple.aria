// Simple GPU PTX test - Phase 2 verification
// Verifies PTX backend can generate GPU code from basic Aria functions

// Layered Safety - Layer 3: Mandatory failsafe
func:failsafe = int32(tbb32:err) {
    // Empty failsafe for testing (documented decision)
    exit(1);
};

func:add = int64(int64:a, int64:b) {
    pass(a + b);
};

func:main = int32() {
    int64:x = 42;
    int64:y = 58;
    int64:result = raw(add(x, y));
    exit(0);
};
