// Test ensures clause

func:abs = int32(int32:value)
  ensures result >= 0
{
    if (value < 0) {
        pass(0 - value);
    }
    pass(value);
};

func:failsafe = NIL(int32:code) { };

func:main = int32() {
    int32:r = abs(-5);
    if (r != 5) {
        pass(-1);
    }
    pass(r);
};
