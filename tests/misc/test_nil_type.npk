func:test_nil = NIL() {
    pass NIL;
};

func:failsafe = int32(tbb32:err) {
    exit 1;
};

func:main = int32() {
    test_nil() ? NIL;
    exit 0;
};

func:main = int32() {
    test_nil() ? NIL;
    exit 0;
};
