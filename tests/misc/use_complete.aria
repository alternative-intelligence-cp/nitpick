// Comprehensive use test — tests working import forms
// Tests: simple module import, wildcard import

// Simple module import
use std.io;

// Wildcard import
use std.mem.*;

func:failsafe = int32(tbb32:err) { exit(1); };

func:main = int32() {
    println("use_complete: module imports working");
    exit(0);
};
