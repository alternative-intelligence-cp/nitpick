// Test: Array fields in structs - comprehensive test
// Validates that array fields work correctly

func:failsafe = NIL(int32:code) { pass(NIL); };

struct:Point = {
    int32:x;
    int32:y;
};

struct:Container = {
    int32:count;
    Point[3]:points;
};

func:main = int32() {
    // Declare struct (uninitialized)
    Container:c;
    
    // Set count
    c.count = 3i32;
    
    // Initialize array elements
    c.points[0].x = 10i32;
    c.points[0].y = 20i32;
    
    c.points[1].x = 30i32;
    c.points[1].y = 40i32;
    
    c.points[2].x = 50i32;
    c.points[2].y = 60i32;
    
    // TODO: Add verification once struct initialization is fixed
    // For now, just test that writing works
    
    pass(0i32);  // SUCCESS - writes completed without error
};
