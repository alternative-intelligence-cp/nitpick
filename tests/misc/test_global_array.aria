// Test: Global array declarations
// Expected: Should compile and allow global arrays

func:failsafe = int32(tbb32:err) { exit(1); };

// Try global array
int32[5]:global_numbers;

func:main = int32() {
    global_numbers[0] = 42i32;
    global_numbers[1] = 99i32;
    exit(0);
};
