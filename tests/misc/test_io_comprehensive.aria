/// I/O Test — Module import and println
// Tests: use std.io, println builtin, FileStream type availability

use std.io;

func:failsafe = int32(tbb32:err) { exit(1); };

func:test_println = int32() {
    println("test_io: hello from std.io import");
    println("test_io: integer printing via string");
    println("test_io: line 3");
    pass(0i32);
};

func:main = int32() {
    int32:result = raw(test_println());
    pass(result);
};
