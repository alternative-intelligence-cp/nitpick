// Test if nit struct literals work with integer values

struct:Point9 = {
    nit:x;
    nit:y;
};

func:failsafe = NIL(int32:code) { pass(NIL); };

func:main = int32() {
    println("Testing nit struct literals...");
    
    // This should work: all values fit in nit range [-4, 4]
    Point9:p = Point9 { x: 0, y: -2 };
    
    println("Success!");
    pass(0);
};
