// Expected: COMPILER ERROR - uses unimplemented path stdlib functions
func:main = int32() {
    string:rel_path = "./test.txt";
    string:abs_path = raw pathAbsolute(rel_path);
    print(rel_path);
    print(abs_path);
    print("");
    
    string:full_path = "/home/user/documents/file.txt";
    string:dir = raw pathDirname(full_path);
    print(full_path);
    print(dir);
    print("");
    
    string:base = raw pathBasename(full_path);
    print(full_path);
    print(base);
    print("");
    
    string:dir_path = "/home/user";
    string:file_name = "test.txt";
    string:joined = raw pathJoin(dir_path, file_name);
    print(dir_path);
    print(file_name);
    print(joined);
    print("");
    
    string:abs_test = "/usr/bin/test";
    string:rel_test = "relative/path";
    
    if (pathIsAbsolute(abs_test) ? false) {
        print(abs_test);
    } else {
        print(abs_test);
    }
    
    if (pathIsAbsolute(rel_test) ? false) {
        print(rel_test);
    } else {
        print(rel_test);
    }
    
    return 0;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
