/**
 * Minimal test to see if Q3 type is recognized
 */

use stdlib.quantum;

func:failsafe = int32(tbb32:err) {
    exit(1);
};

func:main = int32() {
    // Try to just create a Q3 variable
    Q3<int32>:q;
    q.a = 10;
    q.b = 20;
    q.c = 0;
    
    exit(0);
};
