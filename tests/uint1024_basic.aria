// Test uint1024 
func:main = int32() {
    // Note: Can't use literals directly (they're int64, not uint64)
    // Use default initialization
    uint1024:a;
    uint1024:b;
    uint1024:sum;
    
    // Test that type exists and compiles
    return 0;
};

func:failsafe = void(int32:err_code) {
    // Error handling
};
