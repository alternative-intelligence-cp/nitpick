// Test int2048 with println support once formatters are linked
func:main = int32() {
    int2048:a = 12345678;
    int2048:b = 87654321;
    int2048:sum = a + b;
    
    // Once formatters work, this will print the result
    // For now, just verify it compiles and runs
    
    bool:correct = (sum == 99999999);
    if (correct) {
        return 0;
    }
    return 1;
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
