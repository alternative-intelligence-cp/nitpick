// GPU PTX Codegen Validation Test
// This test compiles to PTX and is validated by ptxas
// Self-contained: no runtime dependencies that would cause unresolved externs

func:main = int32() {
    int32:a = 10;
    int32:b = 20;
    int32:sum = a + b;
    pass(0);
};

func:failsafe = NIL(int32:err_code) {};
