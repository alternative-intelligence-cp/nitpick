/**
 * Determinism Test: Loop Computation
 * Tests that loop results are deterministic
 */

func:main = int32() {
    int64:sum = 0;
    int64:i = 0;
    int64:count = 10;

    while (i < count) {
        sum = (sum + i);
        i = (i + 1);
    }

    pass(0i32);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
