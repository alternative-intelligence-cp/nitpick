// Test TBB addition produces correct result
func:main = int64() {
    tbb8:x = 10;
    tbb8:y = 20;
    tbb8:sum = x + y;  // Should be 30
    
    // Just succeed - if we get here without crash, TBB arithmetic works
    pass(42);
};