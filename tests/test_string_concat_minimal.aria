// Minimal string concat test without variables

func:main = int32() {
    // Direct string_concat call with literals
    string:result = string_concat("Hello", "World");
    
    pass(0);
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
