// Minimal test without string operations

func:main = int32() {
    int64:value = 42;
    
    // Manual digit extraction test
    int64:remaining = value;
    int64:digit = remaining % 10;  // Should be 2
    remaining = remaining / 10;     // Should be 4
    
    // Just return success without printing
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
