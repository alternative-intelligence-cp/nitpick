// Simple test to verify borrow checker is integrated and running
// Tests basic functionality without requiring $ or # operators yet

func:simple_function = int32() {
    int32:x = 42;
    int32:y = 100;
    return x + y;
};

func:main = int32() {
    int32:result = simple_function();
    return 0;
};
