// Test empty string handling

func:main = int32() {
    string:empty = "";
    string:hello = "Hello";
    
    // Try to concat with empty string
    string:result = string_concat(hello, empty);
    
    exit(0);
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
