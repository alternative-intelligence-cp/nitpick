// B1 complex cross-module test
use "tests/regression/b1_helper_complex.aria".*;

func:main = int32() {
    int64:a = raw transform(5);
    int64:b = raw transform(a);
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    exit 1;
};
