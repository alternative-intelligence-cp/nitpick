// B1 Regression Test: Cross-module non-pub helper crash
// Expected: Should compile and run without crashing
use "tests/regression/b1_helper_mod.aria".*;

func:main = int32() {
    int64:result = raw public_api();
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    exit 1;
};
