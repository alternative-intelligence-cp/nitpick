// Simple test of function return

func:get_test_string = string() {
    string:result =  "test";
    pass(result);
};

func:main = int32() {
    string:s = get_test_string() ? "";  // Explicit unwrap with default
    drop(println(s));
    exit(0);
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
