// expect-error: NITPICK-RUNG-001
// expect-no-parse-error
//
// Generics are excluded from subset 1 so the SEED need not implement
// monomorphization. The compiler hand-writes concrete collections instead --
// complexity belongs in the audited artifact, not the throwaway one.
// Lifted by cycle 1.0.

func:identity<T> = T(T:x) {
    pass x;
};

func:main = int32() {
    int32:v = raw identity::<int32>(5i32);
    exit 0i32;
};

func:failsafe = int32(tbb32:err) { exit 1i32; };
