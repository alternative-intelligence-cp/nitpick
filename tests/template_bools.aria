// Test: Template literals with boolean interpolation
// Expected: Compiles and runs

func:main = int32() {
    bool:isReady = true;
    bool:isDone = false;
    
    string:msg1 = `Ready: &{isReady}`;
    string:msg2 = `Done: &{isDone}`;
    
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
