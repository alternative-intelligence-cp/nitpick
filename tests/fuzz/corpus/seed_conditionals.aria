func:main = int32() {
    int32:x = 42;
    if (x > 20) {
        print(x);
    } else {
        int32:y = 0;
        print(y);
    }
    return 0;
};

func:failsafe = NIL(int32:err_code) {
    // Error handling
};
