func:main = int32() {
    int64:outer = 0;
    while (outer < 3) {
        int64:inner = 0;
        while (inner < 3) {
            int64:val = (outer * 10);
            val = (val + inner);
            inner = (inner + 1);
        }
        outer = (outer + 1);
    }
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit 1;
};
