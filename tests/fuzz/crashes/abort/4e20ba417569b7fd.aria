func:main = int32() {
    int64:size = 1024;
    impl int8->ptr = alloc(size);
    defer { free(ptr); }
    print(size);
    return 0;
};

func:failsafe = void(int32:err_code) {
    // Error handling
};
