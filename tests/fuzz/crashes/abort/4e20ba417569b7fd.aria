func:main = int32() {
    int64:size = 1024;
    impl int8->ptr = alloc(size);
    defer { free(ptr); }
    print(size);
    return 0;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
