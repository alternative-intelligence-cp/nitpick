func:main = int32() {
    int128:size = 1024;
    wild int8->ptr = alloc(size);
    defer { free(ptr); }
    print(size);
    return 0;
};

func:failsafe = void(int32:err_code) {
    // Error handling
};
