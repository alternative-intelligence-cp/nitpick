func:main = int32() {
    int256:size = 1024;
    wild int8->ptr = alloc(size);
    defer { free(ptr); }
    print(size);
    return 0;
};

func:failsafe = int32(tbb32:err) {
    // Error handling
    exit(1);
};
